## Review round 3

All three blocking findings and all non-blocking findings are resolved.

| Finding | Resolution |
|---|---|
| Blocking 1 — synthetic WS resolution froze before a transcript existed | `ws-rpc.ts` now keeps candidate resolution live with `fs.watch` on the agent/index and nearest candidate directories. When a file appears it hands Claude to the existing worker-backed initial parser plus incremental watcher, and other harnesses to the existing full-parser stream. No HTTP polling or interval was added. A delayed-file regression test exercises the synthetic stream. |
| Blocking 2 — reset could resurrect an old transcript | The complete pointer-clear door is now used by the dashboard reset route. Reset appends an authoritative boundary to `sessions.json` and writes `session-reset`; both candidate adapters return no candidates while that marker exists. Successful spawn/restart clears the marker only after the agent reaches running state. The append-only boundary replaces the racy truncate and preserves later concurrent observations. |
| Blocking 3 — legacy mixed-harness and adapter parity | Untagged entries now expand to the current harness plus Claude compatibility, while tagged entries retain their own reader. Both adapters materialize real Codex/Pi/Kimi/Claude paths in the same shared order; literal `*`/`**` pseudo-paths are gone. A parity test compares both adapters on real Claude and Codex files. |
| Non-blocking 1 — unlocked reset truncate | Removed. Reset is an atomic append-only boundary plus marker, so it cannot erase a concurrent append. |
| Non-blocking 2 — weakened regressions | Restored no-unrelated-freshest behavior; added delayed synthetic discovery, real route/resolver integration, reset authority in both adapters, append-only reset-boundary, and concrete sync/async parity coverage. |
| Non-blocking 3 — removed module documentation | Restored module headers in `jsonl-resolver.ts` and `agent-enrichment.ts`. |

The unrelated freshest-Claude-file fallback and its now-unused helper/re-export were deleted. Existing shared agent-id classification, per-entry harness/model dispatch, append-only session writers, fail-closed retention liveness checks, and the JSONL identity regression remain intact.

Verification:

- `npm run typecheck` — passed (dashboard guard: 18 known errors, none new; frontend guard: 0 known errors).
- `npm run lint` — passed, including file-size, circular-dependency, liveness, ratchet, and Effect diagnostics guards.
- Requested Vitest scope — `Test Files 46 passed (46)` / `Tests 396 passed (396)`.
- Focused touched-test pass — `Test Files 6 passed (6)` / `Tests 69 passed (69)`.
- Production diff versus `origin/main`, excluding tests/docs/`CLAUDE.md`: `+1494/-1510`, net `-16`.

Proposed commit:

```text
fix(dashboard): keep agent transcript resolution live

Watch unresolved synthetic agent transcript candidates and attach the existing
parser as soon as a file appears. Make reset an append-only authority boundary
honored by both adapters, and materialize legacy mixed-harness candidates with
sync/async parity.

Item: W2
Item: W3
```
