## Summary
- Release 0.50.0 (stable)
- Built from v0.49.2...v0.50.0
- Published intentionally from main via tag promotion
- State snapshot: overdeck-state 3b86b7041d9deb68abe86c2206014e56dc02a478


## Highlights
### Features
- Banner + auto-rescan for conversation search failures (PAN-3771)
- Enable conversation search by default
- Pan handoff --title to name the successor conversation
- Surface ohmypi ask questions as pending operator decisions (PAN-3766)
- Fold lessons.md into SKILL.md; explicit model ladder and tool freedom
- Retire closed review-status records (PAN-3753)
- Resolve conversation rows through jobs (PAN-3752)
- Offload Claude initial transcript parse (PAN-3752)
- Coalesce transcript parse jobs (PAN-3752)
- Dispatch transcript snapshots through jobs (PAN-3752)
- Re-admit after sustained calm PSI (PAN-3754)
- Record memory governor trigger provenance (PAN-3754)
- Add transcript parse worker lane (PAN-3752)
- Instrument dashboard db jobs (PAN-3743)
- Move pi reconcile parsing to worker (PAN-3743)
- Move codex reconcile parsing to worker (PAN-3743)
- Cache pi reconcile verdicts (PAN-3743)
- Cache codex reconcile verdicts (PAN-3743)
- Add cost reconcile skip cache (PAN-3743)
- Add transcript loading skeleton (PAN-3744)
- Gate conversation empty states on stream payload (PAN-3744)
- Track first conversation stream payload (PAN-3744)

### Fixes
- Make deacon dead-end respawn tests hermetic against worker-home db pollution
- Let long banner reasons wrap instead of pushing the billing link off-viewport
- Keep pan handoff --title through the fork pipeline (PAN-3774)
- Commit the missing conversation-search status hook
- Bring the resume bar back when a resumed session stops again (PAN-3773)
- Spin Codex conversations while tools run after mid-turn narration (PAN-3770)
- Heal false-failed handoffs on live hook activity; surface switch-model errors
- Correct OpenRouter base URL + pin ox-alpha context window for claude-code harness
- Answer ohmypi ask modals via keystrokes, not messages (PAN-3766)
- Reconcile agent tombstone push races (#3763)
- Wait through omp final startup redraw
- Stabilize omp initial message delivery
- Wait for omp MCP startup before delivery
- Recognize omp v17 ready prompt
- Make claude.permissionMode honest about bypass vs auto (PAN-3760)
- Align retired review schema and test isolation (PAN-3753)
- Skip retired startup readiness repair
- Address PAN-3753 review findings
- Tolerate concurrent CLI rebuilds in skill lint (PAN-3752)
- Validate merge-next membership
- Converge review-status healing loop (PAN-3753)
- Skip retired review-status consumers (PAN-3753)
- Make promoted UAT generations terminal (PAN-3753)
- Address memory governor review findings (PAN-3754)
- Gate merge-train candidates by membership (PAN-3753)
- Gate ready-for-merge derivation by membership (PAN-3753)
- Show real memory runway details (PAN-3754)
- Explain memory patrol triggers accurately (PAN-3754)
- Stream cost batches with backpressure (PAN-3743)
- Bound cost reconciliation batches (PAN-3743)
- Decouple codex cost collector types (PAN-3743)
- Prevent stale conversation payload state (PAN-3744)
- Normalize conversation stream flags (PAN-3744)
- Satisfy cost writer size gate (PAN-3745)
- Align recorded event contract (PAN-3745)
- Stop resetTransport leaking sockets and cascading resets
- Keep swarm finalization with foreman (PAN-3741) (#3742)

_Plus 32 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.50.0
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.49.2...v0.50.0
