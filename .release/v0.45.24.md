## Summary
- Release 0.45.24 (stable)
- Built from v0.45.23...v0.45.24
- Published intentionally from main via tag promotion
- State snapshot: overdeck-state 3716469674bf5c67eb8716a454d68e50e24efeeb


## Highlights
### Features
- Unpause resumes immediately instead of waiting for the deacon patrol (#2908)
- Add --plan-model to pan start so auto-planning can be pinned to a model
- Live-tmux start-blocks offer Stop & restart in the recovery dialog (#2908)
- Every start-block 409 opens the recovery dialog — no more raw CLI text (#2908)
- Show Knowledge setup plan
- Expose Knowledge setup plans
- Prompt for Knowledge runtime setup
- Thread resolved Knowledge command
- Add assisted Knowledge setup ladder
- Write pinned OpenKnowledge shim
- Resolve managed Node 24 runtimes
- Project order books across dashboard (PAN-2377)
- Add cross-surface order promotion (PAN-2377)
- Add order book progress view (PAN-2377)
- Add order book issue rails (PAN-2377)
- Add order book lane editors (PAN-2377)
- Add order book page shell (PAN-2377)
- Complete flywheel order runs (PAN-2377)
- Teach flywheel order-book completion (PAN-2377)
- Derive flywheel order progress (PAN-2377)
- Enforce order book dispatch lanes (PAN-2377)
- Release active order book members (PAN-2377)
- Bind flywheel starts to order books (PAN-2377)
- Add pan orders commands (PAN-2377)
- Add order book API routes (PAN-2377)
- Add order book state doors (PAN-2377)
- Add order book contracts (PAN-2377)
- Standalone eltmon/okf mirror — install docs, mirror script, drift CI
- OKF v2 knowledge viewer — open-knowledge coinstall, /okf open, dashboard Knowledge page (PAN-2066) (#2942)
- Self-identifying resume button + one honest resume path (PAN-2975)
- Recovery dialog covers troubled + paused start gates too (#2908)
- Resumable-session recovery dialog — Resume / Start fresh (#2908)
- Digest-first turns with per-turn line budgets + verdict cards (#2967)
- Cockpit route renders the ONE IssueDetail at page density (#2962)
- Shared rail anatomy in the deck tree expansion (#2962)
- Delete the stream-excerpt fossil from ActiveAgentPanel (#2908)
- Resources facet joins the always-visible right rail (#2908)
- Verification gates move to the always-visible right rail (#2908)
- Drawer plan tab is Plan map — the DAG leads (#2908)
- Drawer Files tab renders the changed-files tree + diff (#2908)
- Done column collapses to the cycle's recent 12 (#2908)
- Hover peeks on board cards and deck-tree rows (#2908)
- Page density for IssueDetail — all three densities exist (#2908)
- The one IssueDetail component + conformance gates in CI (#2908)
- Simple issue page gets the real transcript + Get help routing (#2908)
- ⌘J jump-to-conversation (#2908)
- Freshness rollout — every region bounded, e2e gate (#2908)
- Overnight slices — project-scoped composer, Plan column, dock transcripts, freshness, one detail shell (#2908)
- Talk-it-through hand-off + just-filed surface (#2908)

### Fixes
- Keep supervisor-delivered agents alive when tmux goes missing (PAN-3002)
- Skip resume for already-unpaused agents (PAN-3007) (#3008)
- Pin rebased branch lease to observed head
- Push rebased review branches with a lease
- Keep spawn route under size ceiling
- Preserve strike retry clears in fallback (PAN-2993)
- Persist strike retries and preserve landed state (PAN-2993)
- Rearm recovering strike head (PAN-2993)
- Escalate undelivered strike recovery (PAN-2993)
- Report message delivery outcomes (PAN-2993)
- Retry strike transport failures with backoff (PAN-2993)
- Classify strike merge transport failures (PAN-2993)
- Spawn dashboard under process.execPath, not a dead nvm path
- Pan start --model scopes to the work agent — planning keeps its default (#2908)
- Narrow catch to unknown in stack-health.ts — clears red-main lint (PAN-2999) (#3000)
- Scope stack health to the canonical compose project and sweep stale-naming corpses on rebuild (#2908)
- Never trap the operator in a pending recovery dialog + visible starting labels (#2908)
- Gate UAT stack chip on merge-ready and show truthful state label (#2996)
- Filter advisory checks in GitHub App state (#2994)
- Exclude advisory checks from merge blockers (#2990)
- Surface strike sessions in the issue tree from the agents registry (#2991)
- Show a starting placeholder in the session panel while an agent spawns (#2908)
- Preserve isolated Node setup
- Attempt bounded workspace stack rebuild before 422ing a work-agent start
- Make agent error text selectable on dialog and inline error surfaces
- Require a saved session for canResumeSession so dead transcripts stop offering resume
- Restore launch workspace on resume (PAN-2377)
- Close order-book lifecycle gaps (PAN-2377)
- Enforce order-book review rails (PAN-2377)
- Okf-mirror-drift needs actions/checkout before git commands
- Prune deleted sessions from conversation search so palette hits stop 404ing (PAN-2981)
- Start CTA on card rows uses text-primary, not a filled blue block (PAN-2975)
- No detached StartAgentCta blocks at rail or cockpit density (PAN-2975)
- Resume chip lives inside the issue row, not above it (PAN-2975)
- Boot-reconciliation event-path gate + phase-aware resume kickoff (PAN-2974)
- K3[1m] launches — stale model patterns replaced with canonical MODEL_ID_PATTERN (#2979)
- Membership retry POST sends dashboard mutation headers (#2972)
- Codex re-auth opens a terminal popup instead of navigating the tab (#2973)
- Membership refresh failures logged + Retry forces a re-gather (#2972)
- Revert the per-turn line-budget collapse (#2908 #2967)
- Remove tree rail mount + agent 404 renders 'no saved history' (#2908)
- Verdict cards don't fire on neutral 'blocked' mentions (#2967)
- Re-home Pipeline Band audit marker (#2970)
- Explicit ?tab= deep-links win over conversation-first (#2908)
- Deep-link to a fresh conversation selects it promptly (#2908)
- Pan done completion skips no-op force-push, uses fast-forward push only — no history rewrite (PAN-2961) (#2965)
- Baseline conformance source introspection (#2964)
- Close remaining polyrepo blind spots (#2955)
- Menu open state survives remounts + deacon null guard (#2937 #2946)
- Align review reset session expectation (PAN-2951) (#2953)
- Polyrepo-aware staleness probe and verified-commit comparison (PAN-2948)
- Reset --session targets the reviewer; polyrepo manifest branch from primary repo (PAN-2948)
- Make review context, dispatch pushes, and drift anchors polyrepo-aware (PAN-2948)

_Plus 61 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.45.24
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.45.23...v0.45.24
