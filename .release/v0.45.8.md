## Summary
- Release 0.45.8 (stable)
- Built from v0.45.7...v0.45.8
- Published intentionally from main via tag promotion
- State snapshot: overdeck-state b9a8822a530881759ec63a6cc017f521d8fd6f8b


## Highlights
### Features
- Mount ReviewPolicyControl in the progressive issue cockpit header too (PAN-1874)
- Per-issue review mode + re-review scope overrides — CLI, API, and issue-cockpit control (PAN-1874)
- Warm-parent discovery + convoy fork, cache-miss detection, desktop notify (PAN-1862 Phase A+C, FR-1..FR-5, FR-11/FR-12, NFR-2/3/5)
- Review mode + re-review scope selectors and model-uniformity banner in Settings -> Roles (PAN-1862 FR-10/FR-17)
- Per-reviewer verdicts + selective re-review with carried-forward stubs (PAN-1862 FR-6/FR-7/FR-8/FR-9)
- Review mode 'none' — skip AI review, reviewStatus 'skipped' passes the merge gate (PAN-1862 FR-13/FR-14/FR-16)
- Warm-by-default role sessions — no reap-on-verdict, warm-idle excluded from ceiling, governor sheds warm first (PAN-2579, PAN-1131)
- Sign and notarize the macOS app in CI so the DMG opens without "damaged"
- Honor tiered execution record overrides in dispatch
- Add tiered execution override route
- Add issue tiered execution control
- Return tiered execution plan state
- Record-backed tiered-execution override + resolver precedence

### Fixes
- Skip boot-reconciliation dialog when nothing is resumable
- Unwedge review pipeline — authoritative discovery signal, actual-harness fork mode, sandbox verdict fallback
- Scope recreation tripwire to legacy source, not host
- Align polyrepo state read paths
- Route polyrepo migration through host repo
- Replace stale pan.localhost with overdeck.localhost everywhere
- Warm-idle sessions reap at next-dispatch time; break concurrency->review-status import cycle (PAN-2579, PAN-2228)
- Loud tmux delivery failures — refuse dead-pane targets, surface failed feedback delivery as needs-you (PAN-2228)
- Resurrection-first feedback delivery — resume stopped/gated agents before needs-you (PAN-2209, PAN-2461)
- Never clobber a terminal review verdict with a stale 'reviewing' write (PAN-2578)
- Interleave compaction dividers and surface newest work-log entries (PAN-2576)
- Auto-approve conv-* sessions in PreToolUse hook
- Emit --permission-mode default for auto mode
- Raise Node floor to 22.16 to match node:sqlite requirement (PAN-2504)

_Plus 20 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.45.8
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.45.7...v0.45.8
