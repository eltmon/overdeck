## Summary
- Release 0.45.17 (stable)
- Built from v0.45.16...v0.45.17
- Published intentionally from main via tag promotion
- State snapshot: overdeck-state 9c11a6735f9ca494fefe27fefd4875605a673ea7


## Highlights
### Features
- Add issue staffing policy strip (PAN-2674)
- Add per-issue work staffing resolution (PAN-2674)
- Stream running gate output in the Test/Lint panel (PAN-2665)
- Resume session button on the cockpit session panel header
- Live gate progress in the Test/Lint node (PAN-2665)
- Stack actions (Start/Rebuild/Logs/…) on the UAT environment panel
- Emit Lint node in project tree payload (PAN-2665)
- Prominent UAT environment panel in issue drawer + cockpit overview
- Add Lint node to issue tree (PAN-2665)
- Add reset-to-planned menu action (PAN-2660)
- Expose vbrief task commands (PAN-2648)
- Complete hardened task write door (PAN-2648)
- Add hardened task write door (PAN-2648)
- Add swarm policy controls
- Add inherited policy with off default
- Add dashboard and desktop updater experience
- Add shared Overdeck update service
- Cost limits are opt-in — remove the invented $10/$25/$100 defaults (PAN-2642)
- Stalled bucket for artifact-bearing, agent-less issues (workspace-scvay)
- Open-beads gate blocks completion while issue has open beads (PAN-2602)
- Pan beads sweep backfill command for orphaned beads on closed issues (PAN-2602)
- Sweep orphaned beads during close-out before teardown (PAN-2602)
- SweepOrphanedBeads() closes open/in_progress beads for terminal issues (PAN-2602)
- Render issue-linked conversations under issue nodes (PAN-2602)
- Auto-detect handoff issue from cwd branch (PAN-2602)
- Add --issue flag to pan handoff (PAN-2602)
- Union in-pipeline signal for resource discovery (PAN-2602)
- Tag issueId-linked conversations as conversation resource source
- Compute branchAheadOfMain for feature/* and bypass/* branches
- Per-issue bead rollup service refreshed on beads.freshness_changed
- Prompt-change trailer gate for roles/ and flywheel brief
- Deterministic prompt-invariant vitest suite
- Review synthesis canonical blocker format eval
- Flywheel launch-vs-report golden scenario
- Shared live-model prompt-eval harness\n\nAdds evals/lib/prompt-harness.ts with loadPromptFile, runPromptScenario,\nand extractJsonArray for live-model prompt regression evals. The harness\nrefuses to run without an explicit OVERDECK_EVAL_MODEL and contains no\nhardcoded model fallback. Includes unit tests for the deterministic helpers.\n\nAC-1: rejects when OVERDECK_EVAL_MODEL is unset\nAC-2: resolves prompt files from the repo root\nAC-3: extracts JSON arrays through code fences\nAC-4: typecheck:evals passes with no fallback literal
- File chip preview for non-image attachments (PAN-2596)
- Generic drag-and-drop with vision gate and rejection toasts (PAN-2596)
- Paperclip attach button with multi-file picker (PAN-2596)
- Generic attachment store + composer mirror allowlist (PAN-2596)
- Strip only image attachments for non-vision models
- Extension-driven upload allowlist with UTF-8/PDF validation

### Fixes
- Skip code-repo guards in checkouts without hook tooling (PAN-2677)
- Rank exact search matches first
- Scope state-migration boot gate per project (PAN-2676)
- Route draft/spec writes through the committing write door (PAN-2677)
- Tolerate missing review default
- Expose policies in issue drawer
- Signal REVIEWER_READY from codex-notify-hook (PAN-2675)
- Refresh cold search results
- Await the queued record commit
- Harden issue staffing controls
- Supervise verification outside dashboard
- Clarify done review handoff
- Restore verification fix-forward handoff
- Replace stale bead operator messages
- Remove planning materializer remnants
- Keep resume action mounted for stale sessions
- Run interrupted-verification sweep unconditionally at boot (PAN-2669)
- Reap gate process trees and reconcile interrupted verifications (PAN-2669)
- Reuse existing GitLab merge requests
- Prefer the work agent when all issue agents are stopped
- Move resume session button into SessionPanel header
- Fetch stopped-agent lifecycle so resume session can enable
- Re-drive stopped agents for rework-owing feedback (PAN-2668)
- Offer resume session for stopped codex agents in agent listing
- Use docker ps {{.Label}} template in compose-project discovery
- Stack start/stop/restart work after devcontainer files are cleaned
- Render lint gate output in the tree session panel (PAN-2665)
- Normalize task labels at the issue-reads door (PAN-2665)
- Retarget prompt lint to xBRIEF item workflow
- Keep auto-sync commits pushable (PAN-2664)
- Reject unresolved sync merges (PAN-2664)
- Retire reset agent runtime records (PAN-2660)
- Complete reset-to-planned setup (PAN-2660)
- Make record writes immediately durable (PAN-2541)
- Use optional Rally lifecycle layer (PAN-2660)
- Reset vbrief task progress after planning (PAN-2660)
- Remove residual Beads refs the PAN-2648 scan missed
- Strip tool access from background summarizer spawns (PAN-2657)
- Stop leaking state-doc commits into the real repo (PAN-2658)
- Preserve concurrent sync writes (PAN-2648)
- Auto-switch conversation view to terminal for boot-blocking Claude screens
- Remove legacy Beads agent references
- Make bundles self-validating; drop code-repo checkout in CI
- Isolate bundle checkout in conformance CI template
- Exclude .git from generated knowledge index
- Distribute skills across agent harnesses
- Provision Claude statusline during install
- Migrate registered projects automatically
- Simplify guarded restart workflow (PAN-2651)
- Expose canonical reads and preserve completed reviews
- Stop off swarms and classify provider failures
- Add safe post-planning reset and start gates
- Enforce resolved swarm limits
- Mock cached beads snapshot resolver (#2644)
- Trim resource-discovery below file-size ceiling (PAN-2602)
- Bound branch-ahead and GitHub sweep concurrency (PAN-2602)
- Address PAN-2602 review findings
- Restrict rollup grouping to project-configured issue prefixes
- Emit freshness_changed on local writes by comparing across polls
- Use valid Vitest v4 maxWorkers option for fork limits
- Name rejected files in composer rejection toasts
- Detect Linear-tracked closed issues in isTrackerIssueClosed
- Spawn beads gate waits for the bd lock instead of failing at 500ms
- Use shared classifier for non-vision image gate
- Mirror server image allowlist in frontend attachment classifier
- Enforce filename policy before MIME checks for attachments
- Address PAN-2596 review feedback

### Performance
- Disable bd telemetry during install (PAN-2640)
- Remove synchronous beads fallback (PAN-2640)
- Deacon per-issue bead reads go through the cached snapshot (PAN-2640)
- Cached 10s bulk snapshot for the beads rail read door (PAN-2640)
- One bulk beads read per pass — per-issue bd polls collapsed the dashboard (PAN-2640)

_Plus 69 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.45.17
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.45.16...v0.45.17
