## Summary
- Release 0.45.14 (stable)
- Built from v0.45.13...v0.45.14
- Published intentionally from main via tag promotion
- State snapshot: overdeck-state a41fee068f8b329d3dbb068a2f422748b459bedb


## Highlights
### Features
- Land PAN-2603 docs RAG prompt injection — memory-hook integration, latency bound, live index path
- Inject docs context in memory hook
- Add injection context service
- Materialize docs index live path
- Hint when docs index missing
- Reindex live docs path
- Land PAN-2564 Dolt-native cross-machine authority (WI-1..16)
- Centralize minimum version policy (WI-16)
- Unify canonical project home (WI-14)
- Enforce agent mutation door (WI-13)
- Expose synchronization health (WI-10)
- Standardize Dolt configuration (WI-8)
- Add isolated no-loss reconciliation (WI-7)
- Validate derived JSONL exports (WI-6)
- Stream dashboard freshness updates (WI-5)
- Bootstrap from project Dolt remote (WI-4)
- Establish atomic mutation batches (WI-3)
- Add canonical Dolt read door (WI-2)
- Gate state migration on cutover marker (WI-1)
- Release/publish pipeline visibility on the project HOME tab
- Gate ALLOW_PRIMARY override to non-workspace boots and scrub message (PAN-2322)

### Fixes
- Model-divergence detector no longer fires on git checkout output
- Contain fitted viewport within panel
- Land review verdicts when synthesis.md exists but the verdict never landed
- Skip bootstrap in runMutationBatch when the embedded dolt store exists (PAN-2610) (#2617)
- Auto close-out defaults ON — correct operation must not be opt-in
- Install bd/dolt/ripgrep on runner + hard-fail beads-access audit without rg (PAN-2614) (#2615)
- Never force-remove foreign containers during network teardown
- Close the four gaps that let closed-issue Docker stacks leak forever
- Land PAN-2528 harness-picker ToS gating — blocked ohmypi options now disabled instead of offered
- Land PAN-2322 host dashboard-port guard hardening — workspace checkouts can no longer seize :3011 via ALLOW_PRIMARY
- Cancel timed out docs injection
- Bound prompt docs injection latency
- Stabilize circular dependency lint PAN-2603
- Beads-rail lock acquisition 500ms -> 8s — starved permanently stale under live traffic
- Post-merge runtime fixes for PAN-2564 — event-loop safety + CLI surface
- Harden prompt docs injection
- Register pan beads verbs at top level; fix yield* outside generator in backlog route
- Serialize concurrent canonical operations (WI-9)
- Stop issue cockpit title collapsing to one char per line
- Address PAN-2149 review findings
- Phase-gate the health-check poke loop — never poke or idle-alive-pause warm agents on pipeline-owned issues (PAN-2581 FR-2)
- Disable blocked ohmypi options in shared HarnessSelect (PAN-2528)
- Scrub override env var from pipeline-role refusal path
- Drop unused pickModelForHarness helper (PAN-2528)
- Emit ohmypi decision key (PAN-2528)
- Name Claude Code subscription ToS in ohmypi block reason (PAN-2528 tos-reason-copy)
- Disable blocked harness options in ModelPicker (PAN-2528 slot-3)

_Plus 34 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.45.14
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.45.13...v0.45.14
