## Summary
- Release 0.44.0 (stable)
- Built from v0.43.0...v0.44.0
- Published intentionally from main via tag promotion

## Highlights
### Features
- Live ship-door row in the sidebar issue tree during server-side merges (PAN-2487 parity)
- Running sessions in the sidebar issue tree spin without tool telemetry (PAN-2487 follow-up)
- Ship view with live door log, ci-green merge skip, node spinner (PAN-2487)
- Paved-road pan start — plan-if-needed, --plan dial, deprecations (PAN-2407, squash-assembled) (#2458)
- Flake policy — CI retry semantics, quarantine, non-blocking flake lane (PAN-2373, squash-assembled) (#2460)
- Idle-alive escalation — progress-fingerprinted pokes (PAN-2452 class)
- Batch-don't-ping-pong rule in self-review mode (throughput)
- Add editable tiered-execution control in PlanCard (PAN-2383 bead mdjv5)
- Honor record override in spawn-prep tier resolution (PAN-2383 bead 81zud)
- Add PATCH write door for tiered-execution override (PAN-2383 bead 8y5bb)
- Render server-computed effective state in PlanCard (PAN-2383 bead i3smr)
- Implement record-backed override home (PAN-2383 bead 5x3fx)
- Flywheel prompt — usage-limit halt detection and reset-aware re-dispatch
- Replace logo with control-ring mark across all surfaces
- Tree-lane rows show tokens beside cost (PAN-2393)
- Cockpit round 2 — plan map card, feed noise fix, plain-language pickup title (PAN-2398)
- Quiet detail line restores every gate fact under the journey strip (PAN-2398)
- Per-line-item cost on every issue-tree agent row (PAN-2393)
- 'What just happened' - plain-language activity feed with task-progress badge (PAN-2398)
- The Crew stage - live who-is-doing-what with click-through to each member's session (PAN-2398)
- One plain-language status narrative + journey strip replaces the duplicate chip and gate rows (PAN-2398)
- Replace broken ReactFlow plan DAG with the shared PlanMapViewer (PAN-2400)
- Shared vBRIEF DAG renderer - proper arrows, fit-to-container, status coloring (PAN-2400 step 1)
- 'Use distribution' editor on tier rows + idempotent round-trip validation (PAN-2391, PAN-2397 W2 UI)
- Load-path config degradation + supervisor owns inspection by default (PAN-2395, PAN-2397 W4+W5)
- Weighted model distributions per tier (PAN-2391, PAN-2397 W2 backend)
- Always-tiered staffing resolver - implicit roles.work tier (PAN-2397 W1)
- Ignite commit feed + supervisor review from the swarm merge path (PAN-2385)
- Surface lost/unloadable conversation transcripts in the UI (PAN-2394)

### Fixes
- Register ship-log route in no_loss_matrix (pan-2490) (#2494)
- Migrate @xenova/transformers to @huggingface/transformers (PAN-2491)
- Click-through no longer lies about pane-detected waits (rate-limit/session-resume dialogs)
- Eligibility sweep — merge-eligible issues without flywheel merge verbs now ride the train (PAN-2484)
- Resolve role definitions from package root
- Ignore trailing state commits for verdict freshness
- Break feedback target agent cycle (#2472)
- System-stuck no longer halts swarm coordination; silent itemId-drift skip now logs (PAN-2469)
- Guard the REST PR-lookup fallback so polyrepo roots don't mask the real artifact error (PAN-2465)
- No-loss guard at the record write door — empty closeOut never clobbers populated usage history (PAN-2466)
- Break the verification-gate deadlock at all three roots (PAN-2461)
- Auto-connect traefik to workspace stack networks (PAN-2428)
- Polyrepo gate verification renders placeholders and runs from workspace root (PAN-2461)
- Polyrepo door skips rebase wait for branches already containing the target (PAN-2461)
- Done.ts + deacon patrol convergence (PAN-2207, squash-assembled) (#2453)
- Verify-merged judges unique-to-branch diffs (three-dot) for post-merge main-merges (PAN-2406)
- Verify-merged accepts state-plane-only deltas past the merged head (PAN-2406)
- Merge GITHUB_REPOS env with projects.yaml repos — env no longer shadows registered projects (PAN-2449 defect 1)
- Use canonical fleet cost summary (#2447)
- Summarize health pill cause (#2446)
- Set claude auto compact window for proxied models
- Extend pan start bd lock budget
- Fix stale strike agent badges
- Preserve test verdicts on state-only commits (#2437)
- Add compact recovery surface
- Avoid ready patrol bd lock contention
- Repair frontend tiered execution build (#2434)
- Type VBriefPlan.tieredExecution for PlanCard (PAN-2429 second layer)
- Merge strike/pan-2429 — resolve workspace record import (PAN-2429)
- Fix workspace record import
- Polyrepo auto-start — clean tree, scaffold gitignore, refusal surfacing (PAN-2386) (#2403)
- Cost-limit attribution + limit-window accounting (PAN-2402) (#2411)
- Preserve verdicts for state-only commits
- Pan start --fresh re-runs tier staffing instead of inheriting the dead agent's model (PAN-2410)
- Auto-start AC synthesis — explicit acceptance section only (PAN-2404)
- Raise bd export/import timeout to 120s in close-out beads sync
- Only remove labels present on the issue in close-out label cleanup
- Dag readability floor — never shrink below legible, scroll instead
- Gate backlog sequencer auto-trigger on --no-resume (PAN-2396)
- Plan read door applies record statusOverrides (PAN-2401)
- Fleet view survives partial agent rows during spawn window (PAN-2384)
- Tier and supervisor defaults must never be frontier models
- Never auto-delete conv-* agent dirs — they hold ohmypi/codex transcripts

_Plus 90 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.44.0
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.43.0...v0.44.0
