## Summary
- Release 0.50.2 (stable)
- Built from v0.50.1...v0.50.2
- Published intentionally from main via tag promotion
- State snapshot: overdeck-state ce6df38d05f602de3e898616888e02a77fa213b4


## Highlights
### Features
- Add OpenCode harness with Go and Zen
- Add Muse Code harness and Muse Spark 1.3 tiers (PAN-3790)
- Clarify context scope and agent previews (PAN-3783)
- Show Codex subagent transcripts in conversations
- Refresh catalog and show configured context budgets

### Fixes
- Skip resource queries when no agents are active
- Prune BuildKit cache under disk pressure (#3821)
- Isolate retirement status fixture (PAN-3815) (#3820)
- Hand off agent restart now to reload (PAN-3812) (#3816)
- Preserve OpenCode routing through managed context (PAN-3783)
- Align frontend transcript event fields
- Preserve issue delta consumer updates
- Preserve delivery receipts and reduce recurring work
- Integrate current harness routing with launch context (PAN-3783)
- Load deep links directly and track message delivery outcomes
- Gate dispatch and vitest on CPU load (PAN-3344) (#3801)
- Keep post-merge staging off live generation (PAN-3329) (#3808)
- Use exact Linear issue lookup (PAN-3796) (#3803)
- Relay polled CI failures to work agents (PAN-3804) (#3807)
- Exclude gate queues from worker deadline (#3806)
- Address Muse lifecycle and transcript review findings (PAN-3790)
- Enable reload from the composer
- Exclude native rules from skill copy and pruning (PAN-3783)
- Deliver managed context at launch without native file writes (PAN-3783)
- Preserve Kimi coding context tier in subagent model routing
- Resolve Kimi K2.7 Claude model IDs for the selected endpoint
- Acknowledge live ACP effort changes after provider acceptance
- Carry selected effort through native Kimi runtime spawns
- Honor native Kimi effort and pin provider context budgets
- Pass selected effort and pin managed GPT context windows

### Performance
- Aggregate agent resource costs in database worker
- Parse Codex appends and stream transcript row deltas
- Cache cost and search polling in database workers

_Plus 10 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.50.2
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.50.1...v0.50.2
