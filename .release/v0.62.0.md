## Summary
- Release 0.62.0 (stable)
- Built from v0.61.0...v0.62.0
- Published intentionally from main via tag promotion

## Highlights
### Features
- Add gpt-6-sol and gpt-6-luna to the OpenAI provider (#4234)
- Bare conversations with no Overdeck-injected context (PAN-4185) (#4187)
- Route conversations and pan handoff through the terminal backend (PAN-3921) (#4104)
- Link pull requests to conversations by branch (PAN-3822, core) (#4067)
- Show pan reload deploy progress on the owning project row (PAN-3751) (#4094)
- Relay deacon-lite run status from the deacon child; watchdog stale verdict (PAN-3922) (#4079)
- Add direct subagent input and working status (PAN-3787) (#3788)

### Fixes
- Schedule ready opted-in auto-merges on the merge-train tick (#4066)
- Resolve the channels bridge path from packageRoot, not a walk up from the chunk (#4209)
- Count only Overdeck agent panes toward the work-agent ceiling (#4206) (#4207)
- Re-dispatch review synthesis when every lane reported and the parent died (#4134) (#4153)
- Search watcher resubscribes on EINTR without tripping the breaker (PAN-4193) (#4194)
- Retry a membership gather the forge did not answer (PAN-3527) (#4190)
- An issue pause holds its review convoy (PAN-3911) (#4150)
- Pan tell idle wait reads Herdr agent_status (PAN-4186) (#4188)
- Pan reload carries the running dashboard's boot gates (PAN-3899) (#4179)
- Remove compose networks on every teardown path; sweep orphaned workspace networks (PAN-3900) (#4181)
- Pre-trust every Claude Code launch under Claude Code's config lock, name the pane on ready timeout, fail aborted workspace setup (#4163)
- Route the pan down review sweep and failed-spawn teardown through the terminal backend (#4183)
- The specialists-done override door refuses agent sessions (PAN-3853) (#4139)
- Backend-aware synthesis dispatch guard and reviewer kill (PAN-3939) (#4180)
- Resume an idle agent after 'Connection lost mid-response' (PAN-3948) (#4178)
- Remove dead models.overrides code and retire roles.review.sub.synthesis (#4174)
- Resume an unfinished workspace setup instead of starting in it (PAN-4171) (#4176)
- Reap finished non-Claude one-shot role runs on Herdr from their transcript (#4169) (#4175)
- Retry a guardrail-refused planning hand-off from deacon-lite (PAN-4155) (#4177)
- Judge the sequencer mirror idle label by the finished-run rule (PAN-4172) (#4173)
- Remove the last hardcoded model literals (remote spawn, smart selector, provider fallback) (#4170)
- Skills and CLI docs name only registered pan verbs (PAN-3934) (#4158)
- Status-bar emergency stop shows the stop result (#4125)
- Reap a finished role-run pane whose harness sits idle at its prompt (PAN-3923) (#4162)
- Resolve unset spawn model through role routing, not a literal (#4154)
- Pan doctor stale sync-checkout row and dangling git-hook link pruning (PAN-3881) (#4161)
- Keep keystrokes typed on /projects/new before the backend settles (PAN-3867) (#4147)
- Wait for the authoring mock instead of sleeping 10ms in the fork-pipeline test (#4168)
- Agent-facing text names only registered pan verbs and no deleted status fields (PAN-3868, PAN-3934) (#4143)
- Fail loudly when the planning relaunch cannot resolve the plan model (#4165)
- Conversation-search watcher skips deleted transcripts and restarts after errors (PAN-3915) (#4157)
- Remove the remaining hardcoded model fallbacks (PAN-4160) (#4164)
- Surface a Claude Code exit before its prompt as a spawn error (PAN-3827) (#4137)
- Warn on stale pan sync source checkout and prune deleted hooks (PAN-3881) (#4148)
- Attribute multi-line Effect diagnostics to their own file (#4149) (#4156)
- Bump pinned CLIProxyAPI to v7.3.16 (#4159)
- Show ACP/opencode prompt failures as error rows in the feed (#4141)
- Acknowledge advisory guardrails on planning auto-handoff (#4135)
- Mark reviewer lanes mid-round and done from the current run's folder (#4130)
- Stop recoverStalledReviews journaling no-op re-dispatches (PAN-3914) (#4133)
- Surface stalled ACP turns (opencode provider retry) as prompt_stalled rows (#4152)
- Route the remaining tmuxActive liveness filters through the backend-aware door (#4109) (#4114)
- Apply planned label only after finalize writes the spec (#4146)
- Pre-trust planner workspaces before launching the planner (#4144)
- Carry ACP agent thoughts into the feed as thinking rows (PAN-3893) (#4142)
- Pan start re-resolves the model after pan reset-session (#4138)
- Reject stray positionals on pan restart, reload and up (#4136)
- Scrub inherited OVERDECK_* env from quality gate commands (PAN-3902) (#4132)
- Show convoy lanes based on what the review run did, not config (#4126)
- Progress fingerprint reads the pane through the terminal backend (#4121) (#4124)
- Apply the Request review menu's Full/Quick/None choice to this run (#4120)
- Route muse and kimi-code runtime spawnAgent through launchAgentPane (PAN-3936) (#4112)
- Runtime isRunning and Cloister pokes/crashes ask the backend-aware liveness oracle (#4119)
- Start the memory FTS worker through spawnModuleWorker (#4106)
- Carry hasLivePane through snapshots, events, lifecycle and frontend readers (#4113)
- Pan merge cancel and auto-merge docs use the merge-train routes (#4111)
- Push order book commits from pan orders (#4110)
- Retry transient pipeline-membership failures instead of latching the banner (#4092)
- Remove the dead work.inspect / work.inspect-deep sub-roles (#4095)
- Agent output API, pan status and three skills see Herdr agents (#4097) (#4103)
- Detect crashed and conflicting agents through the liveness oracle (#4105) (#4107)
- Boot the checkpoint worker from dist, and via tsx from source (#4100)
- Merged strikes clean up their pane, worktree and branch (#4070)
- Find Herdr issue workspaces by label after a restore drops their tokens (#4096) (#4099)
- Skills see Herdr agents; pipeline-status resolves the project key (#4093)
- Derive snapshot agent status from the backend inventory (#4101)
- Open unregistered and subagent conversation-search hits (PAN-3982) (#4087)
- Push backlog sequence commits to origin (PAN-3923) (#4084)
- Derive god view liveness from backend panes, read PSI over swap (PAN-3540) (#4089)
- Route liveness readers and swarm counts through the backend-aware door (PAN-3926) (#4088)
- Follow-ups from the 2026-09-24 PR batch (#4086)
- Print the backend-correct attach hint for herdr agents (#4072)
- Boot the DB-task worker from dist, and via tsx from source (#4085)
- Rewrite skills off the deleted status fields and widen the guard (PAN-3929) (#4083)
- Retry sidebar project and conversation fetches, refetch on reconnect (#4082)
- Deny harnesses with no harness-policy decision (#4081)
- Preserve JSONL transcripts outside vendor cleanup roots (#3817)
- Retire stores with readers but no production writer (PAN-4052) (#4080)
- Render a closed-out issue as shipped, with its history (PAN-3420) (#4078)
- Clear the reconnect banner after a restart (PAN-4074) (#4075)
- Close an agent's Herdr panes on stop and warm-idle reap (PAN-3966) (#4076)
- Keep peer dashboards from writing the primary's shared state (#4071)
- Gh graphql stderr + retry-once; github refs in pipeline-membership tracker lens (PAN-3924) (#4073)
- Make orphaned swarm-slot GC polyrepo-aware (PAN-3689) (#4069)
- Remove pan inspect residue from the work item loop and prompt lint (#4068)
- Honor the auto-merge tracker label and explain it in the toggle tooltip (#4064)
- Escalate repeated identical UAT failures once instead of looping (PAN-3580) (#4063)
- FindProjectByPath expands ~, resolves symlinks, prefers deepest root (PAN-4046) (#4062)
- Bound every GitHub App API request with a timeout (#4060)

### Performance
- Lazy-load command implementations (pan --version 560→180 ms) (#4205)
- Stop board routes blocking on gh pr list and per-agent derivation (PAN-3925) (#4091)

_Plus 19 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.62.0
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.61.0...v0.62.0
