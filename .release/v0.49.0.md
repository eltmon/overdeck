## Summary
- Release 0.49.0 (stable)
- Built from v0.48.0...v0.49.0
- Published intentionally from main via tag promotion
- State snapshot: overdeck-state 5ce3f36b95a587bd16bfdecf12b64057fdefc6fc


## Highlights
### Features
- Review-infrastructure bypass goes through the write door (PAN-3512)
- Unsignaled-recovery auto-completes go through the write door (PAN-3512)
- Orphan-restore verdicts go through the write door (PAN-3512)
- Audit Effect diagnostics baseline growth
- Gate lint on Effect diagnostics
- Add Effect diagnostics ratchet
- Add Effect diagnostics tsconfigs
- Region-configurable DashScope upstream via DASHSCOPE_BASE_URL (PAN-3531)
- Add Qwen3.8 Max to the DashScope provider (PAN-3531)
- Backfill durable agent runtime records
- Add durable agent runtime plane
- Block kimi-code/* native-catalog ids on every harness but kimi-code/acp
- Harness-labeled Kimi model-picker rows that actually launch what they say
- Annotate available-models Kimi entries with harness + effort levels
- Create recordReviewVerdict write door module
- Register review.verdict_rejected and review.verdict_dispatched domain events
- Implement verdict write door + dispatch-not-drop + 3 migrations (PAN-3512)
- Honor explicit per-spawn harness picks, policy-gated and fail-loud
- Sweep hardcoded fonts to tokens, Broadsheet badge radius
- Add Overdeck Theme two-card picker to Settings > Appearance
- Add useDesignLanguage hook + pre-mount data-theme boot script
- Surface ui.theme through GET/PUT /api/settings
- Add ui.theme config key (ledger|broadsheet, default broadsheet)
- Introduce data-theme font/display token scope
- Self-host all four fonts, remove Google Fonts CDN
- Pan parked ack — batch-acknowledge needs-you trips with flywheel hand-off (operator-approved)
- Guard-exit invariant + CI lint guard (PAN-3488)
- Pipeline velocity surface — real stage-transition rate (PAN-3491)
- The stall sweeper rendered — beam, thaw, flare, orbit frost (PAN-3490)
- Stall sweeper patrol + sweep.* event stream (PAN-3487, PAN-3489)
- Parked-population resolver + pan parked + /api/parked (PAN-3486)
- God-view orb click opens the in-canvas issue rail (operator reopened D-3)
- Add Confluence hook telemetry trace
- Add event-driven Confluence hook bus
- Link Confluence feed rows to orbs
- Enrich Confluence top bar
- Add Confluence field guide
- Link Confluence orbs to issue drawer
- Add Confluence orb tooltip
- Drive Confluence effects from events
- Port Confluence river canvas
- Add Confluence data hooks
- Add Confluence aurora shader
- Top consumers with kind badges, memory bars, and LEAKED tag
- Attention section with grouped rows and context notes
- Add vitals tiles with meter bars and threshold colors
- Popover header with state badge, summary line, and chip row
- Pure attention-model module for system health popover redesign
- Expose agent plan checklist
- Add Confluence model math (PAN-3447)
- Add hook inventory
- Replace confluence Winamp strip with hook telemetry trace (PAN-3447 D-14)
- Add god-view Confluence mockup — River × Spectrum Deck union (PAN-3447)
- Drag a conversation row onto a sidebar project to move it (PAN-1577)
- Add move action with project picker to conversation menus (PAN-1577)
- Shared optimistic move mutation for conversations (PAN-1577)
- Add pan conversations move <query> <projectKey> (PAN-1577)
- Prefer projectKey override in conversation grouping (PAN-1577)
- Add conversation move endpoint (PAN-1577)
- Add conversation.moved domain event (PAN-1577)
- Add project_key override column (PAN-1577)
- Show issue titles in lane editor
- Compact retained state history (#3403)
- Pin GPT-5.6 to the 272K billing tier, add opt-in [372k] long-context variants (PAN-3388)
- Add project version ship settings
- Add batch version ship controls
- Add lossless version sync settings API
- Add version ship definition-of-done row
- Add cockpit overview lifecycle states
- Add persistent issue awareness rail
- Fold timeline into Activity tab
- Fold change surfaces into one tab
- Consolidate planning in Plan tab
- Build cockpit session workspace
- Merge crew summary into agent spine
- Enrich cockpit pipeline band
- Add prioritized needs-you slot
- Redesign issue cockpit header
- Fold issue tabs into six
- Run version ship during batch promotion
- Add batch version ship runner
- Add legacy conversation dir migration (PAN-3357)
- Add opt-in transcript retention sweep (PAN-3357)
- Reapply version sync project config
- Define version sync project config
- Workspace management actions in WorkspaceView (PAN-3330)
- Entry points for the new-workspace dialog (PAN-3330)
- New-workspace modal with live resolve-before-create preview (PAN-3330)
- Registry routes for resolve, create, relocate, project-targets (PAN-3330)
- Palette action to run the workspace command
- Workspace quick-action band above the panels
- Open workspace in file manager or configured editor
- Per-workspace run command storage and run session
- Workspace git-state and ff-pull routes
- Upstream-correct git state + ff-only pull module
- Open review mode from primary action
- Add review mode submenu actions
- Persist review mode before dispatch
- Show the agent's terminal tail under the generic question card (PAN-3090)

### Fixes
- Never provision the real ~/.claude/settings.json under a test runner
- Resolve the TTS venv from the active checkout when running from a deployment generation
- Salvage completed dead branches (#3613)
- Reconcile dead verify-paused agents (#3612)
- Replace stalled live sessions (#3611)
- Local-install-only tsc in the type ratchet scripts (PAN-3605)
- Never let lint-effect-diagnostics fall back to the npm registry (PAN-3605)
- Escalate repeated post-merge deploy failures (PAN-3601)
- Pin Node 22 PATH inside the post-merge deploy systemd unit
- Align strike and UAT doctrine (#3600)
- Scope pre-push guards to pushes targeting the overdeck repo
- Recover idle terminal sessions (#3598)
- Require CLI label retrieval (#3597)
- Re-gate on the test status the write would persist, not the row's (PAN-3512 review)
- The test-gate re-gate lands after caller extras (PAN-3512 review)
- Throttle state-plane reconciliation (#3588)
- Require named main CI checks (#3592)
- Fallback convoy-parent kill is conditional on the verdict landing (PAN-3512)
- Honor fresh session reset intent (PAN-3583) (#3590)
- Accept main CI for missing verification (#3585)
- Send CSRF header on project rename
- Preserve Docker enumeration failures
- Run cleanup fallback after effect defect
- Fail closed on workspace container guard
- Run test feedback write effect
- Run review feedback write effect
- Report token refresh effect failures
- Run context digest effect
- Timed-out completion check falls back to UNCLEAR instead of silent exit (PAN-3571)
- Cascade cancellation to AC children
- Bound feedback context
- Owed-rework handoffs resume warm — plain start refuses silent fresh (PAN-3555)
- Green main — park/release copy for review_parent_stalled_needs_you, baseline the sweeper source-level test (PAN-3551)
- Parked-population doc matches the observability-only sweeper; register sweep.recommendation
- Spawn placeholder only for genuinely new conversations
- Sweeper recommendations name the canonical doors and flag substrate bugs (PAN-3551)
- Sweeper and review-parent check become observability-only (PAN-3551)
- Boot reconciliation is primary-only (PAN-3547)
- Health classifier reads the effective activity oracle (PAN-3546)
- Review sweep ignores monitor state from dead attempts (PAN-3549)
- Keep peer dashboards from supervising CLIProxy (#3548)
- Drop two stale head-anchor-write-sites entries from the merge
- Convoy launch trusts the tmux liveness oracle over stale state rows (PAN-3545)
- WorkLog activity suppresses conversation empty states (PAN-3544)
- Patient lock retries for durable agent-plane writes (PAN-3513)
- Handed-off agents allow explicit --fresh — rework had no door (PAN-3543)
- Strike push-backs must land as durable issue comments first (PAN-3537, PAN-3540)
- Thrown delivery aborts reach resume's kill-on-failure path (PAN-3541)
- Keep the tmux server alive when a child is oom-killed (PAN-3539)
- Reload activation repoints the global pan CLI link (PAN-3538)
- Provision DashScope into omp's user model registry at spawn (PAN-3531)
- Boot proxy and use models probe for DashScope health checks (PAN-3531)
- Warm dedup, repo-boundary bypass, live metrics, honest docs (PAN-3534)
- Guard fresh reset before state wipe
- Make resume survive compaction
- Repair two frontend tests that were red before this branch (PAN-3511)
- See a burned token on the CLIProxy route (PAN-3528)
- Never poke a stopped agent — boot gates are not idle-alive stalls
- Cover the exec export state-migrate-cancellation's mock omits
- Restore green test baseline
- Reset-session clears the DB session pointer too — the stranded-pointer deadlock
- Un-red main gates after PAN-3513 merge — lint, mock, manifest, cycles
- Accept already-flushed GC tombstones
- Gate admin agent GC on live terminality
- Stream cost event log reads (PAN-3431 round 3) (#3494)
- Mock the prepareWorkspaceForAgentSpawn wrapper, not just its inner function
- Lock the two reviewedAtCommit write sites PAN-3512 left unaudited
- Audit agent GC before cleanup
- Restore stored catalog-only model after the picker catalog syncs
- Reconstruct missing Claude sessions
- Repair four test files left stale by PAN-3512's verdict door + today's explicit-harness refinement
- Clear stale close-out state on reopen
- Show real harness labels for kimi-code/acp conversations
- Unblock red main lint + latent test bugs surfaced by PAN-3512's merge
- Require live terminality before agent GC
- Unwrap bare await in legacy-routes Effect.gen — unbreaks dashboard server build
- Completed-handoff agents are not operator-parked — the pan-done false gate (PAN-3324 class)
- Pty-supervisor tees child output to its log — crash forensics stop dying with the pane
- Reject silent PUT /api/settings theme-change attempts (PAN-3410 cycle 9)
- Close the queued full-document theme-clobber gap (PAN-3410 cycle 8)
- Stop labeling the verification gate as 'testing' (PAN-1577)
- Close remaining PAN-3410 review blockers (cycle 6)
- Drop duplicate parked/velocity no-loss-matrix entries from main merge
- Verdict reader honors quick-mode review.md, not just convoy synthesis.md (PAN-3511)
- Orphan review reset must honor the synthesis artifact — the PAN-1577 verdict-wipe race (substrate)
- Route sync-main to strike worktrees (#3509)
- Unblock RED MAIN — parked.ts typecheck, composer manifest drift, and the stall-sweeper cycle (PAN-3501, PAN-3503) (#3507)
- Drop duplicate all-clear summary box, fill no-loss matrix gaps
- God view fills and re-layouts on browser zoom
- Event-driven orb liveness — God View reads the event stream, not the poller
- The flywheel-kill lesson — orchestrator idle exemption, answerable resume gates, honest active counts
- Restart-all skips one-shot roles instead of failing them (operator-reported '16 failed')
- Repair pan parked ack typo and regenerate stale slash-command manifest
- Resolve PAN-3410 review blockers — theme persistence race, nested-scope CSS leak, doc contradictions
- Seed ui field in tests/lib/settings-api.test.ts config mock
- God view fills the window again (layout regression)
- Honest thaws and honest deliveries — two credulity bugs the operator caught (PAN-3485)
- Unblock PAN-1577 pipeline rework — slash-commands drift + circular-deps baseline
- Sweeper warm-agent re-drive — message live agents, never duplicate-spawn (PAN-3487)
- Swap residency is not pressure — PSI-evidenced runway hold (operator-approved)
- Sweeper type errors — activity source, specialist union, Date.parse (PAN-3487)
- Reap merged slot sessions (#3493)
- Surface quality lint issues (#3484)
- Pass xbrief item ids to tiered handlers (#3483)
- God-view orbs — degraded/paused agents need a known-open issue
- God-view confluence operator-reported fixes — membership, layout, telemetry
- Preserve Confluence failure and lifecycle beats
- Close Confluence review gaps
- Reap merged slot sessions (#3480)
- Preserve Confluence orb identity
- Preserve session_id on agent tombstones (#3479)
- Keep queued blocker current (#3472)
- Remove the per-attach parcel watcher — server leak root cause (PAN-3431 round 2) (#3476)
- Separate readiness from DAG position (#3471)
- Prefer authored workspace spec (#3470)
- Restore commitment diagnostic (#3474)
- Exclude tombstoned agent rows from swarm slot occupancy (#3465)
- Name the agent and humanize durations in inactivity reasons
- Align residue test assertions with implementation contract
- Fix cycle-7 review findings - test callback mock and batch-reader state-home
- Fix cycle-5 review blocking findings
- Fix all cycle-4 review blocking findings
- Address all PAN-3396 review blocking findings
- Fix typecheck errors in workflows and residue integration
- Wire residue disposition through closeOut workflow and fix review findings
- Persist issue-level swarm opt-in on explicit pan swarm start (#3459)
- Recover reviewRunId so convoy dispatch stops dead-ending (PAN-3446) (#3449)
- Make checklist resolution asynchronous
- Preserve ohmypi completion fallbacks
- Preserve pi completion fallbacks
- Use checklist evidence in ohmypi
- Use canonical plan checklist evidence
- Verify the turn actually starts, not just the Enter keystroke (PAN-3422 round 2) (#3457)
- Pan swarm no longer refuses eligible plans containing sequential items
- Share parent CLIProxy cache scope with forked reviewers (PAN-1862)
- Avoid forbidden bd-ready fixture literal (#3452)
- Render absolute file paths as chips on cwd-less transcript surfaces
- Fill real MIN/doldrums issue titles into the god-view snapshot
- Finalize's stopped projection is atomic and observes post-kill liveness
- Animate confluence topbar meters via transform scaleX, not width
- Strip orphaned pre-rebrand PANOPTICON managed region
- Strip unmarked bd-onboard boilerplate from CLAUDE.md/AGENTS.md
- DerivePipelineState accepts canonical 'planned' for awaiting-work
- Enrichment poller stops resurrecting interactive-role agents
- Emit agent.status_changed(stopped) at successful complete-planning
- Survive stale deployment cwd (#3438)
- Enforce file-size guard at the pushed ref, not the working tree
- Pan start no longer inherits pending-work-spawn placeholder model
- Resolve effective project key + address review findings (PAN-1577)
- Bound conversation watcher memory (#3434)
- Composer text submits reliably — tighten blocking-menu detection (PAN-3422) (#3430)
- Issue-tree crew shows live review/test sessions (PAN-3416) (#3425)
- Adopt canonical writes during reconcile (#3421)
- Cockpit Terminal toggle must always render a visible state (PAN-3407) (#3415)
- Retire stale input fallback on activity (#3414)
- Keep infra contention out of verdicts (#3409)
- Address PAN-3367 review cycle 3 — strip all private identifiers
- Read injected clocks lazily (#3405)
- Address PAN-3367 review cycle 2 — evidence contract, MIN diff verification
- Auto-retire stuck flags, merged-aware /unstick, implement pan unstick (PAN-3393) (#3400)
- Address PAN-3367 review feedback — redact private evidence, add citations
- Capture reset-to-pending timestamp per PAN-3367 inspection finding
- Resolve FTS worker from dist root (#3394)
- Bind composer effort to conversation (#3391)
- Preserve restart successor handoff (#3387)
- Expose watchdog restart blockers (#3385)
- Serialize record journal rebuilds (PAN-2952) (#3384)
- Recover frozen warm-resumed reviewers (#3382)
- Retire stale needs-you rows (PAN-3379) (#3381)
- Recover dead convoy lanes (PAN-3368) (#3372)
- Keep dashboard restart moving after teardown races (PAN-3370) (#3371)
- Separate tracker issues from code forge (#3369)
- Make project config lock portable
- Harden project config locking
- Authorize exact version replacements
- Neutralize missing domain fallback (PAN-3363) (#3364)
- Constrain version propagation output
- Require durable version outputs
- Isolate version ship commands
- Route tombstones through agent write door (PAN-3357)
- Recover claims and disk-only linkage (PAN-3357)
- Clear cockpit stuck state
- Make version ship retries safe
- Retain agent linkage until expiry (PAN-3357)
- Settle version propagation safely
- Keep cockpit actions scoped
- Target exact cockpit sessions
- Preserve transcript expiry evidence (PAN-3357)
- Gate transcript expiry on terminal state (PAN-3357)
- Address cockpit review findings
- Preserve projects yaml without final newline
- Preserve arbitrary projects yaml indentation
- Address transcript retention review findings (PAN-3357)
- Guard agent state directory removal (PAN-3357)
- Route agent state cleanup through deletion door (PAN-3357)
- Add transcript-preserving state removal door (PAN-3357)
- Never purge conv-* dirs in cleanupStaleAgentState — they hold non-claude transcripts
- Keep the failed-fetch warning until a fetch succeeds
- Stop rendering an unreadable git status as "clean"
- Make run-session names unique per workspace
- Authenticate automatic dispatch
- Key the band per workspace and make stopping a run idempotent
- Archived main is still a main (PAN-3330)
- Address PAN-3331 review — command disclosure, run-session state, real restart
- Secure review mode dispatch
- Address cycle-2 review findings (PAN-3330)
- Enable close-out for limbo rows
- Reconcile post-merge pipeline lane
- Preserve FeatureItem size ratchet
- Render close-out limbo state
- Reconcile resource state labels
- Address review findings on workspace creation (PAN-3330)
- Register the band routes and run_command with the schema gates
- Resolve sync-sources from the checkout, not a frozen reload generation (#3349)
- Advance the merge queue instead of leaving rows queued forever (PAN-3328) (#3348)
- Only an operator stop may set stoppedByUser (#3346)
- Surface policy save failures
- Settle ownerless pending verification on merged issues (#3347)
- Stub client.issue in linear tracker tests after PAN-3337 (PAN-3342) (#3345)
- Cap frontend vitest threads pool at 4 local / 2 CI workers
- Let a non-claude conversation hand off
- Forward the conversation cwd to the kimi wire resolver
- Resolve Linear issues by identifier through client.issue, not search (PAN-3337)
- Key plan-ready on the written plan, not on a dead process
- Stream the kimi-code wire.jsonl instead of discovering forever
- Keep bot credentials outside the worktree that dies with them
- Resolve tree titles for issues that aged out of the tracker sync window (PAN-3337)
- Never let tsdown clean reach through the dist/node_modules deployment symlink (PAN-3336)
- Stop forcing /compact from Overdeck; gate resume on handed-off agents (PAN-3334)
- Repair kimi-alias test fallout left on main
- Stop indexing background-AI transcripts; open unregistered sessions
- Translate the kimi-code model id at the launcher chokepoint
- Retire the K2.5/K2.6 Kimi generation and fix the kimi-code alias gap
- Stop simple-mode issue titles overflowing their card
- Mark the provider's built-in harness as recommended
- A finished plan is "start work", not a fake question (PAN-2908)
- Discover the Kimi Code CLI in its installer directory
- Pin detached pan spawns to the loopback API url (PAN-3331)
- Classify generation occupants hard/soft; hard-pin the CLI's own generation (PAN-3329)
- Resolve bun before mutating and roll back on create failure
- Never build into a generation with live processes (PAN-3329)

### Performance
- Reuse cockpit query observers

_Plus 334 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.49.0
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.48.0...v0.49.0
