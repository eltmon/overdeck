## Summary
- Release 0.60.0 (stable)
- Built from v0.51.0...v0.60.0
- Published intentionally from main via tag promotion

## Highlights
### Features
- Pan install, sync, up and doctor own the Herdr host setup (#3995)
- One test run per push: tests on CI, touched tests locally, no single-feature batches (#3993)
- Add Claude Opus 5.5 support (PAN-3984)
- Add pan-open-threads, the checkable open-threads ledger
- Backlog page is one ordered list, no Now/Next/Later/Someday bands
- Per-issue pipeline journal, bot-authored review verdicts, verification pass delivery (PAN-3917 follow-up)
- Every reviewer ends with the same verdict line
- Deacon-lite re-dispatches stalled review convoys from the journal
- Pan show prints the pipeline journal
- Journal verification, review, and merge actions at the point of action
- Add append-only per-issue pipeline journal with pipeline.entry event
- The overdeck cut: remove the state layer, herdr default runtime (PAN-3917)
- Plan-freshness preflight before pan start spawns (PAN-3917)
- Merge pan admin migrate-plan-home verb (PAN-3917)
- Pan admin migrate-plan-home verb for the D8 state-worktree bridge
- Merge W8 Herdr and tmux adapters, launcher tokens, observe bridge (PAN-3917)
- Derive issue state and read the backend inventory (PAN-3917 W6)
- Add DerivedIssueState and BackendPane contracts (PAN-3917 W6)
- Herdr and tmux backend adapters, prompt guard, backend-routed launch and delivery (PAN-3917 W8)
- Backend registry, D10 selection, and terminal.backend config (PAN-3917 W2)
- Terminal backend contract types (PAN-3917 W2)

### Fixes
- Route planning, resume and recovery spawns through launchAgentPane (#3992)
- Opencode conversations start and resume on a Herdr host (#3991)
- Record supervised conversation lifecycle events (#3988)
- Strikes open a PR instead of waiting for a removed Deacon routine (#3987)
- Stop closes the Herdr pane through the terminal backend (#3989)
- Show saved OpenCode conversation history (#3986)
- Let handed-off agents resume their saved session
- Surface search request failures instead of rendering 'No results' (PAN-3975) (#3980)
- Stop per-issue git spawns on the sequence route (PAN-3969) (#3978)
- Drop dead issues FKs, fail assemblies loudly, stop after 3 consecutive failures (PAN-3963) (#3972)
- Pan-open-threads template carries the safe-to-close group and keyed rows
- Resolve OpenCode conversation transcripts
- Keep stalled status visible
- Prevent OpenCode subagent permission deadlocks
- Reap a finished sequencer pass through the terminal backend so retries stop being refused
- Main-verify expects the reject-planning-paths check, not the renamed guard job
- Codex host starts a fresh thread when the resume hits an active writer
- Count self-review blockers in the synthesis layout and never stall on zero
- Never prompt a host-backed harness through herdr agent.prompt
- Stalled-review recovery journals only a re-dispatch that launched
- Deliver verification pass to the work agent and stop the confirm/poll instructions
- Post verdicts as github app and fall back to a marker comment on self-review refusal
- Codex and acp readiness waits use backend-aware pane existence (PAN-3917)
- Plan-freshness preflight ignores files the plan intends to create (PAN-3917)
- Merge fix14 per-harness detection policy; pane-bound agents on Herdr (PAN-3917)
- A Herdr pane outlives its process — ask the foreground process, not the pane
- Keep delivery.ts within its file-size ratchet
- Merge fix12 pan done and PR webhook start the review pipeline; pan show backend liveness (PAN-3917)
- Pane-bound Herdr launches for host-process harnesses (PAN-3917 W12)
- The webhook starts review only for a feature/<issue> PR (PAN-3917)
- Pan show reads liveness from the selected terminal backend (PAN-3917)
- Pan done and the PR webhook both start the review pipeline (PAN-3917)
- Merge fix13 spawn-failure reason precedence; CI-shaped liveness mocks (PAN-3917)
- Scope the workspace access/existsSync mocks so they stop faking Herdr
- Stop markSpawnFailed clobbering the kickoff-failure reason
- Merge fix11 pty-supervisor is tmux-only, backend-aware liveness, failed-spawn state (PAN-3917)
- A failed spawn writes stopped + a reason, and records its pane (PAN-3917 W12)
- The PTY supervisor is tmux-only; Herdr owns its own liveness (PAN-3917 W12)
- Supervisor watchdog no longer restarts the dashboard on a missing deacon heartbeat (PAN-3917)
- Merge fix10 once-only primary-only table drop, peer spawns nothing, per-home Herdr session (PAN-3917)
- A peer dashboard starts nothing that can spawn an agent (PAN-3917)
- The pipeline-mirror drop runs once, in primary mode only (PAN-3917)
- Derive the Herdr session from OVERDECK_HOME so no isolated home reaches the live session (PAN-3917)
- Merge fix9a review findings: retention gate, ready set, verdicts, backend liveness, prompts (PAN-3917)
- Merge fix9b unified derived state, polyrepo task done, scoped migration commit (PAN-3917)
- Derived state finds dated specs, falls back to tmux, and reads the issue's own repo (PAN-3917)
- Read the PR from the workspace in pan-oversee, not an unset $REPO (PAN-3917)
- Point live prompts at the surviving verdict doors (PAN-3917)
- One tracker-aware derived issue state for the CLI and the server (PAN-3917)
- A reviewer verdict is the forge's review decision (PAN-3917)
- Fail a Herdr launch that never binds, and close the guard on unknown targets (PAN-3917)
- Count and reconcile agents from the selected backend inventory (PAN-3917)
- Revert the phase/workType/auto cleanAgentState change
- Pan task done spans every repo of the workspace and fails on a failed commit (PAN-3917)
- Migration keeps item status verbatim and commits only copied paths (PAN-3917)
- Stop cleanAgentState dropping phase/workType/auto on round-trip
- Fix duplicate derivedIssueStates key in styleguide e2e fixture
- Split agent-state reads into a leaf module, extend circular baseline
- Re-point lint guards at survivors of the state-layer cut
- Replace dead deacon-strike-landing chunk assertion with a live deacon-entry check
- Merge fix4 dashboard typecheck, guard, lint parse error, tests (PAN-3917)
- Merge migrate-plan-home open-issue fixes; GitHub pagination; Linear duplicate state (PAN-3917)
- The deacon panels read deacon-lite's status, not a patrol ledger (PAN-3917)
- Paginate GitHub issue listing and map GitHub/Linear open-filter correctly
- The polyrepo merge asks the forge which repos still need merging (PAN-3917)
- Uat fixture seeder re-initializes the activity event store
- ListAgentStatesSync resolves OVERDECK_HOME dynamically like its siblings
- Restore the linear_mcp_auth.healthy reactive wake dispatch
- A ship row needs a real ship agent again (PAN-3020, PAN-3917)
- Drop deleted specialist/work-agent stop-hook binaries from the copy list
- Re-point the server at the post-cut merge-set and lifecycle shapes (PAN-3917)
- Migration commit subject uses a commitlint scope (PAN-3917)
- Migrate-pan-home skips dangling symlinks in the state worktree (PAN-3917)
- The dispatch ceiling counts tmux-alive agents again (PAN-3917)
- An unreadable issue state keeps queued feedback, not drops it (PAN-3917)
- Register terminal-backend adapters before pan spawn resolves one
- Surface delivery.failure as MessageDeliveryOutcome.reason
- Gate the api-error pane read on the tmux adapter (PAN-3917 W6)
- Repair the errors the dashboard type ratchet caught (PAN-3917 W6)
- Read the forge from the repo config, not the tracker (PAN-3917 W6)
- Sender identity, stalled prompts, and agent-state events (PAN-3917 W8)
- Keep tmux-only session options on tmux and make spawn guards backend-aware (PAN-3917 W8)
- Batch the derived read, keep stateSince, add the GitLab forge (PAN-3917 W6)
- Route delivery to Herdr only for live Herdr agents (PAN-3917 W8)
- Make resolvePlanHome depth-stable, track .pan, rename migrated continues (PAN-3917 W1)

_Plus 218 internal changes (refactors, tests, tooling)._

## Breaking changes
- None explicitly called out in commit subjects. Review the full changelog before upgrading across versions.

## Install
```bash
npm install -g @overdeck/core@0.60.0
```

## Full changelog
- https://github.com/eltmon/overdeck/compare/v0.51.0...v0.60.0
