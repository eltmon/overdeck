<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened-runtime entitlements required for notarization.

    Electron needs JIT + writable/executable memory for V8. The desktop app
    ALSO re-execs its own bundled binary as a child process to run the
    dashboard server (see src/server.ts spawnServer → process.execPath), so
    library validation must be relaxed for that child to load its (differently
    signed / bundled) code, and dyld env vars must be permitted for the spawn.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
