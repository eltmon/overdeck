# PAN-3950 — Transcripts stay discoverable: keep agent state at close-out, drop `session.id`, issue panel uses the agent transcript route

**Issue:** https://github.com/eltmon/overdeck/issues/3950
**Baseline:** `main` at `cd294a6ac2d` (2026-09-20) for W1–W5; the fix pass below is written against branch `feature/pan-3950` at `e42bb79eb39` (PR #3951). Line numbers below were verified against that commit by the lifecycle audit (`.tmp/transcript-resume-audit.md`, Codex gpt-5.6-sol, 2026-09-20); if a number has drifted, search for the quoted identifier.
**Principle (from the PAN-3917 cut):** Overdeck stores no status it can derive. Append-only records of facts are allowed; mutable "current" pointers that something must keep true are not.

## Glossary

- **Agent directory** — `~/.overdeck/agents/<agent-id>/`. Holds `state.json` (launch metadata + last status), `sessions.json` (append-only list of harness session ids, written by the SessionStart hook), `session.id` (single "current" session pointer — **to be deleted by this PRD**), `lifecycle.log`, `activity.jsonl`, `codex-thread-id`, and for codex agents a per-agent `codex-home*/` with `sessions/**/rollout-*.jsonl` transcripts.
- **Claude transcript** — `~/.claude/projects/<cwd-slug>/<session-id>.jsonl`, written by Claude Code, never by Overdeck. Sacred: never deleted or truncated by Overdeck (CLAUDE.md).
- **Codex rollout** — `<agent dir>/codex-home*/sessions/YYYY/MM/DD/rollout-*.jsonl`. Its first record is `session_meta` with `cwd` = the workspace path.
- **Agent transcript route** — `GET /api/agents/:agentId/conversation` (`src/dashboard/server/routes/agents/conversation.ts:102-197`). Resolves the transcript from agent state (`getAgentJsonlPath`) and the launcher-pinned path.
- **Generic conversation route** — `GET /api/conversations/:name/messages` (`src/dashboard/server/routes/conversations.ts:466-480`) backed by `src/dashboard/server/services/conversation-reads.ts:526-682`. Looks up a `conversations` row by name; work agents never have one.
- **Close-out** — `pan close`; teardown in `src/lib/lifecycle/teardown-workspace.ts`, state deletion in `src/lib/agents/state-dir-removal.ts`.

## Problem

1. **Live agent, empty panel.** The issue Session tab builds a synthetic conversation named `agent-<issue>` and fetches it through the generic route (`ConversationPanel.tsx:243-265`, URL at `:1133-1147`). The exact-name lookup (`src/lib/conversations.ts:1019-1023`) misses because work-agent launch registers no conversation row (`src/lib/agents/spawn.ts:99-166`; rows only for specialists at `:247-254`, `:326-380`). The fallback scan may miss; the frontend converts the 404 into an empty "How can I help you?" instead of an error.
2. **Closed issue, no history.** `removeAgentStateDirs` keeps only regular files ending in `.jsonl` (`state-dir-removal.ts:33-93`, batch at `:178-220`; invoked by close-out at `teardown-workspace.ts:306-340`, reported at `:743-757`). `state.json`, `sessions.json`, `codex-thread-id` are deleted, so no issue→agent→session chain survives even though the Claude JSONL does.
3. **Pointer drift.** `session.id` is a single mutable pointer. Restart never clears it (`src/lib/agents/recovery.ts:218-234`); the hook appends the new id to `sessions.json` (`src/lib/agents/agent-state-service.ts:145-159`); resolvers prefer the stale `session.id`. `resume.ts:474-497` exists only to "repair" this pointer.
4. **Retention skips orphans.** `src/lib/agents/transcript-retention.ts:218-245` skips agent dirs without usable state, so a dir left with only `.jsonl` is never managed again.

## Requirements

- **FR-1** After `pan close`, the issue's Session tab lists every agent that ran on the issue (work, review, review lanes, test, planning, strike) and renders each transcript read-only, with the same fidelity as while live.
- **FR-2** While an agent is live or paused, the Session tab renders its transcript; a resolver miss renders an explicit message naming the agent id and the paths checked, never an empty chat.
- **FR-3** The issue panel reaches transcripts through the agent transcript route only. The generic route no longer attempts agent fallback resolution for `agent-*` names.
- **FR-4** `session.id` is neither written nor read anywhere. The current session of an agent is the newest entry of `sessions.json` (last line); "live" is decided by liveness (`src/lib/agents/liveness.ts`), not by the pointer.
- **FR-5** Close-out deletes only regenerable weight from an agent directory: `*.sock` references, `pending.lock`, and everything under `codex-home*/` except `sessions/`. It keeps `state.json`, `sessions.json`, `lifecycle.log`, `activity.jsonl`, `cv.json`, `codex-thread-id`, `context-receipt.json`, and all transcripts.
- **FR-6** Transcript retention enumerates agent directories regardless of whether `state.json` is present, ages them by the newest transcript mtime, and applies the configured `retention.transcript_days` to codex rollouts and the agent directory. It never touches `~/.claude/projects`.
- **NFR-1** No new store, table, cache, manifest, or module. Net lines deleted > lines added outside tests.
- **NFR-2** No polling added; the panel keeps its existing WS/RPC + fetch pattern.
- **NFR-3** Backward compatibility for one release: a reader that finds `session.id` but no `sessions.json` treats its content as the single entry; nothing writes `session.id`.

## Work items

Each item is one commit with an `Item: <id>` trailer, pushed before the next starts. Scope for commit subjects: `dashboard` (W3), `cloister` (W1, W4), `workspace` (W2 if touching agents lib use `cloister`), `docs`.

### W1 — Close-out keeps agent state (FR-5)

Files: `src/lib/agents/state-dir-removal.ts`, `src/lib/lifecycle/teardown-workspace.ts`, their tests (`tests/unit/lib/agents/state-dir-removal*.test.ts`, `tests/unit/lib/lifecycle/*teardown*.test.ts` — create if absent).

Before (`state-dir-removal.ts:33-93`): removes the agent directory, keeping only regular files named `*.jsonl`.
After: a `pruneAgentStateDir(dir)` that deletes exactly: `pending.lock`, any `*.sock`, and every entry under `codex-home*/` except `sessions/` (recursive). Everything else stays. Return `{ kept: string[], removed: string[], bytesFreed }`. `removeAgentStateDirs` calls it and its summary line becomes `Pruned N agent directories (transcripts and state kept)`. Teardown at `:306-340`/`:743-757` prints that line. Remove the "transcript files preserved" count.

Tests: a fixture dir with `state.json`, `sessions.json`, `codex-home-v2/sessions/2026/09/20/rollout-x.jsonl`, `codex-home-v2/cache/blob`, `pending.lock` → after prune only `cache/blob` and `pending.lock` are gone; `state.json` content unchanged.

### W2 — Delete `session.id`; `sessions.json` is the index (FR-4, NFR-3)

Files: `src/lib/agents/activity.ts` (`saveSessionId`, `getSessionId`, `resolveLatestSessionIdSync`; the PRD originally named a non-existent `src/lib/agents/agent-state-service.ts`) and `src/lib/session-history.ts` (the index reader/writer), `src/lib/agents/resume.ts` (Step 3 pointer repair `:474-497`, the `unlinkSync(... 'session.id')` at `~:404`), `src/lib/agents/recovery.ts` (`:218-234`), `src/lib/agents/spawn.ts` (wherever `saveSessionId` is called at launch), the `session-start` hook under `sync-sources/hooks/` (must only append to `sessions.json`), and every `grep -rn "session.id" src sync-sources --include=*.ts` hit outside tests.

Rules:
- `saveSessionId(agentId, id)` appends `{ sessionId, at, source }` to `sessions.json` (dedupe on sessionId) and writes nothing else.
- `getLatestSessionIdSync(agentId)` = last entry of `sessions.json`; if the file is absent and a legacy `session.id` exists, return its content (NFR-3). Never create `session.id`.
- `resolveLatestSessionIdSync` loses its `needsPointerRepair` branch; `resume.ts` deletes the block at `:474-497` and the `session.id` unlink; restart (`recovery.ts:218-234`) allocates the fresh Claude session id and appends it to `sessions.json` **before** launching, so the newest entry is the running one.
- `getAgentJsonlPath` (used by the agent transcript route) resolves from the newest `sessions.json` entry whose JSONL exists; falls back to older entries.

Tests: `tests/unit/lib/agents/session-index.test.ts` (new): append/dedupe, newest-wins, legacy `session.id` read-only fallback, restart ordering (entry present before launch). Update every test that asserts on `session.id` writes.

### W3 — Issue panel uses the agent transcript route; no fallback, no swallow (FR-1, FR-2, FR-3)

Files: `src/dashboard/frontend/src/components/chat/ConversationPanel.tsx` (`:243-265`, `:1133-1147`), the `DrawerAgentSession` component that builds the agent-backed conversation (grep `DrawerAgentSession`), `src/dashboard/server/services/conversation-reads.ts` (`:526-682`), `src/dashboard/server/routes/agents/conversation.ts` (`:102-197`), tests beside each.

Rules:
- When the panel's conversation is agent-backed (it already receives `agentId`), fetch `GET /api/agents/${agentId}/conversation` and render its messages with the existing renderer. The generic `/api/conversations/:name/messages` is used only for real conversations.
- Delete from `conversation-reads.ts` the specialist/session-file fallback and the global session-UUID scan for `agent-*` names. A generic lookup miss returns 404 with `{ error, lookedUp: name }`.
- Delete the frontend branch that converts an agent-backed 404 into an empty list. On a miss the panel renders: `No transcript found for <agentId>. Checked: <paths>` using the `checked` array the agent route returns (add it to its 404 body: the `sessions.json` entries tried and the launcher-pinned path).
- Closed issue: the Session tab's agent list comes from `listAgentStates` filtered by issue (state dirs now survive, W1), including stopped agents, ordered work → review → review lanes → test → planning → strike; each row loads through the agent route.

Tests: frontend `ConversationPanel.test.tsx`/`DrawerAgentSession` test: agent-backed conversation calls the agent route; 404 renders the explicit message with the checked paths. Server: `conversation-reads` test asserting the fallback code is gone (404 for an unknown `agent-*` name); agent route test for a stopped agent with two `sessions.json` entries where only the older JSONL exists → resolves the older one.

### W4 — Retention enumerates all agent dirs (FR-6)

File: `src/lib/cloister/transcript-retention.ts` (was `src/lib/agents/transcript-retention.ts:218-245` on `main`) and its test.
Remove the "skip when no usable state" condition; age = newest mtime among `codex-home*/sessions/**/*.jsonl` and `activity.jsonl`; when older than `retention.transcript_days`, delete the codex rollouts and then the directory. Assert in a test that nothing under `~/.claude` is ever a candidate (the module must not build such a path).

### W5 — Docs (required)

- `docs/DASHBOARD-ARCHITECTURE.md`: transcript read path = agent route → `sessions.json` newest entry → JSONL; generic route is for real conversations only.
- `docs/MERGE-WORKFLOW.md` close-out section: what close-out prunes vs keeps.
- `docs/THE-CUT.md`: add rows "`session.id` — deleted; `sessions.json` (append-only) is the session index" and "close-out no longer deletes agent state directories".
- `CLAUDE.md` Key Invariants: one line — "`sessions.json` is the append-only session index; there is no `session.id`; close-out prunes caches, never state or transcripts."
- Inline module headers of `state-dir-removal.ts`, `agent-state-service.ts`, `conversation-reads.ts`.

## Acceptance criteria

- **AC-1 (W1)** After `pan close` on a fixture issue, `~/.overdeck/agents/agent-<id>/state.json` and `sessions.json` exist; `codex-home*/sessions/` exists; `codex-home*/cache` does not.
- **AC-2 (W2)** `grep -rn "session\.id" src sync-sources --include=*.ts --include=*.sh | grep -v __tests__ | grep -v "legacy"` returns only the NFR-3 read fallback. Restarting an agent leaves `sessions.json`'s last entry equal to the running session.
- **AC-3 (W3)** With the dashboard on the branch: a live work agent's Session tab renders its transcript; a closed issue's Session tab lists its agents and renders each; deleting the JSONL by hand renders the explicit "No transcript found … Checked: …" message. No request to `/api/conversations/agent-*` appears in the network log for the issue panel.
- **AC-4 (W4)** Retention test: a dir with only rollouts older than the configured days is removed; a dir with a newer `activity.jsonl` is kept; no path under `~/.claude` is ever computed.
- **AC-5 (W5)** The four docs and CLAUDE.md line are updated in the same PR.
- **AC-6 (NFR-1)** `git diff --stat origin/main` shows net deletions outside `tests/` and `docs/`.

## Intersecting repo rules (restated)

- Never delete or truncate files under `~/.claude/projects` (sacred JSONL).
- No `execSync` in server-reachable code; async fs where the caller is async.
- Fake timers for any delay-based test.
- Work in the worktree `workspaces/feature-pan-3950` on branch `feature/pan-3950`; commit per item with `Item: W<n>` trailer; push after each; quality gates before the PR: `npm run typecheck`, `npm run lint`, touched vitest files. Do not run the full suite locally; CI does.
- Open the PR against `main`; do not merge.

---

## Fix pass (round 6) — resolve the round-5 review blockers on `feature/pan-3950`

**Baseline for this section:** branch `feature/pan-3950` at `e42bb79eb39` (PR #3951). Line numbers were verified against that commit on 2026-09-20; if one has drifted, search for the quoted identifier. **Do not recut from `main`.** Every item below is a commit on the existing branch carrying the `Item: <xbrief-item-id>` trailer that `pan task` matches (`src/lib/xbrief/continue-state.ts:305`), e.g. `Item: w6-watcher-retry`, `Item: w8-adapters-use-resolver` — not the W-number. Push before the next item starts.

The five review rounds live at `/home/eltmon/Projects/overdeck/.tmp/pan-3950-review.md` and `pan-3950-review-round{2,3,4,5}.md`; the PR body carries `## Review round 1..4` resolution notes. Round 5 (`pan-3950-review-round5.md`) returned DO NOT MERGE with five blockers and one non-blocking finding. CI run 35512412319 was green on `d11dd21719f` (same code as the branch head minus a stray file), so every blocker is a design or test-strength gap, not a red test.

### Glossary additions

- **Synthetic agent stream** — the `/ws/rpc` conversation stream the issue Session tab opens for an agent-backed panel (`streamSyntheticAgentTranscript`, `src/dashboard/server/ws-rpc.ts:345-365`). It starts by emitting `{ kind: 'discovering' }` and waits for `watchForAgentTranscriptCandidate` (`ws-rpc.ts:271-343`) to yield the first existing transcript candidate.
- **Watch root** — a directory `watchForAgentTranscriptCandidate` attaches `fs.watch` to while no transcript exists: the agent's `sessions.json` directory, the parent of every candidate path, and the harness-owned runtime roots from `listAgentTranscriptWatchRoots` (`src/lib/agents/transcript-resolver.ts:197-223`).
- **Reset marker** — the file `~/.overdeck/agents/<id>/session-reset` (`SESSION_RESET_MARKER`, `src/lib/session-history.ts:16`). Written by `resetSessionIndex` (`:229-243`) together with an append-only `{reset:true}` record in `sessions.json`; while it exists, `listAgentTranscriptCandidates` (`transcript-resolver.ts:232`) and `resolveLatestSessionIdSync` (`src/lib/agents/activity.ts:193`) return nothing. Cleared only by `markAgentRunning` (`src/lib/agents/agent-state.ts:706-725`).
- **Central resolver** — `src/lib/agents/transcript-resolver.ts`, the one async transcript resolver. Per-harness path helpers it exports: `resolveCodexRolloutPath`, `resolveAcpTranscriptPath` (`:170-176`), `resolveKimiWirePath` (`:178-187`), `resolvePiSessionPath` (`:189-194`).
- **Transcript adapter** — `src/lib/conversations/transcript-adapter.ts`; per-harness `resolveSessionFile(conv)` + `serializeTranscript` used by summary forks and handoffs (`src/lib/overdeck/conversation-forks.ts:706-719`, `src/lib/conversations/summary-fork.ts:625-626`).
- **Production lines** — lines in the `origin/main...HEAD` numstat after excluding tests (`tests/`, `__tests__/`, `*.test.ts(x)`), Markdown (`*.md`, `docs/`, `CLAUDE.md`), and `.tmp` artifacts. The exact command is in AC-11.

### Requirements (fix pass)

- **FR-7** A watch root whose `fs.watch` attachment fails is retried on the next filesystem event delivered by any surviving watcher; a permanently failing root never causes a resolve loop (bounded resolver calls per event). No timer, interval, or poll is added.
- **FR-8** `markAgentRunning` fails (throws, status unchanged) when the reset marker exists and cannot be removed for any reason other than `ENOENT`. A missing marker is the normal case and stays silent.
- **FR-9** Transcript adapters for Pi/OhMyPi, ACP, and Kimi resolve their session file through the central resolver's helpers. `transcript-adapter.ts` contains no `readdir`, `existsSync`-on-agent-dir, or harness path formula for those three harnesses.
- **FR-10** The equal-mtime Pi tie-break test fails when the tie-break is removed from `findPiTranscriptPath` (`src/lib/runtimes/pi.ts:137`), i.e. walk order differs from path order in the fixture.
- **FR-11** `sessions.json` reset framing is covered for an unterminated final JSONL record, not only for a legacy array.
- **NFR-4** Production lines of `origin/main...HEAD` are net negative at the end of the pass (AC-6 restated with the exact measurement).
- **NFR-5** Exactly one async transcript resolver exists (`src/lib/agents/transcript-resolver.ts`); no item adds a second scan of harness paths anywhere. Per-harness path formulas stay in `src/lib/runtimes/*`.
- **NFR-6** `src/lib/agent-enrichment.ts` `*Promise` + Effect-wrapper pairs are PAN-3958's; no item touches them.

### Decisions (made here so the executor does not re-decide)

- **D-1 Watcher bookkeeping is per root, not a joined signature string.** Keep a `Map<string, { close(): void }>` keyed by `${root}:${recursive}`. Each `resolveCandidate` pass computes the desired root set, closes watchers whose key is no longer desired, and attaches only roots that have no live watcher. `rerun = true` is set **only when at least one new watcher was attached this pass** (that closes the listing→watch race). A root whose `watch()` throws is left unattached and is retried on the next pass — which is triggered by an event on any surviving watcher. Rationale: if mismatch-versus-desired set `rerun`, a permanently failing root would spin `resolveCandidate` in a tight loop.
- **D-2 All-fail case is accepted.** If every root fails to attach there is no event source; the stream stays in `discovering`, the failure is logged once with `console.warn('[conv-stream] no transcript watch roots attached for <agentId>')`, and a client re-subscribe restarts discovery. No fallback timer.
- **D-3 The reset marker stays in this pass.** Deriving "reset in effect" from `sessions.json` alone (last record is a reset) would change resume semantics for harnesses whose session id is observed after launch (`acp-session-id`, OhMyPi freshest JSONL in `resolveLatestSessionIdSync`, `activity.ts:193-230`). That is a separate design change; here the marker only fails loudly.
- **D-4 `markAgentRunning` clears the marker before mutating `state.status`**, right after `assertAgentCanTransitionToRunning`, so a thrown unlink error leaves the in-memory state untouched and the caller never saves a `running` status.
- **D-5 Adapter delegation passes the conversation's cwd explicitly for Kimi** — `resolveKimiWirePath(conv.tmuxSession, { workspaceOverride: conv.cwd })` — because conversations have no `state.json` workspace to read.
- **D-6 The review's blocker-4 claim is factually wrong and the resolver is not changed.** `jsonl-resolver.test.ts` passes `agentsDir` (the agents **root**, `:31`) as `agentsDirOverride`; the resolver joins the agent id once. The file passes 43/43 and CI `test` is green. The real weakness: `findPiTranscriptPath` walks `sessions/` with name-sorted entries, and ES2019 `Array.prototype.sort` is stable, so two tied files in one directory already come out in path order without the `|| a.path.localeCompare(b.path)` tie-break. The test must put the lexically-later file where it is walked **first** (`sessions/z.jsonl`) and the lexically-earlier file where it is walked **second** (agent-dir root `a.jsonl`), so only the tie-break can produce the expected answer. `src/lib/runtimes/pi.ts` is not edited.
- **D-7 Net-line budget is enumerated, with a fallback list.** Expected deletions: W8 (adapter delegation) ≈ −33 net; W6 helper collapse ≈ −9; W7 dead `isAgentSessionReset` ≈ −5; `readSessionIdHistorySync` (zero callers, `session-history.ts:187-189`) −3. Expected additions: W6 ≈ +2, W7 ≈ +3. Projected net ≈ 49 − 50 + 5 = **+4 → must go negative**, so W11 draws from the fallback list until AC-11 prints a negative number: (a) collapse `createFreshSessionIdentity`'s five-line lifecycle log (`session-history.ts:250-255`, the `indexPersisted=` line is always true after a successful append) to one line; (b) inline `readLegacySessionIdSync` into its only caller `readSessionIndexWithLegacySync` (`:180-196`); (c) trim the three-line comment above `clearSessionResetMarker(state.id)` in `agent-state.ts:721-723` (three comment lines above `clearSessionResetMarker`) to one line. Anything beyond that list is a planning gap — stop and report, do not invent deletions.

### Work items (fix pass)

#### W6 — Watcher attachment retries on the next event (FR-7; round-5 blocker 1)

Files: `src/dashboard/server/ws-rpc.ts` (`nearestExistingDirectory` `:243-251`, `nearestExistingWatchRoot` `:253-261`, `watchForAgentTranscriptCandidate` `:271-343`), `src/dashboard/server/__tests__/ws-rpc-resolved-stream.test.ts` (`describe('synthetic agent transcript discovery')` `:153-199`).

Before (`ws-rpc.ts:311-320`):
```ts
const watched = [...roots].sort(([a], [b]) => a.localeCompare(b));
const signature = watched.map(([path, recursive]) => `${path}:${recursive}`).join('\0');
if (signature !== watchedRoots) {
  watchedRoots = signature;
  closeWatchers();
  for (const [root, recursive] of watched) {
    try { watchers.push(watch(root, { recursive }, () => void resolveCandidate())); } catch { /* retry on another event */ }
  }
  rerun = true; // close the listing→watch race
}
```
After:
```ts
const desired = new Map([...roots].map(([root, recursive]) => [`${root}:${recursive}`, { root, recursive }]));
for (const [key, watcher] of watchers) {
  if (desired.has(key)) continue;
  try { watcher.close(); } catch { /* already closed */ }
  watchers.delete(key);
}
let attached = false;
for (const [key, { root, recursive }] of desired) {
  if (watchers.has(key)) continue;
  try {
    watchers.set(key, watch(root, { recursive }, () => void resolveCandidate()));
    attached = true;
  } catch { /* retried on the next event from a surviving watcher */ }
}
if (attached) rerun = true; // close the listing→watch race
else if (watchers.size === 0 && desired.size > 0) console.warn(`[conv-stream] no transcript watch roots attached for ${agentId}`);
```
`watchers` becomes `const watchers = new Map<string, { close(): void }>()`; `watchedRoots` is deleted; `closeWatchers` iterates `watchers.values()` and then `watchers.clear()`. Collapse the two helpers into one: `nearestExistingWatchRoot(path)` stays. For the index and candidate paths use `nearestExistingWatchRoot(dirname(path))?.path` and register it with `recursive: false` (keep the `roots.set(root, roots.get(root) ?? false)` merge); only runtime roots consume `.recursive`. Never enable recursion on `~/.overdeck/agents/<id>` itself — that would pull `codex-home*` churn into the event stream. Delete `nearestExistingDirectory`.

Tests (same `watch` mock shape as `:164-171`):
1. `retries a watch root whose attachment failed once an event arrives on a surviving watcher` — the mock throws on the first call for root R2, succeeds for R1; fire R1's listener; assert a second `watch` call for R2 occurred and the discovery still completes after R2's listener fires with the transcript present.
2. `does not loop when a watch root always fails` — the mock always throws for R2 and succeeds for R1. Let the initial pass settle (the first pass attaches R1 ⇒ `rerun` ⇒ one more pass; R2 throws in both), record `watch` calls for R2 as `before`; fire R1's listener once; assert R2's count is exactly `before + 1` and that no further calls happen without another event; under `vi.useFakeTimers()` assert `vi.getTimerCount() === 0` to prove no polling. Assert relative growth, never an absolute count.
3. Existing `:154-197` test keeps passing (watchers all closed on unsubscribe — now `watchers.size === 0`).

#### W7 — Reset-marker removal fails loudly (FR-8, FR-11; round-5 blocker 2 + non-blocking 1)

Files: `src/lib/session-history.ts` (`clearSessionResetMarker` `:22-28`, `readSessionIdHistorySync` `:187-189`), `src/lib/agents/agent-state.ts` (`markAgentRunning` `:706-725`), `src/lib/agents/session-pointers.ts` (`isAgentSessionReset` `:13-15`, imports `:1`), `src/lib/agents/__tests__/session-pointers.test.ts` (`:24`, `:59`), `tests/unit/lib/agents/session-index.test.ts`.

Before (`session-history.ts:22-28`):
```ts
export function clearSessionResetMarker(agentId: string): void {
  try {
    unlinkSync(join(getOverdeckHome(), 'agents', agentId, SESSION_RESET_MARKER));
  } catch {
    // The marker is absent for normal launches.
  }
}
```
After:
```ts
export function clearSessionResetMarker(agentId: string): void {
  try {
    unlinkSync(join(getOverdeckHome(), 'agents', agentId, SESSION_RESET_MARKER));
  } catch (error) {
    // The marker is absent for normal launches; anything else must not let
    // a launch report success while candidates stay blocked.
    if ((error as NodeJS.ErrnoException).code !== 'ENOENT') throw error;
  }
}
```
In `markAgentRunning`, move `clearSessionResetMarker(state.id);` to immediately after `assertAgentCanTransitionToRunning(state);` (before `const oldStatus = state.status;`), and shorten its comment to one line: `// Fails loudly (D-4): a blocked launch must not report running.` Delete `readSessionIdHistorySync` (zero callers). Delete `isAgentSessionReset` from `session-pointers.ts` and its now-unused `existsSync`/`SESSION_RESET_MARKER` imports if nothing else in the file uses them (`cleared.push(SESSION_RESET_MARKER)` at `:41` still does — keep that import); repoint `session-pointers.test.ts:24,59` to `isSessionResetMarker` from `session-history.js`.

Tests (`tests/unit/lib/agents/session-index.test.ts`):
1. `markAgentRunning throws and leaves status unchanged when the reset marker cannot be removed` — create a **directory** at `<agentDir>/session-reset` (`mkdirSync`), call `markAgentRunning(state)` with `state.status = 'stopped'`; expect it to throw and `state.status` to still be `'stopped'`; the directory still exists.
2. `markAgentRunning is silent when no marker exists` — no file; status becomes `running` (extend the existing `:176-195` test or add one).
3. `reads a reset that follows an unterminated final JSONL record` — write `sessions.json` as `{"sessionId":"s1",...}` with **no trailing newline**, call `resetSessionIndex`, assert `readSessionIndexSync` → `[]`, `latestSessionResetTime(raw)` not null, and the raw file matches `/\}\n\{"reset":true,/`.

#### W8 — Transcript adapters resolve through the central resolver (FR-9, NFR-5; round-5 blocker 3)

Files: `src/lib/conversations/transcript-adapter.ts` (header `:3-27`, imports `:28-40`, `resolvePiSessionFileFromTmux` `:141-160`, `piAdapter.resolveSessionFile` `:214-216`, `acpAdapter.resolveSessionFile` `:312-315`, `resolveKimiWireFileFromTmux` `:357-370`, `kimiAdapter.resolveSessionFile` `:458-460`), `src/lib/conversations/__tests__/transcript-adapter.test.ts`.

Before (`:214-216`, `:312-315`, `:458-460`):
```ts
async resolveSessionFile(conv) { return resolvePiSessionFileFromTmux(conv.tmuxSession); },
...
async resolveSessionFile(conv) {
  const path = join(getOverdeckHome(), 'agents', conv.tmuxSession, 'acp-session.jsonl');
  return existsSync(path) ? path : null;
},
...
async resolveSessionFile(conv) { return resolveKimiWireFileFromTmux(conv.tmuxSession, conv.cwd); },
```
After:
```ts
async resolveSessionFile(conv) { return resolvePiSessionPath(conv.tmuxSession); },
...
async resolveSessionFile(conv) { return resolveAcpTranscriptPath(conv.tmuxSession); },
...
async resolveSessionFile(conv) { return resolveKimiWirePath(conv.tmuxSession, { workspaceOverride: conv.cwd }); },
```
Import `resolveAcpTranscriptPath, resolveKimiWirePath, resolvePiSessionPath` alongside the existing `resolveCodexRolloutPath` import from `../agents/transcript-resolver.js` (`:35`). Delete `resolvePiSessionFileFromTmux` and its doc comment (`:141-160`), `resolveKimiWireFileFromTmux` and its doc comment (`:357-370`), and the imports that become unused: `readdir` (keep `readFile`), `homedir`, `getOverdeckHome` (keep `sessionFilePath`), `findKimiWirePathAsync`, and `join` from `node:path` (every remaining `.join(` in the file is `Array.prototype.join`). `existsSync` stays for the Claude adapter (`:114`). `runtimes/kimi-code.ts:52` re-exports `findKimiWirePathAsync` from `kimi-context-envelope.ts`, so the resolver and the old adapter used the same function; the parity test is the authority. `acpTranscriptPath(id, root)` is `join(root, id, 'acp-session.jsonl')` (`src/lib/acp/transcript.ts:5-10`), so the ACP path is unchanged. Rewrite the header sentence at `:24-26` to: "Adding a new harness is a `resolveSessionFile` that delegates to `src/lib/agents/transcript-resolver.ts` (the only place that interprets agent directories) plus a `serialize` function and a registry entry."

Tests (`transcript-adapter.test.ts`, using a temp `OVERDECK_HOME`):
1. `ohmypi adapter resolves a nested Pi transcript under sessions/<encoded-cwd>/` — write `agents/<tmux>/sessions/-home-x-ws/2026-09-20T00-00-00-000Z_abc.jsonl`; `getTranscriptAdapter('ohmypi').resolveSessionFile(conv)` returns that path (this is the case that returned HTTP 400 through `handleConversationSummaryFork`).
2. `pi, acp, and kimi adapters return exactly what the central resolver returns` — for each harness, assert `adapter.resolveSessionFile(conv)` equals `resolvePiSessionPath(id)` / `resolveAcpTranscriptPath(id)` / `resolveKimiWirePath(id, { workspaceOverride: conv.cwd })` on the same fixture (kimi fixture: `~/.kimi-code` root via the resolver's `kimiHomeOverride` is not available through the adapter, so set `HOME` to the temp dir for that case, matching how `resolveKimiWirePath` derives `join(homedir(), '.kimi-code')`).
3. `transcript-adapter.ts contains no agent-directory scan` — `readFileSync` the module source and assert it does not match `/readdir\(|'sessions'\)|kimi-session-id|acp-session\.jsonl/`.

#### W9 — Pi equal-mtime test discriminates the tie-break (FR-10; round-5 blocker 4)

File: `src/dashboard/server/routes/__tests__/jsonl-resolver.test.ts` (`:668-680`). **`src/lib/runtimes/pi.ts` is not edited (D-6).**

Before (`:668-680`): both tied files live in `sessions/` (`a.jsonl`, `z.jsonl`); walk order equals path order, so the assertion holds without the tie-break.
After: `sessions/z.jsonl` (walked first by `findPiTranscriptPath`, `pi.ts:109-128`) and root `<agentDir>/a.jsonl` (walked second), both `utimes` to the same instant; expect `resolvePiSessionPath(PI_AGENT_ID, { agentsDirOverride: agentsDir })` to resolve to `join(agentDir, 'a.jsonl')`. Add a one-line comment stating why the two files sit in different directories. Keep the test name.

Verification checkpoint for the executor (no commit): temporarily delete `|| a.path.localeCompare(b.path)` on `pi.ts:137`, run the file, confirm this one test fails, restore the line (`git checkout -- src/lib/runtimes/pi.ts`), run again, confirm 43/43. Do not leave `pi.ts` modified.

#### W10 — Docs for the fix pass (required)

Files: `docs/DASHBOARD-ARCHITECTURE.md` (the "Conversation loading and delivery" bullets at `:162-171`), `src/lib/conversations/transcript-adapter.ts` header (done in W8; listed here for the docs gate), PR #3951 body.

- `docs/DASHBOARD-ARCHITECTURE.md`: after the `GET /api/agents/:agentId/conversation` bullet add two bullets: (1) "One async resolver (`src/lib/agents/transcript-resolver.ts`) serves the agent route, the `/ws/rpc` synthetic agent stream, enrichment, and the summary-fork/handoff transcript adapters; per-harness path formulas live only under `src/lib/runtimes/`." (2) "Before a transcript exists the synthetic stream watches the index directory, candidate parents, and harness runtime roots with `fs.watch`; a root whose watch fails to attach is retried on the next event from a surviving watcher. There is no timer or poll; if no root attaches the stream stays in `discovering` until the client re-subscribes."
- PR #3951 body: append `## Review round 5` with the same resolution-table format as `## Review round 4` (finding → resolved/partially/not resolved → file:line evidence), including the D-6 statement for blocker 4 and the measured AC-11 number for blocker 5. Use `gh pr edit 3951 --body-file <file>` after `gh pr view 3951 --json body --jq .body > <file>` so rounds 1–4 are preserved.

#### W11 — Production lines net negative (NFR-4, AC-6; round-5 blocker 5)

Files: only those named in D-7's fallback list — `src/lib/session-history.ts`, `src/lib/agents/agent-state.ts`.

Run AC-11's command after W6–W9 land. If the net is still ≥ 0, apply the D-7 fallback items in order (a → b → c), one commit, re-measure. If the net is still ≥ 0 after all three, stop and report (`pan tell` per role instructions) — do not invent further deletions.

### Acceptance criteria (fix pass)

- **AC-7 (W6)** `npx vitest run src/dashboard/server/__tests__/ws-rpc-resolved-stream.test.ts` passes with the two new tests present; `grep -c "setInterval\|setTimeout" src/dashboard/server/ws-rpc.ts` prints `1` (the count at `e42bb79eb39`); `grep -c "nearestExistingDirectory" src/dashboard/server/ws-rpc.ts` prints `0`.
- **AC-8 (W7)** `npx vitest run tests/unit/lib/agents/session-index.test.ts src/lib/agents/__tests__/session-pointers.test.ts tests/unit/lib/agents/agent-state-gate.test.ts` passes; the directory-at-marker test proves `markAgentRunning` throws with `state.status` unchanged; `grep -n "isAgentSessionReset\|readSessionIdHistorySync" src tests -r` prints nothing.
- **AC-9 (W8)** `npx vitest run src/lib/conversations/__tests__/transcript-adapter.test.ts src/dashboard/server/routes/__tests__/conversations-fork-recovery.test.ts` passes; `grep -n "readdir\|homedir\|getOverdeckHome\|findKimiWirePathAsync\|FromTmux\|from 'node:path'" src/lib/conversations/transcript-adapter.ts` prints nothing; the nested-Pi adapter test returns the `sessions/<encoded>/...` path.
- **AC-10 (W9)** With `|| a.path.localeCompare(b.path)` removed from `src/lib/runtimes/pi.ts:137` the tie-break test fails; with the line restored `npx vitest run src/dashboard/server/routes/__tests__/jsonl-resolver.test.ts` reports 43 passed; `git diff --stat origin/main...HEAD -- src/lib/runtimes/pi.ts` is unchanged from `e42bb79eb39`.
- **AC-11 (W11, NFR-4)** `git diff --numstat origin/main...HEAD | grep -vE '(^|/)(tests?|__tests__)/|\.test\.tsx?$|\.md$|docs/|\.tmp' | awk '{a+=$1; d+=$2} END {print a-d}'` prints a negative integer. Record the number in the PR body's round-5 table.
- **AC-12 (W10)** `docs/DASHBOARD-ARCHITECTURE.md` contains the string `transcript-resolver.ts` and the phrase `retried on the next event`; `gh pr view 3951 --json body --jq .body | grep -c "## Review round 5"` prints `1`.
- **AC-13 (all)** `npm run typecheck` and `npm run lint` pass on the final head; CI on PR #3951 is green.

### Intersecting repo rules (restated for the fix pass)

- Event-driven discovery only — no `setTimeout`/`setInterval`/polling in `ws-rpc.ts`; any test that needs timers uses `vi.useFakeTimers()` (W6 uses `vi.getTimerCount()` to prove none exist).
- Exactly one async transcript resolver; per-harness path formulas only in `src/lib/runtimes/*`.
- `src/lib/agent-enrichment.ts` Promise/Effect pairs belong to PAN-3958 — do not touch.
- Never `git stash`; never `git checkout <branch>` in the workspace; `git branch --show-current` must print `feature/pan-3950` before the first edit.
- Commit per item with the `Item: <xbrief-item-id>` trailer (e.g. `Item: w7-reset-marker-fails-loud`); push after each; `npm run typecheck`, `npm run lint`, and the touched vitest files before pushing. Do not run the full suite locally; CI does. Do not merge.
