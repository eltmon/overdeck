# PRD — Backlog Sequencer

**Issue:** [PAN-1866](https://github.com/eltmon/overdeck/issues/1866) ·
**Status:** draft · **Author:** Claude (Opus 4.8) · **Date:** 2026-06-13
**Mockup:** [`docs/design/mockups/backlog-sequencer-scaled-opus.html`](../../docs/design/mockups/backlog-sequencer-scaled-opus.html) (canonical — 522-issue scaled view).
The earlier focused-DAG mockup (`backlog-sequencer-opus.html`) was removed from the tree; it remains recoverable in git history at commit `cc465e7f8`.

> This PRD is the implementation source of truth. It is written to be executed by an
> implementation agent that may run on a cheaper model than authored it — every file
> path and line reference below was checked against the codebase on 2026-06-13.

---

## 1. Vision

An AI evaluates the **entire open backlog** and produces one **planned sequence** of every
issue — ranked by real impact, sized by effort, wired into a dependency DAG. A new dashboard
page renders that sequence so you can see the whole backlog, in order, at a glance, with
visual signals for what is already in the pipeline, what has a PRD, what is ready to pick up,
and which issues the AI thinks need refinement or are no longer relevant.

The plan-of-record is a **repo-tracked markdown file** (`.pan/backlog/sequence.md`), committed
to `main`, so anyone who checks out the repo reproduces the identical visualization. SQLite is
a derived cache only.

This is the planning complement to the **Flywheel** (which keeps agents *working* on the next
item, by fixed rules). The Sequencer answers *"what is the next item, across the entire backlog,
and why?"* and makes that answer durable, inspectable, and reproducible.

## 2. Glossary

- **Backlog** — every open issue for a project across configured trackers (today: GitHub for PAN),
  excluding closed/cancelled. Pulled via `src/lib/tracker/`.
- **Sequence** — a total ordering of the backlog by AI-assessed importance, expressed as a DAG.
- **Importance / impact** — the AI's judgement of how much an issue matters, weighing **urgency**
  (time-sensitive / actively bleeding), **relevancy** (still applicable to where the project is now),
  **unblocking power** (does it gate other work), and **reach**. The GitHub priority field **and issue
  age** are inputs, never the answer — this deliberately replaces the Flywheel's age-based oldest-first
  tiebreak, which assumes neither urgency nor relevancy.
- **Size** — AI-estimated effort ∈ {XS, S, M, L, XL}. Encoded as node size.
- **In pipeline** — the issue already has a live workspace/branch/agent/PR or a non-pending review status.
- **PRD** — a `.pan/drafts/<ISSUE>.md` draft exists.
- **Ready** — a vBRIEF spec exists in `.pan/specs/` **and** beads exist, i.e. an agent could pick it up now.
- **`sequence.md`** — the repo-tracked source of truth at `.pan/backlog/sequence.md`: human-readable
  table + per-issue rationale + a fenced JSON block that is the parse target.
- **Sequencer agent** — the configurable-model AI role that (re)writes `sequence.md`.
- **Creation pass** — first-ever run: ranks the entire backlog from scratch.
- **Incremental pass** — default, cheap run: examines only new/updated/closed issues since the last run;
  slots them in, minimally re-ranks, preserves prior order + rationale.
- **Review pass** — periodic deep run: re-evaluates the entire backlog from first principles.
- **Condition** — the AI's assessment of the issue itself, independent of rank: `ok`,
  `needs-refinement` (too poorly specified to act on — a human must fix it), or `stale`
  (likely no longer relevant — a candidate to close).
- **Tier** — a derived presentation band (Now / Next / Later / Someday) computed from rank/score
  thresholds; not stored per-issue, reproducible from the ranking alone.
- **Pinned** — an in-pipeline issue the sequencer never re-ranks or rewrites (only its live state refreshes).
- **Pickup gate** — an operator-set per-issue override ∈ {auto, ready, blocked} stored in `sequence.md`:
  `ready` greenlights/promotes for auto-pickup, `blocked` holds it out, `auto` defers to heuristics.
  Operator-owned; the sequencer preserves it verbatim. `blocked` also sets a parked label.
- **Dependency edge** — a directed cross-issue relation in the sequence DAG (`unblocks`/`informs`) with a
  provenance `source` ∈ {github-ref, operator, ai-inferred}. Distinct from vBRIEF/beads *intra-issue* edges.
- **Planning policy** — a per-issue, AI-suggested + operator-overridable value ∈ {skip, auto, interactive}
  stored in `sequence.md`, telling the Flywheel *how* to plan/launch the issue. `interactive` is a hard
  human gate (surfaced as needs-you). It is **one of three orthogonal axes** (planning policy · pickup gate ·
  condition); `needs-design`/`needs-discussion` decompose across them and are no longer a distinct concept (§10f).

## 3. Motivation (verified references)

- **The Flywheel ranks reactively, by rules — no LLM evaluation.** `docs/flywheel-brief.md` ranks by
  fixed tiers (P0→P3) then oldest-first; it emits per-tick `FlywheelSuggestion[]` but does no holistic,
  content-aware evaluation of the whole backlog (`src/lib/cloister/flywheel.ts`,
  `src/lib/flywheel-merge-order.ts`). No surface shows the *entire* backlog sequenced and explained.
- **GitHub priority is thin.** The tracker layer returns `priority: undefined` for GitHub
  (`src/lib/tracker/github.ts:361`); Linear sets it (`src/lib/tracker/linear.ts:378`). We want
  impact-ranking derived from issue content.
- **Durable-markdown precedent exists.** `docs/FLYWHEEL-STATE.md` is a markdown durable-state file the
  orchestrator reads/writes and commits. A backlog sequence deserves the same treatment.
- **`.pan/` writes already auto-commit.** `src/lib/pan-dir/auto-commit.ts` (`queueAutoCommit`, introduced
  in PAN-1146, 2026-05-16) debounces and commits `.pan/{continues,specs,drafts}` writes to `main`.
  A new `.pan/backlog/sequence.md` rides the same mechanism.

## 4. Decisions

| # | Decision | Rationale |
|---|---|---|
| D1 | **Source of truth = `.pan/backlog/sequence.md`** (repo-tracked markdown, auto-committed). Not GitHub labels. | User preference; mirrors `FLYWHEEL-STATE.md`; reproducible; rides `.pan/` auto-commit. |
| D2 | **SQLite is cache only.** A `backlog_sequence` table holds the parsed + pipeline-joined view; fully rebuildable from the md file. | Reproducibility — the md file is canonical, the DB disposable. |
| D3 | **Reuse the vBRIEF DAG stack** (ReactFlow 11 + `@dagrejs/dagre`) — new node component, same library. | Already a dependency (`PlanDAG.tsx`); zero new viz deps. |
| D4 | **New configurable role `sequencer`**, default `workhorse:expensive`, surfaced in Settings → Roles. | High-judgement task; the per-role model system is the right knob. |
| D5 | **Three passes** (creation / incremental / review). Incremental is default. | Cheap by default, never drifts. |
| D6 | **Condition assessment** stored per node (`ok`/`needs-refinement`/`stale`); surfaced visually; never auto-modifies the tracker. | Catch poor/obsolete issues without silently acting. |
| D7 | **Rationale stored once + tiered detail; tiered visualization.** | 522 open issues — see §7 scale budget. |
| D8 | **Sequencer is the single ranker; the Flywheel becomes the executor** (consumes the sequence, only emergency-promotes). | Avoids two AIs independently ranking; one reproducible order; Flywheel stays fast/reactive. §10c. |
| D9 | **Per-issue planning policy** (skip / auto / interactive / parked), AI-suggested + operator-overridable, stored in `sequence.md`. | Unifies today's ad-hoc `pan start --auto` / `pan plan --auto` / `pan plan` / parked-label choices into one durable field. §10e. |

## 5. Architecture / data flow

```
tracker (open issues) ─┐
prior sequence.md      ─┼─► Sequencer agent (configurable model, chunked for full passes)
pipeline/PRD/ready     ─┘        │
                                 ▼
                     .pan/backlog/sequence.md   ◄── SOURCE OF TRUTH (auto-committed to main)
                                 │ parse (table + fenced JSON)
                                 ▼
                     SQLite backlog_sequence     ◄── CACHE ONLY (rebuildable)
                                 │ join live pipeline/PRD/ready state
                                 ▼
             GET /api/backlog/sequence ──► Backlog Sequencer page (ReactFlow DAG, tiered)
```

## 6. `.pan/backlog/sequence.md` shape

Human-readable **and** machine-parseable. The table/prose is for review; the fenced ```json block is
the authoritative parse target.

**Rationale is stored exactly once (scale rule, §7).** The JSON carries a one-line `why` (≤140 chars)
for every issue; a full-paragraph `rationale` only for the active top tier (≈ top 80). The markdown
table is a compact rendered index; the `## Rationale detail` section renders paragraphs for the top tier only.

```markdown
# Backlog Sequence — overdeck
_Last sequenced: 2026-06-13T15:00Z · model: claude-opus-4-8 · pass: incremental · 522 open_

| Rank | Issue | Size | Importance | Cond | Depends on | Why (one line) |
|------|-------|------|-----------|------|-----------|----------------|
| 1 | PAN-1665 | L | critical | ok | — | Root unblocker; gates the deacon un-freeze. |
| 10 | PAN-1672 | M | medium | ⚠ refine | — | Real but one-sentence body — too vague to plan. |
...

## Rationale detail (top tier only)
### PAN-1665 — rank 1
<full paragraph, preserved across runs unless a delta changes it>

<!-- machine-readable; do not hand-edit below this line -->
```json
{ "version": 1, "project": "overdeck", "generatedAt": "...", "model": "claude-opus-4-8",
  "pass": "incremental", "lastReviewPass": "2026-06-08T...", "openCount": 522,
  "nodes": [
    { "issue": "PAN-1665", "rank": 1, "size": "L", "importance": "critical", "score": 96,
      "condition": "ok", "dependsOn": [],
      "why": "Root unblocker; gates the deacon un-freeze and the next four items.",
      "rationale": "<full paragraph — top tier only>" },
    { "issue": "PAN-1672", "rank": 10, "size": "M", "importance": "medium", "score": 53,
      "condition": "needs-refinement", "dependsOn": [],
      "why": "Real but one-sentence body — too vague to plan; flagged for a human." }
  ],
  "edges": [ { "from": "PAN-1665", "to": "PAN-1801", "type": "unblocks", "source": "github-ref", "confidence": 0.9 } ] }
```
```

Enums: `size ∈ {XS,S,M,L,XL}`, `importance ∈ {critical,high,medium,low}`, `score` 0–100,
`condition ∈ {ok,needs-refinement,stale}`, `pass ∈ {creation,incremental,review}`,
edge `type ∈ {unblocks,informs}`, edge `source ∈ {github-ref,operator,ai-inferred}`, `confidence` 0–1.

## 6a. Dependencies — provenance & reconciliation (FR-16)

Dependencies are first-class: they are the DAG edges (`dependsOn` per node + `edges`). But cross-issue
dependencies are **not available programmatically** the way one might assume:

- **GitHub has no native blocks/blocked-by**, and the tracker reads no relationships for GitHub
  (`src/lib/tracker/github.ts:342` — `getChildIssues` returns `[]`: "GitHub Issues does not support
  hierarchical parent-child relationships"). GitHub gives us only what's in the issue *body*: `#1234`
  references and prose ("blocked by PAN-1665", "after the throttle").
- **vBRIEF / beads edges are intra-issue** — dependencies *between an issue's own work items*
  (`blocks`/`informs`/`suggests`/`invalidates`, rendered by `PlanDAG.tsx`), **not** between backlog issues.
  Distinct graph; do not conflate.
- **Linear** (not PAN) does expose real issue relations — read them where a tracker provides them.

So cross-issue edges come from three sources, and each edge records its **provenance** (`source`):

| `source` | How obtained | Authority |
|---|---|---|
| `github-ref` | parsed from the issue body (`#N`, "blocked by PAN-N", "depends on", "after"); native relations/sub-issues where the tracker exposes them | authoritative |
| `operator` | a human added/edited the edge on the page | authoritative, preserved verbatim |
| `ai-inferred` | the AI proposed it from reading the bodies | advisory, recomputed each pass, carries `confidence` |

**Reconciliation rules:**
- Each pass **re-derives `github-ref` edges** (re-parse bodies / re-read relations) and **preserves
  `operator` edges verbatim** (same discipline as the pickup gate and pinning). `ai-inferred` edges are recomputed.
- **Authoritative edges win.** If an `ai-inferred` edge contradicts a `github-ref`/`operator` edge, the
  authoritative one stands and the conflict is surfaced (an "AI disagrees" marker) for the operator.
- **We read GitHub metadata; we do not write back.** GitHub has no dependency field to round-trip into, so
  `sequence.md` stays the source of truth for the *sequence*. Provenance is what keeps it honest rather than
  a pile of AI guesses. If bidirectional sync is wanted later, the place is the tracker layer
  (`github.ts` — read sub-issues / a `Depends-on:` body convention).
- Operator-authored edges (`source: operator`) are preserved verbatim and the AI never clobbers them.
  *(An on-page edge-drawing/editing UI is **deferred to a follow-up issue**; for now operator edges are
  hand-authored in `sequence.md`.)*

## 7. Scale & cost budget — overdeck has 522 open issues (measured)

This feature is designed for a 500+ issue backlog from day one.

- **Output file:** rationale stored once + tiered detail → `sequence.md` ≈ **50–65k tokens** at 522 issues.
  The naive triplicated-rationale shape would be **~120k tokens** — explicitly rejected (NFR-5).
- **Input cost is the real driver:** average open issue (title+body) ≈ **715 tokens**; reading all 522
  ≈ **~370k tokens**, exceeding one context window. Therefore (NFR-6):
  - **Creation** and **Review** passes are **chunked map-reduce** — rank in batches against a running
    shortlist, then a final merge — never one prompt containing all bodies.
  - **Incremental** passes read only the prior `sequence.md` (~50–65k) + the few changed bodies.
- **Visualization (FR-12):** 522 nodes cannot render at once. The page renders only the selected **tier**
  (Now / Next / Later / Someday) plus dependency neighbors; the long tail collapses behind counts;
  segment pills + filters (importance, condition, in-pipeline, ready) pivot the full 522. See the
  scaled mockup.

## 8. The three passes

| Pass | When | Scope | Cost |
|---|---|---|---|
| **Creation** | first run / no file yet | entire backlog, from scratch | high (once) |
| **Incremental** | default; debounced on backlog delta | only changed issues; preserves prior order + rationale | low |
| **Review** | periodic (configurable) or on demand | entire backlog re-evaluated from first principles | high |

Creation runs automatically when no file exists; the page selects Incremental vs Full review for manual
runs. Each pass stamps `pass` + `generatedAt` so cadence/drift is auditable in git history.

## 9. Issue-condition assessment

- **`needs-refinement`** → too vague to plan. Amber `⚠ REFINE` node (amber = "a human must act"). Still ranked.
- **`stale`** → likely no longer relevant. `⊘ STALE`, surfaced as a candidate to close.
- **`ok`** → no flag.

Stored per node in `sequence.md`; surfaced in the UI; never auto-modifies the tracker.

**Placement of stale issues (FR-11).** A `stale` issue ranks **low** — it sinks to the bottom tier
(Someday) and appears under the **Stale-candidates** filter for one-click closing, so irrelevant work
never competes for the top and doesn't clutter the Now DAG. The **exception is an in-pipeline stale
issue**: it stays **pinned** at its position (FR-13) and is rendered **bright + flagged** (not dimmed) —
the ring + `⊘ STALE` carry the warning — so the operator can **abort** active work the AI now believes
is dead rather than finish it. (A non-pipeline stale issue only ever appears in Now if the backlog is so
small that the bottom tier reaches the top — acceptable, and the close action clears it.)

## 10. The page — node encoding

New top-level tab **Backlog** ("Backlog Sequencer"). DAG via ReactFlow 11 + `@dagrejs/dagre`
(same as `PlanDAG.tsx`), new `IssueNode`.

| Channel | Encodes | Treatment |
|---|---|---|
| Node size | AI size estimate | XS→XL scales footprint |
| Rank badge | sequence position | `#1`, mono, top-left |
| Left-border heat | importance | red→amber→neutral→dim |
| In-pipeline | already being worked | glowing ring in the phase color + live verb badge (reuse `.plan-glow`) |
| PRD chip | `.pan/drafts/<issue>.md` exists | `PRD` |
| Ready chip | vBRIEF + beads exist | `✓ READY` (filled) |
| Condition | AI assessment | `⚠ REFINE` (amber) · `⊘ STALE` (dimmed, strikethrough) |
| Edges | dependency | `unblocks` solid, `informs` dashed |
| Rationale | why this rank | click node → side panel renders rationale from `sequence.md` |

Color follows the graph-viz precedent of `PlanDAG.tsx` while honoring style-guide signal semantics.

## 10a. Tiers (derived view, not an AI field)

The **Now / Next / Later / Someday** bands shown in the scaled mockup are a *derived presentation
layer* over the ranking — **not** a per-issue field the AI assigns. They are computed deterministically
from rank/score thresholds, so they reproduce from the ranking alone (NFR-1). Default bands
(configurable): **Now** = ranks 1–N (N defaults to the active agent capacity / `maxAgents`), **Next** =
the following band, **Later**, **Someday** = the long tail. The AI's job stays "rank + score + condition";
tiering is a function of that output.

**What each tier view shows + DAG-vs-list (the "too much to render" rule).** Selecting a tier shows the
**pinned/in-pipeline set (always) + that tier** — so active work is never hidden no matter which tier you're
on. Render mode is chosen by a **node budget** (ReactFlow + dagre stay smooth/readable to ≈150 nodes; past
that the graph turns to spaghetti):

| Tier (default sizes) | View |
|---|---|
| **Now** (~capacity) | full DAG |
| **Next** (pinned + all Next) | full DAG |
| **Later** (all Later, ~100) | DAG if ≤ ~150 nodes, else virtualized ranked list |
| **Someday** (the long tail, ~hundreds) | **virtualized ranked list** (rank · id · size · importance · condition · flags) — not a DAG |

So "too much to render" has a concrete answer: a ~150-node budget per view; above it we drop to a virtualized
list (still fully ranked and filterable, just not spatial). The Someday tier is a list by default.

## 10b. In-pipeline issues are pinned — never re-sequenced (FR-13)

Work already moving through the pipeline must not get reshuffled under the operator. The sequencer
**reads** each in-pipeline issue's live state (`derivePipelineState`) but **never re-ranks, re-orders,
or changes the `why`/`rationale`** of a pinned issue. Pinned = has a live workspace/branch/agent/PR or a
non-pending review status. New and changed issues slot *around* the pinned set; only a pinned issue's
flags/condition refresh from live state. This is a hard rule in the agent prompt and enforced in
`writeSequence` (a pinned node's rank/why are carried forward verbatim from the prior file). The page
marks pinned nodes with the in-pipeline ring and excludes them from "candidates" lists.

## 10c. Flywheel integration — auto-pickup reads the sequence (FR-14)

Today the Flywheel picks by fixed rule (tier P0–P3, then oldest-first; `docs/flywheel-brief.md`). With the
Sequencer, when `flywheel.auto_pickup_backlog` is enabled the Flywheel consults the **sequence** as its
ranked pickup source instead of oldest-first:

- New read API for the orchestrator: `getBacklogSequence(projectKey)` → the cache view (rank-ordered).
- **Eligibility filter (all must hold):** `pickup gate ≠ blocked` (§10d) · not parked
  (`needs-design`/`needs-discussion`) · not in-pipeline (pinned) · passes the existing author/assignee
  **security filter** (`flywheel-brief.md:37-47` — unchanged, never weakened) · `ready` or has a PRD
  (so there's something to act on).
- The Flywheel starts/plans the **highest-ranked eligible** issue (a `ready` one is startable; a
  PRD-only one is plannable) up to `minAgents`. Impact-ranked-first replaces oldest-first.
- Brief change: `docs/flywheel-brief.md` gains a "prefer the sequence when present" clause; the rule-based
  tiers remain the fallback when no `sequence.md` exists. This is additive — no existing safeguard removed.

**Separation of concerns (D8) — the Sequencer is the single ranker; the Flywheel is the executor.**
Today the Flywheel does *both* "what to work on next" (ranking) and "keep the pipeline moving" (execution).
Those are split:

- **Sequencer = strategist.** Owns the *order* of the backlog — periodic, holistic, reproducible. When a
  `sequence.md` exists it is the **single source of pickup order**; the Flywheel **stops applying its own
  P0–P3 / oldest-first ranking** for backlog pickup (those remain only as the no-sequence fallback).
- **Flywheel = executor.** Reactive and fast: keeps `minAgents` working *in the sequence's order*, runs the
  pipeline lifecycle (classify ghost/stuck/stalled, advance, merge-order), and surfaces blockers. It does
  **not** re-rank the backlog.
- **The one Flywheel→sequence write: emergency promotion.** When the Flywheel discovers a *new
  pipeline-critical* issue (a substrate bug blocking the pipeline, a P0 outage), it must be able to jump it
  to the front without waiting for the next pass. Recommended mechanism (reuses existing machinery, keeps a
  single ranker): the Flywheel **files the issue + sets `gate: ready` (promote) + triggers an incremental
  pass**. The promote makes it immediately pickable at the top; the incremental pass assigns its formal
  rank. The Flywheel never rewrites other issues' ranks — its only sequence mutation is promoting an
  emergency issue. *(Alternative considered: let the Flywheel write ranks directly — rejected, it
  reintroduces two rankers and the non-determinism this split removes.)*

## 10d. Operator pickup gate — mark Ready / Block from the page (FR-15)

The page is the natural place to greenlight or hold an issue. Each issue has an operator-set **pickup gate**
∈ {`auto`, `ready`, `blocked`}:

- **`ready`** — operator greenlight / promote: eligible for auto-pickup (and surfaced as "do this next") even
  if heuristics wouldn't reach it yet.
- **`blocked`** — operator hold: never auto-picked. The Block action also applies a parked label
  (`needs-design` by default) so the **existing** Flywheel skip honors it immediately, before the §10c
  integration lands.
- **`auto`** (default) — defer to heuristics/eligibility.

Storage: `gate` per node in `sequence.md`, **operator-owned**. The sequencer treats it as immutable input —
it must carry the gate forward verbatim and never clobber it (same discipline as pinning). UI: a
segmented control (Auto / ✓ Ready / ⛔ Block) in the rationale panel; nodes show a `📌 PROMOTED` or
`⛔ HELD` badge. Setting a gate writes through `POST /api/backlog/sequence/gate` → updates `sequence.md`
(+ parked label for Block) → auto-commit.

## 10e. Planning policy — how each issue gets planned (FR-17)

Not every issue wants the same planning treatment, and today that choice is **ad-hoc** — decided at launch
time by whoever runs the command:

| Today | Mechanism | Means |
|---|---|---|
| Skip planning | `pan start <id> --auto` | trivial/urgent — synthesize a minimal vBRIEF + beads, spawn work directly |
| Auto-plan | `pan plan <id> --auto` | non-interactive planning end-to-end (escalates to interactive on contradiction) |
| Interactive plan | `pan plan <id>` | a human does Q&A planning |
| Parked | `needs-design` / `needs-discussion` labels | held for a human decision before any planning |

Nothing persists *which* treatment a given issue should get. The Sequencer is the right place to record it —
but rather than add a fourth muddled mode, we **simplify the mental model** (see §10f). Planning policy is
**three values**: ∈ {`skip`, `auto`, `interactive`}, **AI-suggested + operator-overridable**, stored in
`sequence.md`:

- **`skip`** → no planning; `pan start --auto` (synthesize a minimal vBRIEF + beads, spawn work).
- **`auto`** → AI plans it end-to-end; `pan plan --auto`.
- **`interactive`** → a human drives planning. This is a **hard gate** — the Flywheel cannot run a Q&A
  session autonomously, so it surfaces the issue as a **needs-you** item ("ready for your planning session")
  and never auto-starts it.

**AI-suggested defaults (per your guidance):**
- An issue **the AI itself created** (a Flywheel-filed substrate bug, a conversation-agent bugfix/tiny UI
  change) → **non-interactive** (`skip` if tiny, `auto` if it needs a plan). The AI already understands what
  it filed.
- A **big feature** → **`interactive`** by default.
- **Size + origin drive the default; urgency tempers it** (urgent + small → `skip`). The operator can always
  toggle interactivity on the page.

## 10f. Simplified mental model — three orthogonal per-issue axes

`needs-design`/`needs-discussion` overlapped confusingly with "interactive planning." The clean model is
**three independent axes**, each with one job — and the two parked labels *decompose* across them rather
than being a fourth concept:

| Axis | Values | Answers | Set by |
|---|---|---|---|
| **Planning policy** (§10e) | skip / auto / interactive | *How* do we plan it? | AI suggests, operator overrides |
| **Pickup gate** (§10d) | auto / ready / blocked | *Whether/when* may the Flywheel pick it? | operator |
| **Condition** (§9) | ok / needs-refinement / stale | What's the *state of the issue itself*? | AI |

**`needs-design` / `needs-discussion` retire as a distinct concept** — they map onto the axes:
- "needs a human to design/decide it" → **planning `interactive`** (the human resolves it in the session).
- "too vague to even plan" → **condition `needs-refinement`**.
- "on hold, don't touch it yet" → **pickup gate `blocked`**.

So there is **no separate `parked` planning mode**. For backward-compat the sequencer **reads** a legacy
`needs-design`/`needs-discussion` label and maps it to `planning: interactive` (which keeps the Flywheel
from auto-running it, preserving today's skip behavior); the operator can additionally `blocked` it for a
hard hold. New issues use the three axes directly.

**Open for operator sign-off:** the four-mode taxonomy above (and whether `interactive` should be a
hard human-gate or a "prefer interactive but allow auto if no human responds in N" soft gate).

**Functional**
- **FR-1** Sequencer writes a total ordering to `.pan/backlog/sequence.md` (table + rationale + JSON), auto-committed.
- **FR-2** Ranking is AI-derived from content/impact, with **urgency** and **relevancy** as explicit inputs; GitHub priority **and issue age** are inputs, never the sole determinant (this replaces the Flywheel's age-based oldest-first tiebreak).
- **FR-3** Incremental: given the prior file, preserve existing order unless a delta justifies change; each change carries a rationale.
- **FR-4** Sequencer model configurable via `roles.sequencer.model`, surfaced in Settings → Roles.
- **FR-5** A **Backlog** page renders the sequence as an interactive ReactFlow+dagre DAG.
- **FR-6** Each node encodes rank, size, importance, in-pipeline, has-PRD, ready.
- **FR-7** Selecting a node shows the AI's rationale.
- **FR-8** `GET /api/backlog/sequence` returns the parsed file joined with live pipeline/PRD/ready state; `POST /api/backlog/sequence/regenerate` triggers the sequencer.
- **FR-9** SQLite `backlog_sequence` is a cache fully rebuildable from `sequence.md`.
- **FR-10** Three passes (creation/incremental/review); creation auto on first run; incremental default; page selects incremental vs full review; each run stamps `pass`.
- **FR-11** Per-issue `condition` ∈ {ok,needs-refinement,stale}, stored in the file; `⚠ REFINE` (amber) and `⊘ STALE` surfaced; neither auto-modifies the tracker. A non-pipeline `stale` issue ranks into the bottom tier and is reachable via the Stale-candidates filter; an in-pipeline stale issue stays pinned in place, bright and flagged, so the operator can abort it.
- **FR-12** The page handles 500+ issues: top-tier default view + dependency neighbors, collapsed long tail, filters (importance, condition, in-pipeline, ready).
- **FR-13** In-pipeline issues are **pinned**: the sequencer never re-ranks, re-orders, or rewrites them; only their live flags/condition refresh. New/changed issues slot around the pinned set.
- **FR-14** When `flywheel.auto_pickup_backlog` is enabled and a `sequence.md` exists, the Flywheel is the **executor**: it picks the highest-ranked **eligible** issue from the sequence (gate≠blocked, not parked, not in-pipeline, passes the author/assignee security filter, ready-or-has-PRD) and does **not** apply its own P0–P3/oldest-first ranking (those remain only as the no-sequence fallback). The Flywheel's sole sequence write is **emergency promotion** of a newly-discovered pipeline-critical issue (file + `gate: ready` + trigger an incremental pass); it never re-ranks the rest.
- **FR-15** Each issue has an operator-set **pickup gate** ∈ {auto, ready, blocked}, stored in `sequence.md` and preserved verbatim by the sequencer. The page sets it (Auto / ✓ Ready / ⛔ Block); `blocked` also applies a parked label so the existing Flywheel skip honors it immediately; the Flywheel (FR-14) respects it.
- **FR-16** Cross-issue **dependencies** are DAG edges with provenance (`source ∈ {github-ref, operator, ai-inferred}` + `confidence`). `github-ref` edges are parsed from issue bodies/relations and re-derived each pass; `operator` edges are preserved verbatim (hand-authored in `sequence.md`; on-page edge-drawing UI deferred to a follow-up issue); `ai-inferred` edges are advisory. Authoritative (`github-ref`/`operator`) edges override `ai-inferred` ones and conflicts are surfaced. GitHub metadata is **read, not written back**. vBRIEF/beads intra-issue edges are a separate graph and are not used as backlog edges.
- **FR-17** Each issue has a **planning policy** ∈ {skip, auto, interactive}, AI-suggested and operator-overridable, stored in `sequence.md`. The Flywheel maps it to the launch path (`skip`→`pan start --auto`, `auto`→`pan plan --auto`, `interactive`→needs-you, never auto-run). AI defaults: AI-created/tiny → non-interactive; big feature → interactive; operator toggles. `needs-design`/`needs-discussion` are **not** a separate mode — they decompose into the three orthogonal axes (planning `interactive` · condition `needs-refinement` · gate `blocked`); a legacy label is read as `planning: interactive` for backward-compat (§10f).

**Non-functional**
- **NFR-1** Reproducible: a fresh checkout + the same `sequence.md` yields the identical visualization with no AI call.
- **NFR-2** No new frontend viz dependency — reuse ReactFlow 11 + `@dagrejs/dagre`.
- **NFR-3** `sequence.md` stays human-diffable; auto-commit subject `chore(state): update backlog sequence (<project>)`.
- **NFR-4** Style-guide conformance: DM Sans, `font-medium`, semantic tokens, signal-color discipline.
- **NFR-5** File-size budget: ≤ ~65k tokens at 522 issues — rationale once, paragraphs for active tier only. Parser test asserts per-issue footprint stays in budget.
- **NFR-6** Input-scale: Creation/Review are chunked map-reduce (never one prompt with all bodies); Incremental reads only the prior file + changed bodies.

## 12. Work items (deliver all)

1. **`sequence.md` schema + parser/writer** — `src/lib/backlog/sequence-io.ts`: `parseSequence(md): SequenceDoc`,
   `writeSequence(projectRoot, doc)` (renders table + rationale + JSON, calls `queueAutoCommit()` from
   `src/lib/pan-dir/auto-commit.ts`). *Tests:* round-trip parse/write; malformed-block recovery; per-issue
   footprint budget (NFR-5).
2. **Sequencer role + prompt + passes** — extend the `Role` union and `DEFAULT_MODEL_REFS` in
   `src/lib/config-yaml.ts` (~L349) with `sequencer: 'workhorse:expensive'`; add the role to
   `RolesPanel.tsx` (~L88). Prompt takes backlog + prior file + pipeline flags; runs the selected pass and
   assigns conditions. Creation/Review chunked map-reduce; Incremental reads only changed bodies. *Tests:*
   per-pass prompt assembly; incremental-preserve (unchanged → unchanged order); review full-reeval;
   condition-assignment; chunking (N bodies → K batched calls, not one).
3. **SQLite cache table** — `backlog_sequence` (bump `SCHEMA_VERSION` in `src/lib/database/schema.ts`, currently 53);
   rebuild-from-md path. *Tests:* drop-and-rebuild equivalence.
4. **Backend route** — `src/dashboard/server/routes/backlog.ts` (`GET /api/backlog/sequence`,
   `POST .../regenerate`); join `findSpecByIssue` (`src/lib/pan-dir/specs.ts`), `.pan/drafts`, beads
   (`computeBeadCounts`), `derivePipelineState` (`src/dashboard/frontend/src/lib/issuePipelineState.ts`).
   Register `backlogRouteLayer` in `src/dashboard/server/server.ts` `makeRoutesLayer`. **No `execSync`** in
   server-reachable code; defer DB reads off the event loop. *Tests:* route returns joined view; regenerate enqueues agent.
5. **Frontend page + DAG (tiered)** — `pages/BacklogSequencerPage.tsx` + `components/backlog/BacklogDAG.tsx`
   (ReactFlow+dagre, custom `IssueNode`). Wire: `Tab` union in `components/Header.tsx`; `TAB_PATHS` (~L87) +
   render branch in `App.tsx`; nav item in `Sidebar.tsx`. Top-tier default view + long-tail collapse + filters
   (FR-12). *Tests:* node renders all encodings; node-click rationale; long-tail collapse + filter behavior.
6. **Configurable-model surface** — Settings → Roles entry for `sequencer`. *Test:* saving `roles.sequencer.model` persists and is read at spawn.
7. **Condition indicators (frontend)** — `⚠ REFINE` / `⊘ STALE` on nodes + rationale panel; stale node offers
   **Close issue**, refine node offers **Draft PRD**; a "candidates to close" summary lists all stale issues.
   *Tests:* condition class/chip render; stale-summary list.
8. **In-pipeline pinning + operator pickup gate** — `sequence-io.ts` carries pinned nodes' rank/why and the
   per-node `gate` verbatim across re-sequences (FR-13, FR-15). `POST /api/backlog/sequence/gate`
   (`routes/backlog.ts`) sets the gate, writes `sequence.md`, and applies/clears the parked label for Block.
   Frontend: Auto/✓ Ready/⛔ Block segmented control in the rationale panel + `📌 PROMOTED` / `⛔ HELD` node
   badges. *Tests:* pinned node unchanged after re-sequence with a moved sibling; gate round-trips + parked
   label applied on Block; sequencer preserves a gate it did not set.
9. **Flywheel becomes the executor (D8) + emergency promote** — `getBacklogSequence(projectKey)` read API; when
   `auto_pickup_backlog` is on and a sequence exists, the orchestrator picks the highest-ranked eligible issue
   (gate≠blocked, not parked, not in-pipeline, passes author/assignee filter, ready-or-PRD) and **stops applying
   its own ranking** for backlog pickup; the brief gains a "the sequence is the ranker; you execute it" clause,
   keeping P0–P3/oldest-first only as the no-sequence fallback. Add the emergency-promote path (file +
   `gate: ready` + trigger incremental pass) (FR-14). *Tests:* eligibility filter; ranked-pick-over-oldest;
   no-self-ranking-when-sequence-present; emergency-promote jumps the queue without re-ranking; security filter
   still excludes unsolicited third-party issues.
10. **Planning policy (FR-17)** — per-issue `planningPolicy` ∈ {skip,auto,interactive,parked} in `sequence.md`
   (AI-suggested in the prompt, operator-overridable via a rationale-panel control); the Flywheel launch path maps
   it to `pan start --auto` / `pan plan --auto` / needs-you / parked. *Tests:* AI suggestion by size/condition;
   operator override preserved across re-sequence; launch-command routing per policy; `interactive` never auto-runs.
11. **(Follow-up) delta auto-trigger** — debounced incremental pass on backlog change via `IssueDataService`;
   periodic review cadence (configurable). Shipped behind the on-demand pass buttons if time-boxed.

## 13. Acceptance criteria (map 1:1 to FRs)

- [ ] Running the sequencer writes `.pan/backlog/sequence.md` (table + rationale + valid JSON), committed to main. *(FR-1, FR-3)*
- [ ] Ordering reflects impact, not the GitHub priority field. *(FR-2)*
- [ ] Re-running with an unchanged backlog produces a byte-identical ordering (timestamp aside). *(FR-3)*
- [ ] Settings → Roles shows **Sequencer**; changing its model changes the model on next regenerate. *(FR-4)*
- [ ] The Backlog page renders open issues as a DAG sized by AI size, heat-bordered by importance, with ranks + edges. *(FR-5, FR-6)*
- [ ] In-pipeline issues show a glowing ring; PRD/READY chips appear only when those artifacts exist on disk. *(FR-6)*
- [ ] Clicking a node reveals the rationale. *(FR-7)*
- [ ] Deleting the SQLite cache and reloading reproduces the identical view from `sequence.md` alone. *(FR-9, NFR-1)*
- [ ] First run with no file does a Creation pass; default run is Incremental (changed issues only); Full review re-ranks all and stamps `pass: review`. *(FR-10)*
- [ ] An underspecified issue is flagged `needs-refinement` (`⚠ REFINE`); an obsolete issue is `stale` (`⊘ STALE`, candidate to close); neither changes the tracker. *(FR-11)*
- [ ] At 522 open issues, generated `sequence.md` ≤ ~65k tokens. *(NFR-5)*
- [ ] A Creation/Review pass completes without sending all bodies in one prompt (chunked map-reduce). *(NFR-6)*
- [ ] The page defaults to a top-tier view and stays usable at 500+ issues; long tail collapsed + filterable. *(FR-12)*
- [ ] An in-pipeline issue keeps its rank/rationale across a re-sequence even when a sibling moves; only its live flags refresh. *(FR-13)*
- [ ] With `auto_pickup_backlog` on, the Flywheel starts the highest-ranked eligible issue from the sequence (not oldest-first); a `blocked`/parked/in-pipeline/unsolicited-third-party issue is never picked. *(FR-14)*
- [ ] Marking an issue ✓ Ready or ⛔ Block from the page persists in `sequence.md`, survives the next re-sequence, and is honored by the Flywheel (Block also applies a parked label). *(FR-15)*
- [ ] A dependency parsed from an issue body (`#N` / "blocked by") is tagged `github-ref` and survives a re-sequence; an operator-added edge is preserved verbatim; an `ai-inferred` edge contradicting an authoritative one is flagged, not silently applied. *(FR-16)*
- [ ] A stale non-pipeline issue sinks to the bottom tier and appears under the Stale-candidates filter; a stale in-pipeline issue stays pinned in place, bright, and flagged. *(FR-11)*
- [ ] With a `sequence.md` present, the Flywheel executes the sequence order and does not apply its own ranking; it can emergency-promote a new pipeline-critical issue (gate→ready + incremental pass) without re-ranking the rest. *(FR-14, D8)*
- [ ] Each issue carries an AI-suggested ({skip,auto,interactive}), operator-overridable planning policy; AI-created/tiny defaults non-interactive, big features default interactive; the Flywheel launches `skip`→`pan start --auto`, `auto`→`pan plan --auto`, surfaces `interactive` as needs-you (never auto-run); a legacy `needs-design`/`needs-discussion` label reads as `interactive`. *(FR-17)*
- [ ] No new frontend viz dependency. *(NFR-2)*

## 14. Restated repo rules the executor must honor

- **No `execSync`** in dashboard-server-reachable code (route + services). Use async; defer DB reads off the event loop.
- **Fake timers** (`vi.useFakeTimers()`) for any debounce/backoff test (the sequencer trigger debounce, auto-commit debounce).
- **Style guide** for the page: DM Sans, `font-medium`, semantic tokens, signal-color discipline; the DAG is the one viz surface where richer status color is allowed (per `PlanDAG.tsx` precedent).
- **PAN-1124 invariant** is unaffected — `sequence.md` is a new artifact, not a spec; it does not touch `.pan/specs/` immutability.
- **Sequencer is a server-spawned special role** (like the flywheel orchestrator), not a work agent; spawn via the pipeline's `spawnRun`/role machinery, not Claude's `Agent` tool.

## 15. Decisions resolved with the operator

- **Now tier size = active agent capacity (`maxAgents`).** Confirmed.
- **`interactive` planning is a hard human gate** (never auto-run; surfaced as needs-you). Confirmed.
- **AI-suggested planning defaults:** AI-created issues (Flywheel substrate bug, conversation-agent
  bugfix/tiny UI change) → non-interactive; big features → interactive; operator can toggle. Confirmed.
- **Planning policy is three modes** {skip, auto, interactive}; `needs-design`/`needs-discussion` retire as a
  separate concept and decompose across the three orthogonal axes (§10f). Confirmed.
- **Tier render mode:** pinned set always shown; DAG up to a ~150-node budget per view, virtualized list
  beyond it; Someday is a list by default. Confirmed.

## 16. Future enhancements (out of scope for this issue)

These are explicitly **deferred** — noted so they aren't lost, but they are lower priority than other backlog
work and must not expand this issue's scope:

- **"Over-the-horizon" ghosts** — render the top of the *next* tier as faint dotted-outline shadow nodes
  inside the current tier's DAG, so the operator can see a little past the current horizon without switching
  tiers. Nice-to-have; revisit as its own issue.
- **On-page dependency-edge drawing/editing** (drag node→node → `source: operator`). Deferred per §6a.
- **Bidirectional GitHub dependency sync** — reading GitHub sub-issues / a `Depends-on:` body convention in
  the tracker layer, and (maybe) writing edges back. Deferred per §6a.

## Planning Q&A

This issue was planned via `pan plan --auto` (non-interactive) on 2026-06-19 by Claude (Opus 4.8).
No questions were posed to the operator; the PRD above already settled every major decision (D1–D8,
FR-1–FR-17, NFR-1–NFR-6). Discovery instead verified every PRD codebase reference against the live tree
and resolved the one architectural point the PRD left open — **how the sequencer agent actually runs** —
along with a handful of defensible defaults. These are recorded in `.pan/spec.vbrief.json`
(`plan.autoDecisions[]`) and `.pan/continue.json` (`decisions[]` D1–D9, `hazards[]` H1–H8).

Key inferred decisions (full text in continue.json):

- **D2 — spawn mechanism.** The sequencer runs as a tmux-hosted role agent via
  `spawnRun(runId,'sequencer',{agentId:'sequencer-runner',…})`, mirroring `spawnFlywheelAgent`
  (`src/lib/cloister/flywheel.ts:117`). Verified there is **no direct-LLM-API path** for role runs in
  this codebase — every role is a tmux Claude session.
- **D3 — chunked map-reduce (NFR-6).** Realized *inside* the agent session: `collectOpenBacklog` returns
  a compact manifest + a batched body accessor; `roles/sequencer.md` forbids inlining the whole backlog
  and mandates batched reads against a running shortlist. (The full backlog is ~370k tokens — one
  context window cannot hold it; H1.)
- **D6 — in-pipeline server-side.** Computed from `review_status` (non-pending) OR live
  workspace/branch/agent — the frontend `derivePipelineState` helper must **not** be imported into the
  Effect server (H7).
- **D8 — MVP scope.** On-demand pass buttons (Creation/Incremental/Review) are the acceptance-critical
  MVP; the debounced delta auto-trigger + periodic review cadence is the lowest-priority bead
  (`backlog-delta-autotrigger`) and may land last, per the PRD ("not a blocker").

The 8 PRD work items were decomposed into **19 beads** (see the vBRIEF). Two carry `requiresInspection:true`
(deep): `sequence-schema` (the cross-cutting JSON contract every other bead encodes assumptions about) and
`sequencer-prompt` (the agent pass/condition/preservation contract the spawn + regenerate route depend on).
