# PAN-2543 — Recovery-net reconciler: derive-and-re-drive every pipeline obligation

**Issue:** [PAN-2543](https://github.com/eltmon/overdeck/issues/2543)
**Status:** draft
**Author:** conversation (operator-directed), 2026-07-09

This epic closes the recovery/dispatch wedge cluster in one delivery:
[PAN-2536](https://github.com/eltmon/overdeck/issues/2536) · [PAN-2534](https://github.com/eltmon/overdeck/issues/2534) · [PAN-2524](https://github.com/eltmon/overdeck/issues/2524) · [PAN-2520](https://github.com/eltmon/overdeck/issues/2520) · [PAN-2519](https://github.com/eltmon/overdeck/issues/2519) · [PAN-2518](https://github.com/eltmon/overdeck/issues/2518) · [PAN-2498](https://github.com/eltmon/overdeck/issues/2498) · [PAN-2485](https://github.com/eltmon/overdeck/issues/2485) · [PAN-2486](https://github.com/eltmon/overdeck/issues/2486) · [PAN-2537](https://github.com/eltmon/overdeck/issues/2537) · [PAN-2522](https://github.com/eltmon/overdeck/issues/2522) · [PAN-2503](https://github.com/eltmon/overdeck/issues/2503) · [PAN-2502](https://github.com/eltmon/overdeck/issues/2502) · [PAN-2475](https://github.com/eltmon/overdeck/issues/2475)

All file:line references verified against `main` on 2026-07-09. Where a line may drift, the code string to search for is quoted.

---

## Glossary

| Term | Meaning |
| --- | --- |
| **Deacon** | The Cloister supervisor process. Runs a 60s patrol loop (`startDeacon()` at `src/lib/cloister/deacon.ts:3513`, `setInterval` at `:3537`, `patrolIntervalMs = 60_000` at `:214`) plus reactive event handlers (`handleCloisterDomainEventPromise`, `src/lib/cloister/service.ts:440`). |
| **Patrol reconciler** | One of the ~50 checks inside `runPatrol()` (`deacon.ts:2713`) that derives desired state from durable records and re-drives a missing step. Patrols are "thin safety nets for dropped events" (PAN-1908). |
| **Pipeline obligation** | A step the pipeline owes an in-flight issue: dispatch review, dispatch re-review, dispatch test, start work agent after planning, requeue a swarm slot, merge, close out, clear a stale label. |
| **Re-drive** | Idempotently performing an obligation that a dropped event or hung call left undone. |
| **Derive** | Computing what obligation is owed from durable state (review-status DB, per-issue records, GitHub PR/issue truth, vBRIEF specs) — never from remembered in-process state. |
| **Gate** | A flag that blocks autonomous re-drive of an agent: `paused` (operator/scheduler hold), `stoppedByUser` (deliberate user stop), `troubled` (circuit-breaker output). Schema: `src/lib/agents/agent-state.ts:51` (`stoppedByUser`), `:53` (`paused`), `:73-74` (`troubled`/`troubledAt`). |
| **Circuit breaker** | A bounded retry counter (e.g. the 25-requeue breaker in `checkDeadEndAgents`, `deacon.ts:1920`) that converts a loop into a one-time needs-you escalation. |
| **needs-you** | The operator-attention surface (dashboard Needs-you lane + notification). The terminal state of every tripped breaker — never silent. |
| **Admission gate** | The memory governor (`assessMemoryPressure()`, `src/lib/cloister/memory-governor.ts:116`) plus concurrency ceiling that every autonomous spawn/resume must pass (PAN-2500/PAN-2507). |
| **runId idempotency guard** | The dispatch-dedup check in `spawnReviewRoleForIssuePromise` (`src/lib/cloister/review-agent.ts:432-489`): a review session tagged `agent-<issue>-review-<head8>` is stale if its persisted `reviewRunId` mismatches the current HEAD. |
| **Read model** | The dashboard's in-memory/SQLite view of issue state (labels, stages), rebuildable from GitHub + git records. |

## Background & motivation

On 2026-07-08/09 the pipeline stalled repeatedly with **agents alive and main green** — every stall traced to the same architectural gap: pipeline advancement depends on **one-shot event triggers and blocking signal calls**, and when a trigger is dropped or a call hangs, either no patrol re-derives the obligation, or the patrol that exists is blocked by an inconsistently-checked gate. Fourteen open issues are individual edges of this one gap:

1. **Gate inconsistency** — `checkDeadEndAgents` checks `paused` (`deacon.ts:1966-1969`) but not `stoppedByUser`, so a user-stopped review-failed agent loops rebuild-and-start forever ([PAN-2536](https://github.com/eltmon/overdeck/issues/2536)); the boot-reconciliation empty-candidate race terminally commits `resume_all` before crashed agents are even marked stopped ([PAN-2502](https://github.com/eltmon/overdeck/issues/2502)).
2. **Signal fragility** — the review-verdict signal can hang after printing success, and nothing reconciles `review.approved` afterward ([PAN-2524](https://github.com/eltmon/overdeck/issues/2524)); `pan specialists done` hung on an unbounded comment POST ([PAN-2518](https://github.com/eltmon/overdeck/issues/2518), now partially bounded); `pan admin db restore-verdicts` hangs after finishing its work ([PAN-2475](https://github.com/eltmon/overdeck/issues/2475)).
3. **Dispatch gaps** — re-review silently no-ops while a finished reviewer lingers idle-alive ([PAN-2534](https://github.com/eltmon/overdeck/issues/2534)); auto-plan finalizes but the work agent never starts ([PAN-2522](https://github.com/eltmon/overdeck/issues/2522)); stack-unhealthy defers respawn forever ([PAN-2520](https://github.com/eltmon/overdeck/issues/2520)); failed swarm WORK slots are logged and dropped, never requeued ([PAN-2498](https://github.com/eltmon/overdeck/issues/2498), [PAN-2485](https://github.com/eltmon/overdeck/issues/2485)).
4. **Wedge blindness** — liveness = "tmux session exists ∧ pane not dead", so modal-blocked agents look idle-alive: codex's rate-limit dialog wedged three agents for hours and its default silently downgraded them to gpt-5.4-mini ([PAN-2486](https://github.com/eltmon/overdeck/issues/2486)); wedged rework agents were parked as `troubled` instead of killed for respawn ([PAN-2519](https://github.com/eltmon/overdeck/issues/2519) — partially landed, see WI-9).
5. **Missing reconcilers** — no patrol reconciles pipeline labels or the read model against durable/terminal truth: ~90 issues wear stale `verifying-on-main`/`planning` labels ([PAN-2537](https://github.com/eltmon/overdeck/issues/2537)); 729 agent rows accumulate because terminal detection is broken and gc is manual-only ([PAN-2503](https://github.com/eltmon/overdeck/issues/2503)).

**This epic is NOT a new engine.** The deacon already implements derive-and-re-drive ~50 times (see the patrol inventory in `runPatrol`, `deacon.ts:2713-3321`). The epic completes the net: one shared gate predicate applied everywhere, bounded-and-exiting signal paths, the missing re-drive branches, wedge-aware liveness, the two absent reconcilers, and deterministic (never `Math.random()`) recovery cadence.

## Decisions (made here — do not re-litigate downstream)

| # | Decision | Rationale |
| --- | --- | --- |
| D1 | **One classifier, one intent-aware policy.** Extend `getAgentResumeGateBlockReason` into a pure `ResumeGateBlock` classifier covering `paused`, `troubled`, `stoppedByUser`, and failure backoff. A separate `decideResumeGate(block, intent)` owns policy for `intent = autonomous | operator-start | message-delivery`; no path hand-rolls subsets. Operator-start clears `stopped-by-user`, does not clear paused/troubled (use `unpause`/`untroubled`), and overrides failure-backoff with a logged warning. | Classification and policy are separate so autonomous recovery cannot accidentally impose its rules on explicit starts or message delivery. |
| D2 | **`stoppedByUser` is intent-aware.** An explicit mid-work operator stop with **no completed handoff marker** sticks absolutely against autonomous re-drive and surfaces one needs-you trip. A completed done-handoff that now owes rework (`review` blocked/failed, `test` failed, or `verification` failed) may autonomously re-drive despite the historical flag, matching `pan-871-auto-resume.test.ts:418-462`. Historical flags are classified by completion-marker presence plus durable rework status. Explicit operator start/rebuild/swarm-recover clears the flag. | Preserves a real stop while satisfying PAN-2536 and the existing PAN-1614 done-handoff recovery contract. |
| D3 | **Every breaker trips to a durable needs-you identity.** Persist `{ issue, recoveryPath, obligationGeneration, tripCount }` in the per-issue record. Exactly one needs-you is emitted per identity/trip; restart does not duplicate it. Operator acknowledgement or a successful explicit action resets the open trip, and a later obligation generation may create a distinct trip. | Process-local cooldowns both duplicate after restart and suppress later genuine failures. PAN-2498's swarm sat dead invisibly; silence is the failure mode. |
| D4 | **Signal paths are durable-first, bounded, and exiting.** Every `pan` CLI verb that records a verdict/completion writes durable state FIRST, wraps every subsequent network/exec call in a bounded timeout (`AbortController` / `execAsync` `timeout` + `killSignal`, patterns at `src/cli/commands/done.ts:952` and `src/lib/review-verdict-feedback.ts:93`), treats post-durable failures as non-fatal warnings, and **guarantees `process.exit`** (explicit exit after flush; no open handles keeping the loop alive). | PAN-2524/2518/2475 are one family: work done, process never returns, agent waits forever. |
| D5 | **Review completion and obligation are durable.** An idle review whose runId is absent/mismatched is stale → kill/respawn on request; a durably emitted verdict makes its session terminal and reapable; independently, a patrol compares current HEAD with last-reviewed SHA and dispatches when no live run covers the current obligation. | Covers all PAN-2534 requirements: direct replacement, no lingering post-verdict blocker, and recovery when the request event is lost. |
| D6 | **Deterministic cadence.** Correctness-critical recovery sweeps run on a fixed cycle-modulo, never `Math.random()` gates. Today `cleanupStaleAgentState` and `cleanupAbandonedFeedback` fire at `Math.random()<0.003` (`deacon.ts:3196`, `:3205`) and `cleanupOrphanReviewerSessions` at `<0.01` (`:3214`) — i.e. roughly once per 5.5 hours *in expectation*, unbounded in the tail. | A dropped verdict-feedback file must be recovered within a bounded number of patrols, not "eventually with probability 1". |
| D7 | **Wedge detection is pane-content-aware.** Liveness gains a third signal beyond session-exists and pane-dead: known-modal pattern match on captured pane content (the codex rate-limit dialog first; the detector is a table of patterns → actions so future modals are additions, not new machinery). Same family as the existing AUQ/consent detection. | PAN-2486/2519: "alive" without "responsive" is the blind spot. |
| D8 | **Harness dialogs never make model decisions.** The codex rate-limit dialog is auto-answered "Keep current model"; the rate limit surfaces as the existing usage-limit halt. Live-model divergence is claimed only if WI-10's captured before/after checkpoint proves an authoritative post-switch source; otherwise attribution remains launch-model-only and pane detection surfaces attempted/confirmed switches. | No-hardcoded-model-fallback rule, without pretending launch metadata proves live session state. |
| D9 | **Label reconciliation derives from records + GitHub, repairs labels — not vice versa.** The patrol treats the per-issue permanent record (`pipeline` block) and GitHub issue/PR state as truth; pipeline labels (`verifying-on-main`, `planning`, `in-progress`, `in-review`) on issues whose truth is terminal (merged/closed/completed) are removed. Read-model rows that contradict GitHub are re-derived. Dry-run mode first; the initial ~90-issue sweep runs once with a log of every change. | Two-door tenet: labels are a projection, records are canonical. |
| D10 | **Agents-table terminal detection keys off permanent truth, with event-driven prune first.** After durable close-out, stopped rows for all roles prune immediately; live/non-stopped rows survive and surface. Scheduled resolver-driven GC remains the backstop for GitHub/external closes, and `--dry-run` remains. | Fixes the broken `review_status` terminal test without waiting an hour for the ordinary close-out path. |
| D11 | **Boot reconciliation waits for liveness truth.** On an empty candidate snapshot, `startBootReconciliation()` opens the grace window in `pending` instead of terminally committing `resume_all`; candidates arriving from `reconcileAgentLiveness` (which runs ~1s later in the deacon child) join the pending set; the window closes on decision or timeout. | PAN-2502: the empty→`resume_all` fast path races the liveness reconcile on full reboots and ungates the exact post-reboot flood the dialog exists to prevent. |
| D12 | **Every new re-drive passes the admission gate.** Any respawn/resume/dispatch added by this epic calls through `assessMemoryPressure()` / the concurrency ceiling exactly as `deacon-auto-resume.ts` and `concurrency.ts` do, and honors the preemptive scheduler's `paused` (PAN-2507). Read the cached verdict (`memory-verdict-cache.ts`) — don't re-poll. | The reconciler must not fight the resource governor (PAN-2500) or the preemptive scheduler. |
| D13 | **Swarm failed-slot requeue uses a new monotonically increasing slot index.** A failed slot with no FailedMergeBlock is superseded: rename its branch/worktree to `slot-<N>-failed-<timestamp>` (preserving artifacts outside active occupancy), record the archived attempt, clear its active assignment, reset the item, and let wave dispatch allocate an index greater than every current or archived index. `recordSlotAssignment` records only the new attempt; `reconcileSlotState` ignores archived names for active occupancy but reports them as superseded history; GC removes archived attempts only under an explicit retention rule. Manual recover uses the same path; D3 bounds redispatches. | Current occupancy treats every live branch and worktree as occupied, so preserving them under the active slot name cannot free capacity. Archival plus a new index preserves work without aliasing attempts. |

## Requirements

### Functional

- **FR-1** All spawn/resume/rebuild/message paths use the shared classifier plus intent-aware policy (D1); autonomous paths apply the D2 completion-marker/rework matrix.
- **FR-2** Explicit operator starts clear `stoppedByUser`, override failure backoff with a warning, and remain blocked by paused/troubled until the separate clearing verb runs; dashboard and CLI behavior agree.
- **FR-3** Every breaker persists one needs-you identity `{ issue, recoveryPath, obligationGeneration, tripCount }`; restart does not duplicate it, acknowledgement/explicit recovery resets it, and a later obligation can trip independently. *(Trace accounting remains WI-1, WI-7, WI-8, WI-9.)*
- **FR-4** The review-verdict signal path and every specialist-completion CLI verb is durable-first, bounded, and exiting (D4); a hung/failed post-durable call is recovered by a deterministic patrol within 2 patrol cycles.
- **FR-5** Rework dispatches a current-HEAD review despite a lingering old session or lost request; verdict-terminal sessions reap and the HEAD-vs-last-reviewed patrol self-heals within one cycle (D5).
- **FR-6** After `complete-planning` finalizes with auto-start requested, a work agent spawns within one patrol cycle; a dropped finalize→spawn handoff is re-derived from the proposed spec + auto-start stamp (not from the lost event).
- **FR-7** A respawn deferred on `stack-unhealthy` triggers rebuild-and-start automatically under its existing breaker/cooldown ([PAN-2520](https://github.com/eltmon/overdeck/issues/2520)'s spec).
- **FR-8** Failed swarm WORK slots requeue automatically per D13; `pan swarm recover --action retry` covers non-merge failures; merged idle slots are reaped promptly; N-failures escalate to needs-you.
- **FR-9** A wedged-but-alive work agent owing rework is killed for respawn under the ceiling; known harness modals are detected/answered per D7/D8; model attribution follows WI-10's observability checkpoint and never claims an unverified live value.
- **FR-10** A label-reconciliation patrol removes stale in-flight pipeline labels from terminal issues and repairs read-model drift against GitHub (D9), with dry-run support and per-change logging.
- **FR-11** Terminal agent rows are detected via permanent truth and pruned across all roles immediately after durable close-out, with scheduled GC as backstop; live/non-stopped rows are preserved and surfaced.
- **FR-12** Boot reconciliation on a full reboot presents the operator dialog (or holds pending) instead of terminally committing `resume_all` on an empty racing snapshot (D11).
- **FR-13** All recovery sweeps currently gated on `Math.random()` run on deterministic cycle-modulo cadence (D6).
- **FR-14** The GitHub label/comment calls on the `pan done` path (`done.ts:190`, `:205`, `:212` — currently unbounded `fetch`) and the remaining unbounded exec sites on dispatch paths (`review-agent.ts:453`, `:574`; `deacon.ts:3254`) gain bounded timeouts using the existing wrapper patterns.
- **FR-15** `pan admin db restore-verdicts` completes and exits on real runs (root-cause the hang — likely an open handle after work completes — under D4's exit discipline).

### Non-functional

- **NFR-1** The whole epic ships as one issue — no partial delivery (operator direction; phases below are organizational).
- **NFR-2** Every retry/backoff/delay test uses `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync()` — never real timers (repo rule).
- **NFR-3** No `execSync` in server-reachable code; async `execFile`/`spawn`/`execAsync` only; tmux via `sendKeysAsync` (repo rules).
- **NFR-4** New patrol work must not measurably extend the 60s patrol budget: expensive sweeps (labels, gc) run on cycle-modulo cadence and are single-flighted alongside `deaconPatrolGuard` (`deacon.ts:3351`).
- **NFR-5** Every behavior change lands with a regression test named in its work item; the pre-existing in-flight-guard test (`tests/unit/lib/cloister/in-flight-guard.test.ts`) and postMergeLifecycle idempotency stay green.

## Design

The epic is four completions of the existing net plus two new reconcilers:

1. **Gates** (WI-1, WI-2): one classifier plus intent-aware policy, boot race fixed.
2. **Signals** (WI-3, WI-4): durable-first + bounded + exiting CLI verbs; deterministic post-hoc recovery patrols.
3. **Dispatch** (WI-5–WI-8): the four missing re-drive branches (re-review, plan→work, stack rebuild, swarm requeue).
4. **Wedges** (WI-9, WI-10): pane-content-aware liveness; modal table; model-divergence check.
5. **New reconcilers** (WI-11, WI-12): labels/read-model; agents-table gc.
6. **Cross-cutting** (WI-13–WI-16): deterministic cadence, admission-gate compliance audit, docs, close-out sweep.

Interaction contract for every re-drive: `derive durable obligation/generation → classify gates → decide by intent → admission gate → act idempotently → count attempt → persist breaker trip identity → needs-you`.

## Work items

> Executor notes — repo rules restated (do not skip):
> • Fake timers for ALL retry/delay tests; never real `setTimeout` waits.
> • No `execSync` anywhere server-reachable; `sendKeysAsync` only for tmux.
> • Never `git stash`; commit, discard, or surface. Commit early and often; the deacon and other agents share `main` — path-scope every commit, never `--amend`.
> • The deacon may be globally paused during development — reactive handoffs must not be the only path (that is the point of this epic).
>
> **Execution mode (operator-directed, 2026-07-09):** this epic is implemented **directly on `main`** by an operator-supervised GPT-5.6 agent (same mode as PAN-2541 and PAN-2545) — not in a feature workspace, because the change hardens the recovery machinery that would otherwise supervise it. Sequencing: PAN-2541 lands first (its PRD's execution-mode note governs that work), then this epic. Consequences: deacon stays frozen throughout; commit per work item, path-scoped, never `--amend`; no pushes and no server restarts by the agent — the operator reviews, pushes, and coordinates any restart; the WI-11 one-time label sweep and any runtime patrol verification happen only after an operator-driven restart into the new dist.
> • Line anchors below were verified 2026-07-09; if drifted, search the quoted code strings.

### WI-1 — Gate classifier + intent policy + `stoppedByUser` semantics ([PAN-2536](https://github.com/eltmon/overdeck/issues/2536))
**Files:** `src/lib/agents/agent-state.ts` (`getAgentResumeGateBlockReason`, line 679), `src/lib/cloister/deacon.ts` (`checkDeadEndAgents` gate at 1966-1969 — search "agentState?.paused === true"), `src/lib/cloister/orphan-proposed-reconciler.ts` (spawn/rebuild gates at 242, 459-460; `classifySpawnSkip` 277-280; `triggerRebuildAndStart` 326), `src/lib/cloister/deacon-auto-resume.ts` (`deliberatelyStopped` at 732), `src/lib/cloister/concurrency.ts` (flag-clear pattern 325-330), `src/dashboard/server/routes/workspaces.ts` (rebuild-and-start route ~992), `src/cli/commands/start.ts`.
**Steps:**
1. Extend `getAgentResumeGateBlockReason` into a pure classifier returning:
   ```ts
   type ResumeGateBlock =
     | { gate: 'paused'; reason: string; pausedReason?: string; yieldedByScheduler?: boolean }
     | { gate: 'troubled'; reason: string; troubledAt?: string }
     | { gate: 'stopped-by-user'; reason: string }
     | { gate: 'failure-backoff'; reason: string; consecutiveFailures: number };
   type ResumeIntent = 'autonomous' | 'operator-start' | 'message-delivery';
   // null ⇒ no classified gate; caller may proceed to policy/admission.
   ```
2. Add `decideResumeGate(block, intent, context)`. Autonomous policy applies D2: no completion marker means stopped-by-user blocks; completion marker + durable owed rework permits re-drive. Operator-start clears stopped-by-user, refuses paused/troubled with the existing `unpause`/`untroubled` guidance, and overrides failure-backoff with a logged warning. Message delivery does not mutate gates and returns an explicit delivery decision.
3. Route `checkDeadEndAgents`, orphan-proposed spawn/rebuild, auto-resume, swarm requeue, CLI start, dashboard Rebuild & Start, and messaging through classifier + policy. Persist D3 trip identity in the issue record rather than reusing a process-local cooldown; operator acknowledgement or successful explicit action closes it.
4. Historical `stoppedByUser` classification is deterministic: completed/completed.processed marker plus failed review/test/verification is owed rework; without the marker it is an operator stop and sticks.
**Tests:** classifier union tests; policy matrix across intents and gates; completion-marker × owed-rework × stoppedByUser cases matching `pan-871-auto-resume.test.ts:418-462`; real `pan start`, dashboard Rebuild & Start, swarm recover, and message-delivery tests; restart-during-open-trip dedup and second-distinct-obligation trip tests.

### WI-2 — Boot reconciliation empty-candidate race ([PAN-2502](https://github.com/eltmon/overdeck/issues/2502))
**Files:** `src/lib/cloister/boot-reconciliation.ts` (`startBootReconciliation`, the empty→`resume_all` fast path; candidate predicate at 79), `src/lib/cloister/deacon.ts` (`reconcileAgentLiveness` 1766, startup ordering 3525).
**Steps:** on empty candidate snapshot open the window in `pending`; re-snapshot after `reconcileAgentLiveness` completes (or subscribe to its completion) and admit late candidates; only commit `resume_all` when the post-liveness snapshot is also empty; window closes on operator decision or timeout with the safe default (`hold`).
**Freshness note (PAN-2545, landed 2026-07-09):** `reconcileAgentLiveness` now early-returns `[]` when `isDeaconGloballyPaused()` — the gate lives in the deacon.ts wrapper (moved there by `64f41014ea`; originally added in `ee6b2a1647`). On a globally-paused boot no candidates are ever produced, by design. Add `src/lib/cloister/deacon-auto-resume.ts` to this item's scope (the liveness implementation lives there; `deacon.ts:1766` is only the wrapper), and the "candidates appear post-liveness" test must mock `isDeaconGloballyPaused → false`.
**Tests:** simulated full-reboot fixture (agents rows `running`, tmux empty): dialog candidates appear post-liveness; dashboard-only restart fixture (rows already `stopped`): behavior unchanged. Fake timers for the window.

### WI-3 — Verdict-signal durability + deterministic reconcile ([PAN-2524](https://github.com/eltmon/overdeck/issues/2524))
**Files:** `src/lib/review-status.ts` (`emitReactiveLifecycleEvent` 20, emit at 537, host-side journal reconcile 822-839, `fixStuckReadyForMerge` 896-915), the verdict-recording CLI — **note there is no `src/cli/commands/review/` directory**: the verdict write lives in `src/cli/commands/specialists/done.ts` and the review verbs sit at top level (`src/cli/commands/request-review.ts`, `abort-review.ts`, …) — `src/lib/cloister/deacon.ts` (`checkCompletedButUnsignaledReviews` at 3026).
**Steps:**
1. Signal CLI: durable write first (it already is), then bounded post-work (D4), then guaranteed exit. Root-cause the observed hang (same class as the finalize hang — open handles after success).
2. Make the journal→DB reconcile at `review-status.ts:828-839` (comment: "the deacon may be frozen") ALSO run as a deterministic patrol step so a stranded `review.approved` reconciles within 2 cycles without requiring a read to trigger it.
3. Verify `checkCompletedButUnsignaledReviews` catches the "artifact written, signal hung, agent killed" sequence — add the PAN-2524 reproduction as its regression fixture.
**Tests:** fixture where verdict artifact + journal exist but DB says pending → patrol reconciles `review.approved` and `readyForMerge` derivation runs; CLI hang test: mock a never-resolving comment POST → process exits within the bound (fake timers).

### WI-4 — CLI exit discipline + timeout sweep ([PAN-2518](https://github.com/eltmon/overdeck/issues/2518) residual, [PAN-2475](https://github.com/eltmon/overdeck/issues/2475), FR-14)
**Files:** `src/cli/commands/specialists/done.ts` (existing 30s bound at 174-194 — verify, keep), `src/lib/cloister/review-verdict-feedback.ts` (bounded exec at 93 — verify), `src/cli/commands/done.ts` (unbounded fetches at 190/205/212), `src/cli/commands/db.ts` (`restoreReviewStatusFromRecords` at 131 — the restore-verdicts hang), `src/lib/cloister/review-agent.ts` (execs at 453/574), `src/lib/cloister/deacon.ts` (exec at 3254).
**Steps:** introduce (or reuse) one bounded-fetch/bounded-exec helper; apply at every listed site; audit each listed CLI verb for guaranteed exit (explicit `process.exit(0)` after durable flush where handles may linger — restore-verdicts first, reproducing the 60/90/240s timeouts from the issue). Document the D4 contract in the CLI module header.
**Tests:** per-site unit tests with a hanging mock → bounded completion (fake timers); restore-verdicts integration: real-shaped run completes and exits.

### WI-5 — Re-review dispatch staleness ([PAN-2534](https://github.com/eltmon/overdeck/issues/2534))
**Files:** `src/lib/cloister/review-agent.ts` (idempotency guard 432-489), verdict completion/reaper path (`sessionsToReapForRole`), review metadata/read door, and a deterministic review-obligation patrol wired into Deacon.
**Steps:** implement D5 — stale idle reviewer (absent/mismatched runId) is killed and respawned; same runId + active pane still dedupes. After a verdict is durably emitted, mark the review run terminal and reap its session through `sessionsToReapForRole`. Add a durable obligation patrol comparing current feature HEAD to the last-reviewed SHA/run metadata; when HEAD is newer and no live reviewer covers it, dispatch without depending on a possibly lost request event.
**Tests:** stale/matching run cases; verdict-emitted → terminal reap; PAN-2534 direct request reproduction; lost-request/head-advanced patrol recovery; multi-round review↔rework fixture completes without manual session kills.

### WI-6 — Planning-finalize → work-start obligation ([PAN-2522](https://github.com/eltmon/overdeck/issues/2522))
**Files:** `src/cli/commands/plan-finalize.ts` (auto-spawn flag read 131-132, `promotePlanning` 312/410, POST body `autoSpawn` 424), `src/lib/overdeck/planning-sessions.ts` (`autoSpawnOnFinalize` stamp 375), the dashboard `complete-planning` route (consumer of `autoSpawn`), `src/lib/cloister/orphan-proposed-reconciler.ts` (`reconcileOrphanProposedSpecs` — the backstop, invoked `deacon.ts:2808`).
**Steps:**
1. Instrument/root-cause the dropped handoff: finalize commits spec + transition but the spawn never fires (two live reproductions: PAN-2360, PAN-2510 — dashboard.log shows `complete-planning` completing with no subsequent spawn).
2. Make the obligation durable: the proposed spec + auto-start stamp IS the obligation record; `reconcileOrphanProposedSpecs` must deterministically spawn from it (audit its cooldown/gate deferrals at `orphan-proposed-reconciler.ts:242`, 459-460 so a healthy unstarted proposed spec never waits more than one cycle).
3. Fix the spawn-drop in the route itself (partial-success of the `complete-planning` POST must not lose the stamp).
**Tests:** finalize-with-autoSpawn fixture where the route's spawn step is made to fail → next patrol spawns the work agent; healthy path spawns immediately; no double-spawn when both fire (idempotency via existing agent-exists check).

### WI-7 — Stack-unhealthy auto rebuild-and-start ([PAN-2520](https://github.com/eltmon/overdeck/issues/2520))
**Files:** `src/lib/cloister/deacon.ts` (`checkDeadEndAgents` stack-unhealthy branch 1984-1993), `src/lib/cloister/orphan-proposed-reconciler.ts` (`triggerRebuildAndStart` 326), `src/dashboard/server/routes/workspaces.ts` (rebuild-and-start route ~990-992).
**Steps:** finish and verify the existing path — gate with the 25-requeue breaker, per-issue cooldown, WI-1 classifier/policy (mid-work operator stop blocks; completed handoff owing rework may auto-rebuild), D12 admission gate, and durable D3 needs-you trip on repeated failure.
**Tests:** stack-unhealthy fixture → rebuild-and-start invoked within cooldown cadence; repeated rebuild failure trips breaker → one needs-you, loop stops (fake timers).

### WI-8 — Swarm failed-slot requeue + recover coverage + gc ([PAN-2498](https://github.com/eltmon/overdeck/issues/2498), [PAN-2485](https://github.com/eltmon/overdeck/issues/2485))
**Files:** `src/lib/cloister/deacon-swarm.ts` (`coordinateSwarmSlots` 263/335-346, `classifyInFlightSlots` 357, failure lifecycles 383/398/455-460, `recordStalledSlotRecovery` 675-680, `dispatchNextWave` 723, `occupiedSlotIndexes` 747, `recoverFailedMergeSlot` 604, gc functions, advance backoff 554-570), `src/cli/commands/swarm.ts` (recover verb 632, 189).
**Steps:** implement D13 —
1. New coordinator branch for `lifecycle === 'failed'` with no FailedMergeBlock: atomically rename its branch/worktree to `slot-<N>-failed-<timestamp>`, persist a superseded-attempt entry (old index, item, agent, archive names, reason, timestamp), clear the active assignment, reset the item, and allocate a **new monotonically increasing index** greater than all active and archived indexes. `recordSlotAssignment` records only the new attempt; `reconcileSlotState` excludes archived names from occupancy while exposing them as history; GC retains them until the documented superseded-attempt retention threshold. Per-item redispatch counter → durable D3 breaker trip.
2. `pan swarm recover <id> <slot> --action retry` handles dead-session/crash/auth-death failures via the same requeue path.
3. `gcMergedSlots` reaps merged + session-alive + idle-past-threshold slots so capacity frees (the PAN-2485 40-min hold).
**Tests:** real-occupancy fixture containing branch + worktree + assignment: archive rename frees old occupancy and redispatch uses a higher index while artifacts survive; reconcile/record/GC superseded-attempt tests; breaker/restart dedup; recover-verb non-merge failure; merged idle GC (fake timers throughout).

### WI-9 — Wedged-rework kill-for-respawn: verify + complete ([PAN-2519](https://github.com/eltmon/overdeck/issues/2519))
**Files:** `src/lib/cloister/stuck-remediation.ts` (`evaluateWedgedReworkAgent` 67, `hasPendingRework` 78, kill at 112, troubled ceiling at 106, ordering at 197; normal-path early returns at 178).
**Steps:** the core fix has landed (verified 2026-07-09: `evaluateWedgedReworkAgent` kills for respawn under the requeue ceiling). Remaining: (a) confirm the kill→dead-end-respawn bridge fires end-to-end with WI-1's predicate (a killed-for-respawn agent must not be blocked by a leftover gate); (b) ensure non-rework wedges still park as troubled but now ALSO emit needs-you (D3 — today parking is silent); (c) close the issue with the end-to-end regression test it asked for.
**Tests:** end-to-end fixture: idle work agent + pending `.pan/feedback` → killed → dead-end respawn spawns fresh agent whose kickoff drains feedback; chronic re-death trips ceiling → troubled + needs-you.

### WI-10 — Codex rate-limit modal: detect, answer, surface ([PAN-2486](https://github.com/eltmon/overdeck/issues/2486))
**Files:** new modal-detection table module under `src/lib/cloister/` (pane pattern → action), wired into `checkStuckAgentRemediation`/`nudge` paths (which already capture panes); the codex launcher/config (`src/lib/runtimes/codex.ts`) for the spawn-time "hide rate limit reminders" investigation; model-divergence check where agent state records model (`src/lib/agents/agent-state.ts`).
**Steps:** per D7/D8 —
1. Detector: pane pattern for the rate-limit dialog (and a table structure for future modals); action: send the keystrokes selecting "Keep current model (never show again)" via `sendKeysAsync` with the load-buffer discipline where text is involved. **Fixture + keystrokes (decided here — no repo fixture exists yet, so this text IS the fixture until a live capture replaces it):** the codex dialog reads approximately `rate limit reminder — switch model?` with options `Switch to gpt-5.4-mini` (DEFAULT, selected) and `Keep current model (never show again)`, footer `Press enter to confirm`. Detection pattern: pane contains both `rate limit` and `Keep current model`. Action: arrow-Down to move highlight to "Keep current model (never show again)", then Enter — **never a bare Enter** (that confirms the default downgrade, the exact PAN-2486 failure). After sending, re-capture the pane and verify the dialog is gone and the model line is unchanged; if still present, escalate needs-you rather than retry-spam. Capture a real pane fixture into the test on first live encounter.
2. Surface the rate limit as the existing usage-limit halt class (pause + needs-you "usage limit" + auto-resume at window reset).
3. Spawn-time config: if codex supports disabling the reminder, set it in the launcher (cleanest fix; keep the detector as backstop).
4. **Implementation checkpoint — live-model observability:** capture before/after fixtures from a real model-switch dialog and verify whether Codex rollout JSONL (and Claude transcript metadata where applicable) records the post-switch model. If an authoritative post-switch value exists, compare it with agent state and surface divergence. If not, explicitly descope this check to **launch-model attribution only** and move live-switch detection to the pane detector (detect the dialog/confirmation/model banner and escalate); never claim JSONL proves the live model. Pi/ohmypi remains `unobservable` in v1.
**Tests:** captured pane fixtures; keep-selection sequence; usage-limit surfacing; before/after JSONL checkpoint fixture. The fallback test proves launch attribution remains accurate and a pane-observed switch triggers escalation when no authoritative live field exists.

### WI-11 — Label/read-model reconciliation patrol ([PAN-2537](https://github.com/eltmon/overdeck/issues/2537))
**Files:** new `src/lib/cloister/label-reconciler.ts` + patrol wiring in `runPatrol` (cycle-modulo cadence per NFR-4), `src/lib/tracker/github.ts` (label ops 311-329), `src/lib/backlog/label-ops.ts` (22-39), `src/lib/pipeline-membership.ts` (the read-side interpreter, 85), per-issue records read door.
**Steps:** per D9 —
1. Derive truth per labeled issue: permanent record `pipeline`/`closeOut` terminal OR GitHub issue closed OR merged-with-no-inflight-machinery ⇒ stale in-flight labels (`verifying-on-main`, `planning`, `in-progress`, `in-review`, `in-planning`) are removed; `needs-close-out` applied where close-out never ran (so the close-out backstop owns teardown).
2. Read-model drift: rows contradicting GitHub state (PAN-1919-class) re-derived from GitHub.
3. `--dry-run` CLI (`pan admin reconcile-labels --dry-run`) prints the change plan; the first real sweep (the ~90-issue backlog) is run once with full per-change logging, then the patrol keeps it clean at cadence.
4. **Cadence (decided here):** every 10 patrol cycles (~10 min), max 20 issues reconciled per cycle (rate-limit safety); the one-time backlog sweep is exempt from the batch cap but logs every change.
**Tests:** fixture matrix (merged+Done+labeled, Todo+planning-label, closed-on-GitHub+in-review-read-model) → exact expected label removals/repairs; dry-run mutates nothing; patrol respects rate limits (batched, bounded per cycle).

### WI-12 — Agents-table terminal detection + event-driven/scheduled gc ([PAN-2503](https://github.com/eltmon/overdeck/issues/2503))
**Files:** `src/cli/commands/db.ts` (gc-agents), durable terminal helper, close-out completion path/canonical writer, patrol wiring, `src/lib/cloister/reap-terminal-sessions.ts`.
**Steps:** per D10 — fix terminal detection and extend GC to all roles. **Event-driven primary path:** after close-out's permanent record is durably written, immediately prune every stopped agent row for that issue through the canonical agent writer; a live session or non-stopped row is preserved and surfaced as an explicit close-out warning/escalation. **Backstop:** retain `--dry-run` and schedule the same resolver-driven sweep every 60 patrol cycles (~1 h) for externally closed/bypass cases.
**Tests:** close-out durable-write ordering then immediate stopped-row prune; live/non-stopped row preserved and surfaced; separate hourly sweep fixture for closed-out-with-pruned-review-status; in-flight rows survive.

### WI-13 — Deterministic recovery cadence (D6, FR-13)
**Files:** `src/lib/cloister/deacon.ts` — the three `Math.random()` gates at 3196 (`cleanupStaleAgentState`), 3205 (`cleanupAbandonedFeedback`), 3214 (`cleanupOrphanReviewerSessions`).
**Steps:** replace each with cycle-modulo cadence (choose: feedback recovery every 5 cycles ≈ 5 min — it is correctness-critical post-WI-3/4; the two session/state janitors every 60 cycles ≈ 1 h). Keep single-flight.
**Tests:** patrol-loop test with fake timers asserting each fires at its modulo, never randomly.

### WI-14 — Admission-gate + preemption compliance audit (D12)
**Files:** every dispatch/respawn site this epic adds or touches (WI-1, 6, 7, 8, 9); reference implementations `src/lib/cloister/deacon-auto-resume.ts`, `src/lib/cloister/concurrency.ts`, `src/lib/cloister/memory-governor.ts` (116, 185), `src/lib/cloister/memory-verdict-cache.ts`.
**Steps:** audit that each new re-drive calls the WI-1 classifier + intent policy and the admission gate; add a structural test enumerating re-drive entry points and asserting both calls.
**Tests:** the enumeration test; a memory-pressure fixture: under `holding`, no new re-drive dispatches; under recovery, deferred re-drives fire.

### WI-15 — Documentation (required, not a follow-up)
**Files:** `docs/RESOURCE-GOVERNOR.md` (reconciler-vs-governor interaction), `docs/AGENT-STATE-PLANES.md` (classifier + intent policy matrix, completion-marker stoppedByUser semantics), a new `docs/RECOVERY-NET.md` (obligation → classify → intent policy → admission → re-drive → durable trip identity → needs-you), root `CLAUDE.md` "Agent Auto-Resume Gates" section, module headers of every touched file.
**Acceptance:** the gate matrix appears in AGENT-STATE-PLANES; RECOVERY-NET documents every WI's reconciler; CLAUDE.md gate section matches the shipped predicate.

### WI-16 — Cluster close-out sweep
**Steps:** after all ACs pass: close [PAN-2536](https://github.com/eltmon/overdeck/issues/2536), [PAN-2534](https://github.com/eltmon/overdeck/issues/2534), [PAN-2524](https://github.com/eltmon/overdeck/issues/2524), [PAN-2520](https://github.com/eltmon/overdeck/issues/2520), [PAN-2519](https://github.com/eltmon/overdeck/issues/2519), [PAN-2518](https://github.com/eltmon/overdeck/issues/2518), [PAN-2498](https://github.com/eltmon/overdeck/issues/2498), [PAN-2485](https://github.com/eltmon/overdeck/issues/2485), [PAN-2486](https://github.com/eltmon/overdeck/issues/2486), [PAN-2537](https://github.com/eltmon/overdeck/issues/2537), [PAN-2522](https://github.com/eltmon/overdeck/issues/2522), [PAN-2503](https://github.com/eltmon/overdeck/issues/2503), [PAN-2502](https://github.com/eltmon/overdeck/issues/2502), [PAN-2475](https://github.com/eltmon/overdeck/issues/2475) — each with a comment naming the WI and test that resolved it. Any issue whose scope this epic deliberately trimmed gets the residue split into a new issue, not silently dropped (no-loss rule).

## Acceptance criteria (mapped to work items)

- **AC-1** (WI-1) Classifier + intent-policy tests pass on real CLI/dashboard/message paths. Mid-work stop without completion marker produces one durable needs-you and no re-drive; completed handoff owing rework autonomously re-drives; operator start clears stopped-by-user, warns/overrides backoff, and still requires unpause/untroubled. Trip identity survives restart and permits a later distinct trip.
- **AC-2** (WI-2) Full-reboot fixture surfaces the dialog with the late-arriving candidates; empty-after-liveness still fast-paths; dashboard-only restart unchanged.
- **AC-3** (WI-3) Stranded-verdict fixture (journal passed, DB pending) reconciles `review.approved` within 2 patrol cycles; signal CLI exits under a hanging POST.
- **AC-4** (WI-4) Every FR-14 site has a bounded call (asserted by tests); `pan admin db restore-verdicts` completes a real-shaped run and exits.
- **AC-5** (WI-5) PAN-2534 direct and lost-request reproductions dispatch current-HEAD review; verdict-terminal sessions reap; same-runId active review dedupes; multi-round rework needs no manual kill.
- **AC-6** (WI-6) Finalize-with-autoSpawn whose spawn step fails → work agent spawns on the next patrol; healthy path spawns immediately; never double-spawns.
- **AC-7** (WI-7) Stack-unhealthy fixture auto-rebuilds within cadence; chronic rebuild failure trips the breaker to one needs-you.
- **AC-8** (WI-8) A failed occupied slot archives branch/worktree under the timestamped superseded name and redispatches on a higher monotonic index; artifacts remain, active occupancy frees, recover/GC work, and N failures persist one D3 trip.
- **AC-9** (WI-9) End-to-end wedge fixture: kill → respawn → feedback drained; ceiling → troubled + needs-you; non-rework park now emits needs-you.
- **AC-10** (WI-10) Pane detector selects "Keep current model" and surfaces usage-limit halt. The before/after checkpoint either proves authoritative post-switch divergence or activates the documented launch-attribution + pane-switch fallback.
- **AC-11** (WI-11) Label fixture matrix reconciles exactly as specified; dry-run mutates nothing; the one-time backlog sweep log accounts for every changed issue.
- **AC-12** (WI-12) Durable close-out immediately prunes stopped rows; live/non-stopped rows survive and surface; the separate hourly backstop handles externally closed/pruned-review-status fixtures.
- **AC-13** (WI-13) No `Math.random()` cadence gate remains in `runPatrol`; modulo tests pass.
- **AC-14** (WI-14) The structural gate-compliance test enumerates all re-drive entry points and passes; memory-pressure fixture defers under `holding`.
- **AC-15** (WI-15) Docs grep: the gate matrix, RECOVERY-NET contract, and CLAUDE.md section exist and match the shipped predicate names.
- **AC-16** (WI-16) All fourteen cluster issues are closed with WI/test references (or residue split into named follow-ups).

## Out of scope (explicitly)

- The state-plane orphan-branch move — that is [PAN-2541](https://github.com/eltmon/overdeck/issues/2541) (land it first; it removes the churn that amplifies re-dispatch storms, and this epic's label reconciler then reads records from the state worktree via the same read doors).
- [PAN-2469](https://github.com/eltmon/overdeck/issues/2469)'s remaining swarm issue-level state machine beyond the failed-slot strand (WI-8 covers the slot-death strand; 2469 stays open for anything beyond it).
- Rewriting the deacon patrol architecture (PAN-2526's file-size refactor of `deacon.ts` is independent; this epic adds/fixes reconcilers within the existing structure).
- New wedge-modal coverage beyond the codex rate-limit dialog (the WI-10 table makes additions cheap; other dialogs are follow-ups as observed).

## Related

[PAN-2541](https://github.com/eltmon/overdeck/issues/2541) (state plane — land first) · [PAN-2500](https://github.com/eltmon/overdeck/issues/2500) (memory governor — D12 integrates) · [PAN-2507](https://github.com/eltmon/overdeck/issues/2507) (preemptive scheduler — pause reuse respected) · [PAN-2209](https://github.com/eltmon/overdeck/issues/2209)/[PAN-1665](https://github.com/eltmon/overdeck/issues/1665) (dead-end respawn + dispatch budget — extended, not replaced) · [PAN-2076](https://github.com/eltmon/overdeck/issues/2076) (boot reconciliation — WI-2 fixes its fast path).
