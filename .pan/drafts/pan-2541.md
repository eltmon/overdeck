# PAN-2541 — Move pipeline state to a same-repo orphan branch (`overdeck-state`) with a dedicated writer worktree

**Issue:** [PAN-2541](https://github.com/eltmon/overdeck/issues/2541)
**Status:** draft
**Author:** conversation (operator-directed), 2026-07-09

---

## Glossary

| Term | Meaning |
| --- | --- |
| **State plane** | The git-mirrored durable pipeline state: per-issue records, continue states, vBRIEF specs, PRD drafts, review/test/feedback artifacts, backlog sequence, and the beads database exports. Enumerated today by `STATE_PLANE_PATHS` in `src/lib/state-plane.ts:6`. |
| **Code branch** | `main` and `feature/*` branches — the branches that carry product source and go through CI/review/merge. |
| **State branch** | `overdeck-state` — a same-repo **orphan branch** (no common ancestor with `main`) whose tree contains *only* state-plane content. |
| **Orphan branch** | A branch created with `git checkout --orphan <name>`: it starts with an empty tree and shares no history with any other branch. |
| **State worktree** | A second checkout of the same repository, pinned to `overdeck-state`, living at `${OVERDECK_HOME}/state/<projectKey>`. Created with `git worktree add`. The **only** place state commits happen. |
| **Write door** | The single code path allowed to commit/push state: `queueAutoCommit` / `flushAutoCommits` in `src/lib/pan-dir/auto-commit.ts` (exports at lines 189 and 241; search for `export function queueAutoCommit`). |
| **Read door** | The per-domain canonical resolvers in `src/lib/pan-dir/` (`specs.ts`, `records.ts`, `continue.ts`, `drafts.ts`, `feedback.ts`) and `src/lib/backlog/sequence-io.ts`. |
| **Context layer** | Human-authored project configuration at `.pan/context/` (e.g. `project.md`, `codebase/*.md`) that renders into harness context files (CLAUDE.md / AGENTS.md). **Not** machine state. |
| **Workspace-local runtime files** | Gitignored, per-workspace operational files (`continue.json`, `kickoff.md`, `agent-mcp.json`, `events/`, `convoy/`, `prompts/`, `pid/`) — see `.gitignore:142-151`. Never committed anywhere; not part of the state branch. |
| **bd / beads** | The external issue tracker CLI. Its discovery is hardcoded to a `.beads/` directory found in the working directory or its ancestors (verified: `bd --help` — "default: auto-discover .beads/*.db"; `bd init` "creat[es] a .beads/ directory"). |

## Background & motivation

Today the deacon and pipeline mirror the state plane into git by committing to **the same branch and the same worktree as the code**. Three incident classes on 2026-07-09 alone share this root:

1. **Review-reset loop ([PAN-2468](https://github.com/eltmon/overdeck/issues/2468)).** High-frequency state commits on `main` force feature branches to rebase constantly; each rebase rewrites every commit SHA; the deacon's post-review guard read a byte-identical branch as "new code" and reset a passed review in a loop. Patched at the guard layer (commit `9887913c73`, patch-id contribution comparison) — but the *trigger* (state churn on main) remains.
2. **Writer race on the shared worktree.** The deacon's auto-commit (`chore(state): batch update …`) runs in the primary worktree concurrently with human/conversation commits. Two agents were bitten the same day by `git commit --amend` silently absorbing a concurrent deacon state commit.
3. **History and pipeline pollution.** `git log main` interleaves state noise; state pushes trigger CI; `src/lib/pipeline-state-paths.ts` and `src/lib/state-plane.ts` exist largely to discriminate state commits from code commits *after the fact*.

Moving the state plane to an orphan branch written through a dedicated worktree removes the trigger conditions structurally. Prior art: `gh-pages`, CI benchmark-data branches, Gerrit `refs/meta/config`; Overdeck already uses same-repo custom refs (`refs/pan/turn/*`) for checkpoints.

## Decisions (made in this document — do not re-litigate downstream)

| # | Decision | Rationale |
| --- | --- | --- |
| D1 | The state branch is named **`overdeck-state`**. | Self-describing post-rebrand name. Not `pan-state` (legacy name). Not `overdeck/state` (a branch named `overdeck` could never coexist — git ref namespace collision). |
| D2 | The state branch tree is **flat**: `records/`, `continues/`, `specs/`, `drafts/`, `review/`, `test/`, `feedback/`, `backlog/`, `notes/`, `.beads/` at its root. **No `.pan/` wrapper.** | The dot-directory existed only to hide state inside the code checkout. On a dedicated branch the branch *is* the namespace. This is also the clean break from the `pan` legacy name: the new home never contains it. |
| D3 | **No symlinks anywhere** in this design. | Operator decision (2026-07-09): symlinks cause more confusion than they solve, and D2+D4 make them unnecessary. |
| D4 | `.beads/` lives at the **state worktree root**, keeping its literal name so bd's hardcoded discovery works natively (bd runs with `-C <state worktree>` or with cwd inside it). It is removed from code `main`. **Workspace-level beads copies are unchanged** — they are workspace-operational, not the canonical store. | Avoids both the symlink and any bd fork. Verified: bd supports `-C` (global flag) and `--db` explicit path. |
| D5 | The context layer moves **out of the state directory entirely**: `.pan/context/` → **`.overdeck/context/`** on `main`, tracked and code-reviewable. `main`'s `.overdeck/` directory holds *only* human-authored, code-owned config. | Operator decision: no carve-out/negation patterns. One directory = one owner. Context governs the code, so it travels and reviews with the code. |
| D6 | Workspace-local runtime files rename `.pan/` → `.overdeck/` (they are gitignored; rename = code constants + a read-compat fallback for `.pan/`). | Completes the clean break at the only other place the `.pan` name would survive. |
| D7 | **No internal module rename** in this PRD: `src/lib/pan-dir/` keeps its name; the CLI stays `pan`. | Surgical-changes rule. The `pan-dir` module name is internal; renaming it is churn with no behavioral value. May be a follow-up. |
| D8 | `main` and `overdeck-state` **never merge**. Reconciliation is referential (records carry issue IDs / PR numbers / commit SHAs) plus a release-time snapshot: `pan release stable` records the current `overdeck-state` SHA in `.release/<tag>.md`. | The trees are disjoint; a merge is meaningless and would import thousands of state commits into code history. |
| D9 | Legacy `.pan/` **read fallbacks** are kept during the migration window (same pattern as the existing `vbrief/` legacy fallback, `findLegacyVBriefByIssue`). Writes go only to the new home. | Non-destructive migration; old checkouts keep working read-only. |
| D10 | Multi-machine writer semantics: before each push the writer runs `git fetch` + `git rebase origin/overdeck-state`; on conflict it aborts the rebase, re-reads the canonical value through the read door, re-applies the queued write, and retries (bounded, 3 attempts). Per-issue record files make conflicts rare; last-writer-wins per file is acceptable for this plane. | Records are per-issue JSON files with a single writer per machine. |
| D11 | The existing benign-HEAD-move guards in `checkPostReviewCommits` (tree-identical, effective-code, patch-id — `src/lib/cloister/deacon.ts:1603-1636`) are **kept as backstops**, not removed. | No-loss principle. Feature branches may still rebase for genuine code conflicts. Removal is a separate, later cleanup once telemetry shows they no longer fire. |

## Requirements

### Functional

- **FR-1** All state-plane writes for a project land as commits on the `overdeck-state` branch, made **only** from the state worktree at `${OVERDECK_HOME}/state/<projectKey>`, serialized by the existing write-door lock.
- **FR-2** No new commit on `main` or any `feature/*` branch may contain state-plane paths. Enforced mechanically (gitignore + CI guard), not by convention.
- **FR-3** The `overdeck-state` tree contains only state-plane content (D2 layout). Enforced by a CI guard on pushes to `overdeck-state`.
- **FR-4** All read doors resolve state from the state worktree; when it is absent (not yet migrated), they fall back to the legacy `<projectRoot>/.pan/` locations read-only (D9).
- **FR-5** The context layer is served from `<projectRoot>/.overdeck/context/` on `main`, with read fallback to `.pan/context/` (D9).
- **FR-6** `pan install` / `pan doctor` create or self-heal the state worktree (correct branch, correct path, clean status).
- **FR-7** A migration command (`pan admin state migrate <project>`) performs the one-time cutover per project (see WI-2) and is idempotent — re-running after success is a no-op.
- **FR-8** The canonical project-root `.beads/` database and JSONL exports live at the state worktree root; project-scope bd invocations in Overdeck source pass `-C <state worktree>`. Workspace beads flows are unchanged.
- **FR-9** `pan release stable` records the `overdeck-state` HEAD SHA of every registered project in `.release/<tag>.md`.
- **FR-10** The PAN-1124 invariant survives verbatim: specs are immutable after planning except `plan.status` flips via `updateSpecStatus()`; the single writer is the write door. Only the storage location changes.

### Non-functional

- **NFR-1** Migration is non-destructive: no history rewrite on `main`; legacy `.pan/**` blobs remain reachable in old commits. The migration makes ordinary commits only (recoverable-actions principle).
- **NFR-2** State pushes trigger **zero** CI workflows (branch filter), and stop appearing in `git log main`.
- **NFR-3** Every new test that exercises delay/retry logic (the writer's rebase-retry, D10) uses `vi.useFakeTimers()` — never real timers (repo rule, restated below).
- **NFR-4** No `execSync` in any server-reachable code added by this work; async `execFile`/`spawn` only (repo rule).
- **NFR-5** The whole feature ships as one issue — no partial delivery. Phases below are organizational, not separate deliverables.

## Design

### Target layout

```
main (code branch)                     overdeck-state (orphan branch)
├── src/ …                             ├── records/      pan-2541.json …
├── docs/ …                            ├── continues/    pan-2541.vbrief.json …
├── .overdeck/                         ├── specs/        2026-…-PAN-2541-….vbrief.json …
│   └── context/                       ├── drafts/       pan-2541.md …
│       ├── project.md                 ├── review/  test/  feedback/
│       └── codebase/*.md              ├── backlog/      sequence.md …
├── .gitignore  (ignores state paths)  ├── notes/
└── (NO .pan/, NO .beads/)             └── .beads/       issues.jsonl, *.db …

state worktree: ${OVERDECK_HOME}/state/<projectKey>  ⟶ checked out on overdeck-state
```

### Resolution chokepoint

`resolveInfraRepo()` in `src/lib/projects.ts:351` already returns `{ repoPath, recordsPath }` with `recordsPath = project.pan_records?.path ?? '.pan'` (line 355). It grows a successor, `resolveStateHome(project)`, that returns the state worktree path with `recordsPath: '.'` once the project is migrated (presence of the worktree + a `state_home` marker in project config), and the legacy values otherwise. Every read/write door resolves through it. Verified: no project in the live `~/.overdeck/projects.yaml` sets `pan_records`, so there are no existing overrides to preserve (the polyrepo code path in lines 358-371 is kept working).

**Before (`src/lib/projects.ts:355`):**
```ts
const recordsPath = project.pan_records?.path ?? '.pan';
```
**After (sketch — exact shape to match file style):**
```ts
// PAN-2541: migrated projects resolve to the dedicated state worktree on
// branch 'overdeck-state'; recordsPath collapses to '.' (flat tree, D2).
const home = resolveStateHome(project);
if (home.migrated) return { repoPath: home.worktreePath, recordsPath: '.' };
const recordsPath = project.pan_records?.path ?? '.pan';
```

### Write door

`queueAutoCommit` / `flushAutoCommits` (`src/lib/pan-dir/auto-commit.ts`, exports at lines 189/241) currently derive a repo root from the changed path and commit there. After this change they (a) refuse to commit state paths anywhere except the state worktree, (b) assert `git branch --show-current` = `overdeck-state` before every commit (worktree-discipline rule applied to the machine), and (c) push after flush using the D10 fetch/rebase/retry loop.

### Guards

1. **CI guard on code branches** (new script wired into the existing lint/CI): fail if `git diff --name-only origin/main...HEAD` (or the tracked tree on main) contains any state-plane path.
2. **CI/hook guard on `overdeck-state`**: fail if the tree contains any path *outside* the D2 layout.
3. **Writer self-check**: unit-tested branch assertion in the write door (WI-4's named test).

## Work items

> Executor notes — repo rules restated (do not skip):
> • Any retry/delay test uses `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync()`; never real timers.
> • No `execSync` in server-reachable code; use `execFileAsync`/`spawn`.
> • Never `git stash`; commit, discard, or surface.
> • Never `checkout` another branch inside a worktree; verify `git branch --show-current` before edits.
> • The deacon auto-commits on main concurrently — commit your own work path-scoped (`git commit -- <files>`), never `--amend` in the primary worktree.

### WI-1 — `resolveStateHome()` + state worktree manager
**Files:** `src/lib/projects.ts` (extend near `resolveInfraRepo`, line 351), new `src/lib/state-home.ts` (worktree ensure/self-heal), `src/cli/commands/doctor.ts` (self-heal hook), `pan install` path.
**Steps:** add `state_home` config parsing (`projects.yaml`: `state_home: { branch: overdeck-state }`, default branch name constant); implement `ensureStateWorktree(project)` → `git worktree add ${OVERDECK_HOME}/state/<key> overdeck-state` (create branch remotely if missing — only during migration, WI-2); doctor reports missing/dirty/wrong-branch state worktrees and repairs.
**Tests:** `src/lib/__tests__/state-home.test.ts` — creates a temp repo, asserts worktree creation, wrong-branch detection, idempotent re-ensure.

### WI-2 — Migration command `pan admin state migrate <project>`
**Files:** new `src/cli/commands/admin/state-migrate.ts`, registered under the existing `pan admin` tree.
**Steps (single command, idempotent):**
1. Create orphan branch `overdeck-state` seeded from the current `main` contents of `.pan/records|continues|specs|drafts|review|test|feedback|backlog|notes` (flattened per D2) and `.beads/` — via a temp index (`git read-tree`/`git commit-tree`), no checkout gymnastics in the primary worktree.
2. Push `overdeck-state`; create the state worktree (WI-1).
3. On `main` (one ordinary commit): `git rm -r --cached` the migrated paths, move `.pan/context/` → `.overdeck/context/` (`git mv`), append the new `.gitignore` block (`.pan/`, `.beads/`), and triage stray files at `.pan/` root (verified present on 2026-07-09: `prd.md`, `pan-*.json`, `flywheel-snapshot.json`, `prd-writer-*.md`, `handoff-flywheel-reload.md`) into `notes/` on the state branch.
4. Stamp `state_home` into the project's config so `resolveStateHome` flips.
**Checkpoint (explicit):** run against a throwaway clone first (`git clone . /tmp/...`); the command must print a dry-run plan with `--dry-run` before any write. **Fallback:** the command makes only ordinary commits — recovery from any failure is `git revert` on main plus branch deletion of a half-seeded `overdeck-state`; re-run is then clean.
**Tests:** integration test on a fixture repo asserting: state branch tree matches D2; main tree contains no state paths post-migration; re-run is a no-op.

### WI-3 — Repoint read doors
**Files:** `src/lib/pan-dir/specs.ts` (`findSpecByIssue`, line 216), `records.ts`, `continue.ts`, `drafts.ts`, `feedback.ts`, `src/lib/backlog/sequence-io.ts` (writer subject at line 148).
**Steps:** each resolves its base directory through `resolveStateHome`; keep D9 legacy fallback (read-only) when unmigrated. Audit the remaining direct `.pan/` references (135 files / 426 refs as of 2026-07-09 — `git grep -c "\.pan/" src/ scripts/`) and route state reads through the doors; workspace-local runtime references are WI-6's scope, not this item's.
**Tests:** extend `src/lib/pan-dir/__tests__/` suites: resolve-from-state-worktree and legacy-fallback cases per domain.

### WI-4 — Harden the write door
**Files:** `src/lib/pan-dir/auto-commit.ts` (`queueAutoCommit` line 189, `flushAutoCommits` line 241).
**Steps:** state-path commits route to the state worktree only; branch assertion before commit; post-flush push with D10 fetch/rebase/retry (bounded 3, fake timers in tests); refuse (log + surface, don't throw away) a state write when the worktree is missing.
**Tests:** extend `src/lib/pan-dir/__tests__/auto-commit.test.ts` — asserts (a) state write commits on `overdeck-state` in the state worktree, (b) branch-assertion failure blocks the commit, (c) rebase-retry loop with `vi.useFakeTimers()`.

### WI-5 — Beads relocation (project scope)
**Files:** call sites invoking bd at project scope (audit `git grep -n "\.beads" src/` — 206 refs; workspace-scoped ones e.g. `src/dashboard/server/routes/backlog.ts:86` are out of scope per D4), `src/cli/commands/beads.ts`, `src/cli/commands/sync.ts`.
**Steps:** project-scope bd invocations gain `-C <state worktree>`; the beads auto-commit (`queueBeadsAutoCommit`, `auto-commit.ts:227`) targets the state worktree; the beads-enforcement gate for workspaces is unchanged.
**Tests:** unit test asserting project-scope bd command construction includes `-C ${stateWorktree}`; existing workspace beads tests stay green (no-loss).

### WI-6 — Context layer + workspace runtime dir rename
**Files:** context readers/renderers (`src/cli/commands/project.ts`, the context API route feeding `ContextPage.tsx`, `pan sync` context assembly), `src/lib/pan-dir/context.ts` (workspace context io, exports at lines 8/20), `.gitignore` (lines 57-151 block rewritten for the new names).
**Steps:** context resolves `<root>/.overdeck/context/` with `.pan/context/` fallback (FR-5); workspace-local runtime dir constants `.pan/` → `.overdeck/` with read fallback (D6); update the context-nomenclature rule table and `docs/CONTEXT-LAYERS.md` + `configuration/context-layers.mdx` in the same commit (rule-authoring requirement).
**Tests:** context-resolution unit tests covering new-path, fallback, and both-present (new wins) cases.

### WI-7 — Mechanical guards
**Files:** new `scripts/guard-state-plane-branches.sh` wired into CI (and the pre-push hook beside `scripts/guard-agent-main-push.sh`), CI workflow branch filters (`.github/workflows/*.yml`) so `overdeck-state` pushes run only the state-tree guard, nothing else.
**Tests:** script-level tests (fixture trees) for both guard directions (FR-2, FR-3).

### WI-8 — Release snapshot
**Files:** the `pan release stable` implementation (`src/cli/commands/release*.ts` — locate via `git grep -n "release stable" src/cli`).
**Steps:** append an `## overdeck-state` section with each registered project's state-branch HEAD SHA to `.release/<tag>.md` (FR-9, D8).
**Tests:** release-notes generation unit test asserting the section renders when a state worktree exists.

### WI-9 — Documentation (required, not a follow-up)
**Files:** `docs/AGENT-STATE-PLANES.md` (permanent plane's git home), `docs/STATE-PLANE-COMMIT-POLICY.md` (rewritten around the write door + state worktree), `docs/CONTEXT-LAYERS.md` + `configuration/context-layers.mdx` (context's new home), `docs/VBRIEF.md` (artifact locations table), root `CLAUDE.md` (vBRIEF/lifecycle path references, "Commit and Push When Working on Main" section gains the state-branch note), `.claude/rules/context-nomenclature.md` routing table, module headers of every file WI-1–WI-8 touches.
**Acceptance:** every path this PRD relocates appears with its new location in the docs above; `git grep -l "\.pan/records\|\.pan/specs\|\.pan/drafts" docs/` returns only files that explicitly describe the legacy/migration state.

### WI-10 — No-loss audit gate
**Files:** new `tests/integration/state-branch-no-loss.test.ts`.
**Steps:** per the no-loss-audit rule, enumerate every state surface the old layout exposed (each `STATE_PLANE_PATHS` entry, context, drafts, backlog, beads exports) and assert each has a working read path in the new layout (or an explicitly documented retirement). The migration (WI-2) is blocked on this suite passing against a migrated fixture.

## Acceptance criteria (1:1 with work items)

- **AC-1** (WI-1) `pan doctor` on a migrated project reports a healthy state worktree; deleting the worktree and re-running doctor recreates it on `overdeck-state`.
- **AC-2** (WI-2) After `pan admin state migrate overdeck`: `git ls-tree origin/main` contains no `.pan/` (except nothing — context now lives at `.overdeck/context/`) and no `.beads/`; `git ls-tree overdeck-state` matches the D2 layout; re-running the command exits 0 with "already migrated".
- **AC-3** (WI-3) `findSpecByIssue` resolves a spec that exists only on `overdeck-state`; with the worktree absent it resolves the same spec from legacy `.pan/specs/`.
- **AC-4** (WI-4) A queued state write on a migrated project produces a commit on `overdeck-state` (never `main`), and the named auto-commit tests (branch assertion, fake-timer retry) pass.
- **AC-5** (WI-5) `bd ready` executed by Overdeck at project scope operates on the state worktree's `.beads/`; workspace beads enforcement (422 gate) behaves exactly as before.
- **AC-6** (WI-6) Context renders identically from `.overdeck/context/`; a repo with only legacy `.pan/context/` still renders (fallback); `.gitignore` blocks `.pan/` and `.beads/` on main.
- **AC-7** (WI-7) CI fails a synthetic PR that adds `records/x.json` under a state path to `main`; CI fails a synthetic push to `overdeck-state` containing `src/anything.ts`.
- **AC-8** (WI-8) `.release/<tag>.md` generated on a migrated machine contains the `overdeck-state` SHA section.
- **AC-9** (WI-9) Docs acceptance grep (WI-9) passes; Mintlify pages updated in the same PR.
- **AC-10** (WI-10) The no-loss suite passes against the migrated fixture; it fails if any legacy surface loses its read path.

## Out of scope (explicitly)

- Renaming the `pan` CLI, the `src/lib/pan-dir/` module, or issue prefixes (D7; see the PAN-1952 naming policy — prefixes stay).
- Removing the `checkPostReviewCommits` benign-move guards (D11 — kept as backstops).
- Multi-tenant / separate-repo state homes (the `pan_records.repo` polyrepo path keeps working but is not extended here).
- Workspace beads redesign (D4 — unchanged).

## Related

[PAN-2468](https://github.com/eltmon/overdeck/issues/2468) (incident + guard patch `9887913c73`) · PAN-1908 (state planes) · PAN-1124 (single-spec-on-main invariant — preserved, relocated) · PAN-1952 (naming policy) · PAN-2318 (orphaned servers, independent).
