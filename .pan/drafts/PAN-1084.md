# PAN-1084 — Work agent must not auto-approve permission prompts in subagent sessions

**Planning outcome: HALTED — already implemented. No vBRIEF / no beads generated.**

## Summary

Discovery during this planning session established that every acceptance criterion
of PAN-1084 is **already implemented on `main`, wired into the spawn path, and locked
by tests**. The work was delivered by PR #1096 (Meta-B PAN-1091) on 2026-05-11 — the
same meta-issue whose comment marked PAN-1084 as "Superseded by Meta-B #1091".

This is a genuine contradiction between authoritative inputs (the issue asks to build
a feature that already exists), so per the `--auto` planning protocol the session halts
and escalates rather than finalizing a plan.

## Acceptance-criteria audit (verified against the current tree at 679a7b507)

| AC (from issue body) | Status | Evidence |
|---|---|---|
| `work.md` prompt contains the explicit prohibition | DONE | `src/lib/cloister/prompts/work.md:113-118` — "## CRITICAL: Never Approve Permission Prompts" section; `roles/work.md:179`. Locked by `src/lib/__tests__/role-definitions.test.ts:64-65`. |
| Tool/hook-level enforcement blocks `tmux send-keys` against any non-self session | DONE (stronger than asked) | `src/lib/claude-settings-overlay.ts:76-80` adds deny patterns `Bash(tmux send-keys:*)`, `Bash(tmux -L overdeck send-keys:*)`, `Bash(tmux paste-buffer:*)`, `Bash(tmux -L overdeck paste-buffer:*)`. Injected into each workspace at spawn via `injectOverdeckInfraDeny()` (`src/lib/agents.ts:3038-3039`). Locked by `src/lib/__tests__/claude-settings-overlay.test.ts:27-50`. The deny is blanket (all send-keys/paste-buffer), which is stricter and more durable than the "non-self session only" filter the issue proposed. |
| Test: work agent does NOT send keys, emits notification instead | DONE | Covered by the two unit guards above. The harness-level permission deny makes the proposed integration test moot — the `tmux send-keys` tool call is rejected by Claude Code before the agent can reason about driving the prompt, so there is no agent-level behavior left to observe. |

## Why this happened

Meta-B PAN-1091 ([#1091](https://github.com/eltmon/overdeck/issues/1091)) body states:
"Original issues #1084 and #1087 will be closed as superseded." When PR #1096 merged
(2026-05-11), **#1087 was closed** but **#1084 was left OPEN** — an oversight. PAN-1084
later received `planned` / `ready` labels and was routed into this planning session.

## Recommended disposition

Close PAN-1084 as **already-done / superseded by #1091 (PR #1096)**. An evidence
comment with the audit above has been posted to the issue. No implementation work
remains. If a real residual gap is ever identified, re-scope the issue body to name
only that gap before re-planning.

## Planning Q&A

None — this session ran in `--auto` mode and halted on a verified contradiction
before any clarifying questions were warranted.
