# PAN-3957: Overdeck-owned project memory: curated facts in the repo, injected into every managed harness

**Issue:** https://github.com/eltmon/overdeck/issues/3957 (labels `enhancement`, `parked`)
**Planned:** 2026-09-23, against `origin/main` `0260c28298d` (every file:line below was checked at that SHA)
**Related:** PAN-3920 (Agents directory + `pan worker run`), PAN-3940 (plugin/hook system), PAN-3917 (the Cut),
PAN-1201 (context layers), PAN-3286 / PAN-1990 (observation memory), PAN-3998 (legacy `.pan/` ignore repair)
**Host facts verified while planning:** the operator's Claude auto-memory store for this repo,
`~/.claude/projects/-home-eltmon-Projects-overdeck/memory/`, holds 223 fact files + `MEMORY.md` (1.1 MB).
`MEMORY.md` links 160 of them; the other 63 are unindexed ("Archived" in its own words). 216 files keep
their type under `metadata.type`, 6 use a top-level `type:`, 1 has no `description:`. 29 files contain
`/home/eltmon`, `/tmp/` or `localhost:<port>`. The host has 27 Claude memory stores, one per Claude
project slug. `~/.codex/memories/` holds only `.agents/ .codex/ .git` (created 2026-05-06, no facts), and
`~/.codex/config.toml` has no `memor` key. `~/.pi/agent`, `~/.omp/agent` and `~/.kimi` have no memory store.

---

## 0. Open questions for the operator

Each question has a recommended answer. The rest of this document assumes that answer. If the operator
picks an alternative, only the named decision changes.

| # | Question | Recommendation | Alternatives considered |
| --- | --- | --- | --- |
| **Q1** | **Where do curated facts live?** | `<planHome>/.overdeck/memory/<id>.md`, committed on the code branch. For a single repository, `planHome` is the project root. For a polyrepo, it is the `pan_records.repo` sub-repo (D1). | (a) `.pan/memory/`: myn, myn-cli and lexerra still ignore `.pan/` wholesale until PAN-3998's repair runs, so the files would be silently untracked. (b) `memory/` on `overdeck-state`: gone. The Cut removed the state worktree (`docs/THE-CUT.md:158`, `:162`). (c) The OKF bundle: a separate, opt-in repository that only this project has, and it is PR-gated for long-form concepts (Q3). |
| **Q2** | **What is the curation workflow?** | Git is the curation. `pan memory add` writes an **uncommitted** file into the checkout that owns the cwd. In a workspace, the fact rides the feature branch and its PR. On the primary checkout, it goes live for this machine at once and the author commits it. Launch injection reads the **primary checkout's working tree** (D3), the same way the project layer does today. | An inbox under `~/.overdeck` plus a `pan memory accept` verb. This adds a second store and state to keep true, and the PR diff already does the job. |
| **Q3** | **How does this relate to OKF?** | They stay separate and complementary. Facts are short, operational, and agent-writable. OKF concepts are long-form and PR-gated in their own repository. Facts use the same frontmatter keys OKF's indexer reads (`type`, `title`, `description`, `tags`), so `/okf author` can promote a fact without reformatting it. Prompt-time OKF injection (`injection.ts:459`) does not change. | Store facts inside the OKF bundle as `type: Memory` concepts. Every project would then need a second repository, and agents could not write facts without a PR in another repository. |
| **Q4** | **What happens to Claude's native auto-memory in managed sessions?** | In phase 2, add `memory.harness_native_memory: off \| conversations \| on`, default `conversations`. Pipeline agents (work, review, plan, and the rest) launch with `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1`. Operator conversations keep auto-memory until the operator sets `off` after importing. Overdeck never edits `~/.claude/settings.json` and never deletes native files. | (a) Default `off` at once: the operator loses a working tool before the import is reviewed. (b) Redirect `autoMemoryDirectory` into the repository: this means per-slug settings writes into a user-owned file, which breaks the no-touch boundary (`docs/CONTEXT-LAYERS.md` "Respecting your existing context"). |
| **Q5** | **What happens to existing harness memory?** | A one-shot `pan memory import claude`, run on a branch (W9). `type: user` files are skipped, or sent to the operator store with `--include-user`. Files that trip secret redaction are skipped. Unindexed (63) and machine-specific (29) files are imported with a `review:` marker, and a marked fact is **never injected** until a reviewer deletes the file or removes the marker. The native store is read-only. | Import only indexed files: this loses 63 facts the operator may still want. Auto-drop machine-specific files: a regex cannot tell a stale path from a real fact. |
| **Q6** | **Where does user-level memory live** (the issue: "who the operator is stays outside the repo")? | `~/.overdeck/context/memory/<id>.md`, injected as `## Operator memory` into every managed session on this machine. It sits beside the global layer. | `~/.overdeck/memory/user/`. This collides with the observation home `~/.overdeck/memory/{projectId}/` if a project is ever keyed `user`. |
| **Q7** | **Must this wait for PAN-3920?** | No. There is no code dependency (§8). The two only touch the same launch-context composition, so land them in either order. | Hold until PAN-3920 lands. Nothing in this PRD needs its code. |

---

## 1. Glossary

| Term | Meaning |
| --- | --- |
| **Fact** (curated memory) | One durable, human-reviewable statement stored as one markdown file with YAML frontmatter. Examples: a working agreement ("make routine reversible calls yourself"), an operator correction, or a non-obvious project truth ("`schema.ts` is `panopticon.db`, not `overdeck.db`"). This issue adds facts. |
| **Observation memory** | The existing, automatically extracted session log substrate: `~/.overdeck/memory/{projectId}/{workspaceId}/{observations,pending,status.json,summaries,archive}` plus the FTS index (`docs/WORKSPACES-AND-PROJECTS.md:287-303`). The 12 existing `pan memory` verbs (`search`, `status`, `timeline`, `read`, `reset`, `summary`, `doctor`, `backfill`, `pin`, `unpin`, `pins`, `config`) operate on it. **Out of scope** here (issue: "Out of scope"). |
| **Project store** | The directory of project-scoped facts: `<planHome>/.overdeck/memory/`. It is flat, one file per fact, and the filename stem is the fact id. |
| **Operator store** | The directory of user-scoped facts: `~/.overdeck/context/memory/`. It is per machine and never committed. |
| **Plan home** | The checkout that holds a project's committed Overdeck artifacts: `resolvePlanHome(root)` in `src/lib/pan-dir/paths.ts:46`. For a single repository it is the checkout itself. For a polyrepo it is the `pan_records.repo` sub-repo. |
| **Primary checkout** | `ProjectConfig.path` from `~/.overdeck/projects.yaml`: the operator's main clone, not a `workspaces/feature-*` worktree. `findProjectByPathSync(workspace).path` resolves it (`src/lib/projects.ts:767`). |
| **Memory index** | The rendered, budgeted list of active facts (one line per fact) that is injected at launch. It is **never stored**. It is derived from frontmatter every launch, and it takes over the job of Claude's `MEMORY.md`. |
| **Launch composition** | The Overdeck-owned context artifact built for one managed session. It comes from `renderManagedLaunchContext` (`src/lib/context-layers/materialize.ts:29`) or, for Muse, `materializeMuseContext` (`src/lib/runtimes/muse-context.ts:12`). |
| **Harness-native memory** | A memory store owned by the harness itself. Only Claude Code's auto-memory (`~/.claude/projects/<slug>/memory/`) is active on this host (see header facts). |
| **Claude project slug** | Claude Code's directory name for a cwd: every non-alphanumeric character becomes `-`. `/home/eltmon/Projects/overdeck` becomes `-home-eltmon-Projects-overdeck`, and `/home/eltmon/.paperclip/...` becomes `-home-eltmon--paperclip-...` (both verified in `~/.claude/projects/`). |
| **Review marker** | The frontmatter key `review: [<flag>, …]`. A fact that carries a non-empty marker is listed by `pan memory list --review` but is excluded from the memory index. |
| **Active fact** | A parseable fact file with no review marker. |

---

## 2. Problem (current state, verified)

1. **Every harness keeps its own memory, and only Claude's is live.** Claude Code writes
   `~/.claude/projects/<slug>/memory/*.md` and preloads `MEMORY.md` (first ~200 lines / 25 KB) into every
   session (`docs/research/claude-code-memory-integration.md:8`). Codex managed sessions run with a
   per-agent `CODEX_HOME=~/.overdeck/agents/<id>/codex-home-v2` (`src/lib/runtimes/codex.ts:812`), so
   they never see any native store. Pi, OMP and Kimi have none. A Codex or Kimi agent on the same issue
   learns nothing from the 223 facts the operator's Claude sessions have built up.
2. **Overdeck already has a memory store that is fragmented even for Claude.** Claude's store follows the
   cwd, and the host has 27 stores, several for single workspaces (for example
   `-home-eltmon-Projects-myn-workspaces-feature-min-1039`). Facts do not travel to another machine or a
   teammate, and the project cannot review them.
3. **The context layers explicitly exclude it.** `docs/CONTEXT-LAYERS.md:254-255` says: "Claude auto
   memory is another input, normally under `~/.claude/projects/<project>/memory/`; it is not a
   context-layer source."
4. **The seams already exist.** Every managed launch composes one Overdeck-owned artifact:
   - `renderManagedLaunchContext` (`materialize.ts:29-47`, anchor `if (project) sections.push(renderProjectLayer(project.path, harness));`)
     is used by `claudeSystemPromptFiles` (`src/lib/context-layers/launch-sources.ts:29`) for claude-code,
     codex, ohmypi and kimi-code, and by `materializeAcpContextFile` (`src/lib/acp/context.ts:11`) for acp
     and opencode.
   - `materializeMuseContext` (`src/lib/runtimes/muse-context.ts:14-16`) builds the same sections separately for muse.
   - The dashboard Context page preview composes its own copy in `previewForHarness`
     (`src/dashboard/server/routes/context.ts:398-405`). `docs/CONTEXT-LAYERS.md` promises that "the
     preview always matches what ships".
5. **The frontmatter shape is already indexed.** `parseConcept` (`src/lib/memory/injection.ts:600-613`)
   reads `id` / `type` / `description` (falling back to `title`) from OKF concept files for the prompt-time
   knowledge index. Facts that use the same keys are OKF-compatible.
6. **The workspace layer has a dead memory slot.** `assembleWorkspaceContext` accepts `memoryContext`
   (`src/lib/context-layers/assemble.ts:75`, `:95`), but no production caller passes it
   (`grep -rn "memoryContext:" src --include=*.ts` finds only tests). It is frozen at workspace
   creation, so it is the wrong carrier for facts that change. This PRD does not use it.

---

## 3. Decisions (made here; do not re-decide during implementation)

- **D1: Store paths.** Project store: `join(resolvePlanHome(root), '.overdeck', 'memory')`. Operator
  store: `join(getOverdeckHome(), 'context', 'memory')`. Both are flat. Facts use the plan home, not
  `projectContextDir` (`layers.ts:60`, which uses the bare `projectRoot`). The reason: a polyrepo
  wrapper ignores `.overdeck/` wholesale (`src/lib/workspace-manager/create.ts:81-88`, anchor
  `runtimeEntries.push('.overdeck/')`), and the plan home is the one tracked repository Overdeck already
  picks for polyrepos. For a single repository both resolve to the same directory.
- **D2: File format** (OKF-compatible, Claude-importable). Filename `<id>.md`, where
  `id` matches `/^[a-z0-9][a-z0-9-]{0,79}$/`. Frontmatter:

  ```yaml
  ---
  type: feedback            # required: feedback | project | reference | user
  title: Be decisive        # optional; defaults to the id with dashes as spaces, first letter upper-cased
  description: Make routine reversible calls yourself; ask only for irreversible or operator-owned ones.
                            # required, one line, <= 200 chars; this is the index line
  tags: [workflow]          # optional string array
  updated: 2026-09-23       # optional YYYY-MM-DD; add/edit set it; sort key
  source: claude:6c30e00a-54f1-4a0e-9d4b-1c2f3e4d5a6b   # optional provenance
  review: [unindexed]       # optional; non-empty means excluded from the index
  ---
  <body: the fact, then **Why:** and **How to apply:** lines, free markdown>
  ```

  The type `user` is valid only in the operator store. A `user` file in a project store is a problem
  (reported, skipped). Files named `README.md`, files starting with `.`, and non-`.md` files are ignored,
  so a human README may sit in the directory. Subdirectories are ignored.
- **D3: What launch reads.** The project section reads the **primary checkout's** project store
  (`findProjectByPathSync(workspace).path`), the same source the project layer already uses
  (`materialize.ts:31-32`). This includes uncommitted files in that working tree. A fact added in a
  workspace reaches other agents when its branch merges and the primary checkout pulls. The primary
  checkout can be on a non-main branch (it is on `feature/pan-3920` today); that is accepted, matching
  the project layer.
- **D4: What writes target.** `pan memory add|edit|retire` target the project store of the checkout that
  owns `process.cwd()` (`resolvePlanHome(process.cwd())` is depth-idempotent, `paths.ts:38-45`).
  `--project <key>` targets that project's primary checkout instead. `--user` targets the operator store.
  If the cwd is outside a registered project and neither flag is given, the command refuses. Writes are
  **never committed** by Overdeck, and the command prints the path to commit.
- **D5: No stored index.** Nothing writes a `MEMORY.md` or index file. The index is rendered at launch
  from frontmatter (CLAUDE.md: "Overdeck stores no status it can derive"). Claude's store shows why:
  160 of 223 files are indexed.
- **D6: The index format and budget.** One line per active fact: `- <type>/<id>: <description>`. Order by
  type (`feedback`, `project`, `reference`, `user`), then `updated` descending (missing sorts last), then
  id ascending. Budgets: project section 12,000 chars, operator section 4,000 chars, each counted on the
  rendered line list. When the next line would exceed the budget less 80 chars, stop and append
  `- … <n> more facts: run \`pan memory list\``. Bodies are never injected. An agent opens the file.
- **D7: Section text** (exact; W2 renders it):

  ```markdown
  ## Project memory

  Source: <absolute project store path> (<n> active facts, curated and committed with this repository)
  One line per fact. Open the fact (`pan memory show <id>`) before relying on its details.
  Record a new durable fact with `pan memory add`; never write harness-native memory.

  - feedback/be-decisive: Make routine reversible calls yourself; ask only for irreversible or operator-owned ones.
  ```

  The operator section is identical with the heading `## Operator memory`, the source path of the
  operator store, `(… active facts, this machine only)`, and `pan memory show --user <id>`. A store with
  zero active facts renders **no section** (not an empty heading).
- **D8: Placement in the composition.** Project memory goes after the project layer and before the
  workspace section. Operator memory goes right after project memory. Facts get no `{{#harness:X}}`
  rendering: they are harness-neutral text.
- **D9: Memory never breaks a launch.** `renderManagedMemorySections` catches every error, calls
  `console.warn('[memory] facts unavailable: <reason>')`, and returns `''`. A malformed fact file is
  skipped, never fatal.
- **D10: Secret guard.** `writeMemoryFact` refuses (`MemoryFactSensitiveError`) when
  `redactSensitiveText(serialized) !== serialized` (`src/lib/secret-redaction.ts:97`). That function
  redacts env assignments only for sensitive names (`SENSITIVE_ENV_NAME_PATTERN`, line 9), so
  `OVERDECK_RTK_ENABLED=0` passes. There is no override flag: rephrase the fact.
- **D11: Git ignore guard.** Before writing into a project store, `pan memory add` runs
  `git -C <planHome> check-ignore -q .overdeck/memory/<id>.md` through promisified `execFile`. Exit 0 (the
  path is ignored) means refuse, with the fix `add "!.overdeck/memory/" to <planHome>/.gitignore`. Exit 1
  means proceed. Any other exit (not a git repository) means print a yellow warning and proceed.
- **D12: `retire` deletes.** `pan memory retire <id>` unlinks the file. Git history is the archive, and
  the command prints `git -C <planHome> rm`-style guidance and the suggested commit subject
  `docs(memory): retire <id>`.
- **D13: Search.** `pan memory search <q>` (human output) prints matching facts **first** (the project
  store for the cwd or `--project`, then the operator store; at most 5 each). A fact matches when every
  whitespace-separated term of `<q>` appears case-insensitively in `id + title + description + tags +
  body`. `--json` output is **unchanged** (an array of observation hits), because
  `src/lib/compliance/status.ts` and scripts consume it. `pan memory list --query <q> --json` is the
  machine-readable fact search.
- **D14: Harness-native memory switch (phase 2).** Config `memory.harness_native_memory`, default
  `conversations`. The launcher emits `export CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` (a documented, stable
  knob: research doc line 19 and line 31) when the harness behavior's `contextLayerKind` is `claude` and
  either the mode is `off`, or the mode is `conversations` and `spawnMode !== 'conversation'`. Mode `on`
  emits nothing. The launcher never emits `unset`: a user who disabled auto-memory globally stays
  disabled. Operator conversations launch with `spawnMode: 'conversation'`
  (`src/lib/overdeck/conversation-runtime.ts:761`). Codex, Pi, OMP and Kimi need no switch (header
  facts).
- **D15: Import rules (phase 2).** These are in W9 and are listed there in full.

---

## 4. Requirements

### Functional

- **FR-1** `readMemoryFactsSync(dir, scope)` returns every valid fact in a flat directory and a `problems`
  list for every invalid `.md` file. It never throws for a missing directory (it returns empty).
- **FR-2** `writeMemoryFact` writes `<dir>/<id>.md` atomically (temp file + rename), refuses an existing
  id unless `overwrite`, refuses sensitive content (D10), and refuses `type: user` in a project store.
- **FR-3** Every managed launch composition (`renderManagedLaunchContext`, `materializeMuseContext`) contains
  `## Project memory` when the primary checkout's project store has ≥ 1 active fact, and
  `## Operator memory` when the operator store has ≥ 1 active fact. It follows D6, D7 and D8.
- **FR-4** Facts with a non-empty `review` marker never appear in a launch composition.
- **FR-5** The dashboard Context page preview shows the same memory sections as the launch composition
  for the selected project.
- **FR-6** `pan memory add|edit|show|list|retire` exist with the flags in W5. `add`, `edit` and `retire` touch
  exactly one file.
- **FR-7** `pan memory search` prints fact matches before observation hits (D13); `--json` is byte-identical
  to today for the same observation results.
- **FR-8** A universal bundled rule tells every managed agent to record durable facts with
  `pan memory add` and not in harness-native memory. The `context-nomenclature` rule routes
  "remember that …" to it.
- **FR-9** `pan memory import claude` maps a Claude auto-memory store into facts per D15/W9. It is dry-run
  by default, and `--apply` writes. It never modifies the source directory.
- **FR-10** `memory.harness_native_memory` (`off | conversations | on`, default `conversations`) controls
  `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` in managed Claude launchers per D14.
- **FR-11** Docs updated per W8 and W11.

### Non-functional

- **NFR-1** No `execSync` anywhere new. The only subprocess (`git check-ignore`, D11) is CLI-only and
  runs through `promisify(execFile)`. The launch path uses sync `fs` reads only because
  `renderManagedLaunchContext` is already synchronous (`materialize.ts:36`, `readFileSync`).
- **NFR-2** No test touches the real `~/.claude`, `~/.overdeck`, or a real project. Tests set
  `OVERDECK_HOME` to a `mkdtempSync` directory (the `tests/lib/context-layers/materialize.test.ts:31-35`
  pattern) and pass explicit source/target directories.
- **NFR-3** The file-size ratchet (`bash scripts/lint-file-size.sh`, ceiling 1000 lines):
  `src/lib/launcher-generator.ts` is 1031 lines and allowlisted at 1031
  (`scripts/file-size-allowlist.txt:89`). W10 adds at most 2 lines there. If the lint fails, append
  `<lines> src/lib/launcher-generator.ts # PAN-3957` to the allowlist in the same commit. All new logic
  lives in new modules under `src/lib/memory/facts/` and `src/cli/commands/memory-facts.ts`, not in
  `src/lib/memory/cli.ts` (736 lines) or `src/cli/commands/memory.ts` (483 lines).
- **NFR-4** Skills and the CLI stay in lockstep: `sync-sources/skills/pan-memory/SKILL.md` lists every new verb
  and flag in the same commit that adds them, and `bash scripts/lint-skills.sh` passes.
- **NFR-5** The launch index is bounded: the project section is ≤ 12,000 chars and the operator
  section is ≤ 4,000 chars, whatever the number of facts.

---

## 5. Intersecting repo rules (restated)

- **Never `execSync` in dashboard-server-reachable code** (`sync-sources/rules/no-execsync-server.md`). The
  launch composition and the Context route are server-reachable (NFR-1).
- **Native instruction files and harness-native stores are a strict no-touch boundary**
  (`docs/CONTEXT-LAYERS.md` "Respecting your existing context"). Import reads `~/.claude/projects/*/memory`
  and never writes, moves or deletes there. Phase 2 uses an env var in Overdeck's own launcher and never
  edits `~/.claude/settings.json`.
- **JSONL session files are sacred** (CLAUDE.md). Import reads only `*.md` in the memory directory.
- **Skills ↔ CLI lockstep** (NFR-4). **File-size ratchet** (NFR-3).
- **Worktree discipline / never `git stash`.** Implement in `workspaces/feature-pan-3957/`.
- **Bundled universal rules must be terse** (`sync-sources/rules/rule-authoring.md` step 3: "costs
  context in every session on every machine").
- **Quality gates** (CLAUDE.md "Project Structure"): `npm run typecheck`, `npm run lint`, and
  `npx vitest run <the test files you touched>`. Never run the full `npm test` on the host. The full suite
  runs once, on CI, against the PR head. `npm run typecheck` excludes `*.test.ts`, so running the named
  tests is the only local proof they compile.
- **Fake timers:** no work item here adds a delay, retry or timeout. If one appears, use
  `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync()`.

---

## 6. Phasing and proposed child issues (not filed)

| Phase | Work items | Ships | Proposed tracking |
| --- | --- | --- | --- |
| **P1: store + injection** | W1–W8 | Facts exist, are injected into every managed harness, are editable through `pan memory`, and are documented. | PAN-3957 itself |
| **P2: migration + cutover** | W9–W11 | Import of Claude auto-memory, and the `harness_native_memory` switch. | Child: "Import Claude auto-memory into repo facts and switch off native memory in managed sessions" |
| **P3: deeper integration** (not specified here) | none | Facts as an FTS `docType: 'fact'` for prompt-time retrieval of bodies; a dashboard fact editor on the Context page; capture hooks on PAN-3940 (for example, a `PreToolUse` redirect of Claude memory writes into `pan memory add`, research doc line 44). | Child: "Fact retrieval, dashboard editor, and capture hooks for repo memory" (blocked on PAN-3940 for hooks) |

P1 and P2 are fully specified below. P2 depends on P1's store library (W1) only.

---

## 7. Work items

Each item names its files, the exact change, and the tests that prove it.

### W1: Fact store library (FR-1, FR-2, D1, D2, D10)

**New file:** `src/lib/memory/facts/store.ts`. **Tests:** `tests/unit/lib/memory/facts/store.test.ts`.

```ts
/**
 * Curated memory facts (PAN-3957): one markdown file per fact with OKF-compatible
 * frontmatter. Project facts live in `<planHome>/.overdeck/memory/`, operator facts
 * in `~/.overdeck/context/memory/`. Nothing here stores an index: the index is
 * rendered from frontmatter at launch (render.ts).
 */
import { existsSync, readdirSync, readFileSync } from 'node:fs';
import { mkdir, rename, unlink, writeFile } from 'node:fs/promises';
import { join } from 'node:path';
import { randomUUID } from 'node:crypto';
import { parse as parseYaml, stringify as stringifyYaml } from 'yaml';
import { getOverdeckHome } from '../../paths.js';
import { resolvePlanHome } from '../../pan-dir/paths.js';
import { redactSensitiveText } from '../../secret-redaction.js';

export const MEMORY_FACT_TYPES = ['feedback', 'project', 'reference', 'user'] as const;
export type MemoryFactType = (typeof MEMORY_FACT_TYPES)[number];
export type MemoryFactScope = 'project' | 'user';
export const MEMORY_FACT_ID = /^[a-z0-9][a-z0-9-]{0,79}$/;
export const MEMORY_FACT_DESCRIPTION_MAX = 200;

export interface MemoryFact {
  readonly id: string;
  readonly scope: MemoryFactScope;
  readonly type: MemoryFactType;
  readonly title: string;
  readonly description: string;
  readonly tags: readonly string[];
  readonly updated: string | null;   // YYYY-MM-DD
  readonly source: string | null;
  readonly review: readonly string[];
  readonly body: string;
  readonly file: string;             // absolute
}
export interface MemoryFactProblem { readonly file: string; readonly reason: string }
export interface MemoryFactReadResult { readonly facts: MemoryFact[]; readonly problems: MemoryFactProblem[] }
export class MemoryFactSensitiveError extends Error {}
export class MemoryFactExistsError extends Error {}

export function projectMemoryDir(checkoutPath: string): string {
  return join(resolvePlanHome(checkoutPath), '.overdeck', 'memory');
}
export function userMemoryDir(): string {
  return join(getOverdeckHome(), 'context', 'memory');
}
export function defaultFactTitle(id: string): string { /* 'be-decisive' -> 'Be decisive' */ }
export function parseMemoryFact(file: string, raw: string, scope: MemoryFactScope): MemoryFact | MemoryFactProblem;
export function readMemoryFactsSync(dir: string, scope: MemoryFactScope): MemoryFactReadResult;
export function serializeMemoryFact(fact: Omit<MemoryFact, 'file' | 'scope'>): string;
export async function writeMemoryFact(dir: string, scope: MemoryFactScope,
  fact: Omit<MemoryFact, 'file' | 'scope'>, opts?: { overwrite?: boolean }): Promise<string>;
export async function removeMemoryFact(dir: string, id: string): Promise<string>;
export function isActiveFact(fact: MemoryFact): boolean { return fact.review.length === 0; }
```

Steps:

1. `parseMemoryFact`: match `/^---\n([\s\S]*?)\n---\n?/` (the same regex as `injection.ts:601`). A missing
   match gives the problem `no frontmatter`. Parse with `parseYaml`. Problems, each with its exact
   reason string: `invalid id "<stem>"` (stem fails `MEMORY_FACT_ID`), `invalid type "<t>"`,
   `user facts belong in the operator store (~/.overdeck/context/memory)` (scope `project` and type
   `user`), `missing description`, `description must be one line`, and
   `description exceeds 200 characters`. `tags` and `review` accept only string arrays (otherwise the
   problem `tags must be a list of strings` / `review must be a list of strings`). `updated` must match
   `/^\d{4}-\d{2}-\d{2}$/` when present (otherwise `invalid updated date`). Body = the text after the
   frontmatter, `trim()`med.
2. `readMemoryFactsSync`: when `!existsSync(dir)`, return `{ facts: [], problems: [] }`. Otherwise
   `readdirSync(dir, { withFileTypes: true })`, keep regular files ending `.md` that do not start with `.`
   and are not `README.md`, and parse each. Output is sorted by id.
3. `serializeMemoryFact`: frontmatter keys in the fixed order `type, title, description, tags, updated,
   source, review`. Omit `title` when it equals `defaultFactTitle(id)`, omit empty `tags`/`review`, and
   omit null `updated`/`source`. Then `---\n\n<body>\n`.
4. `writeMemoryFact`: validate by round-tripping through `parseMemoryFact` (throw `Error(reason)` on a
   problem). Throw `MemoryFactSensitiveError('fact contains secret-like content; rephrase it')` when
   `redactSensitiveText(text) !== text`. Throw `MemoryFactExistsError` when the file exists and not
   `overwrite`. Then `mkdir(dir, { recursive: true })`, write `<file>.<uuid>.tmp`, and `rename`.
   Return the path.
5. `removeMemoryFact`: when the file is absent, throw `Error('no fact "<id>" in <dir>')`. Otherwise
   unlink it and return the path.

Tests (`store.test.ts`): `parses a Claude-shaped body with Why/How lines`; `reports each problem
reason` (one case per reason in step 1); `skips README.md, dotfiles, subdirectories and non-md`;
`missing directory reads as empty`; `serialize → parse round-trips every field`; `writeMemoryFact
refuses an existing id without overwrite`; `refuses sk-ant key and GITHUB_TOKEN=… but accepts
OVERDECK_RTK_ENABLED=0`; `refuses type user in a project store`; `projectMemoryDir of a workspace path
resolves to the workspace checkout` (mock `../../../src/lib/projects.js` like
`materialize.test.ts:7-10`, adding `resolveInfraRepo: (_p, root) => ({ repoPath: root, recordsPath: '.pan' })`).

### W2: Index renderer (FR-3, FR-4, D5–D9, NFR-5)

**New file:** `src/lib/memory/facts/render.ts`. **Tests:** `tests/unit/lib/memory/facts/render.test.ts`.

```ts
export const PROJECT_MEMORY_CHAR_BUDGET = 12_000;
export const OPERATOR_MEMORY_CHAR_BUDGET = 4_000;

export function renderMemoryIndexSection(input: {
  scope: MemoryFactScope; dir: string; facts: readonly MemoryFact[]; budget: number;
}): string;

/** Project memory (primary checkout) + operator memory. Never throws (D9). */
export function renderManagedMemorySections(projectRoot: string | null): string;
```

Steps:

1. `renderMemoryIndexSection`: filter `isActiveFact`. Return `''` when none remain. Sort per D6 (type
   rank `feedback 0, project 1, reference 2, user 3`, then `updated` descending with null last, then id).
   Build lines `- ${type}/${id}: ${description}` and add them while `used + line.length + 1 <= budget -
   80`. If any are left out, append `- … ${omitted} more facts: run \`pan memory list\``, or with
   `pan memory list --user` for the operator scope. Prefix the exact D7 header text.
2. `renderManagedMemorySections(projectRoot)`: inside `try`, build the project section from
   `readMemoryFactsSync(projectMemoryDir(projectRoot), 'project')` when `projectRoot` is non-null, and
   the operator section from `readMemoryFactsSync(userMemoryDir(), 'user')`. Join the non-empty sections
   with `\n\n---\n\n`. In `catch (error)`: call
   `console.warn(\`[memory] facts unavailable: ${error instanceof Error ? error.message : String(error)}\`)`
   and return `''`. Problems (malformed files) are silently skipped here. `pan memory list` reports them.

Tests: `renders the exact D7 header and one line per active fact`; `orders feedback, project, reference,
then updated desc, then id`; `excludes facts with a review marker`; `returns '' when no active facts`;
`stops at budget and appends the overflow line` (300 facts with 150-char descriptions; assert
`section.length <= 12_000` and the line `more facts: run`); `operator section uses --user in its hints`;
`renderManagedMemorySections swallows a throwing reader and warns once` (spy on `console.warn`).

### W3: Inject into every launch composition (FR-3, D3, D8)

**Files:** `src/lib/context-layers/materialize.ts`, `src/lib/runtimes/muse-context.ts`,
`tests/lib/context-layers/materialize.test.ts`, `tests/unit/lib/runtimes/muse-context.test.ts` (create
this file if it does not exist; check with `ls tests/unit/lib/runtimes/ src/lib/runtimes/__tests__/ | grep muse`
and extend the existing muse-context test if one is found).

**Before** (`materialize.ts:29-33`):

```ts
export function renderManagedLaunchContext(workspace: string, harness: Harness): string {
  const sections: string[] = [renderGlobalLayer(harness, isDevMode())];
  const project = findProjectByPathSync(workspace);
  if (project) sections.push(renderProjectLayer(project.path, harness));

  const workspaceFile = resolveWorkspaceContextFile(workspace);
```

**After:**

```ts
export function renderManagedLaunchContext(workspace: string, harness: Harness): string {
  const sections: string[] = [renderGlobalLayer(harness, isDevMode())];
  const project = findProjectByPathSync(workspace);
  if (project) sections.push(renderProjectLayer(project.path, harness));
  // PAN-3957: curated facts, harness-neutral, read from the primary checkout.
  sections.push(renderManagedMemorySections(project?.path ?? null));

  const workspaceFile = resolveWorkspaceContextFile(workspace);
```

Add `import { renderManagedMemorySections } from '../memory/facts/render.js';`. Empty strings are already
filtered by the existing `.filter((section) => section.length > 0)` at `materialize.ts:44`.

**Before** (`muse-context.ts:14-16`):

```ts
  const sections = [renderGlobalLayer('muse', isDevMode())];
  const project = findProjectByPathSync(workspace);
  if (project) sections.push(renderProjectLayer(project.path, 'muse'));
```

**After:** add `sections.push(renderManagedMemorySections(project?.path ?? null));` as the next line.
The existing `sections.filter(section => section.trim())` at line 26 drops an empty result.

Test changes in `materialize.test.ts`:
- Extend the `vi.mock('../../../src/lib/projects.js', …)` factory (lines 7-10) with
  `resolveInfraRepo: (_project: unknown, root: string) => ({ repoPath: root, recordsPath: '.pan' })`,
  because `projectMemoryDir → resolvePlanHome` calls it.
- New cases: `includes Project memory between the project layer and the workspace section` (write
  `<projectRoot>/.overdeck/memory/be-decisive.md`; assert `indexOf('## Project context') <
  indexOf('## Project memory') < indexOf('## Workspace context')`); `includes Operator memory from
  OVERDECK_HOME/context/memory`; `the codex and claude renders carry the identical memory section`;
  `a review-marked fact is absent`; `no memory heading when both stores are empty`.

Muse test: `materializeMuseContext writes a Project memory section` (mock `projects.js` the same way;
`agentId` must pass `museDataHome` validation. Reuse whatever id the existing muse tests use, found
with `grep -rn "materializeMuseContext" tests src --include=*.test.ts`).

### W4: Context page preview matches the launch (FR-5)

**Files:** `src/dashboard/server/routes/context.ts`, `tests/dashboard/context-routes.test.ts`.

**Before** (`context.ts:398-405`):

```ts
async function previewForHarness(layers: readonly ResolvedLayer[], drafts: ReadonlyMap<string, string>, harness: Harness): Promise<string> {
  const title = getHarnessBehavior(harness).displayName;
  return [
    `# Overdeck injected context preview (${title})`,
    renderLayerSections(layers, drafts, harness),
    await renderBundledRulesAsync(harness),
  ].filter((section) => section.trim().length > 0).join('\n\n---\n\n');
}
```

**After:**

```ts
async function previewForHarness(layers: readonly ResolvedLayer[], drafts: ReadonlyMap<string, string>, harness: Harness): Promise<string> {
  const title = getHarnessBehavior(harness).displayName;
  return [
    `# Overdeck injected context preview (${title})`,
    renderLayerSections(layers, drafts, harness),
    renderManagedMemorySections(previewProjectRoot(layers)),
    await renderBundledRulesAsync(harness),
  ].filter((section) => section.trim().length > 0).join('\n\n---\n\n');
}

/** The primary checkout of the project the preview is scoped to, if any (PAN-3957). */
function previewProjectRoot(layers: readonly ResolvedLayer[]): string | null {
  const scoped = layers.find((layer) => layer.kind !== 'global');
  if (!scoped || scoped.kind === 'global') return null;
  return getProjectSync(scoped.projectKey)?.path ?? null;
}
```

Import `getProjectSync` from `../../../lib/projects.js` and `renderManagedMemorySections` from
`../../../lib/memory/facts/render.js`. The preview adds memory after the layers, while the launch adds it
before the workspace section. The preview is a source-labelled audit view, not a byte copy, so this order
difference is accepted. The docs (W8) say so.

Tests (`context-routes.test.ts`): `project-scoped preview contains ## Project memory for a project with
facts`; `global-only preview contains ## Operator memory and no Project memory`. Follow the file's
existing setup for projects and `OVERDECK_HOME`.

### W5: `pan memory add | edit | show | list | retire` (FR-6, D4, D11, D12)

**New file:** `src/cli/commands/memory-facts.ts` (≤ 400 lines). **Changed:** `src/cli/commands/memory.ts`
(one import, one call). **Tests:** `tests/unit/cli/memory-facts.test.ts`.

**Before** (`memory.ts:395-398`, anchor `return memory;`): the last registration is `config`, then
`return memory;`. **After:** insert `registerMemoryFactCommands(memory);` right before `return memory;`
and add `import { registerMemoryFactCommands } from './memory-facts.js';`.

Command surface (exact flags):

```text
pan memory add <id> --type <feedback|project|reference|user> --description <text>
               [--title <text>] [--body <text> | --body-file <path>] [--tags <a,b>]
               [--project <key> | --user] [--json]
pan memory edit <id> [--type <t>] [--title <text>] [--description <text>]
                [--body <text> | --body-file <path>] [--tags <a,b>] [--clear-review]
                [--project <key> | --user] [--json]
pan memory show <id> [--project <key> | --user]
pan memory list [--type <t>] [--query <q>] [--review] [--project <key> | --user] [--json]
pan memory retire <id> [--project <key> | --user] [--json]
```

Steps:

1. `resolveFactTarget({ project, user })` returns `{ scope, dir, root }`: `--user` gives
   `userMemoryDir()`. `--project <key>` gives `getProjectSync(key)` (error `unknown project "<key>"`),
   then `projectMemoryDir(project.path)`. With neither flag, use `findProjectByPathSync(process.cwd())`.
   If that is null, error:
   `not inside a registered project; pass --project <key> or --user`. Otherwise use
   `projectMemoryDir(process.cwd())`. `root` = `resolvePlanHome(...)` of the same path (null for user).
   `--project` together with `--user` is an error.
2. `add`: `type` defaults to nothing and is required. `--type user` without `--user` is an error
   (`user facts go in the operator store: add --user`). The body comes from `--body`, else the
   `--body-file` contents, else `''`; both flags together is an error. Set `updated` = today
   (`new Date().toISOString().slice(0, 10)`). For project scope, run the D11 ignore check first. Then
   `writeMemoryFact`. Print `✓ Wrote <path>` and, for project scope,
   `  Commit it: git -C <root> add .overdeck/memory/<id>.md`.
3. `edit`: read the existing fact with `readMemoryFactsSync` (error `no fact "<id>"`). Refuse when no
   change flag is given (`nothing to change; pass a field flag or edit <path> directly`). Merge the given
   fields, set `updated` = today, `--clear-review` sets `review: []`, then write with `overwrite: true`.
4. `show`: print `Source: <path>` then the raw file contents. `list`: filter by `--type`, by
   `matchMemoryFacts(facts, query)` (W6), and by `--review` (only facts with a marker; default: only
   active facts). Human output is one line per fact, `<type>/<id>  <description>`, followed by a
   `Problems:` block with `<file>: <reason>` lines. Exit code 1 when any problem exists. `--json` prints
   `{ scope, dir, facts, problems }`.
5. `retire`: `removeMemoryFact` prints `✓ Removed <path>` and
   `  Commit it: git -C <root> rm --cached --ignore-unmatch .overdeck/memory/<id>.md && git -C <root> commit -m "docs(memory): retire <id>"`
   (the project scope only).
6. Errors print with `chalk.red` and `exitCli(1)` (the `memory.ts:53-57` pattern).
7. The ignore check is `promisify(execFile)('git', ['-C', root, 'check-ignore', '-q', rel])`. Resolve
   means ignored (refuse), `code === 1` means proceed, and anything else means warn and proceed (D11). The
   check takes an injectable `checkIgnored` parameter for tests.

Tests: `add writes the file and prints the commit hint`; `add refuses outside a registered project`;
`add --type user without --user is refused`; `add refuses when check-ignore reports ignored` (inject
`checkIgnored: async () => true`); `edit changes only the given fields and bumps updated`; `edit with no
flags is refused`; `list --review shows only marked facts`; `list exits 1 and prints Problems for a
malformed file`; `retire removes the file`; `--project and --user together is an error`.

### W6: Facts-first `pan memory search` (FR-7, D13)

**Files:** `src/lib/memory/facts/store.ts` (add the matcher), `src/cli/commands/memory.ts`
(`memorySearchCommand`, lines 37-70), `tests/unit/cli/memory-facts.test.ts`.

Add to `store.ts`:

```ts
export function matchMemoryFacts(facts: readonly MemoryFact[], query: string): MemoryFact[] {
  const terms = query.toLowerCase().split(/\s+/).filter(Boolean);
  if (terms.length === 0) return [...facts];
  return facts.filter((fact) => {
    const hay = [fact.id, fact.title, fact.description, fact.tags.join(' '), fact.body].join('\n').toLowerCase();
    return terms.every((term) => hay.includes(term));
  });
}
```

In `memorySearchCommand`, **after** the `--target` validation block (ends at `memory.ts:49`) and
**before** `let results`, add:

```ts
  if (!options.json) printMatchingFacts(query, { project: options.project });
```

`printMatchingFacts` lives in `memory-facts.ts`. It resolves the project store the same way
`resolveFactTarget` does, but **never errors**: outside a project it silently skips the project store. It
prints at most 5 active project matches under `chalk.bold('Project memory:')` and at most 5 operator
matches under `chalk.bold('Operator memory:')`, each as `  <type>/<id>: <description>`. When there are no
matches it prints nothing. The `--json` branch and the observation printing are untouched.

Tests: `search prints Project memory lines before observation lines` (mock `searchMemory` to return one
hit and assert the order in the captured stdout); `search --json output is unchanged` (assert
`JSON.parse(stdout)` is the mocked array); `matchMemoryFacts requires every term`.

### W7: Bundled rule + nomenclature routing (FR-8)

**New file:** `sync-sources/rules/project-memory.md` (exact content):

```markdown
---
scope: universal
---
### Record durable facts with `pan memory add`, never in harness-native memory

A durable fact (an operator correction, a working agreement, a non-obvious project truth) goes in the
repo: `pan memory add <id> --type feedback|project|reference --description "<one line>" --body
"<fact, then **Why:** and **How to apply:**>"`. Operator-level facts: add `--user`. Facts reach every
harness's next session through the Project memory index. Never write `~/.claude/projects/*/memory/` or
any other harness-native memory in a managed session.
```

**Changed:** `sync-sources/rules/context-nomenclature.md`. Add this row to the table after the
**machine rule** row:

```markdown
| **memory** ("remember that …") | one project (`--user`: this operator, every project) | `pan memory add` → `<planHome>/.overdeck/memory/<id>.md` (operator: `~/.overdeck/context/memory/`) |
```

Tests: none new. `tests/…` rule-rendering suites pick up rules by directory. Verify with
`grep -rn "project-memory" ~/.overdeck/context/claude-global.md` after `node dist/cli/index.js sync` in
the workspace (manual, recorded in the PR).

### W8: Documentation, phase 1 (FR-11)

**Files:**

- **New** `configuration/memory.mdx`. Frontmatter: `title: Project Memory`, `description: Curated facts
  stored in your repository and injected into every managed agent session`. Sections, in order:
  **What a fact is** (D2 format with the example block); **Where facts live** (D1 paths, polyrepo plan
  home, operator store); **How agents see them** (the D7 section text verbatim, the D6 budgets, review
  markers are never injected, and new facts reach *new* sessions only); **Adding and curating facts**
  (every W5 command with one example each, the commit hints, D4 "writes land in your checkout; the PR is
  the review", D10 secret refusal, D11 ignore fix); **Search** (D13); **Facts vs. observation memory vs.
  OKF knowledge** (a three-row table: what, who writes, where, how injected); **Previewing**
  (Context page preview, with memory after the layers).
- `docs.json`: add `"configuration/memory"` directly after `"configuration/context-layers"` (currently
  line 105).
- `configuration/context-layers.mdx`: add a short "Project memory" subsection pointing to
  `/configuration/memory`, with one sentence on placement (after the project layer).
- `docs/CONTEXT-LAYERS.md`: (a) add two rows to "Instruction source inventory":
  `| Project memory | <planHome>/.overdeck/memory/*.md | Rendered index (one line per active fact) after the project layer, every harness |`
  and `| Operator memory | ~/.overdeck/context/memory/*.md | Rendered index after project memory, every harness on this machine |`;
  (b) replace the sentence at lines 254-255 ("Claude auto memory is another input, … it is not a
  context-layer source.") with: "Claude auto memory (`~/.claude/projects/<project>/memory/`) is a
  harness-native input that Overdeck does not read at launch. Curated project memory replaces it for
  managed sessions (see `configuration/memory.mdx`)."
- `docs/WORKSPACES-AND-PROJECTS.md` "Memory homes" (line 287): add a first paragraph that separates
  **observation memory** (this section) from **curated facts** (`configuration/memory.mdx`).
- `CLAUDE.md` "Key Invariants": add
  `- Curated facts live in \`<planHome>/.overdeck/memory/<id>.md\` (operator: \`~/.overdeck/context/memory/\`); the index is rendered at launch into every managed harness and never stored. Write them with \`pan memory add\`, never into harness-native memory.`
- `sync-sources/skills/pan-memory/SKILL.md`: change `description` to also mention "curated facts
  (add/edit/show/list/retire)". Add the five W5 command lines to the `## Commands` block and a short
  "Curated facts vs observations" paragraph.
- Inline module headers: `store.ts` and `render.ts` carry the header comment shown in W1.

**Verification:** `grep -n '"configuration/memory"' docs.json` hits; `grep -c "harness-native input"
docs/CONTEXT-LAYERS.md` is 1; `grep -c "pan memory retire" sync-sources/skills/pan-memory/SKILL.md
configuration/memory.mdx` is ≥ 1 for each; `bash scripts/lint-skills.sh` passes.

### W9: `pan memory import claude` (FR-9, D15), phase 2

**New files:** `src/lib/memory/facts/import-claude.ts`, CLI wiring in `src/cli/commands/memory-facts.ts`.
**Tests:** `tests/unit/lib/memory/facts/import-claude.test.ts` with fixtures under
`tests/fixtures/claude-memory/` (one file per variant listed below; synthetic content, no real
transcripts).

```text
pan memory import claude [--project <key>] [--source <dir>] [--include-user] [--apply] [--json]
```

```ts
export function claudeProjectSlug(path: string): string { return path.replace(/[^A-Za-z0-9]/g, '-'); }
export type ImportAction =
  | { kind: 'import'; id: string; type: MemoryFactType; review: string[]; scope: MemoryFactScope }
  | { kind: 'skip'; reason: 'index-file' | 'no-frontmatter' | 'no-description' | 'sensitive' | 'user-type' | 'exists' };
export interface ImportRow { source: string; action: ImportAction }
export function planClaudeImport(input: {
  sourceDir: string; existingIds: ReadonlySet<string>; existingUserIds: ReadonlySet<string>; includeUser: boolean;
}): ImportRow[];
export async function applyClaudeImport(rows: readonly ImportRow[], dirs: { project: string; user: string }): Promise<string[]>;
```

Rules (D15):

1. **Source:** `--source` if given. Otherwise `~/.claude/projects/${claudeProjectSlug(project.path)}/memory`,
   where `project` is `--project <key>` or `findProjectByPathSync(process.cwd())`, with the path taken
   from the **primary checkout**. If the directory is missing, error
   `no Claude memory at <dir>`. The source is only read: no write, rename or delete.
2. **Target:** the project store resolved exactly like W5 `resolveFactTarget` without `--user`. That is
   the cwd checkout, so running from `workspaces/feature-…` lands the import on a branch. `user` rows go
   to `userMemoryDir()`.
3. **Per file** (`*.md` in the source, sorted by name):
   - `MEMORY.md` → skip `index-file`.
   - No frontmatter → skip `no-frontmatter`. No `description` → skip `no-description`.
   - The text of the file trips `redactSensitiveText` → skip `sensitive`.
   - **id:** filename stem, lower-cased, with `_` replaced by `-`, a leading
     `(user|feedback|project|reference)-` removed, runs of `[^a-z0-9-]` replaced by `-`, and trimmed to
     80 chars without leading/trailing `-`. On a collision with an id already planned in this run,
     append `-2`, `-3`, and so on. If the id exists in the target store → skip `exists` (the import is
     re-runnable).
   - **type:** `metadata.type` ?? top-level `type` ?? the filename prefix (`feedback_`, `project_`,
     `user_`, `reference_`) ?? `project`. An unknown value becomes `project`.
   - `type: user` → skip `user-type`, unless `--include-user`, which imports it with `scope: 'user'`
     into the operator store.
   - **review flags** (in this order, deduplicated): `unindexed` when `MEMORY.md` exists and has no link
     `](<filename>)`, and `machine-specific` when the text matches
     `/\/home\/[^/\s]+\/|\/Users\/[^/\s]+\/|\/tmp\/|localhost:\d+|127\.0\.0\.1/`.
   - **Fields:** `title` = `defaultFactTitle(id)`; `description` = the Claude `description` with
     newlines collapsed to spaces, cut to 200 chars (and when cut, the full original is prepended to the
     body as `> Original description: …`); `updated` = `metadata.modified` as `YYYY-MM-DD` if parseable,
     else the file's mtime date; `source` = `claude:<metadata.originSessionId>` if present, else
     `claude:<filename>`; `tags` = `[]`; body = the Claude body, trimmed.
4. **Dry run (default):** print one line per row, `<source file> → <type>/<id> [review: a,b]` or
   `<source file> → skip (<reason>)`, then the totals
   `import: n (review-marked: m), skip: k by reason`. `--json` prints the rows. Nothing is written.
5. **`--apply`:** write every `import` row with `writeMemoryFact` (no overwrite), print the totals and
   `Review marked facts with: pan memory list --review`, then
   `Commit on a branch and open a PR; the diff is the review.`

Tests (fixtures): `metadata.type file imports with its type`; `top-level type file imports`; `file
without description is skipped`; `MEMORY.md is skipped and drives the unindexed flag`; `a
/home/<user>/ path adds machine-specific`; `sk-ant key file is skipped as sensitive`; `user file is
skipped, or imported to the operator store with includeUser`; `feedback_be_decisive.md maps to id
be-decisive`; `project-two-databases-schema-door name maps to id two-databases-schema-door`; `second run
skips every row as exists`; `dry run writes nothing and never modifies the source` (compare a sha256 of
the source dir listing and contents before and after); `claudeProjectSlug('/home/eltmon/.paperclip/x')
=== '-home-eltmon--paperclip-x'`.

### W10: `memory.harness_native_memory` switch (FR-10, D14), phase 2

**Files:** `src/lib/config-yaml/schema.ts` (`MemoryConfig` lines 52-67 and the normalized `memory`
block at line 884), `src/lib/config-yaml/defaults.ts` (line 133 block), `src/lib/config-yaml/merge.ts`
(default clone at line 156 and the YAML apply block at line 493), **new**
`src/lib/memory/facts/harness-native.ts`, `src/lib/launcher-generator.ts`. **Tests:**
`tests/unit/lib/memory/facts/harness-native.test.ts`, `src/lib/__tests__/launcher-context.test.ts`, and the
existing config-merge test (find it with `grep -rln "promptTimeInjectionEnabled" tests src --include=*.test.ts`).

1. Config: in `MemoryConfig` add
   `harness_native_memory?: 'off' | 'conversations' | 'on';`. In the normalized `memory` add
   `harnessNativeMemory: 'off' | 'conversations' | 'on';`. In `defaults.ts` add
   `harnessNativeMemory: 'conversations'`. In `merge.ts:156` copy the default. In the `if (config.memory)`
   block (line 493) add:

   ```ts
      if (config.memory.harness_native_memory === 'off' || config.memory.harness_native_memory === 'conversations'
        || config.memory.harness_native_memory === 'on') {
        result.memory.harnessNativeMemory = config.memory.harness_native_memory;
      }
   ```

   Invalid values keep the default.
2. `harness-native.ts`:

   ```ts
   import { getHarnessBehavior } from '../../runtimes/behavior.js';
   import { loadConfigSync } from '../../config-yaml.js';
   import type { LauncherConfig } from '../../launcher-generator.js';

   export type HarnessNativeMemoryMode = 'off' | 'conversations' | 'on';

   export function readHarnessNativeMemoryMode(): HarnessNativeMemoryMode {
     try { return loadConfigSync().config.memory.harnessNativeMemory; } catch { return 'conversations'; }
   }

   /** PAN-3957 D14: launcher lines that switch off Claude auto-memory for managed sessions. */
   export function harnessNativeMemoryEnvLines(
     config: Pick<LauncherConfig, 'harness' | 'spawnMode'>,
     mode: HarnessNativeMemoryMode = readHarnessNativeMemoryMode(),
   ): string[] {
     if (getHarnessBehavior(config.harness ?? 'claude-code').contextLayerKind !== 'claude') return [];
     const disable = mode === 'off' || (mode === 'conversations' && config.spawnMode !== 'conversation');
     return disable ? ['export CLAUDE_CODE_DISABLE_AUTO_MEMORY=1'] : [];
   }
   ```

   Use `import type` for `LauncherConfig` so there is no runtime import cycle.
3. `launcher-generator.ts`, **before** (lines 359-364):

   ```ts
     // Extra env exports
     if (config.extraEnvExports) {
       for (const expr of config.extraEnvExports) {
         lines.push(expr);
       }
     }
   ```

   **After:** add one line right after the closing `}` of that block:
   `  lines.push(...harnessNativeMemoryEnvLines(config));` plus the import
   `import { harnessNativeMemoryEnvLines } from './memory/facts/harness-native.js';`. Apply NFR-3 if
   `lint-file-size.sh` fails.
4. **Implementation checkpoint:** run
   `grep -rn "spawnMode: 'conversation'" src --include=*.ts | grep -v __tests__` and confirm every
   operator-conversation launcher passes it (verified at planning time: only
   `conversation-runtime.ts:761`). **Fallback:** if a conversation path launches Claude without
   `spawnMode: 'conversation'`, pass it there. Do not widen the D14 condition.

Tests: `harness-native.test.ts`: the table `{claude-code, undefined, conversations} → disable`,
`{claude-code, 'conversation', conversations} → []`, `{claude-code, 'conversation', off} → disable`,
`{claude-code, undefined, on} → []`, `{codex, undefined, off} → []`,
`{ohmypi, undefined, off} → []`; `readHarnessNativeMemoryMode falls back when loadConfigSync throws`.
`launcher-context.test.ts`: `a work launcher contains export CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` and
`a conversation launcher does not` (mock the mode to `conversations`). Config test:
`harness_native_memory: off normalizes to harnessNativeMemory off; an invalid value keeps conversations`.

### W11: Documentation, phase 2 (FR-11)

**Files:** `configuration/memory.mdx` (add the sections **Importing Claude auto-memory**, covering the W9
command, rules 1-5 in prose, the review workflow, and "the Claude store is never modified", and
**Harness-native memory**, covering the D14 table of modes × launch kinds, the default, "set `off` after
you import", and why Codex/Pi/OMP/Kimi need no switch); `sync-sources/skills/pan-memory/SKILL.md` (the
`import claude` line); `docs/CONTEXT-LAYERS.md` (one sentence after the W8(b) replacement:
"`memory.harness_native_memory` controls whether managed Claude sessions keep auto memory; see
`configuration/memory.mdx`."); `docs/research/claude-code-memory-integration.md` (append an "Adopted
(PAN-3957)" line under "Overdeck integration levers" naming lever 5 as the one used).

**Verification:** `grep -c "harness_native_memory" configuration/memory.mdx docs/CONTEXT-LAYERS.md` ≥ 1
each; `grep -c "import claude" sync-sources/skills/pan-memory/SKILL.md` ≥ 1; `bash scripts/lint-skills.sh`
passes.

---

## 8. Dependencies

- **PAN-3920 (Agents directory, `pan worker run`): no code dependency, either order works.** Workers
  that PAN-3920 spawns through Overdeck's launcher (`pan worker run`) get facts automatically, provided
  they launch through `claudeSystemPromptFiles` / `renderManagedLaunchContext`. PAN-3920's PRD should
  state that requirement (note for that PRD, not a blocker here). Agents started outside Overdeck and
  only *registered* with it (the Codex-plugin workers in PAN-3920's first scope addition) never receive
  Overdeck launch context, so they cannot receive facts. That gap is one of PAN-3920's arguments for
  `pan worker run`, and this PRD does not solve it. Both issues add text to the launch composition
  (PAN-3920 adds "a one-line pointer in the launch context"), which is merge-conflict surface only.
- **PAN-3940 (plugin/hook system):** P3 capture hooks ride on it. P1 and P2 need nothing from it.
- **PAN-3998 (legacy `.pan/` ignore repair):** not needed, because facts avoid `.pan/` (Q1).

---

## 9. Non-goals

- Replacing or changing observation memory, its FTS index, prompt-time injection budgets
  (`PROMPT_TIME_MEMORY_BUDGETS`, `injection.ts:14-20`) or the SessionStart briefing.
- Injecting fact **bodies**, or retrieving facts per prompt (P3).
- A dashboard fact editor or a Settings toggle for `harness_native_memory` (P3).
- Committing, pushing or opening PRs on the author's behalf.
- Reaching sessions that are already running. Facts reach **new** sessions, the same as every context
  layer.
- Importing from Codex, Pi, OMP or Kimi (none has a native store with facts on this host).
- Changing `pan context diff`. It renders only the global layer today (`context-layers.ts:179-188`),
  shows no project layer either, and stays as is. The Context page preview is the full view (W4).
- Deleting, moving or editing any file under `~/.claude/`.
- Unmanaged Claude sessions (the operator running `claude` directly). They keep native auto-memory,
  untouched.

---

## 10. Acceptance criteria (map 1:1 to work items)

- **AC-W1** `store.test.ts` passes every case named in W1. `parseMemoryFact` returns each W1 problem
  reason verbatim. `writeMemoryFact` refuses `sk-ant-…` content and accepts `OVERDECK_RTK_ENABLED=0`.
- **AC-W2** `render.test.ts` passes. A 300-fact project section is ≤ 12,000 chars and ends with the overflow
  line. A review-marked fact is absent. An empty store renders `''`.
- **AC-W3** `materialize.test.ts` shows `## Project context` < `## Project memory` < `## Workspace context`
  by index, identical memory sections for `claude-code` and `codex`, and `## Operator memory` from
  `$OVERDECK_HOME/context/memory`. The muse test finds `## Project memory` in `muse-context.md`.
  `grep -n "renderManagedMemorySections" src/lib/context-layers/materialize.ts src/lib/runtimes/muse-context.ts`
  shows one call in each.
- **AC-W4** `context-routes.test.ts`: a project-scoped preview contains `## Project memory`, and a
  global-scoped preview contains `## Operator memory` and not `## Project memory`.
- **AC-W5** `memory-facts.test.ts` passes every W5 case. `node dist/cli/index.js memory --help` lists
  `add`, `edit`, `show`, `list` and `retire`.
- **AC-W6** A search with a fact match prints `Project memory:` before the first observation line.
  `--json` output parses to exactly the mocked observation array.
- **AC-W7** `sync-sources/rules/project-memory.md` exists with `scope: universal`, and
  `context-nomenclature.md` contains `pan memory add`.
- **AC-W8** Every W8 verification command passes. `configuration/memory.mdx` contains the strings
  `.overdeck/memory/`, `~/.overdeck/context/memory/`, `pan memory add` and `never injected`.
- **AC-W9** `import-claude.test.ts` passes every W9 case, including the unchanged-source hash check and
  the idempotent second run.
- **AC-W10** `harness-native.test.ts` table passes. A generated work launcher contains
  `export CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` and a conversation launcher does not. The config test
  passes. `bash scripts/lint-file-size.sh` passes.
- **AC-W11** Every W11 verification command passes.
- **AC-all** `npm run typecheck` and `npm run lint` pass locally, `npx vitest run` passes on every test
  file named in W1–W10, and the CI test job passes on the PR head.
  `grep -rn "execSync" src/lib/memory/facts/ src/cli/commands/memory-facts.ts` is empty.

---

## Planning Q&A

Written as a research-only PRD pass (2026-09-23). No operator questions were asked. §0 records the seven
decisions that need the operator, each with the recommendation this document assumes.
