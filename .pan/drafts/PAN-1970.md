# PRD: Minimal New Project flow (PAN-1970)

> Part of the **minimum new-user onboarding** (demo readiness). **Explicitly NOT the full #654 wizard** — no Setup Agent, templates, archetypes, `SetupRpcGroup`, or `pan setup`. Clone-from-GitHub/GitLab is a **later** release.

## Summary
A minimal dashboard flow to create/register a project, with two modes:
1. **Add existing** — browse to an existing folder → register it.
2. **New project** — type a name, pick a parent folder (default `~/Overdeck/<name>`) → create folder + `git init` → register.

End state: an entry in `~/.overdeck/projects.yaml`, visible in the sidebar + Home (PAN-1969), ready to open. Mirrors the reference flow in the issue screenshots (add existing / new project / default folder / choose parent folder → Start workspace).

## Glossary
- **Project** — entry in `~/.overdeck/projects.yaml` (`ProjectConfig`).
- **registerProjectSync(key, config)** — the one helper that writes a project to projects.yaml.
- **Project key** — slug derived from name/folder: `name.toLowerCase().replace(/[^a-z0-9-]/g, '-')`.

## Problem
Projects can only be registered via `pan project add <existing-path>` in a terminal (`src/cli/commands/project.ts:85`). That command **requires the path to already exist** and **does not run `git init`**. There is **no dashboard endpoint** to register a project, **no server-side folder browser**, and **no "create folder + git init"** path. A brand-new user has no UI way to get their first project in.

## Goal / Non-goals
- **Goal:** from PAN-1969's "+ New Project", a user can Add existing OR New project, ending with a registered project visible in sidebar + Home.
- **Non-goals (explicit):** Setup Agent, templates/archetypes, `SetupRpcGroup`, `pan setup` (those are #654); clone from GitHub/GitLab (later); tracker config / quality gates / workspace-build config in the wizard (use minimal defaults, editable later in projects.yaml).

## Verified references (current code)
- `pan project add`: `src/cli/commands/project.ts` `projectAddCommand` (85). Derives name/key (97-98); dup-check via `getProjectSync` (101-107); builds **minimal** `ProjectConfig { name, path, issue_prefix?, rally_project? }` (120-131); `registerProjectSync(key, config)` (133); `ensureProjectLayer(fullPath)` (137); `preTrustDirectory(fullPath)` (186-209 — installs git hooks, **NO git init**).
- Helpers: `src/lib/projects.ts` — `registerProjectSync` (246), `getProjectSync` (454), `loadProjectsConfigSync` (198), `saveProjectsConfigSync` (221); `ProjectConfig` type (99-167); `PROJECTS_CONFIG_FILE = join(OVERDECK_HOME, 'projects.yaml')` (18); `OVERDECK_HOME = process.env.OVERDECK_HOME || join(homedir(), '.overdeck')` (`src/lib/paths.ts:6`).
- Projects API (read): `GET /api/registered-projects` (`routes/misc.ts` 826-851).
- **Confirmed missing (audit):** no create-project endpoint; no server-side `git init`; no directory-browser endpoint. (`POST /api/project-mappings` at misc.ts:364 is a different store — NOT a substitute.)
- Frontend entry: PAN-1969's `onNewProject()`.

## Requirements
### Backend
- **FR-1** — `POST /api/projects` — body `{ mode: 'existing' | 'new', path?, parentDir?, name? }`. Registers the project and returns the created project record.
  - `existing`: validate `path` exists; derive key; dup-check; `registerProjectSync`; `ensureProjectLayer`; `preTrustDirectory`.
  - `new`: target = `<parentDir>/<slug(name)>`; reject if it exists and is non-empty; `mkdir -p`; `git init` (async); then the same register path.
  - Reuse the registration core from `projectAddCommand` (factor a shared helper rather than duplicating).
- **FR-2** — `GET /api/fs/list-dirs?path=<abs>` — returns immediate subdirectories of `path` (`{ name, path }[]`) plus the parent, for the folder picker. **Auth-gated** (internal token, like other routes). Default start path = `~` (or `~/Overdeck`). Must **not** traverse outside the user's home unless explicitly navigated there.
- **FR-3** — `git init` runs **asynchronously** (`execAsync`/`spawn`), never `execSync`.
### Frontend
- **FR-4** — A "New Project" modal/page with a top toggle **Add existing | New project**:
  - **Add existing** → folder picker (FR-2) → **Start** → POST `mode='existing'`.
  - **New project** → name field + parent-folder picker (default `~/Overdeck/`, "choose parent folder…") → live preview `<parent>/<slug>` → **Start** → POST `mode='new'`.
- **FR-5** — On success, invalidate/refetch `['command-deck-projects']` + `['registered-projects']` and select/open the new project.
### Non-functional
- **NFR-1** — No `execSync` in server code; async only (PAN-70 rule).
- **NFR-2** — Single write door: register **only** via `registerProjectSync`; do not write projects.yaml directly anywhere new.

## Work items
- **WI-1 — `POST /api/projects`** (`src/dashboard/server/routes/projects.ts`). Factor the registration core out of `projectAddCommand` into a shared helper (name/key derivation, dup-check, build minimal ProjectConfig, `registerProjectSync`, `ensureProjectLayer`, `preTrustDirectory`); add the `new`-mode mkdir + async `git init` in front of it. Return the project.
- **WI-2 — `GET /api/fs/list-dirs`** (`routes/misc.ts` or a new `routes/fs.ts`). Async `readdir` (dirs only) of an absolute path; `{ name, path }` + parent; auth-gated; clamp to the user's home.
- **WI-3 — New Project modal** (`src/dashboard/frontend/src/components/...`). The Add-existing/New-project toggle + folder picker (consumes WI-2) + name field + path preview; **Start** → POST WI-1. Match the reference flow's shape.
- **WI-4 — Wire `onNewProject()`** (from PAN-1969) to open the modal; on success run FR-5.

## Acceptance criteria
- **AC-1 (FR-1/4):** "New project", name "Bugs", parent `~/Overdeck` → creates `~/Overdeck/bugs`, runs `git init`, registers key `bugs` in projects.yaml; appears in the sidebar + Home.
- **AC-2 (FR-1/4):** "Add existing" → browse to an existing repo folder → registers it.
- **AC-3:** Duplicate key, or a non-empty `new` target → clear error, **no partial write**.
- **AC-4 (FR-2):** The folder picker lists host subdirectories and navigates; gated by auth; cannot escape the user's home without explicit navigation.
- **AC-5 (NFR-1):** No `execSync` introduced; `git init` is async.
- **AC-6 (NFR-2):** `projects.yaml` is written only through `registerProjectSync`.

## Intersecting repo rules
- **Server:** NO `execSync` (PAN-70) — async git init / readdir.
- **Single source of truth:** register via `registerProjectSync` only (the write door).
- **Security:** the folder-browse endpoint must not allow arbitrary host traversal beyond the user's home without explicit navigation; auth-gate it like other internal routes.
- **Tests:** route tests for `POST /api/projects` (both modes + dup + error) and `GET /api/fs/list-dirs`; frontend Vitest for the modal.

## Planning Q&A

Planning ran in **`--auto` (non-interactive) mode** on 2026-06-19 (agent: claude-opus-4-8), so no questions were asked. The choices normal planning would have surfaced were resolved as defensible defaults and recorded below + in `.pan/continue.json` decisions. None of these contradicted the PRD/issue.

1. **Folder-browser scope — clamp to home vs. allow explicit out-of-home navigation?** FR-2's "must not traverse outside the user's home *unless explicitly navigated there*" is ambiguous. **Decision: hard-clamp `GET /api/fs/list-dirs` to the user's home** (default start `~`, reject `..`/absolute escapes with 400). Rationale: cleanly satisfies AC-4, safe for a demo, and this user's projects live under `~/Projects` (within home). Out-of-home browsing is a NonGoal (deferred).
2. **Where does the shared registration core live?** **Decision: a new server-safe module `src/lib/project-registration.ts` (`registerProjectFromPath`)**, consumed by both `projectAddCommand` and `POST /api/projects`. Rationale: single-sources the write door (NFR-2) without bloating `projects.ts`; no chalk/console/execSync.
3. **Sync vs async in the registration core.** **Decision: keep the sanctioned sync write door (`registerProjectSync`) + sync hook symlinks; make ONLY `git init` async** (`execAsync`/spawn). Rationale: PAN-70/NFR-1 targets unbounded subprocess blocking; the yaml write is the established single write door.
4. **No-partial-write ordering for 'new' mode.** **Decision: dup-check and non-empty-target check reject BEFORE any `mkdir`/`git init`** (D3). Rationale: AC-3.
5. **Route placement + auth.** **Decision: `POST /api/projects` in the existing `routes/projects.ts` (CSRF gate `rejectUnsafeDashboardMutationRequest`); `GET /api/fs/list-dirs` in a new `routes/fs.ts` (read gate `rejectUnauthorizedDashboardRequest`), merged into `server.ts` `makeRoutesLayer`.** Rationale: auth is per-route here, not global; match the established gates.
6. **PAN-1969 coordination.** `onNewProject()` / the "+ New Project" button come from sibling issue PAN-1969, which may not be merged yet (only PRDs are on main). **Decision: the wiring bead is self-contained — implements `onNewProject()` to open the modal and ensures exactly one "+ New Project" affordance exists (reuse PAN-1969's if present, else add it).** Rationale: PAN-1970 stays independently shippable/demoable.
7. **Decomposition.** Split into **8 beads**: `register-core` (foundation, inspection-gated) → `api-projects-existing` → `api-projects-new`; `fs-list-dirs` → `folder-picker` → `new-project-modal` → `wire-new-project`. Rationale: each bead is independently reviewable, mergeable, and testable; the registration core is the one foundation other beads encode assumptions about.
