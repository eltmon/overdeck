# PAN-1787: Provider Default Harnesses, Role-Level Harness Overrides, and Real Brand Logos

**Issue:** [PAN-1787](https://github.com/eltmon/panopticon-cli/issues/1787)
**Status:** Draft
**Date:** 2026-06-11 (rev 2 — switching semantics, 60s grace + Escape interrupt, converter kept as experimental)

This PRD is written to be executed by an implementation agent without further
research. Every claim below was verified against the codebase on 2026-06-11; every
work item names its exact files, functions, and line numbers. If a line number has
drifted, search for the quoted code — do not guess.

---

## Glossary (read first)

| Term | Meaning |
| --- | --- |
| **Harness** | The coding-agent CLI an agent runs inside: `claude-code`, `pi`, or `codex`. Type `RuntimeName` (`src/lib/runtimes/types.ts:20`) and `Harness` (`packages/contracts/src/types.ts:49-51`). |
| **Provider** | The model vendor: `anthropic`, `openai`, `google`, `kimi`, `minimax`, `zai`, `mimo`, `openrouter`, `nous`, `dashscope`. Registry: `PROVIDERS` in `src/lib/providers.ts`. A model's provider is derived via `getProviderForModelSync(modelId)` (`src/lib/providers.ts:~180`). |
| **Role** | Pipeline role of a spawned agent: `plan`, `work`, `strike`, `review`, `test`, `ship`, `flywheel`. Config: `RolesConfig` (`src/lib/config-yaml.ts:347-365`). |
| **Spawn site** | Any code path that launches an agent and must therefore decide its harness. The five sites are listed in "Current State". |
| **Policy gate** | `canUseHarnessSync(harness, model, authMode)` (`src/lib/harness-policy.ts:69-97`). Blocks ToS-violating combos (Pi + Anthropic + subscription auth). Blocked ⇒ collapse to `claude-code`. NEVER bypass or weaken it. |
| **continue.json** | `<workspace>/.pan/continue.json` — the agent's own session-state file (progress, decisions, hazards, resume point). The re-onboarding mechanism after any restart. |
| **PTY supervisor** | `dist/pty-supervisor.js` wrapper that owns Claude's PTY for claude-code agents/conversations. tmux keystrokes still reach the harness TUI through it. |
| **Transcript converter** | `convertConversationTranscript` (`src/lib/session-format-converter.ts:185-308`) — converts a conversation transcript between harness JSONL formats when a conversation switches harness. |
| **Switch tiers** | `decideSwitchStrategy` (`src/lib/conversations/switch-strategy.ts:41-60`): tier 1 = in-session `/model` command; tier 2 = respawn+resume (provider routing changed); tier 3 = compact then respawn (context overflow); tier 4 = harness changed (uses the converter). |

---

## Vision

One coherent harness-routing story: every agent spawn resolves its harness through a
single, predictable chain — explicit request → role override → provider override →
**built-in provider default** — and every surface that shows a provider or harness
shows its real brand logo, consistently. Switching the model or harness of an
already-running agent is session-safe by construction.

## Motivation

1. **No smart defaults.** Every unset harness silently falls back to `claude-code`,
   even for OpenAI/Google/Kimi models. `DEFAULT_CONFIG.providerHarnesses` is `{}`
   (`src/lib/config-yaml.ts:967`) and the terminal fallback at every spawn site is
   hardcoded `'claude-code'`.
2. **Role harness override exists in schema but not in UI.** `RoleConfig.harness`
   (`src/lib/config-yaml.ts:349`) is honored at every spawn site, but Settings →
   Roles exposes a harness picker only for Flywheel (`RolesPanel.tsx:512-525`).
3. **Logos are inconsistent.** Only Anthropic and OpenAI have real SVG marks
   (`src/dashboard/frontend/src/components/chat/ProviderIcons.tsx`); eight providers
   are colored letter badges; Settings → Providers uses generic lucide icons
   (`SettingsPage.tsx:423-433`); harness rows are text-only; Pi agents render with
   the **Anthropic** starburst (`AgentIcon.tsx:14-19` has no pi branch).
4. **Live-switch semantics are implicit.** Restarting an agent with a new
   model/harness happens to start a fresh session, but nothing guarantees it; the
   graceful grace period (30s) is too short when the agent is mid-action; and
   conversation harness switching silently runs an unsupported transcript converter.

## Current State (all verified 2026-06-11)

| Piece | Where | Status |
| --- | --- | --- |
| Harness union `'claude-code' \| 'pi' \| 'codex'` | `packages/contracts/src/types.ts:49`, `src/lib/runtimes/types.ts:20` | ✅ done (PAN-1574) |
| Per-provider override storage | YAML `models.providers.<p>.harness` → `NormalizedConfig.providerHarnesses` (`config-yaml.ts:719,1167`) | ✅ done |
| Per-provider override API | `models.provider_harnesses` (`src/lib/settings-api.ts:137,595,731-739,859-861,987-998`) | ✅ done |
| Per-provider override UI | "Default harness" select per provider (`SettingsPage.tsx:1609,1821-1832`; openrouter `:1907`) | ⚠️ displays `?? 'claude-code'` — cannot show or reset to the real default |
| Per-role override storage | `RoleConfig.harness` (`config-yaml.ts:349`), read at all spawn sites | ✅ done |
| Per-role override UI | `RolesPanel.tsx:512-525` | ⚠️ Flywheel only |
| Resolution chain | `resolveEffectiveHarness()` (`src/lib/agents.ts:329-336`); duplicated inline in `spawn-planning-session.ts:513-516` | ⚠️ duplicated; falls back to `claude-code`, never a provider-appropriate default |
| ToS policy gate | `canUseHarnessSync()` (`src/lib/harness-policy.ts:69-97`) | ✅ done — do not modify |
| Legacy parallel config | `model_selection.specialist_harnesses` (`src/lib/cloister/config.ts:157-163,347`, PAN-636) — `claude-code \| pi` only, predates Codex | ⚠️ needs reconciling |
| Provider logos | `chat/ProviderIcons.tsx` — Anthropic + OpenAI inline SVG; 8 providers letter-badged | ⚠️ partial |
| Harness logos | none anywhere | ❌ missing |
| Agent model/harness switch | `restartAgent` (`agents.ts:4690-4801`) — fresh session + continue.json prompt; never `--resume`; 30s graceful grace | ⚠️ correct shape; grace too short; invariant untested |
| Agent JSONL resume path | `spawnMode: 'resume'` + `resumeSessionId` → `--resume` (`launcher-generator.ts:427,529`; `agents.ts:3107-3142`) | ⚠️ no guard against resuming across a model/harness change |
| Conversation harness switch | switch-model tier 4 (`routes/conversations.ts:2944-2971`) + transcript converter | ⚠️ works but unlabeled — to be marked **experimental/unsupported** |
| Conversation model switch | tiers 1–3 (`switch-strategy.ts:41-60`) | ✅ status quo — out of scope |
| Planning → work auto-spawn | `pan plan finalize` posts `complete-planning` with `autoSpawn: true` by default (`plan-finalize.ts:226`); planning agents run finalize as their final action (`roles/plan.md:75-76`); endpoint then POSTs `/api/agents {role:'work'}` (`routes/issues.ts:241-296`) | ⚠️ **always-on** — to become opt-in (flywheel only by default) |
| Done button / `pan plan done` / kanban "Done planning" | `PlanDialog.tsx:385` sends `{skipKill:true}`; `plan-done.ts` sends no body; action runner sends `{}` | ✅ none auto-spawn (route requires explicit `autoSpawn === true` + origin check) |
| Contradictory messaging | `plan-finalize.ts:127-137` emits activity + TTS "planning is done, **awaiting your approval**" — then immediately auto-promotes and spawns the work agent | ⚠️ message lies; fix with the gating |
| Stale comment | `routes/issues.ts:~1254-1260` claims "the plan-role Stop hook POSTs this endpoint on EVERY turn-end" — no such hook exists in the repo or installed settings | ⚠️ rewrite (keep the AskUserQuestion guard — still valid defense) |

**Spawn sites (the five places a harness is decided):**

1. Plan kickoff — `src/lib/planning/spawn-planning-session.ts:513-516`
2. Work agent — `spawnAgent`, `src/lib/agents.ts:3330-3341`
3. Role runs (review/test/ship/flywheel) — `spawnRun`, `src/lib/agents.ts:2969-3013`
4. Restart/resume — `restartAgent`, `src/lib/agents.ts:4736-4742`
5. Dashboard start route — `src/dashboard/server/routes/agents.ts:3156-3165,3435`

Conversations pin their harness at creation (`routes/conversations.ts:2741`) and are
not a spawn site for this resolution chain.

## Goals

1. Built-in provider→harness defaults: `anthropic → claude-code`,
   `openai → codex`, **all other providers → pi**; user-overridable per provider.
2. Role-level harness override UI for every role.
3. One unified, exported `resolveHarness()` used by every spawn site.
4. Session-safe live switching: 60s graceful grace + Escape interrupt; fresh-session
   invariant for any agent model/harness change; conversation harness switching
   labeled experimental.
5. Real brand logos for all providers and harnesses from a single shared registry.
6. Approval-gated planning handoff: human-initiated planning never auto-spawns a
   work agent (the human's Start Agent click / `pan start` is the gate);
   flywheel-initiated planning keeps the autonomous plan→work chain via an
   explicit opt-in flag. Fix the messaging/comments that currently contradict
   the actual flow.

## Non-Goals

- Sub-role harness overrides (`RoleSubConfig` has only `model`, `config-yaml.ts:339-341`).
- New harnesses beyond claude-code / pi / codex.
- Per-project harness configuration (`projects.yaml`).
- Changing conversation **model**-switch behavior (tiers 1–3 stay exactly as-is).
- Removing or improving the transcript converter (kept, labeled experimental — WI-6).

---

## Work Items

### WI-1: Built-in provider defaults on the provider registry

**File:** `src/lib/providers.ts`

1. Add to `ProviderConfig` interface (around line 20): `defaultHarness: RuntimeName;`
   (import `RuntimeName` from `./runtimes/types.js`).
2. Set on every entry of `PROVIDERS`:

```ts
anthropic:  { ..., defaultHarness: 'claude-code' },
openai:     { ..., defaultHarness: 'codex' },
google:     { ..., defaultHarness: 'pi' },
kimi:       { ..., defaultHarness: 'pi' },
minimax:    { ..., defaultHarness: 'pi' },
zai:        { ..., defaultHarness: 'pi' },
mimo:       { ..., defaultHarness: 'pi' },
openrouter: { ..., defaultHarness: 'pi' },
nous:       { ..., defaultHarness: 'pi' },
dashscope:  { ..., defaultHarness: 'pi' },
```

3. Export a helper so callers never hand-roll the lookup:

```ts
export function getBuiltInDefaultHarness(provider: ProviderName): RuntimeName {
  return PROVIDERS[provider]?.defaultHarness ?? 'claude-code';
}
```

**Test:** `tests/unit/lib/providers.test.ts` (or co-located existing suite) — assert
the three-way mapping (anthropic→claude-code, openai→codex, sample of others→pi).

### WI-2: Unified `resolveHarness()` + call-site migration

**New file:** `src/lib/harness-resolve.ts`

```ts
import type { RuntimeName } from './runtimes/types.js';
import type { Role } from './config-yaml.js';

export interface ResolveHarnessInput {
  explicit?: RuntimeName | undefined;  // --harness flag / dashboard body / options
  role?: Role | undefined;             // looks up config.roles[role].harness
  model: string;                       // provider derived via getProviderForModelSync
}

/**
 * Resolution order (first defined wins):
 *   1. explicit
 *   2. config.roles[role].harness            (Settings → Roles override)
 *   3. config.providerHarnesses[provider]    (Settings → Providers override)
 *   4. getBuiltInDefaultHarness(provider)    (WI-1 built-in)
 *   5. 'claude-code'                         (unknown provider only)
 * The winner ALWAYS passes through canUseHarnessSync(); blocked ⇒ 'claude-code'.
 * If the winning harness binary is not installed (pi/codex), collapse to
 * 'claude-code' and log a warning (reuse the doctor-style availability check).
 */
export async function resolveHarness(input: ResolveHarnessInput): Promise<RuntimeName>;
```

**Migrate these call sites to one `resolveHarness()` call each** (delete the local
`options.harness ?? roles[role].harness` prefixes and the duplicated planning logic):

| Site | File | Current code to replace |
| --- | --- | --- |
| `resolveEffectiveHarness` | `agents.ts:329-336` | becomes a thin wrapper or is deleted in favor of direct calls |
| `spawnRun` | `agents.ts:3011-3013` | `options.harness ?? loadYamlConfig().config.roles?.[role]?.harness` then `resolveEffectiveHarness(...)` |
| `spawnAgent` | `agents.ts:3330-3341` | same pattern |
| `restartAgent` | `agents.ts:4736-4742` | keep `newHarness ?? agentState.harness` as the `explicit` argument |
| Plan kickoff | `spawn-planning-session.ts:513-516` | the four-line inline chain |
| Dashboard start route | `routes/agents.ts:3156-3165` | keep body validation; pass validated value as `explicit` via the `pan start --harness` flag it already builds at `:3435` |

**CLI flags are the "explicit" tier and already exist** (`pan start --harness`,
`pan strike --harness`, `pan plan --harness`; no CLI-side validation — values pass
through to the resolver, and `codex` already works at runtime). Fix their stale
help strings, which still read `claude-code (default) | pi`:

- `src/cli/index.ts:374` (plan), `:512` (start), `:530` (strike) — new text:
  `'Coding-agent harness: claude-code | pi | codex (defaults to role/provider settings)'`
- `src/cli/index.ts:533` — widen the strike action's harness type from
  `'claude-code' | 'pi'` to `RuntimeName`.
- Skills-convention rule: `--help` text changes must update the wrapper skills in
  the same commit (`skills/pan-start/SKILL.md` etc. — `scripts/lint-skills.sh`
  cross-checks and fails CI on drift).

**Do not change:** `canUseHarnessSync` itself; the conversation pinning at
`routes/conversations.ts:2741`.

**Tests:** new `tests/unit/lib/harness-resolve.test.ts` — full precedence matrix:

- explicit beats role beats provider-override beats built-in;
- empty config + anthropic model ⇒ `claude-code`; + openai model ⇒ `codex`;
  + kimi/google/zai model ⇒ `pi`;
- policy-blocked winner collapses to `claude-code` (e.g. explicit `pi` +
  anthropic model + subscription auth);
- missing harness binary collapses to `claude-code` with warning.

### WI-3: Reconcile legacy `specialist_harnesses` (PAN-636)

**Files:** `src/lib/cloister/config.ts:157-163,347`,
`src/lib/cloister/__tests__/specialist-harnesses-config.test.ts`,
`src/lib/cloister/__tests__/specialist-harness-router.test.ts`,
`src/lib/cloister/router.ts` (`getSpecialistHarness`).

It must not survive as an undocumented fourth resolution tier. Treat
`model_selection.specialist_harnesses.<role>` as a **deprecated alias** read at the
same precedence as `roles[<role>].harness` (role tier), with `roles[*].harness`
winning when both are set, and log a one-time deprecation warning when the legacy
key is the one that applied. Update both test suites to the alias semantics.

### WI-4: Graceful restart — 60s grace + Escape interrupt

**File:** `src/lib/agents.ts:4716-4721` (inside `restartAgent`, `graceful` branch).

Current code:

```ts
const warning = 'Restarting in 30s. Update .pan/continue.json now with all progress, decisions, hazards, and resume point.';
try {
  await Effect.runPromise(sendKeys(normalizedId, warning));
} catch { /* non-fatal — session may already be dead */ }
await new Promise(r => setTimeout(r, 30_000));
```

Changes:

1. Extract a module-level constant `const GRACEFUL_RESTART_GRACE_MS = 60_000;` and
   update the warning text to say `60s`. Keep the existing stale-continue.json check
   (`agents.ts:4724-4732`) unchanged.
2. **Interrupt in-flight work before delivering the warning.** Agents can be minutes
   deep in a tool call and never see the warning otherwise. Add an async helper in
   `src/lib/tmux.ts` (async variants only — see repo rule "Prefer async tmux
   primitives"):

```ts
/** Send the Escape key to a session's TUI (no Enter). */
export async function sendEscapeKeyAsync(sessionName: string, times = 1): Promise<void>
// implementation: for each press → tmux send-keys -t <exact target> Escape ; 250ms gap
```

3. In the graceful branch, **before** sending the warning, when the agent's harness
   is `claude-code` or `codex` (tmux-TUI input path): call
   `sendEscapeKeyAsync(normalizedId, 2)` (two presses, 250ms apart — the
   user-requested interrupt), wait ~1s, then deliver the warning text via the
   existing delivery path.
   - ⚠️ **Implementation checkpoint:** in Claude Code, Esc during streaming
     interrupts; a second Esc on an *idle* prompt opens the history-rewind dialog.
     Verify in a live session that the warning text still lands and is visible after
     the double-Esc (use `tmux -L panopticon capture-pane -t <session> -p` to
     check). If the rewind dialog swallows the message, drop to a single Esc and
     note the decision in the PR description. Either way the message delivery path
     (paste-buffer + delayed Enter) must remain the one that already exists.
   - `pi` harness has no TUI keystroke path (FIFO `rpc.in` protocol). Check
     `writePiAgentPrompt` / the Pi RPC protocol for an interrupt op; if none exists,
     skip the interrupt for pi — the 60s grace still applies.
4. The non-graceful path is unchanged.

**Tests:** restart-flow unit test MUST use fake timers (repo rule — never real
waits): `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync(60_000)`. Assert the
warning text contains `60s`, Escape helper called twice for claude-code/codex, not
called for pi.

### WI-5: Fresh-session invariant — never `--resume` across a model/harness change

**Facts:** `restartAgent` already always builds a fresh session (it never passes
`resumeSessionId` into `buildAgentLaunchConfig`) and then prompts:
*"You are resuming work on `<issue>`. Read `.pan/continue.json` for context and pick
up where you left off."* (`agents.ts:4787`). The JSONL-resume path is separate:
`spawnMode: 'resume'` + `resumeSessionId` (`agents.ts:2647,3107-3142`) produces
`--resume <id>` in the launcher (`launcher-generator.ts:427,529`) and is used by
deacon auto-resume and specialist resumes.

**Changes:**

1. Persist, at session-creation time, the model+harness the JSONL session was
   created with (extend agent runtime state where `claudeSessionId` is stored —
   `agents.ts:2037,2159-2167`).
2. In every path that sets `spawnMode: 'resume'`: if the agent's current
   `state.model` or `state.harness` differs from the persisted session
   model/harness, **drop the resume id** (fall back to fresh session + the
   continue.json re-onboarding prompt) and log why.
3. Decision rule, stated plainly: **a model change, a harness change, or both ⇒
   fresh harness session. JSONL `--resume` is only ever used when both are
   unchanged.** No transcript conversion for agents, ever (the converter is
   conversation-only).

**Tests:** unit — restart with `newModel` only / `newHarness` only / both ⇒
launcher script contains no `--resume`; resume path with unchanged model+harness
keeps `--resume`; resume path with drifted model drops it.

### WI-6: Conversation harness switching — keep, label experimental

**Decision (revised):** conversation harness switching stays functional, converter
included, but is **explicitly experimental and unsupported** — kept open for future
first-class support, not a guarantee. We are not deleting it and not blocking it.

**Changes (small, surface-level only):**

1. `src/dashboard/frontend/src/components/chat/ModelPicker.tsx:410-430` (harness
   rows): when the picker is operating on a **live conversation** (it already knows
   — the same component serves the new-conversation composer; thread a boolean prop
   such as `liveConversation` from `ChatPanel` if not already distinguishable) and
   `opt.id !== currentHarness`, render a small muted `Experimental` tag on the row
   (style per `pan-style-guide`: no pill colors, muted text).
2. Confirmation copy on switch (the picker already keeps the dropdown open per
   PAN-1067): tooltip/description for a live-conversation harness switch reads
   "Experimental: converts the transcript to the new harness format. May lose
   fidelity; falls back to a fresh session on failure."
3. Docs: `configuration/harnesses.mdx` gains a short "Switching harness on a live
   conversation (experimental)" subsection stating: unsupported, best-effort
   converter, fresh-session fallback, report issues but no support promise.
4. **No endpoint changes. No converter changes. No test deletions.** The tier-4
   path (`routes/conversations.ts:2944-2971`), `decideSwitchStrategy`
   (`switch-strategy.ts:47-49`), `session-format-converter.ts`, and their tests all
   stay exactly as they are.

### WI-7: Settings → Providers — honest default-harness select

**Files:** `SettingsPage.tsx:1609,1821-1832,1907`; save path
`settings-api.ts:731-739` (`providerConfigForSave`).

Current select renders `value={provider_harnesses[p] ?? 'claude-code'}` — it lies
about the effective default and cannot distinguish "unset" from explicit
`claude-code`.

1. First option: value `''`, label `Default (Codex)` / `Default (Pi)` /
   `Default (Claude Code)` — computed from `getBuiltInDefaultHarness(provider)`
   (expose the built-in map to the frontend through the existing settings GET
   payload; do not duplicate the mapping in TSX).
2. `value` becomes `provider_harnesses[p] ?? ''`.
3. Selecting `''` **deletes** the override key. Follow the Flywheel pattern
   (`RolesPanel.tsx:517` `<option value="">Provider default</option>` →
   `patch: { harness: undefined }`). Verify `providerConfigForSave`
   (`settings-api.ts:731-739`) removes the `harness` key from the YAML when the
   incoming value is absent — today it only merges; add explicit delete handling.
4. Apply identically to the openrouter select at `SettingsPage.tsx:1907`.

**Tests:** settings round-trip — save with override, save with `''` (key removed
from YAML), load shows `''` when unset. Extend the existing settings-api suite.

### WI-8: Settings → Roles — harness override for every role

**File:** `RolesPanel.tsx`. The Flywheel card already has the exact control needed
(`:512-525`): a `<select>` with `<option value="">Provider default</option>` +
three explicit options, saving via
`saveMutation.mutate({ role, patch: { harness } })`.

1. Extract that select into a small local component (`RoleHarnessSelect`) and render
   it on **every** role card (plan, work, strike, review, test, ship, flywheel),
   next to the existing per-role `ModelPicker` in the right column.
2. The empty option's label should read `Provider default` (resolution then falls
   through to provider override → built-in).
3. Backend persistence already works (`saveRoleConfig`); no server change.

**Tests:** existing RolesPanel test pattern — each role card exposes
`aria-label="<Role> harness"`; saving patches `roles.<role>.harness`; clearing sets
it undefined.

### WI-9: Shared brand registry (providers + harnesses)

**Promote** `src/dashboard/frontend/src/components/chat/ProviderIcons.tsx` to
`src/dashboard/frontend/src/components/shared/branding/` with a registry:

```ts
interface BrandDef {
  id: string;
  label: string;   // kills the duplicated PROVIDER_LABELS maps
  color: string;   // existing PROVIDER_COLORS values
  Icon: ComponentType<SVGProps<SVGSVGElement>>;
}
export const PROVIDER_BRANDS: Record<ProviderName, BrandDef>;
export const HARNESS_BRANDS: Record<Harness, BrandDef>;
// Components: <ProviderLogo provider .../>, <HarnessLogo harness .../>, <ProviderDot .../>
```

**Provider marks** (inline SVG path data, `currentColor`, same pattern as the
existing `AnthropicIcon`/`OpenAIIcon` at `ProviderIcons.tsx:6-18`; source path data
from official press kits or the MIT-licensed lobehub/simple-icons sets):

| Provider | Mark | Provider | Mark |
| --- | --- | --- | --- |
| anthropic | starburst (in repo) | zai | Z.ai / Zhipu GLM |
| openai | flower (in repo) | mimo | Xiaomi "MI" |
| google | Gemini spark | openrouter | OpenRouter |
| kimi | Moonshot AI | nous | Nous Research |
| minimax | MiniMax | dashscope | Qwen / Alibaba Cloud |

**Harness marks:** `claude-code` → Anthropic/Claude mark; `codex` → OpenAI mark;
`pi` → styled π glyph in a brand-colored tile (pi-ai has no formal logo).
`ProviderLetterBadge` survives only as the fallback for unknown ids.

### WI-10: Logo rollout + dead-code cleanup

| Surface | File | Change |
| --- | --- | --- |
| Composer harness rows + indicator chips | `chat/ModelPicker.tsx:354-358,410-430` | add `HarnessLogo` |
| Composer trigger / sidebar / dots | `chat/ModelPicker.tsx:347-350,394,522` | re-point imports at the registry (mechanical) |
| Settings → Providers list | `SettingsPage.tsx:423-433,1633` | replace lucide `Code/Lightbulb/Globe/Zap/Brain` with `ProviderLogo` |
| Settings provider harness selects | `SettingsPage.tsx:1821-1832,1907` | harness logo adjacent to the native select (or custom listbox) |
| Roles harness selects | `RolesPanel.tsx` (WI-8 component) | same treatment; delete its local `PROVIDER_LABELS` copy (`:148-160`) |
| Shared ModelPicker | `shared/ModelPicker/ModelPicker.tsx:17-21,59-68` | logos on harness options + provider group headers; delete its `PROVIDER_LABELS` copy |
| Agent dock | `Stage/HomePane/AgentIcon.tsx:14-35` | replace string-sniffing `providerForAgent` with harness→`HarnessLogo`; **pi must render π, not the Anthropic starburst** |
| Conversation feed | `sessionFeed/ConversationFeedCard.tsx:24-45` | `AGENT_ICON_PROVIDER` map → `HarnessLogo` |
| Dead code | `Settings/Provider/ProviderCard.tsx` | unimported; still references the Material Symbols font removed in a37f8c890 — delete (it is dead) |

Follow `pan-style-guide` (no decorative color, no pills); logos inherit the existing
size classes (`.pickerProviderIcon` 14px, `.pickerProviderBtnIcon` 15px,
`.agentLogo` — `command-deck.module.css:4317-4415`, `stage.module.css:152-163`).

### WI-11: Approval-gated planning handoff (auto-spawn off for humans, on for flywheel)

**Decision:** a work agent starts only on explicit human action (Start Agent click,
`pan start`) — EXCEPT when planning was launched with an explicit autonomy opt-in,
which is what the flywheel uses. The signal is deterministic state, never prompt
text (the planning agent is an LLM; the gate must not depend on it choosing flags).

**The new flow, human-initiated** (`pan plan <id>`, `pan plan <id> --auto`,
dashboard Plan button):

```
pan plan <id>                          # no autonomy stamp
  → planning agent researches, writes vBRIEF, creates beads
  → agent's final action: pan plan finalize
      → beads materialized, spec promoted to main (plan.status=proposed),
        issue → "Planned", planning session terminated
      → autoSpawn NOT sent — no work agent
      → activity + TTS: "PAN-X planning is done, awaiting your approval"  (now TRUE)
  → human reviews the plan (vBRIEF viewer)
  → human clicks Start Agent / runs pan start <id>   ← THE gate
      → work agent spawns (plan.status → running)
```

**The new flow, flywheel-initiated:**

```
flywheel runs: pan plan <id> --auto --auto-start     # explicit autonomy opt-in
  → spawnPlanningSession stamps autoSpawnOnFinalize: true
    into the planning agent's state.json (~/.panopticon/agents/planning-<id>/)
  → planning agent finishes → pan plan finalize
      → finalize reads the stamp → posts complete-planning {autoSpawn: true}
      → work agent spawns immediately (existing guards still apply:
        pending AskUserQuestion no-op, stack-health, guardrails, paused/troubled)
      → activity + TTS: "PAN-X planned — starting implementation"
```

**Implementation steps:**

1. `pan plan` gains `--auto-start` ("After planning completes, automatically start
   the work agent — used by autonomous orchestrators; default off"). Composable
   with `--auto`. A human passing it is a valid explicit opt-in (this also keeps
   the old chain reachable — no behavior becomes unreachable).
2. `spawnPlanningSession` (`src/lib/planning/spawn-planning-session.ts`) gains
   `opts.autoSpawnOnFinalize?: boolean` (default false) and persists it into the
   planning agent's `state.json`. Thread from the CLI flag and from the dashboard
   planning route (`routes/issues.ts:1045`) / `agent-spawner.ts:205-208` (both
   default false — dashboard Plan button is human-initiated).
3. `plan-finalize.ts`: delete the hardcoded `autoSpawn: true`
   (`plan-finalize.ts:226`). Instead read `autoSpawnOnFinalize` from the planning
   agent's `state.json` (issue id is already derived from the workspace name) and
   send `autoSpawn` only when stamped. `--no-promote` behavior unchanged. Do NOT
   add an `--auto-spawn` flag to `pan plan finalize` itself — the planning agent
   runs that command and must not be able to opt itself into spawning.
4. Server route unchanged (`autoSpawn === true` + origin check already correct).
5. `roles/flywheel.md`: every `pan plan <id> --auto` becomes
   `pan plan <id> --auto --auto-start` (lines 74, 88, 90, 95, 136, 138); update the
   line 136 parenthetical to describe the flag.
6. Messaging becomes truthful (two variants in `plan-finalize.ts:127-137` and the
   console summary at the bottom of `planFinalizeCommand`):
   - no stamp → "planning is done, awaiting your approval" + console hint
     `Run pan start <id> or click Start Agent to begin implementation.`
   - stamped → "planned — starting implementation" (drop the approval wording).
7. Kill-timing note: `completePlanningAutoSpawnAndKill` (`routes/issues.ts:297-340`)
   kills the planning session immediately when autoSpawn ran, deferred 1500ms
   otherwise — that split stays as-is.
8. Update `tests/.../auto-promote-chain.integration.test.ts`: chain fires only when
   stamped; default path promotes WITHOUT spawning. Update the `pan-plan` wrapper
   skill for the new flag (`scripts/lint-skills.sh` enforces same-commit sync).

**Out of scope:** `pan start --auto` (skip-planning fast path) is an explicit start
command — unchanged.

### WI-12: Fix the inconsistencies found during investigation

| # | What | Where | Fix |
| --- | --- | --- | --- |
| 1 | "awaiting your approval" emitted, then auto-spawn contradicts it | `plan-finalize.ts:127-137` | covered by WI-11 step 6 |
| 2 | Stale comment: "plan-role Stop hook POSTs this endpoint on EVERY turn-end" — no such hook exists (verified: not in repo, not in installed settings) | `routes/issues.ts:~1254-1260` | rewrite the comment to name the real callers (`pan plan finalize`, `pan plan done`, PlanDialog Done, kanban action); KEEP the pending-AskUserQuestion guard — still valid defense |
| 3 | `--harness` help strings missing codex + wrong default wording | `cli/index.ts:374,512,530,533` | covered by WI-2 (CLI block) |
| 4 | `roles/plan.md:48,75-76` describes finalize as unconditional promotion; line 48 calls Done "legacy" | `roles/plan.md` | rewrite for the two-mode flow: finalize always promotes to Planned; work agent start is gated (human) or stamped (flywheel); Done button is the manual/`--no-promote` path, not "legacy" |
| 5 | `roles/flywheel.md:136` "(then auto-promotes to a work agent)" — becomes wrong once WI-11 lands | `roles/flywheel.md` | covered by WI-11 step 5 |
| 6 | Misleading console summary "⚠ Auto-promoted but work agent spawn was skipped … Run `pan start` to retry" shown even when no spawn was ever requested | `plan-finalize.ts` (bottom of `planFinalizeCommand`) | print spawn-related lines only when a spawn was attempted (stamped path) |

---

## Behavior Change (call out loudly)

With built-in defaults active, spawns with **no** explicit/role/provider override
change harness: OpenAI models `claude-code` (CLIProxy) → **codex**; Google/Kimi/
Z.ai/MiniMax/MiMo/OpenRouter/Nous/DashScope models → **pi**. Mitigations:

- Explicit settings always win; one Settings change restores old behavior
  (e.g. `provider_harnesses.openai = claude-code`).
- Policy gate unchanged; blocked combos still collapse to `claude-code`.
- Conversations unaffected (harness pinned at creation).
- Emit a one-line activity-log notice the first time a spawn uses a built-in
  default ("harness codex chosen by provider default — override in Settings →
  Providers"), plus a release-notes entry.

**Second behavior change (WI-11):** human-initiated `pan plan` (including `--auto`)
no longer auto-starts the work agent when planning completes — the issue lands in
"Planned" and waits for Start Agent / `pan start`. Anyone relying on the old
hands-off chain outside the flywheel must add `--auto-start`. Flywheel autonomy is
preserved via the flag (`roles/flywheel.md` updated in the same change). Release
notes must call this out.

## Acceptance Criteria

1. Empty `provider_harnesses` + no role overrides: Anthropic model ⇒ `claude-code`;
   OpenAI model ⇒ `codex`; any other provider ⇒ `pi` — at all five spawn sites.
2. Precedence holds: explicit > role > provider override > built-in; the duplicated
   inline logic in `spawn-planning-session.ts:513-516` is deleted; everything goes
   through `resolveHarness()` (WI-2 test matrix green).
3. ToS gate untouched: Pi + Anthropic + subscription still collapses to
   `claude-code`; existing `harness-policy` tests pass unmodified.
4. Settings → Providers select shows `Default (<built-in>)` when unset; explicit
   override persists; clearing removes the YAML key (round-trip tested).
5. Every role card exposes a harness override with a "Provider default" state,
   persisted to `config.roles[<role>].harness`.
6. `specialist_harnesses` is a deprecated alias at role-tier precedence with a
   warning; its two test suites updated and green.
7. Graceful agent restart: 60s grace (constant `GRACEFUL_RESTART_GRACE_MS`),
   warning text says 60s, Escape interrupt sent (×2, 250ms apart) for
   claude-code/codex before the warning, not for pi; fake-timer tests only.
8. Fresh-session invariant: any agent model and/or harness change ⇒ launcher has no
   `--resume`; resume paths drop the JSONL session id when stored model/harness
   differs from the session's origin; unchanged resume still uses `--resume`.
9. Conversation harness switching unchanged functionally; live-conversation harness
   options carry an `Experimental` tag + warning copy; docs updated; converter and
   its tests untouched. Conversation model switching (tiers 1–3) byte-for-byte
   unchanged.
10. All ten providers render real SVG marks (no letter badges for known providers)
    in composer picker, Settings Providers, shared ModelPicker; all three harnesses
    render marks in every harness picker/chip; pi agents never show the Anthropic
    starburst.
11. Label/color/icon maps exist exactly once (the branding registry);
    `ProviderCard.tsx` deleted.
12. Human-initiated planning (`pan plan`, `pan plan --auto`, dashboard Plan button)
    completes to "Planned" with NO work agent spawned; activity/TTS says "awaiting
    your approval"; `pan start` / Start Agent then spawns. `pan plan finalize` has
    no self-serve spawn flag.
13. Flywheel path: `pan plan <id> --auto --auto-start` stamps
    `autoSpawnOnFinalize` into planning state; finalize auto-spawns the work agent;
    messaging says "starting implementation"; existing guards (pending AUQ,
    stack-health, paused/troubled, guardrails) still suppress the spawn.
    `roles/flywheel.md` uses the flag everywhere it launches planning.
14. The stale Stop-hook comment in `routes/issues.ts` is rewritten (AUQ guard
    kept); `roles/plan.md` describes the two-mode flow; the misleading
    "spawn was skipped" console summary appears only when a spawn was attempted.
15. Quality gates green: `npm run typecheck`, `npm run lint`, `npm test`.

## Test Plan

- **Unit:** WI-2 precedence matrix; WI-1 built-in map; WI-4 graceful flow
  (`vi.useFakeTimers()` — NEVER real 60s waits, repo rule); WI-5 resume-guard
  matrix; WI-7 settings round-trip incl. key deletion; WI-3 alias semantics.
- **Unit (WI-11):** finalize without stamp posts complete-planning WITHOUT
  `autoSpawn`; finalize with stamp posts `autoSpawn: true`; `pan plan --auto-start`
  persists the stamp via `spawnPlanningSession`; dashboard planning route defaults
  to no stamp.
- **Existing suites that must stay green:** `harness-policy`,
  `specialist-harnesses-config` + `specialist-harness-router` (updated per WI-3),
  `switch-strategy` (unmodified), session-format-converter tests (unmodified),
  `complete-planning.test.ts` (route semantics unchanged),
  `auto-promote-chain.integration.test.ts` (updated: chain is stamp-gated).
- **UAT (Playwright):** Settings Providers shows `Default (Codex)` for OpenAI; role
  card harness select saves and clears; composer dropdown shows logos for all 10
  providers + 3 harnesses; agent dock pi tile shows π; live-conversation harness
  rows show `Experimental`; graceful restart shows the 60s warning in the pane
  (verify with `tmux -L panopticon capture-pane`); a human-initiated `pan plan` on
  a scratch issue ends in "Planned" with the Start Agent button showing and no
  `agent-<id>` tmux session present.

## Implementation Rules (repo-specific, restated because they bite here)

- Async tmux primitives only (`sendKeysAsync`, new `sendEscapeKeyAsync`); never add
  sync tmux callers; no `execSync` anywhere server-reachable.
- Fake timers for ALL delay-based tests (the 60s grace especially).
- Do not weaken `canUseHarnessSync` or the `postMergeLifecycle` guard test.
- Frontend follows `pan-style-guide` (no pills, no decorative color).
- Dashboard runs Node 22 from `dist/` — `npm run build` before restarting it.
- Commit subjects: conventional-commit, lower-case start, ≤100 chars.

## Implementation Phases (guidance only — single deliverable, ship complete)

1. **Resolution core** — WI-1, WI-2, WI-3.
2. **Switching semantics** — WI-4, WI-5, WI-6.
3. **Planning handoff** — WI-11, WI-12.
4. **Settings UI** — WI-7, WI-8.
5. **Branding** — WI-9, WI-10.
