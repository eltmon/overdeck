{
  "version": "1",
  "issueId": "pan-1014",
  "created": "2026-05-07T13:50:00.000Z",
  "updated": "2026-05-07T13:56:00.000Z",
  "gitState": {
    "branch": "feature/pan-1014",
    "sha": "4127d87",
    "dirty": true
  },
  "decisions": [
    {
      "id": "D1",
      "summary": "Fix canResumeSession to add &&!isRunning so isRunning and canResumeSession are never simultaneously true. Add isRunningButStuck flag (isRunning && runtime===idle||suspended) to make the stuck case explicit. Fix recommendedAction to check isRunningButStuck BEFORE the generic 'running' branch so stuck agents get 'resume' not 'none'.",
      "recordedAt": "2026-05-07T13:56:00.000Z"
    },
    {
      "id": "D2",
      "summary": "assertCanResumeSession also updated to accept isRunningButStuck — otherwise all callers of assertCanResumeSession would incorrectly block stuck-agent resumes. Route will check canResumeSession||isRunningButStuck explicitly.",
      "recordedAt": "2026-05-07T13:56:00.000Z"
    },
    {
      "id": "D3",
      "summary": "Tests use setAgentRuntimeMirror() directly to populate runtime state, because saveAgentRuntimeState is async and relies on the dashboard event system which is not running in unit tests. The mirror is the in-process source of truth for getAgentRuntimeState.",
      "recordedAt": "2026-05-07T13:56:00.000Z"
    }
  ],
  "hazards": [
    {
      "id": "H1",
      "summary": "suspended runtime state is a legacy value never emitted by the new event path (PAN-800). Only 'idle' is reachable via the current Activity enum. The isRunningButStuck check includes it for backward-compat, but the suspended test uses 'idle' as the real-world equivalent.",
      "mitigation": "The test comment documents this clearly. No behavior change needed — the guard is safe to keep."
    }
  ],
  "resumePoint": {
    "description": "Bead workspace-bvqq (lifecycle fix) complete. Next: claim bead workspace-b15a and fix the resume route in src/dashboard/server/routes/agents.ts to gate on canResumeSession||isRunningButStuck and return {before,after} lifecycle in the response.",
    "beadId": "workspace-b15a",
    "filesToRead": [
      "src/dashboard/server/routes/agents.ts",
      "src/lib/work-agent-lifecycle.ts"
    ]
  },
  "beadsMapping": {
    "workspace-bvqq": "Fix getWorkAgentLifecycleState: add isRunningButStuck, fix canResumeSession, fix recommendedAction precedence",
    "workspace-b15a": "Fix resume route: gate on canResumeSession||isRunningButStuck, return {before,after} lifecycle in response"
  },
  "agentModel": "claude-opus-4-5",
  "sessionHistory": [
    {
      "timestamp": "2026-05-07T13:56:00.000Z",
      "reason": "bead-complete",
      "note": "Implemented bead workspace-bvqq: added isRunningButStuck to WorkAgentLifecycleState interface and computation; fixed canResumeSession to include &&!isRunning; fixed recommendedAction branch ordering so isRunningButStuck check fires before the generic running+live-tmux='none' branch; updated assertCanResumeSession to accept isRunningButStuck. Updated tests: added isRunning/isRunningButStuck assertions to existing test, added two regression tests for the PAN-1014 stuck-running case using setAgentRuntimeMirror for reliable runtime state in unit tests.",
      "agentModel": "claude-opus-4-5"
    }
  ]
}
