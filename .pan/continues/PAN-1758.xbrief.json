{
  "version": "1",
  "issueId": "PAN-1758",
  "created": "2026-06-14T20:36:56.907Z",
  "updated": "2026-06-15T01:00:15.054Z",
  "gitState": {
    "branch": "feature/pan-1758",
    "sha": "819799ad9",
    "dirty": true
  },
  "decisions": [
    {
      "id": "D1",
      "summary": "Scope is the operator's RUN-35 continuous-convergence direction, NOT the issue body's layered fixes #1-#3. Git proves the agent-based ship lane that caused the 37 re-dispatches was removed in 30f545272 (2026-06-10) and PAN-1747 (missing roles/ship.md) is completed (a6410d602). Shipping is server-side. The operator launched this --auto plan AFTER writing the RUN-35 comment (962e6e0b7).",
      "recordedAt": "2026-06-14T20:52:13.000Z"
    },
    {
      "id": "D2",
      "summary": "Fix lives entirely in the UAT-off queue lane (auto-merge-executor + pending_auto_merges). No new reconciler subsystem. The lifecycle trace shows blocked/failed pending_auto_merge rows STRAND: requeueToPending only does merging->pending; listDuePendingAutoMerges only selects pending; nothing resurrects blocked/failed. Convergence today depends on a server restart (startup fixStuckReadyForMerge sweep) or the LLM flywheel re-POSTing schedule.",
      "recordedAt": "2026-06-14T20:52:13.000Z"
    },
    {
      "id": "D3",
      "summary": "Dropped the originally-considered 'continuous fixStuckReadyForMerge' fix as a no-op. In current code readyForMerge is event-driven (PAN-1650, review-status.ts:268-295); the executor's markBlocked never touches review_status; genuine-conflict failures in triggerMerge set mergeStatus='failed' + blockerReasons[merge_conflict] (workspaces.ts:5337-5349), which the sweep deliberately excludes (review-status.ts:520) and which hasBlockers forces false anyway. A continuous sweep would change nothing in scope.",
      "recordedAt": "2026-06-14T20:52:13.000Z"
    },
    {
      "id": "D4",
      "summary": "Two cap regimes. Cheap eligibility re-checks (transient ineligibility = CI pending, mergeable still computing, momentary not-mergeable) defer with exponential backoff and are NOT counted against the merge cap. Only EXPENSIVE failed triggerMerge attempts are hard-capped at 3, then surfaced as stuck (TTS + activity + auto-merge/problems), mirroring checkPendingTestDispatch. This honors issue fix #2 (cap re-dispatches) without recreating 37 attempts.",
      "recordedAt": "2026-06-14T20:52:13.000Z"
    },
    {
      "id": "D5",
      "summary": "Issue fix #1 ('do not require exact main-currency') needs NO change: triggerMerge (workspaces.ts:4861) already rebases the feature branch onto current main and re-runs quality gates at merge time, and isAutoMergeEligible keys on GitHub mergeable (clean-behind => mergeable=true), not exact-currency. A stale-but-clean PR merges fine on its turn.",
      "recordedAt": "2026-06-14T20:52:13.000Z"
    },
    {
      "id": "D6",
      "summary": "Process due entries in PAN-1691 order via orderMergeCandidates (src/lib/flywheel-merge-order.ts:39): disjoint first, conflicts broadest-footprint first, issue-number tiebreak. The executor currently sorts FIFO by scheduledMergeAt,id (auto-merge-executor.ts:74). MergeCandidateMeta needs {issueId, footprint, conflictCount} computed via pairwise branch diffs (reuse the changedFilesVsMain git pattern in flywheel-merge-order.ts:105).",
      "recordedAt": "2026-06-14T20:52:13.000Z"
    }
  ],
  "hazards": [
    {
      "id": "H1",
      "summary": "Server/LLM double-acting. roles/flywheel.md:88,96 currently tells the LLM to investigate/sync-main/rebase-strike blocked/merge-blocker entries. Once the server deterministically resurrects recoverable entries, both actors could fire on the same row (server requeues a transient block while the LLM also launches a rebase strike).",
      "mitigation": "Carve the boundary in the role-doc bead (B6): server owns transient retry + resurrection of recoverable entries; the LLM acts ONLY on genuinely-stuck (capped/terminally-blocked) entries surfaced via auto-merge/problems. Add an AC asserting capped entries remain visible to the LLM loop."
    },
    {
      "id": "H2",
      "summary": "Convergence-vs-churn trap (the root disease in this issue). 'Re-verify on every main move' can never converge: main moves every few minutes, a rebase+verify takes 10+. Naive retry recreates the 37-redispatch bug.",
      "mitigation": "The executor NEVER spawns a review/test convoy. Transient ineligibility is a cheap GitHub re-check with backoff; the expensive triggerMerge path runs only when GitHub already reports green+mergeable, and triggerMerge preserves verification across a clean rebase. Process the queue HEAD-wise (serialized), not parallel re-verification."
    },
    {
      "id": "H3",
      "summary": "Schema migration risk. pending_auto_merges is defined both in the main CREATE TABLE (schema.ts:508) and in migration blocks (schema.ts:1315+, 1338+). Adding the attempts column needs an idempotent ALTER and a version bump consistent with the existing migration pattern (current top version was v51 per memory; verify the live max version at implementation time).",
      "mitigation": "B2 is requiresInspection:true. Follow the existing column-add migration pattern exactly; ALTER TABLE ... ADD COLUMN attempts INTEGER NOT NULL DEFAULT 0 guarded by a column-exists / try-catch check as the surrounding migrations do. Add attempts to BOTH the main CREATE TABLE and a migration step."
    },
    {
      "id": "H4",
      "summary": "Race conditions on status transitions. markBlocked/transitionToMerging/requeueToPending all use guarded UPDATE ... WHERE status=? returning changes===1. New defer/resurrect primitives must use the same guarded-CAS pattern so concurrent ticks (single-flight today, but defense-in-depth) cannot double-process.",
      "mitigation": "deferPendingAutoMerge updates WHERE status='pending'; resurrectStrandedAutoMerge updates WHERE status IN ('blocked','failed'). Both return boolean changes===1; callers handle the lost-race branch by skipping, exactly like the existing executor code (auto-merge-executor.ts:94-103)."
    },
    {
      "id": "H5",
      "summary": "Operator cancellation must be preserved. cancelPending sets status='cancelled' from pending/blocked/failed (pending-auto-merges-db.ts:231). The resurrect sweep must never touch 'cancelled' or 'merged'.",
      "mitigation": "resurrectStrandedAutoMerge only matches status IN ('blocked','failed'); add an AC asserting a cancelled entry is never resurrected."
    }
  ],
  "resumePoint": null,
  "beadsMapping": {},
  "agentModel": "agent:claude-opus-4-8",
  "sessionHistory": [
    {
      "timestamp": "2026-06-14T20:36:56.907Z",
      "reason": "planning",
      "note": "Planning session started for PAN-1758",
      "agentModel": "agent:claude-opus-4-8"
    },
    {
      "timestamp": "2026-06-14T20:52:13.000Z",
      "reason": "planning",
      "note": "Auto-mode discovery complete. Traced the UAT-off auto-merge lifecycle; root cause = stranded blocked/failed pending_auto_merge rows. Scoped to operator RUN-35 continuous convergence; ship-lane fixes obsolete (patrol removed, PAN-1747 done). 6 beads.",
      "agentModel": "agent:claude-opus-4-8"
    }
  ],
  "feedback": []
}