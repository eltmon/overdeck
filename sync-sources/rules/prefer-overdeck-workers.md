---
scope: universal
---
### Delegate to Overdeck workers, not harness plugins

To delegate work to another agent, use `pan worker run` (skill `/pan-worker`), not a harness plugin such as the Codex plugin: the worker is visible, issue-linked, cost-attributed and controllable, and its report comes back to you.
