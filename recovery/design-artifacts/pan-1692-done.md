## ✅ All four placements landed on `main` + verified live (2026-06-09)

Commits `88a2dae49` (pipeline badge) and `8a4c0a305` (flywheel roster) complete the set. Every placement screenshot-verified on the running dashboard:

- **D · Awaiting Merge** — segmented Auto/Hold next to the Merge button. ✓
- **A · Issue slide-out** — Auto/Hold in the `IssueHeader` gates/PR/cost strip. ✓
- **C · Pipeline page** — `🔒 default` click-to-flip badge per row (via a new `trailingBadge` slot on the shared `IssueRow`). ✓
- **B · Flywheel "Merge policy" roster** — every in-flight issue with a per-row badge + live `N auto · M hold · K active` summary. ✓

PAN-1692 is complete.
