# PAN-3192 — No mandated quality gate typechecks the dashboard frontend

- **Failure (negAt):** 2026-07-27T05:35:48.544Z, `blocked/pending`, note (verbatim): "Verification deferred:
  a dashboard deploy is queued (requested 2026-07-27T04:22:17.553Z by deploy-patrol, merge-step0). It
  re-runs automatically after the deploy." — source: `worklist.md` / `evidence/commit-counts.json`.
  **This note is stale, not the real finding** — see next paragraph.
- **⚠ Methodology correction (discovered during this audit):** `worklist.md`'s single-row `negNotes` for
  this issue is misleading. The reproduction script's event-collapse step dedups consecutive events by
  `reviewStatus/testStatus` string alone, so when the state stayed `blocked/pending` across multiple events
  carrying *different* notes, only the first note survived into the worklist row. The full raw series in
  `evidence/events-pan-3192.json.gz` (53 rows) shows the actual sequence: the "Verification deferred" note
  (an infra scheduling message, sequence 862972) was superseded four seconds later, in the same collapsed
  state, by the real finding at sequence 862986 (2026-07-27T05:35:52.725Z): **"Frontend typecheck guard
  suppresses compiler failures and can report success without a completed tsc run."** This is a
  correctness finding, not an infra note. Downstream batches should read the full `events-<issue>.json.gz`
  dump rather than trust `worklist.md`'s `negNotes` cell alone when a note looks environment-flavored or
  empty — see continue.json hazard H11 for the general caution.
- **Reset / pass:** reset 2026-07-27T05:50:13.044Z, pass 2026-07-27T06:10:50.739Z; commits between: 2
  (method: pr-commits, `gh pr view 3198 --json commits`) — both genuine, on-topic: `fix: fail frontend
  guard on compiler crash (PAN-3192)` (2026-07-27T05:49:55Z) and the subsequent `Merge remote-tracking
  branch 'origin/main'`; branch verified: yes.
- **Failing AC (quoted):** none of the four checklist items in PR #3198's body name this defect directly
  (they cover creating the ratchet script, wiring it into `npm run typecheck`, adding a regression test,
  and updating `CLAUDE.md`) — the finding is a review-discovered correctness gap in the *implementation* of
  those items, not a distinct issue-level AC. Quoted from the review event itself: "Frontend typecheck
  guard suppresses compiler failures and can report success without a completed tsc run" — source:
  `evidence/events-pan-3192.json.gz`, sequence 862986/862988.
- **Classification:** genuine-finding. The review correctly identified that the original `lint-frontend-types.sh`
  ran `output=$(npx tsc --noEmit -p "$TSCONFIG" 2>&1 || true)` and counted `error TS` lines without
  checking `tsc`'s exit status — so a `tsc` crash or launcher failure that produced zero `error TS` lines
  in its output would silently report "0 errors" (a false pass) instead of failing the gate.
- **Merged diff:** [PR #3198](https://github.com/eltmon/overdeck/pull/3198), merge commit
  `d7ace34adcbc46ff55b91220bffc94ad92820bb8`. Fix commit: `7abc9b85f420e49a8a970f06f437f6d6eef50e4`
  ("fix: fail frontend guard on compiler crash (PAN-3192)").
- **Diff evidence:** `scripts/lint-frontend-types.sh:45-53` (at merge commit `d7ace34adc`) — the fix
  commit changes the unconditional `output=$(npx tsc --noEmit -p "$TSCONFIG" 2>&1 || true)` to capture
  `tsc_status` via an `if`/`else` at lines 45-47, then adds at lines 52-53: `if (( tsc_status != 0 &&
  count == 0 )); then echo "✖ dashboard frontend typecheck command failed (exit $tsc_status) without
  TypeScript diagnostics." >&2; ...; exit 1; fi` — the guard now fails loudly on
  a crash instead of silently passing. `tests/unit/scripts/lint-frontend-types.test.ts` gains a new case,
  `'fails when the compiler exits without TypeScript diagnostics'`, which simulates `FAKE_TSC_EXIT=137`
  with no `error TS` lines in the output and asserts the guard exits non-zero with the exact "without
  TypeScript diagnostics" message. The final review event (sequence 864088) reads: "All review dimensions
  pass; compiler-crash false-green is fixed and covered by regression tests" — the reviewer's own
  post-fix note names this exact defect as resolved.
- **Verdict:** satisfied. The fix directly closes the false-green gap the review identified, and the
  regression test exercises exactly the failure mode described (compiler crash with no parseable
  diagnostics), not just the general ratchet behavior already covered by the PR's original scope.
- **Follow-up:** none — satisfied.
