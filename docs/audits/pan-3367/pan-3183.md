# PAN-3183 — agents-table boot reconciliation writer audit / commit-aware stop reporting

- **Failure (negAt):** 2026-07-27T06:31:02.917Z, `blocked/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **⚠ Methodology correction (H11):** the real finding is essentially concurrent in the same collapsed run
  (`evidence/events-pan-3183.json.gz`, sequence 865439, 2026-07-27T06:31:09.648Z): **"Boot reconciliation
  treats failed event/upsert writes as committed transitions, so two-door drift can recur."**
- **Reset / pass:** reset 2026-07-27T07:06:08.383Z, pass 2026-07-27T07:24:10.918Z; commits between: 1
  (method: pr-commits, `gh pr view 3201 --json commits`); branch verified: yes.
- **Failing AC (quoted):** none of the issue-body checklist items name this exact defect directly (they
  cover confirming the writer audit, `backfillAgentsSync` reporting, legacy backfill parity, boot-callers
  emitting `agent.status_changed`, and `mergeRuntimeBySequence`) — the finding is a correctness gap in the
  event-emission robustness of the reconciliation machinery this PR introduces. Quoted from the review
  event — source: `evidence/events-pan-3183.json.gz`, sequence 865439/865439.
- **Classification:** genuine-finding, and a significant one given the project's own "single source of
  truth — two doors" tenet (`CLAUDE.md`): if a state-write's event append silently failed but the caller
  still reported/logged it as a committed transition, the DB and event log could drift out of agreement —
  exactly the class of bug the two-door architecture exists to prevent.
- **Merged diff:** [PR #3201](https://github.com/eltmon/overdeck/pull/3201), merge commit
  `9968f0543e2ea7eea62850556818cd5ee04ab97a`. Fix commit: `7f1c66a332708d8b4ac6b6a9ad8c166985167a64`
  ("fix(db): make boot reconciliation commit-aware").
- **Diff evidence:** `src/dashboard/server/services/boot-reconciled-stop-events.ts:11-34` (at merge commit
  `9968f0543e`) — `appendStopOnce` (defined line 11) now captures the `sequence` returned by
  `store.appendAsync` and, at line 34, treats `sequence <= 0` as failure ("Event append returned invalid
  sequence …"), rather than assuming any non-throwing call succeeded; the same function deduplicates
  concurrent emitters for the same `(id, previousStatus)` key via a per-store pending-map (referenced at
  line 53's call site) so two boot patrols can't double-report the same transition. New/extended tests in
  `boot-reconciled-stop-events.test.ts` (+68 lines), `agent-backfill.test.ts`, and
  `backfill-agents-fk.test.ts`.
- **Verdict:** satisfied. The fix directly closes the "failed write reported as committed" gap the finding
  names, with a sequence-validity check and deduplication, backed by new regression coverage.
- **Follow-up:** none — satisfied.
