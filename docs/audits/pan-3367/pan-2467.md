# PAN-2467 — merge-completeness gate (findMergedArtifact / unmerged_sibling_repo)

- **Failure (negAt):** 2026-07-25T22:34:35.893Z, `blocked/pending`, note (verbatim): "Review dispatch
  skipped — already running: agent-pan-2467-review" — source: `worklist.md`. **Stale (H11)**: the real
  finding follows 6 seconds later in the same collapsed run at sequence 782687
  (2026-07-25T22:34:41.772Z): **"GitHub merged-artifact lookup accepts open PR test merge commits, and
  CLI forge failures are not classified as unverifiable"** — source: `evidence/events-pan-2467.json.gz`.
- **Reset / pass:** reset 2026-07-25T22:47:49.624Z, pass 2026-07-25T23:22:15.348Z; commits between: 1
  (method: pr-commits, `gh pr view 3080 --json commits`); branch verified: no (`feature/pan-2467` deleted
  post-merge — commit count computed via the branch-independent PR-commits method per hazard H2).
- **Failing AC (quoted):** none of the 8 issue-body checklist items name this exact defect directly (they
  cover `findMergedArtifact`, the completeness assessor, `unmerged_sibling_repo`, etc.) — the finding is a
  review-discovered correctness gap in the `findMergedArtifact` implementation this PR introduces. Quoted
  from the review event — source: `evidence/events-pan-2467.json.gz`, sequence 782687/782687.
- **Classification:** genuine-finding — two distinct correctness bugs in new code: (1) GitHub's PR API
  populates a `mergeCommit` field even on open, unmerged PRs (a speculative test-merge commit), and the
  original code treated its presence as proof of merge; (2) the CLI fallback path swallowed `gh`/`glab`
  process failures with `2>/dev/null || true`, silently returning "not merged" instead of surfacing an
  unverifiable state.
- **Merged diff:** [PR #3080](https://github.com/eltmon/overdeck/pull/3080), merge commit
  `d932128a1d31932b9bad12e783b07ea6ea6f17bd`. Fix commit: `7a24c641c5437a1c2d27168e10de6b3a7faf413c`
  ("fix(review): reject unverifiable merge artifacts").
- **Diff evidence:** `src/lib/forge.ts:199` (at merge commit `d932128a1d`) — `findMergedGitHubArtifact`'s
  merge check changed from `prs.find((pr) => pr.merged || pr.mergedAt || pr.mergeCommit)` to
  `prs.find((pr) => pr.merged === true || pr.mergedAt != null)`, removing `pr.mergeCommit` from the
  condition (fixes bug 1). The `gh pr list` invocation at `src/lib/forge.ts:210` and the `glab mr list`
  invocation at `src/lib/forge.ts:230` both dropped their `2>/dev/null || true` suffix, so a CLI failure
  now throws/propagates instead of resolving to an empty array (fixes bug 2). New coverage:
  `tests/unit/lib/cloister/merge-completeness-forge-error.test.ts` (new, 86 lines) and additions to
  `tests/unit/lib/forge-merged-artifact.test.ts`.
- **Verdict:** satisfied. Both halves of the finding are addressed by name in the same fix commit, with
  the corresponding condition/error-handling change visible at the exact call sites the finding
  identifies, and dedicated regression tests for the forge-error path.
- **Follow-up:** none — satisfied.
