# PAN-3356 — issue cockpit redesign (six-tab IA, needs-you slot, crew spine)

- **Failure (negAt):** 2026-07-31T20:45:43.264Z, `blocked/pending`, note (verbatim): "[correctness]
  Review-stuck recovery invokes the wrong state transition — `src/dashboard/frontend/src/components/
  issue-view/NeedsYouSlot.tsx:46`" — source: `worklist.md` / `evidence/commit-counts.json`, confirmed
  against the full raw series (`evidence/events-pan-3356.json.gz`, 118 rows — this note is stable across
  the negAt→pass run, not a stale first-note artifact like PAN-3192/PAN-3206/PAN-3230).
- **Full episode context:** this issue had a long review history (118 raw events) with several earlier
  findings and fixes before the one this audit evaluates: a wrong-agent messaging bug ("Session can message
  the wrong agent"), a Needs-You mistargeting bug ("Needs You actions can operate on a different agent than
  the alert names" — `NeedsYouSlot.tsx:67`), and a scope-boundary finding ("Current-cycle exact-session fix
  modifies the explicitly protected `IssueActionMenu` subsystem"). All were fixed in earlier cycles before
  the one audited here. The audited failing AC is the *last* blocking finding before the final pass.
- **Reset / pass:** reset 2026-07-31T21:15:00.405Z, pass 2026-07-31T21:31:46.047Z; commits between: 1
  (method: pr-commits, `gh pr view 3360 --json commits`, filtered from 19 total commits on the long-lived
  PR): `fix(dashboard): clear cockpit stuck state` (2026-07-31T21:14:42Z); branch verified: yes.
- **Failing AC (quoted):** no single issue-body AC names this exact defect (PAN-3356 is the large cockpit
  redesign umbrella issue); the finding is a review-discovered correctness gap in the "review-stuck"
  Needs-You recovery action introduced by this PR. Quoted from the review event itself, matching negNotes
  above — source: `evidence/events-pan-3356.json.gz`, sequence 1002009.
- **Classification:** genuine-finding — a specific, named correctness defect in new code
  (`NeedsYouSlot.tsx:46`), not infra/environment noise.
- **Merged diff:** [PR #3360](https://github.com/eltmon/overdeck/pull/3360), merge commit
  `62b63bc11af0af61298c4a673623f42cf9bca695`.
- **Diff evidence:** fix commit `4a9225c5b3b` ("fix(dashboard): clear cockpit stuck state") adds a
  `item.kind === 'stuck'` branch to `useCockpitNeedsYouActions` (`src/dashboard/frontend/src/components/
  Stage/cockpit/useCockpitNeedsYouActions.ts:14-22`) that returns a dedicated `{ label: 'Clear stuck gate',
  invoke: () => actions.unstick.mutate({ issueId }) }` action, instead of falling through to the
  generic exact-agent-session action path that the finding describes as "invokes the wrong state
  transition." `IssueMissionControl.test.tsx` gains a matching regression case, `'clears review-stuck
  state through the issue unstick action'`, asserting `exactAgentActions.unstick.mutate` is called with
  `{ issueId: 'PAN-1661' }` and `exactAgentActions.recover.mutate` is **not** called — directly pinning the
  "wrong state transition" failure mode. Confirmed present unchanged on current `main` at
  `useCockpitNeedsYouActions.ts:14`.
- **Verdict:** satisfied *for this specific failing AC*. The stuck-gate recovery now dispatches the
  `unstick` action instead of the generic agent-recovery action, and the regression test would fail if that
  wiring regressed.
- **⚠ Broader-scope caveat (AC4) — 3 acceptance criteria remain `cannot determine`.** This verdict covers
  only the narrow correctness finding this audit traces. PAN-3367's own issue body states PAN-3356's
  *cockpit-level* acceptance criteria were **never verified end-to-end**, because
  [PAN-3362](https://github.com/eltmon/overdeck/issues/3362) ("No way to seed tracker-backed issue
  fixtures in workspace containers — every UI-redesign UAT is environment-blocked") is still **OPEN**
  (confirmed via `gh issue view 3362 --json state` at audit time) and blocks the browser UAT that would
  exercise them. Enumerated, these are 3 distinct criteria, each `cannot determine` rather than
  `satisfied`:
  1. **The six-tab issue-cockpit information architecture** renders and navigates correctly end-to-end.
  2. **The Needs-You slot** surfaces the correct prioritized operator decision across the redesign's
     agent/session states (not just the single stuck-gate transition this audit's narrow finding covers).
  3. **The crew spine** (the agent-tree/session summary rail) renders correctly across the redesign's
     states.
  PAN-3356 is deliberately **not closed out** pending this audit and PAN-3362, per the PAN-3367 issue body
  and this plan's NonGoals. The audit answers a narrower, code-checkable question for one finding (was the
  specific flagged correctness defect fixed) that does not stand in for the full UAT across these 3
  criteria.
- **Follow-up:** none for the audited finding (satisfied); the cockpit-level ACs' re-verification
  dependency is [PAN-3362](https://github.com/eltmon/overdeck/issues/3362) — already filed and open, no
  new follow-up needed.
