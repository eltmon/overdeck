# PAN-1525 — harness-independent composer commands (/pan router)

- **Failure (negAt):** **event evidence lost to 7-day compaction (hazard H1).** `~/.overdeck/overdeck.db`'s
  `events` table only retains 7 days; at the time this audit ran, the oldest surviving
  `review.status_changed` row for PAN-1525 was already `passed/passed` (2026-07-25T03:06Z onward) —
  every negative-verdict and reset event is gone, verified at planning time and re-confirmed during this
  batch (`gunzip -c evidence/events-pan-1525.json.gz` does not exist because the reproduction script found
  no candidate row for this issue at all).
- **Failure reason (reconstructed from GitHub, not the event log):** GitHub PR comments are not subject to
  the 7-day DB retention and survive independently. `gh pr view 3033 --repo eltmon/overdeck --comments`
  lists five distinct "Review CHANGES REQUESTED for PAN-1525" comments in chronological order; the last
  one, at 2026-07-25T01:53:28Z, reads: **"Rebase introduced committed composer-manifest drift: workspace
  add-repo syntax is stale and a clean checkout fails the required manifest lint gate."** This is the most
  probable failing state audited here — not a certainty the way the event-log-backed reports in the other
  batches are, since there is no timestamp correlation to an actual `negAt`/`passAt` pair. Corroboration:
  the permanent record `~/.overdeck/state/panopticon-cli/records/pan-1525.json`'s `pipeline.reviewNotes`
  (captured at the final pass) reads "The regenerated composer manifest is committed, matches the rebased
  CLI registry, and passes the canonical drift gate and contract test" — independently confirming (from a
  different source, the record rather than PR comments) that manifest/drift-gate freshness was the last
  thing fixed before merge.
- **Reset / pass:** unavailable (H1). `panDoneRecoveredAt: 2026-07-24T23:12:31.996Z`, `updatedAt (pass):
  2026-07-25T07:11:59.337Z` — source: the permanent record's `pipeline` block.
- **Failing AC (quoted):** none of PAN-1525's dozen-plus issue-body checklist items name manifest-drift
  directly — the finding is about a **repo-wide lint gate** (the generated composer-command manifest must
  match the CLI's actual registry after a rebase), the same class of mechanical-freshness failure audited
  for PAN-3097 in `audit-batch-c`. Quoted verbatim above from PR #3033's comment thread.
- **Classification:** genuine-finding, mechanical — a real gate failure (stale generated artifact after
  rebase), not a logic defect in the issue's substantive `/pan` router work.
- **Merged diff:** [PR #3033](https://github.com/eltmon/overdeck/pull/3033), merge commit
  `15ed251c84dc19981fa4d78143abc20e5b393572` (per `gh pr view 3033 --json mergeCommit`; distinct from the
  record's `reviewedAtCommit`/`lastVerifiedCommit` of `25e54cb59b2cb853b4f0dcb49780c6b302a6d771`, which is
  the last commit reviewed on the feature branch before the squash-merge produced a new commit sha).
- **Diff evidence:** `packages/contracts/src/composer-commands.generated.ts` includes a
  `pan-workspace-add-repo` entry (`display: "/pan workspace add-repo"`) with the `--dry-run`, `--group`,
  `--new`, `--project` flags — confirmed present and current on `main` at
  `packages/contracts/src/composer-commands.generated.ts:102`. `scripts/lint-slash-commands.sh:20` is the
  "canonical drift gate" the record's pass note names: `echo "composer-command manifest has drifted from
  the CLI registry — run 'npm run generate:slash-commands' and commit the result" >&2` — confirmed present
  on `main`.
- **Verdict:** satisfied, with reduced confidence relative to other batches because the negAt/negNotes/
  reset timestamps are unrecoverable (H1) — this verdict rests on GitHub PR-comment chronology and the
  permanent record's pass-time note independently agreeing on the same failure class (manifest/drift-gate
  staleness), plus direct confirmation that the current manifest and drift gate both exist and are
  consistent. It does not rest on a timestamped negAt→fix-commit→pass chain the way the event-log-backed
  reports in batches a-d do.
- **Follow-up:** none — satisfied. (This is a `genuine-finding`, not a `cannot determine`, so per the PRD's
  Method step 4 a follow-up would only be filed on a `not satisfied` verdict.)
