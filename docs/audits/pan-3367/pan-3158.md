# PAN-3158 — merge-train idle-project filtering / hidden-section empty state

- **Failure (negAt):** 2026-07-26T20:49:03.785Z, `failed/pending`, note (verbatim): "Verification failed
  at test" — source: `worklist.md`, confirmed as the note for this state in the collapsed run
  (`evidence/events-pan-3158.json.gz`) — not stale in the H11 sense, but not the substantive finding
  either; see below.
- **Full episode context:** the test-failure note repeats across several transitional states (a genuine
  test-suite red, distinct from tmux infra noise elsewhere in this batch), then the real review finding
  appears at sequence 848945 (2026-07-26T20:58:07.194Z, `blocked/pending`): **"Project filtering can
  falsely show the global no-ready-work message while another project has ready work."**
- **Reset / pass:** reset 2026-07-26T20:51:00.705Z (worklist row; the richer finding's own reset was
  2026-07-26T21:01:27.150Z), pass 2026-07-26T21:10:09.850Z; commits between (worklist window): 2 (method:
  pr-commits, `gh pr view 3161 --json commits`); branch verified: yes.
- **Failing AC (quoted):** "Hide idle enabled sections in MergeTrainView via a renderedSections filter" and
  "Render a footer counting hidden idle projects" — source: PR #3161 body. The finding is about a
  side-effect of that filtering: the empty-state copy didn't distinguish "nothing anywhere" from "nothing
  in your filtered selection."
- **Classification:** genuine-finding — a real UX-correctness bug: filtering to a subset of projects could
  show "No features are ready to merge in any project" even when an unselected project had ready work,
  misleading the operator into believing the whole train was empty.
- **Merged diff:** [PR #3161](https://github.com/eltmon/overdeck/pull/3161), merge commit
  `08339cace429979b95f998e222d0cd2239cafb29`. Fix commit: `5c9d4daa28f47c7412237c7dec2d20c4fe337572`
  ("fix(dashboard): scope filtered merge train empty state").
- **Diff evidence:** `src/dashboard/frontend/src/components/merge-train/MergeTrainView.tsx:488-490` (at
  merge commit `08339cace4`) — the empty message is now conditional on `visibleSections.length ===
  sections.length`: unfiltered emptiness keeps
  the original "No features are ready to merge in any project" text, while a filtered-but-nonempty-overall
  state now reads "No features are ready to merge in the selected projects. Other projects may still have
  ready work." New test `'scopes the empty message to selected projects when filtered-out work is ready'`
  in `MergeTrainView.test.tsx` sets up exactly this scenario (one filtered-out project with ready work) and
  asserts the scoped message renders while the global one does not.
- **Verdict:** satisfied. The fix distinguishes the two empty-state cases the finding conflated, and the
  new test reproduces the exact "another project has ready work" scenario the finding names.
- **Follow-up:** none — satisfied.
