# PAN-3097 — AUQ dialog shows identifier + title, self-contained-question guidance

- **Failure (negAt):** 2026-07-26T03:30:18.325Z, `failed/pending`, note (verbatim): "Review role spawn
  failed: Command failed: tmux -L overdeck -f /home/eltmon/.overdeck/tmux/overdeck.tmux.conf send-keys -t
  agent-pan-3097-review C-m\ncan't find pane: agent-pan-3097-review" — source: `worklist.md`, confirmed as
  the first note in this state's collapsed run (`evidence/events-pan-3097.json.gz`).
- **Full episode context:** all 8 issue-body checklist items had already landed as `feat`/`docs` commits
  *before* negAt (02:41Z–03:17Z). The episode after negAt is: further tmux dispatch failures, then a real
  gate failure at sequence 801538 (2026-07-26T03:33:41.988Z, `failed/pending`): "Verification failed at
  lint" — a repo-wide generated-artifact freshness check (the composer command manifest), unrelated to
  PAN-3097's own AC content. Fixed by `fix(cli): refresh composer command manifest` (03:36:25Z); the
  next approval reads "Approved after rebase: PAN-3097 behavior remains correct and the refreshed
  generated command manifest matches the CLI registry." Later cycles ("Approved: exact HEAD … was fully
  reviewed", "Approved after main merge: … files are unchanged from the prior approved head") are re-review
  churn from repeated main-syncs, not new findings.
- **Reset / pass:** reset 2026-07-26T03:30:55.924Z, pass 2026-07-26T03:50:03.364Z; commits between: 10
  (method: pr-commits, `gh pr view 3102 --json commits`) — the bulk are the 8 already-landed feature/docs
  commits whose `committedDate` falls in this window plus the manifest-refresh fix and a later main-merge
  commit; branch verified: no (`feature/pan-3097` deleted post-merge).
- **Failing AC (quoted):** none of the 8 issue-body checklist items concern build-artifact freshness — the
  actual failing check was a **repo-wide lint gate** (generated CLI command manifest must match the
  registry), not one of PAN-3097's own stated ACs.
- **Classification:** genuine-finding, but mechanical — a real lint gate failure (stale generated
  artifact), not a logic defect in PAN-3097's own feature work. All 8 substantive checklist items were
  already implemented and unchanged by the fix.
- **Merged diff:** [PR #3102](https://github.com/eltmon/overdeck/pull/3102), merge commit
  `0c85f48d27e6f0db286afd2ade819fbf1bbfe2c4`. Fix commit: `fix(cli): refresh composer command manifest`.
- **Diff evidence (correcting an earlier draft of this report, which cited a nonexistent
  `src/components/AskUserQuestion` path):** `src/dashboard/frontend/src/lib/issueLabel.ts:1` exports
  `formatIssueRef(...)` (the helper checklist item), with unit coverage in
  `src/dashboard/frontend/src/lib/__tests__/issueLabel.test.ts`.
  `src/dashboard/frontend/src/components/AskUserQuestionDialog.tsx` (plus its test) is one of the four
  subject-construction sites populating `issueTitle` (the AUQ dialog checklist item); the manifest-fix
  commit itself only touches `packages/contracts/src/composer-commands.generated.ts:255-256`, updating
  two unrelated CLI flag descriptions (`--health-timeout <ms>` → `--health-timeout <duration>`, reflecting
  a *different*, already-landed issue's change to `pan reload`/`pan restart`) to match the CLI registry —
  a pure build-artifact freshness sync, confirming it touches none of PAN-3097's own feature files.
- **Verdict:** satisfied. Every stated AC was implemented before the failing gate ever ran; the one real
  failure (stale manifest) was a mechanical freshness check unrelated to this issue's substance, and was
  fixed by regenerating the artifact with no behavior change, as the reviewer's own note confirms.
- **Follow-up:** none — satisfied.
