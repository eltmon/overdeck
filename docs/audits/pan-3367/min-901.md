# MIN-901 — Compass/Daily Debrief 404s with GLM selected (z.ai completions path)

- **Failure (negAt):** 2026-07-25T12:52:13.226Z, `blocked/pending`, note (verbatim, and confirmed stable,
  not H11-stale — the real finding is present at negAt already): "Vite defines server.watch twice, so the
  later property overwrites the new .pnpm-store exclusion and the ENOSPC mitigation never takes effect."
  — source: `worklist.md`.
- **⚠ This finding does not apply to MIN-901's diff — confirmed false-positive, and the pipeline's own
  final review says so.** MIN-901's tracked MR (`mind-your-now-backend` MR !68) is entirely Java/backend:
  `docs/ai-providers.md`, `AIConfig.java`, `ZaiConnectionProperties.java`, `DebriefBatchService.java`,
  `AiProviderErrorClassifier.java`, two `application-*.properties` files, and three test files — no
  `vite.config.*` or any frontend file. The final approval note reads: **"Approved: the prior duplicate
  Vite watch blocker is absent from the PR diff, the effective watcher excludes .pnpm-store, and the API
  remains clean at 025ec1f3"** — the reviewer itself concluded, on re-review, that the finding referred to
  a shared-repo config state outside this MR's own changes, not a defect the diff introduced. The
  identical finding text is also attached to MIN-858 (same batch, different repo) around the same
  timestamp — see that report; this looks like a review-context cross-contamination affecting multiple
  concurrent MRs, not a per-MR defect.
- **Reset / pass:** reset 2026-07-25T12:54:03.350Z, pass 2026-07-25T13:08:06.763Z; commits between: 0
  (method: mr-commits, `glab api …/merge_requests/68/commits`) — 0 is correctly explained: the HEAD never
  changed (`025ec1f3`, named explicitly in the final approval) because no fix was needed for an
  inapplicable finding.
- **Failing AC (quoted):** the real work — "AC-1: With `app.zai.base-url=https://api.z.ai/api/paas/v4`, a
  GLM chat call issues its request to `https://api.z.ai/api/paas/v4/chat/completions`," "AC-2:
  `ZaiConnectionProperties` exposes `completionsPath` with a `/chat/completions` default, and
  `AIConfig.zaiChatModel` applies it" — source: Linear MIN-901 description
  (`https://linear.app/mind-your-now/issue/MIN-901`).
- **Classification:** environment-blocked (the recorded blocking note was a scope-mismatched
  cross-contamination, explicitly recognized as such by the pipeline's own re-review).
- **Merged diff:** [MR !68](https://gitlab.com/eltmon/mind-your-now-backend/-/merge_requests/68).
- **Diff evidence:** confirmed via `glab api projects/eltmon%2Fmind-your-now-backend/merge_requests/68/
  changes` — `ZaiConnectionProperties.java` gains `private String completionsPath = "/chat/completions";`
  with a matching doc comment; `AIConfig.java`'s `zaiChatModel` method gains
  `if (StringUtils.hasText(props.getCompletionsPath())) { builder.completionsPath(props.getCompletionsPath()); }`
  immediately after the existing `baseUrl` guard — matching the issue's own prescribed fix (mirroring the
  Anthropic bean's existing `completionsPath` handling) verbatim. `AIConfigZaiUrlTest.java` is present
  (matching AC-1's required WireMock-backed test) and `AiProviderErrorClassifier.java` plus
  `AiProviderErrorClassifierTest.java` are present (matching AC-3/AC-5's error-classification
  requirement).
- **Verdict:** satisfied. The recorded blocking finding is a confirmed false positive unrelated to this
  MR's actual files; the substantive AC-1/AC-2 fix (and the AC-3/AC-5 error-classification work) is present
  in the diff and matches the issue's own specification precisely.
- **Follow-up:** none — satisfied.
