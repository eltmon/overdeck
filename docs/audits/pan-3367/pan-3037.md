# PAN-3037 — polyrepo primary-workspace-repo resolution (sync-main, branch detection)

- **Failure (negAt):** 2026-07-25T22:38:57.993Z, `failed/passed`, note (verbatim): "Review role spawn
  failed: Command failed: tmux -L overdeck -f /home/eltmon/.overdeck/tmux/overdeck.tmux.conf new-session
  -d -s agent-pan-3037-review …" — source: `worklist.md`. **This is an infra note (H6)**, but it is not
  the only or the last blocking state in this episode — see below.
- **Full episode context:** PAN-3037 went through an unusually long review cycle (multiple infra retries
  interleaved with genuine findings) before its final pass. The substantive finding this audit traces is
  at sequence 784723 (2026-07-25T22:50:42.520Z, `blocked/passed`): **"Configured target-branch conflicts
  are auto-resolved from origin/main, risking wrong or deleted pipeline artifacts."** Later cycles in the
  same episode (`Verification failed at test`, `Verification error: Verification worker 670617 exited
  before writing a result`) are infra/test-flakiness retries with no additional named findings, and a
  final "Documentation advisory is resolved; current-cycle executable behavior is unchanged" note
  confirms the last delta before pass was docs-only. Source: `evidence/events-pan-3037.json.gz` (full
  raw series cross-checked; see H11 general caution).
- **Reset / pass:** reset 2026-07-25T23:03:49.515Z, pass 2026-07-25T23:31:49.741Z (worklist row); commits
  between: 3 (method: pr-commits, `gh pr view 3079 --json commits`); branch verified: yes.
- **Failing AC (quoted):** none of the 8 issue-body checklist items name the target-branch conflict bug
  directly (they list `resolvePrimaryWorkspaceRepoDirSync`, `syncMainIntoWorkspace` polyrepo iteration,
  per-repo sync results, review-restart/dispatch branch detection, and git-status resolution) — the
  finding is a correctness gap in the sync-main conflict-resolution logic this PR touches. Quoted from
  the review event itself — source: `evidence/events-pan-3037.json.gz`, sequence 784723/784723.
- **Classification:** genuine-finding — a correctness/safety defect: pipeline-owned merge-conflict
  auto-resolution always preferred `origin/main`, even for a polyrepo whose configured target branch is
  something else (e.g. `develop`), risking the wrong branch's pipeline artifacts being staged or a valid
  artifact being deleted because it doesn't exist on `main`.
- **Merged diff:** [PR #3079](https://github.com/eltmon/overdeck/pull/3079), merge commit
  `87c4dccd5e2615403bc722dbde8a1294e7eac95d`. Fix commit: `89a3f4cd394b4d74b8ee63156d36624a87ac54e1`
  ("fix(cloister): resolve conflicts from target branch").
- **Diff evidence:** `src/lib/cloister/merge-agent.ts:898-901` (at merge commit `87c4dccd5e`) —
  `resolveMainPreferredSyncConflicts` gained a `targetBranch` parameter; its `git checkout origin/main --
  ${path}` call at line 913 became `git checkout origin/${targetBranch} -- ${path}`; its caller in
  `syncMainIntoRepo` at line 1314 now passes the repo's actual `targetBranch` instead of assuming `main`.
  New regression test
  `'auto-resolves pipeline-owned conflicts from the configured target branch'` in
  `src/lib/cloister/__tests__/sync-main-polyrepo.test.ts` sets up a repo whose `targetBranch` is
  `develop` (not `main`), creates a genuine conflict, and asserts the resolved content matches
  `develop`'s state, not `main`'s.
- **Verdict:** satisfied. The fix replaces every hardcoded `origin/main` reference in the conflict path
  with the repo's configured target branch, and the new test specifically exercises a non-`main` target
  branch — the exact scenario the finding describes as unhandled.
- **Follow-up:** none — satisfied.
