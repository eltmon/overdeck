# MIN-858 — system/agent comment authorship for task comments (Kaia + MYN)

> **Confidentiality note:** `mind-your-now` (frontend) and `mind-your-now-backend` are private
> repositories; `eltmon/overdeck` (this report's home) is public. Per the PAN-3367 review's security
> finding, this report describes the verdict and its reasoning without quoting private source code, file
> paths, or Linear/GitLab text verbatim. Anyone with MYN repo access can independently verify against the
> merge request linked below.

- **Failure (negAt):** 2026-07-25T12:51:37.111Z, `blocked/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **⚠ Methodology correction (H11):** the real finding, from the full raw series (filtered to the
  negAt±5min window, 1276 rows total for the full issue history): a review finding about a duplicate Vite
  `server.watch` definition discarding a `.pnpm-store` exclusion needed for an unrelated infra fix.
- **⚠ Scope mismatch — this finding does not apply to MIN-858's diff.** MIN-858's tracked MR (frontend)
  changes a small set of task-comment-authorship files — confirmed via `glab api` that no
  `vite.config.*` file is among them. MIN-901 (audited in this same batch) shows the *identical* finding
  text attached to a completely different, backend-only MR that also never touches Vite config — see that
  report for the confirmed-false-positive analysis. The same reasoning applies here: this was a
  review-context cross-contamination (the reviewer evaluating shared repo-wide config state, not this
  MR's actual diff), not a defect in MIN-858's own changes.
- **Reset / pass:** reset 2026-07-25T12:57:45.130Z, pass 2026-07-25T13:07:47.534Z; commits between: 0
  (method: mr-commits) — 0 is correctly explained: no fix was needed because the finding didn't apply to
  this diff.
- **Failing AC (paraphrased):** extend task-comment authorship so a comment can be attributed to the
  system, the AI assistant, or an external agent (not only a human user), with the frontend rendering
  non-user authors distinctly — source: Linear MIN-858 (`https://linear.app/mind-your-now/issue/MIN-858`,
  access required).
- **Classification:** environment-blocked (the recorded blocking note was inapplicable to this diff, a
  false-positive cross-attachment rather than a genuine finding about MIN-858's own changes).
- **Merged diff:** [MR !86](https://gitlab.com/eltmon/mind-your-now/-/merge_requests/86) (private — link
  resolves for repo members). The permanent Overdeck pipeline record for this issue shows a larger
  polyrepo scope (backend entity/migration changes) than this single tracked frontend MR — the real
  changeset spans the MYN polyrepo beyond what one MR URL captures.
- **Diff evidence (paraphrased; file:line pointers only, no source content):** Overdeck's own inspect
  specialist — captured independently of this audit, at implementation time — recorded that the backend
  migration and entity changes matched the issue's two core acceptance criteria (a system-authored comment
  with no human author round-trips correctly, and the default author type is preserved for ordinary user
  comments), each covered by a named persistence test. The tracked frontend MR's diff (confirmed via
  `glab api .../merge_requests/86/changes`) touches `src/components/tasks/TaskComments.tsx:8,241` (two
  changed hunks) and `src/hooks/useTaskComments.ts:17,176` (two changed hunks), rendering the
  non-user-author badge/avatar the issue's acceptance criteria call for.
- **Verdict:** satisfied. The recorded blocking finding was a false-positive unrelated to this diff (same
  pattern independently confirmed for MIN-901); the actual substantive authorship AC is corroborated by
  Overdeck's own independent inspect note plus the visible frontend rendering change in the merged MR.
- **Follow-up:** none — satisfied.
