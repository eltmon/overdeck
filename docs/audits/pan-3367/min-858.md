# MIN-858 — system/agent comment authorship for task comments (Kaia + MYN)

- **Failure (negAt):** 2026-07-25T12:51:37.111Z, `blocked/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **⚠ Methodology correction (H11):** the real finding, from the full raw series
  (`evidence/events-min-858.json.gz`, filtered to the negAt±5min window, 1276 rows total for the full
  issue history): sequence 736413 (2026-07-25T12:52:01.830Z, `blocked/pending`): **"Vite defines
  server.watch twice, so the later property discards the new .pnpm-store exclusion and leaves the ENOSPC
  verification blocker unresolved."**
- **⚠ Scope mismatch — this finding does not apply to MIN-858's diff.** MIN-858's tracked MR (GitLab
  `mind-your-now` MR !86) changes exactly 3 files:
  `src/components/tasks/__tests__/TaskComments.authorTypes.test.tsx`,
  `src/components/tasks/TaskComments.tsx`, `src/hooks/useTaskComments.ts` — none is `vite.config.*`.
  MIN-901 (audited in this same batch) shows the *identical* finding text attached to a completely
  different, backend-only MR that also never touches `vite.config.*` — see that report for the
  confirmed-false-positive analysis. The same reasoning applies here: this was a review-context
  cross-contamination (the reviewer evaluating shared repo-wide config state, not this MR's actual diff),
  not a defect in MIN-858's own changes.
- **Reset / pass:** reset 2026-07-25T12:57:45.130Z, pass 2026-07-25T13:07:47.534Z; commits between: 0
  (method: mr-commits, `glab api projects/eltmon%2Fmind-your-now/merge_requests/86/commits`) — 0 is
  correctly explained: no fix was needed because the finding didn't apply to this diff.
- **Failing AC (quoted):** the issue's actual scope — "Extend task-comment authorship so comments can be
  authored by: USER (unchanged), SYSTEM …, KAIA …, AGENT … Frontend renders non-user authors with distinct
  avatar/badge (no fake user identity)" — source: Linear MIN-858 description
  (`https://linear.app/mind-your-now/issue/MIN-858`).
- **Classification:** environment-blocked (the recorded blocking note was inapplicable to this diff, a
  false-positive cross-attachment rather than a genuine finding about MIN-858's own changes).
- **Merged diff:** [MR !86](https://gitlab.com/eltmon/mind-your-now/-/merge_requests/86) (frontend
  changes). The permanent record `~/.overdeck/state/mind-your-now/records/min-858.json` shows a much
  larger polyrepo scope (`api/src/main/java/com/myn/entities/TaskComment.java`,
  `api/.../V256__Add_author_type_to_task_comment.sql`, and matching backend tests) that isn't in MR !86's
  own diff — this issue's real changeset spans the polyrepo wrapper (`~/Projects/myn`) beyond the single
  tracked frontend MR.
- **Diff evidence:** the record's `inspectNotes` (Overdeck's own inspect specialist, captured independent
  of this audit) reads: "V256 has exactly the 3 required ALTER statements; TaskComment.java adds
  authorType (nullable=false, @Builder.Default=USER) and authorLabel matching migration; author_id
  nullable() dropped. TaskCommentAuthorTypePersistenceTest covers AC1 (SYSTEM+null author round-trip) and
  AC2 (builder default USER)." This independently corroborates the substantive backend authorship work
  (SYSTEM/KAIA/AGENT author types) was implemented and tested. `TaskComments.tsx` in MR !86 renders the
  frontend badge/avatar side.
- **Verdict:** satisfied. The recorded blocking finding was a false-positive unrelated to this diff (same
  pattern independently confirmed for MIN-901 below); the actual substantive authorship AC is corroborated
  by Overdeck's own independent inspect note plus the visible frontend rendering change in MR !86.
- **Follow-up:** none — satisfied.
