# MIN-858 — system/agent comment authorship for task comments (Kaia + MYN)

> **Confidentiality note:** the source repositories for this issue are private (Mind Your Now frontend
> and backend); `eltmon/overdeck` (this report's home) is public. Per the PAN-3367 review's security
> findings (cycles 1 and 2), this report states only the verdict and its classification. It deliberately
> omits merge request URLs, commit identifiers, file paths, line numbers, endpoint names, and any
> description of the private diffs' contents or behavior — all of that was verified directly against the
> private repositories but is not reproduced here. A reader with access to the Mind Your Now repositories
> can locate the relevant merge requests via the issue tracker (MIN-858) and verify independently.

- **Failure (negAt):** 2026-07-25T12:51:37.111Z — a review finding was recorded that, on inspection, did
  not concern this issue's own changes (see "Scope mismatch" below).
- **Scope mismatch — confirmed false positive.** The finding text recorded against this issue's negative
  verdict is identical to a finding independently recorded against a *different* issue in the same batch,
  audited elsewhere in this report set, whose diff also does not touch the file the finding concerns. This
  looks like review-context cross-contamination across two concurrently-reviewed merge requests in the
  same repository, not a defect in this issue's own changes.
- **Reset / pass:** reset 2026-07-25T12:57:45.130Z, pass 2026-07-25T13:07:47.534Z; commits between: 0 —
  consistent with no fix being needed for an inapplicable finding.
- **Failing AC (paraphrased, no verbatim source-tracker text):** extend a comment-authorship model so a
  comment can be attributed to something other than a human user (a system process or an AI/automation
  actor), with the persistence layer allowing that case and the UI rendering it distinctly.
- **Classification:** environment-blocked (the recorded blocking note was inapplicable to this diff, a
  false-positive cross-attachment rather than a genuine finding about this issue's own changes).
- **Merged diff:** verified directly against two private merge requests spanning this issue's frontend and
  backend changes (URLs withheld per the confidentiality note above) — this issue's full changeset spans
  both a frontend and a backend repository.
- **Diff evidence:** independently confirmed, by reading both merged diffs directly against the private
  repositories, that (a) the backend schema and entity changes add exactly the fields and constraint
  relaxation this authorship model requires, verified against dedicated persistence tests covering both
  the non-human-author round-trip case and the default-to-human-user case, and (b) the frontend change
  renders the non-human-author case distinctly, as the issue requires. No file names, line numbers, or
  code content are reproduced here.
- **Verdict:** satisfied. The recorded blocking finding was a confirmed false positive unrelated to this
  issue's actual changes; the substantive authorship requirements were independently verified against both
  private diffs directly, not via any third party's summary of them.
- **Follow-up:** none — satisfied.
