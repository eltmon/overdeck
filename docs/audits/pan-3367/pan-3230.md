# PAN-3230 — orphan-proposed-reconciler reads legacy `<project>/.pan/specs`

- **Failure (negAt):** 2026-07-28T19:07:34.616Z, `blocked/pending`, note recorded as empty in the collapsed
  worklist row — source: `worklist.md` / `evidence/commit-counts.json`.
- **⚠ Methodology correction — this is the richest example found in the batch.** PAN-3230's full raw
  series (`evidence/events-pan-3230.json.gz`, 66 rows) shows **two separate genuine findings and two
  separate fix-and-repass cycles**, not the single empty-note episode the worklist row implies:
  1. `blocked/pending` (sequence 902797, 2026-07-28T19:19:21.295Z): "Auto-synthesis bypasses the state
     write door, and the path guard misses multiline joins," later restated more specifically at
     903098 (19:47:11.762Z): "`[correctness] Auto-synthesis now writes canonical state outside the spec
     write door` — `src/lib/xbrief/auto-synthesize.ts:148`." Fixed; re-review note at 903348: "auto-synthesis
     now uses `writeSpecForIssue` with a clean-and-pushed state-worktree durability test."
  2. `blocked/pending` (sequence 903781, 2026-07-28T19:52:44.022Z): "Shared pan-dir writer changed outside
     scope, and template-literal state paths still bypass the lint guard." Fixed; final pass note
     (904965 onward): "All prior blockers are resolved; state paths, durable writes, and lint enforcement
     meet the requirements."
  The worklist's negAt (19:07:34.616Z) lands on an earlier `blocked/pending` transition whose note happened
  to be empty at that instant; the two substantive findings above are what the reviewer actually blocked
  on. See hazard H11 — the collapsed worklist row systematically understates multi-round episodes.
- **Reset / pass (episode used for commit counting):** the worklist's negAt→passAt window (19:07:34.616Z →
  20:05:45.836Z) contains 2 commits (method: pr-commits, `gh pr view 3239 --json commits`, filtered from
  24 total commits on the PR — most of the rest are unrelated `git merge main` noise with a shared
  `committedDate`, see `worklist.md` finding #2): `fix(infra): enforce durable state path writes`
  (19:17:32Z) and `fix(infra): close state path review gaps` (19:38:28Z). Branch verified: yes.
- **Failing AC (quoted):** "Write auto-synthesized proposed specs to the canonical specsDir so `findPlan`
  resolves them on migrated projects" and "Add lint-state-paths guard to the lint chain and document the
  path authority" — source: PR #3239 body checklist (`gh pr view 3239 --json body`). Both items map
  directly onto the two findings above.
- **Classification:** genuine-finding (both rounds) — file-path/state-write-door correctness defects in
  the new auto-synthesis and lint-guard code this PR introduces, not infra noise.
- **Merged diff:** [PR #3239](https://github.com/eltmon/overdeck/pull/3239), merge commit
  `0297fa8f4bc687f640a17a970da11aa2706adbc5`.
- **Diff evidence:** `6b894c9ac4f` ("fix(infra): enforce durable state path writes") touches
  `scripts/lint-state-paths.sh`, `src/lib/pan-dir/specs.ts`, and `src/lib/xbrief/auto-synthesize.ts` — the
  exact file the first finding cites (`auto-synthesize.ts:148`). `d5a0bf4e352` ("fix(infra): close state
  path review gaps") further touches `scripts/lint-state-paths.sh` and
  `tests/unit/scripts/lint-state-paths.test.ts` (+39 lines of new regression coverage) — matching the
  second finding's "template-literal state paths still bypass the lint guard."
- **Verdict:** satisfied. Both flagged findings have file-scoped fix commits landing between their
  respective blocked and pass events, and the reviewer's own final note ("All prior blockers are resolved;
  state paths, durable writes, and lint enforcement meet the requirements") names all three concerns
  (state paths, durable writes, lint) the two findings raised.
- **Follow-up:** none — satisfied.
