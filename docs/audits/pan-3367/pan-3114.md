# PAN-3114 — uat-promote-verification module (DoD row 3 verdict source)

- **Failure (negAt):** 2026-07-26T10:38:07.947Z, `failed/pending`, note (verbatim): "Review role spawn
  failed: write failed for agents-db:agent-pan-3114-review: database is locked" — source: `worklist.md`,
  confirmed against the full raw series (`evidence/events-pan-3114.json.gz`, 7 rows — short and
  unambiguous, no H11 masking here).
- **Full episode context:** the reset event itself explicitly states no finding was made: "Agent re-review
  request (1/25): Retry review after agents-db lock during role spawn; no code findings were emitted."
  The subsequent and final review reads: "Approved: all requirements implemented; correctness, security,
  and performance review clean; focused tests, typecheck, lint, and diff check pass."
- **Reset / pass:** reset 2026-07-26T10:39:12.236Z, pass 2026-07-26T11:09:56.090Z; commits between: 0
  (method: pr-commits, `gh pr view 3126 --json commits`); branch verified: yes (`feature/pan-3114` still
  exists on origin) — 0-commit result consistent with the explicit "no code findings were emitted" note.
- **Failing AC (quoted):** all 4 issue-body checklist items — create `uat-promote-verification` module
  with stamp builder and verdict recorder, record verification verdicts at UAT promote time via injected
  dependency, heal past UAT promotions in `closeOut` before the DoD gate, document `uat-promotion` as a
  DoD row 3 verdict source — source: PR #3126 body.
- **Classification:** environment-blocked. The database-lock failure is explicitly disclaimed by the
  agent's own re-review request note ("no code findings were emitted") — the clearest possible
  confirmation that the negative state was pure infra, not a review outcome.
- **Merged diff:** [PR #3126](https://github.com/eltmon/overdeck/pull/3126), merge commit
  `ac121d640e59b53f4bf042ae0d2d6d2d1219adff`.
- **Diff evidence (independently read from the merged diff, not from the review's own note):**
  - **Checklist item 1** (new module with stamp builder + verdict recorder): `src/lib/cloister/
    uat-promote-verification.ts` is a new 130-line file. `buildUatPromotionStamp()` (the stamp builder,
    lines ~17-32) returns a no-op `{}` when a verdict is already `passed`/`skipped`, otherwise returns
    `{ verificationStatus: 'passed', verificationNotes: 'uat-promotion: operator UAT of batch
    ${evidence.generationName}...', lastVerifiedCommit: ... }`. `recordUatPromotionVerdicts()` (the
    verdict recorder, lines ~35-51) iterates a UAT generation's members, builds each member's stamp, and
    writes it through `setReviewStatusSync` — the canonical review-status write door, not a direct store
    write.
  - **Checklist item 2** (injected dep at UAT promote time): `src/lib/cloister/uat-promote.ts:71` adds an
    optional `recordVerification?: (generation, mergeSha) => void` field to `UatPromoteDeps`; inside
    `promoteUatGeneration()` (around line 177), immediately after `deps.store.update(gen.name, { status:
    'promoted' })`, a new `try { deps.recordVerification?.(gen, mergeSha) } catch (err) { log(...) }`
    block invokes it, non-fatally.
  - **Checklist item 3** (heal past promotions in `closeOut` before the DoD gate): `src/lib/lifecycle/
    workflows.ts`, inside `closeOut()` (around line 174) — a new `uatEvidenceStep` calls
    `healUatPromotionVerification(ctx.issueId)` and is pushed onto `allSteps` **before** the call to
    `evaluateDodGate()` a few lines below it in the same function — confirming the ordering the checklist
    item requires (heal, then gate).
  - **Checklist item 4** (docs): `docs/DEFINITION-OF-DONE.md` row 3's verdict-source column is edited from
    "supervised verification worker" alone to add "uat-promotion (recorded at batch promotion,
    PAN-3114)"; a paragraph is added below the table explaining the heal-at-close-out path for batches
    promoted before this write path existed. `docs/UAT-BATCH-TRAINS.md` is also updated in the diff.
  - Test coverage: `tests/unit/lib/cloister/uat-promote-verification.test.ts` (new, 198 lines) and
    `tests/unit/lib/cloister/uat-promote.test.ts` are both present in the diff.
- **Verdict:** satisfied. All 4 checklist items are independently visible in the merged diff at the
  specific functions/line ranges cited above — this verdict does not rest on the review's own approval
  note, though that note (and the reset event's explicit "no code findings were emitted") corroborates it.
- **Follow-up:** none — satisfied.
