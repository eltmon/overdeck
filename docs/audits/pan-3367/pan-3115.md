# PAN-3115 — pipeline-membership default-branch resolution / gather-failure typing

- **Failure (negAt):** 2026-07-26T11:22:23.081Z, `failed/pending`, note (verbatim): "Review role spawn
  failed: Command failed: tmux -L overdeck -f /home/eltmon/.overdeck/tmux/overdeck.tmux.conf new-session
  -d -s agent-pan-3115-review …" — source: `worklist.md`, confirmed as the first note in this state's
  collapsed run.
- **⚠ Methodology correction (H11):** the real finding is a later state in the same episode
  (`evidence/events-pan-3115.json.gz`, sequence 819294, 2026-07-26T11:35:08.777Z, `blocked/pending`):
  **"Default-branch resolution accepts dangling/unborn symbolic refs without verifying targets."**
- **Reset / pass:** reset 2026-07-26T11:26:17.938Z (worklist row); pass 2026-07-26T12:00:33.370Z; commits
  between (worklist window): 2 (method: pr-commits, `gh pr view 3134 --json commits`); branch verified:
  yes.
- **Failing AC (quoted):** none of the 5+ issue-body checklist items name this exact defect directly (they
  cover the `MembershipUnavailableReason` contract, failing closed on non-git-repo paths, resolving each
  repo's effective default branch "from repo reality," typed gather failures by lens boundary, and
  propagating `unavailableReason` through the membership snapshot) — the finding is a review-discovered
  gap in the "resolve default branch from repo reality" item specifically. Quoted from the review event —
  source: `evidence/events-pan-3115.json.gz`, sequence 819294/819294.
- **Classification:** genuine-finding — a real correctness gap: `git symbolic-ref` can return a branch
  name for a dangling or unborn ref (one that doesn't point at any commit, e.g. an empty repo or a
  misconfigured remote HEAD) without erroring, and the original code trusted the name without checking the
  ref actually resolves to something.
- **Merged diff:** [PR #3134](https://github.com/eltmon/overdeck/pull/3134), merge commit
  `35528719a916dbc72b2c7aebd666711740a0e995`. Fix commit: `ab7e17d578655ae232255a7193700f34373f6e92`
  ("fix(cloister): verify symbolic default branches").
- **Diff evidence:** `src/lib/pipeline-membership-gather.ts:365-378` (at merge commit `35528719a9`) —
  `resolveRepositoryDefaultBranch` (line 365) changed its symbolic-ref fallback at line 378 from
  `if (branch) return branch;` to `if (branch && await hasGitRef(repoPath, branch, run)) return branch;`,
  and the function's earlier checks at lines 371-372 apply the same new `hasGitRef()` helper (defined at
  line 349) to the configured/origin-qualified branch names too — the symbolic-ref result is now verified
  against `git rev-parse --verify --quiet refs/heads/<branch>` before being trusted at every fallback
  tier, not just the final one. New regression test `'rejects an unborn symbolic HEAD whose target ref does not
  exist'` in `tests/unit/lib/pipeline-membership-gather.test.ts` asserts the function now throws
  `{ reason: 'default_branch_unresolved', ... }` for exactly this case, and an updated
  `snapshotBranchRefs` test (`'falls through a dangling origin HEAD to a verifiable current branch'`)
  covers the fallback path. The final review note confirms: "Approved: symbolic branch verification fixes
  the prior blocker; all review dimensions and gates pass."
- **Verdict:** satisfied. The fix adds exactly the verification step the finding says was missing, with a
  regression test that directly reproduces the dangling/unborn-ref scenario the finding describes.
- **Follow-up:** none — satisfied.
