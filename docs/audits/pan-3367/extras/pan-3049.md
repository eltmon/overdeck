# PAN-3049 — canonical compose-project naming / Rally container recognition (derived extra)

- **Failure (negAt):** 2026-07-28T16:42:28.831Z, `failed/pending`, note (verbatim, stable — not H11-stale):
  "Review role spawn failed: Role run agent-pan-3049-review already running. Use 'pan tell' to message
  it." — source: `worklist.md`, a genuine infra note.
- **⚠ H12 — the recorded pass was a bypass, and it reverted to a real finding.** The full raw series
  (`evidence/events-pan-3049.json.gz`) shows the worklist's recorded `passAt`
  (2026-07-28T17:12:06.549Z) came via "Review bypassed: verification passed but review infrastructure
  repeatedly failed" — an infra bypass, not a completed re-review. ~7 minutes later it reverted to
  `blocked/passed` with a genuine finding (2026-07-28T17:19:02.708Z): **"Doctor treats support containers
  as canonical health and can recommend stopping the only serving stack."** That was fixed, then a
  **second** genuine finding surfaced (2026-07-28T17:55:13.250Z): **"Deacon's anchored container parser
  drops supported Rally issue IDs, so crashed Rally frontend/server containers are never restarted."**
  The true durable pass came after both were fixed: "Rally and standard workspace container parsing now
  preserves final-suffix attribution; no blocking findings remain."
- **Reset / pass (corrected):** true durable pass 2026-07-28T18:14:45.289Z (not the worklist's
  17:12:06.549Z bypass). Commits between negAt and the *true* pass: `fix: keep container health parser
  within size ratchet (PAN-3049)` (17:34:06Z) and `fix: recognize Rally workspace containers (PAN-3049)`
  (17:57:38Z) — the worklist's narrower window (0 commits) only covers the infra-bypass episode before
  either real finding appeared.
- **Failing AC (quoted):** none of the issue-body checklist items name either defect directly (the
  checklist is about canonical compose-project naming for devcontainer workspaces) — both findings are
  correctness gaps in the container-health/doctor tooling this PR touches as a side effect of the naming
  change. Sources: `evidence/events-pan-3049.json.gz`.
- **Classification:** genuine-finding (both) — Finding 1 is an operational-safety bug (doctor could
  recommend stopping the only running stack by misclassifying a support container as canonical); Finding
  2 is a regex-coverage gap that broke crash-recovery for one workspace-naming convention (Rally-style
  issue IDs like `f29698`, `us12345`) while working for the standard one.
- **Merged diff:** [PR #3223](https://github.com/eltmon/overdeck/pull/3223), merge commit
  `09ac74127d5b9f59a61b0a8f1e4df1c99860a481`. Fix commit for Finding 2:
  `2e3178403d0858fa06f04847031f32559bb6ca5a` ("fix: recognize Rally workspace containers (PAN-3049)").
- **Diff evidence:** the Finding-2 fix changes `src/lib/cloister/deacon.ts:2356`'s container-name regex
  from
  `/feature-([a-z0-9]+-\d+)-(frontend|server)-\d+$/i` to
  `/feature-((?:[a-z]+-\d+)|(?:(?:f|us|de|ta|tc)\d+))-(frontend|server)-\d+$/i` — adding the Rally-prefix
  alternation (`f`, `us`, `de`, `ta`, `tc` followed by digits, no hyphen) alongside the original
  `word-digits` pattern — directly matching "anchored container parser drops supported Rally issue IDs."
  New parametrized test cases cover `f29698-server` and `us12345-frontend` container names.
- **Verdict:** satisfied. Finding 2's fix is confirmed exact; Finding 1's resolution is corroborated by the
  final review note explicitly naming "duplicate-stack doctor now emits diagnosis-only rebuild guidance
  and never infers a safe teardown from container names" as addressed feedback.
- **Follow-up:** none — satisfied.
