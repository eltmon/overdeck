# PAN-3076 — HeadAnchor/workspace-anchor-drift branded type + write-site guard (derived extra)

- **Failure (negAt):** 2026-07-25T23:00:51.154Z, `blocked/pending`, note (verbatim, confirmed stable — the
  worklist correctly captured this note): "Unchanged HEAD still preserves passed review on failed Git
  inspection; write-site guard still misses raw writes in allowlisted files" — source: `worklist.md`.
- **⚠ H12 — the recorded pass was transient.** The full raw series
  (`evidence/events-pan-3076.json.gz`, 100+ rows) shows the worklist's recorded `passAt`
  (2026-07-26T00:00:33.233Z, "Approved: all prior blockers resolved; typecheck, lint, and 110 focused
  regression tests pass") reverted ~53 minutes later, after further re-review cycling with test/infra
  failures interleaved, to a genuine **second finding** (2026-07-26T00:53:44.016Z, `blocked/pending`):
  **"Lint is red: PAN-3076 grows `src/lib/review-status.ts` from main's 1000 lines to 1006, violating the
  file-size gate."** The true durable settle came after a further fix cycle (final events read "Agent
  re-review request (1/25): Fixed review issues" then stable `passed/passed`).
- **Reset / pass (corrected):** the worklist's negAt→passAt window (23:00:51→00:00:33, 0 commits recorded)
  covers only Finding 1; Finding 2 and its fix land after that window.
- **Failing AC (quoted):** none of the issue-body checklist items name either defect directly (the
  checklist covers the branded `HeadAnchor` type, the composite-aware drift evaluator, and the write-site
  stamps across four call sites) — both findings are correctness gaps in the drift-evaluator/write-site
  implementation this PR introduces. Sources: `evidence/events-pan-3076.json.gz`, sequences 785749
  (Finding 1) and the file-size-gate event (Finding 2).
- **Classification:** genuine-finding (both) — Finding 1 is a fail-open correctness bug (a failed git
  inspection defaulted to treating the review as still current instead of failing closed, and the
  write-site guard didn't catch raw writes to allowlisted files); Finding 2 is a mechanical file-size-gate
  violation from the same change.
- **Merged diff:** [PR #3082](https://github.com/eltmon/overdeck/pull/3082), merge commit
  `e224e941abb8142dbcc9cd07087fefcf3e18bf60`. Fix commits: `712da16873a9e409b71dd26affffccc1a0a86a97`
  ("fix(review): fail closed on anchor uncertainty", Finding 1) and
  `39d5480caead737c18ee0f37a62de1d3fcb5085b` ("refactor(review): relocate anchor update type",
  Finding 2).
- **Diff evidence (at merge commit `e224e941ab`):** the Finding-1 fix commit's message states directly:
  "Lock each anchor write by exact AST occurrence so raw writes in allowed files are rejected," and its
  diff to `src/lib/workspace-anchor-drift.ts:25` changes the `unreadable` verdict variant to drop its
  optional `currentAnchor` field (no longer treating an unreadable read as "current" — line 80's
  `if (!currentAnchor) return { kind: 'unreadable' };` now returns the trimmed variant), and
  `isBenignMove()` (defined line 52) wraps each of its three independent benign-move checks (starting
  with `checks.sameTree` at line 60) in its own `try`/`catch` so a failed proof falls through to the next
  check instead of silently passing — directly addressing "preserves passed review on failed Git
  inspection." The Finding-2 fix moves a type out of `src/lib/review-status.ts` (12 lines removed, net)
  into `workspace-anchor-drift.ts`, addressing the file-size-gate violation.
- **Verdict:** satisfied. Both findings — including the one masked by the H12 transient-pass artifact —
  have file-scoped fix commits whose diffs match their descriptions directly.
- **Follow-up:** none — satisfied.
