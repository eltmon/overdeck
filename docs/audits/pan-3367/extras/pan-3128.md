# PAN-3128 — canonical `pan reload` build isolation / deployment generation bounds (derived extra)

- **Failure (negAt):** 2026-07-26T14:45:23.661Z, `blocked/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **⚠ Methodology correction (H11):** the real finding, in the same collapsed run
  (`evidence/events-pan-3128.json.gz`, sequence 828881, 2026-07-26T14:45:27.567Z, `blocked/pending`):
  **"Failed restarts leak unbounded multi-gigabyte deployment worktrees because failed generations are
  neither rolled back nor swept."**
- **⚠ Additional methodology hazard — rebase-timestamp deflation (extends H9).** The worklist's recorded
  commit count for this window is 0 (method: pr-commits, `committedDate`-filtered) because **the entire
  branch was rebased later** (2026-07-26T17:41:47Z-17:42:08Z), shifting every commit's `committedDate`
  outside the negAt-passAt window regardless of when the work actually happened. Checking `authoredDate`
  instead (`gh pr view 3141 --json commits`) shows two genuine fix commits landing inside the window:
  `fix: bound dashboard deployment generations (PAN-3128)` (14:55:13Z) and `fix: preserve timed-out
  dashboard deployments (PAN-3128)` (15:22:30Z) — matching Finding 1 above and a second finding
  ("Health-timeout rollback deletes the newly spawned dashboard that the lifecycle layer deliberately
  leaves running for slow-boot recovery and inspection," sequence 830098) respectively. This is the
  inverse of H9 (which inflates counts with noise) — a rebase can also **deflate** a window's commit count
  to zero even when genuine in-window work happened, if the primary method trusts `committedDate` alone.
- **Reset / pass:** reset 2026-07-26T14:58:02.407Z, pass 2026-07-26T15:38:54.181Z; commits between (by
  `authoredDate`, corrected per the hazard above): 2; branch verified: yes.
- **Failing AC (quoted):** none of the issue-body checklist items name either defect directly (the
  checklist covers routing dashboard reload through a canonical build-from-origin path) — both findings
  are correctness gaps in the deployment-generation lifecycle this PR introduces. Sources:
  `evidence/events-pan-3128.json.gz`, sequences 828881 and 830098.
- **Classification:** genuine-finding (both) — real resource-management bugs: unbounded worktree/disk leak
  on failed restarts, and a deliberately-kept slow-boot recovery deployment being destroyed by an
  over-eager rollback.
- **Merged diff:** [PR #3141](https://github.com/eltmon/overdeck/pull/3141), merge commit
  `b591820302767a21b8330b0a8b402819d1e8357e`. Fix commits: `9e15552b814b1e3f4f224905644b81c53a0bbb52`
  ("fix: bound dashboard deployment generations") and `6efc905e3235aa4579b58d036e43b416f9eb3ea8`
  ("fix: preserve timed-out dashboard deployments").
- **Diff evidence (at merge commit `b591820302`):** the "preserve timed-out dashboard deployments" fix
  adds `recovery?: 'dashboard-left-running'` to `StageFailure` (`src/lib/platform-lifecycle.ts:110`),
  sets it when a health-timeout leaves the new dashboard running for inspection (line 377), and exports a
  new `leavesDashboardRunning(err)` predicate (lines 438-439). `src/cli/commands/reload.ts:213` — the
  reload command's failure-handling catch block now branches on `leavesDashboardRunning(error)`: when
  true, it calls `activation?.commit()` and only sweeps orphaned deployments, instead of unconditionally
  calling `activation?.rollback()` and `removeDashboardDeployment(...)` (the previous behavior that
  deleted the intentionally-kept slow-boot deployment). Both fix commits also touch
  `src/lib/deploy/build-from-origin.ts`, the module the earlier findings in this same episode ("Canonical
  reload deletes its installed runtime dependencies before restart," "Canonical reload now deletes and
  replaces the active primary worktree's node_modules") also targeted — consistent with an iterative
  hardening of the same reload/generation-tracking machinery. The final review note ("Canonical
  deployment review passed: timeout recovery preserves slow-boot deployments, other failures roll back
  safely, storage stays bounded, and provenance requirements are complete") names all the findings raised
  across this issue's review cycles as resolved.
- **Verdict:** satisfied. Both in-window findings have matching fix commits confirmed via diff, and the
  final review note explicitly enumerates the same concerns as resolved.
- **Follow-up:** none — satisfied.
