# PAN-3093 — polyrepo UAT assembly / promote merge-evidence completeness (derived extra)

- **Failure (negAt):** 2026-07-26T18:36:03.067Z, `blocked/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **⚠ Methodology correction (H11):** the real finding, in the same collapsed run
  (`evidence/events-pan-3093.json.gz`, sequence 843521, 2026-07-26T18:36:39.977Z, `blocked/pending`):
  **"A stamped repo with no recoverable merge SHA can still finalize without usable post-merge evidence
  and permanently close the retry path."** (This episode has an unusually long history — 9 prior review
  rounds surfacing and fixing separate findings, including a `[security]` finding about read-only member
  repos treated as writable UAT targets — all resolved in earlier cycles before this audit's window.)
- **⚠ Same rebase-timestamp deflation hazard as PAN-3128.** The recorded 0-commit count is an artifact of
  a later branch-wide rebase (all commits' `committedDate` shifted to ~19:10-19:11Z). Checking
  `authoredDate` shows the genuine fix landing inside the window: `fix(cloister): require complete merge
  evidence before finalizing a polyrepo batch` (authored 18:46:35Z, inside the 18:36:03→19:09:28 window).
- **Reset / pass:** reset 2026-07-26T18:46:48.193Z, pass 2026-07-26T19:09:28.090Z; commits between (by
  `authoredDate`): 1; branch verified: yes.
- **Failing AC (quoted):** none of the issue-body checklist items name this exact defect (the PR's scope is
  the broader polyrepo UAT assembly engine and two-phase promote) — the finding is a correctness gap in
  the promote-finalization logic. Source: `evidence/events-pan-3093.json.gz`, sequence 843521/843521.
- **Classification:** genuine-finding — a real correctness/durability bug: a repo row stamped `promotedAt`
  but missing `mergeSha` was treated as "out of the pending set" (i.e. done) rather than unresolved, so a
  batch could finalize as `promoted` while permanently unable to supply per-repo merge evidence, and the
  only mechanism that could still recover the SHA (retry) was blocked by the terminal status.
- **Merged diff:** [PR #3145](https://github.com/eltmon/overdeck/pull/3145), merge commit
  `f180ed4b8c910b81d4342819c81685a394b7d1a6`. Fix commit: `76dcef83764310c66aa7567d64182b9e79ab5861`
  ("fix(cloister): require complete merge evidence before finalizing a polyrepo batch").
- **Diff evidence:** `src/lib/cloister/uat-promote.ts:288-429` (at merge commit `f180ed4b8c`) —
  `unprovenStampFailure()` (defined line 288) builds a retryable failure message from an `unproven` list;
  the promote loop (lines 349-429) pushes onto that list when a repo has no promote-git deps to inspect
  (line 370) or fails other proof checks (line 397), and at line 429, `if (unproven.length > 0)` routes to
  the retryable-failure path instead of finalizing. `finishPromote()` (called at line 240) is the single
  call site that turns a generation terminal, so this check cannot be bypassed by another caller. The fix
  commit's own message states the mechanism precisely: "Promote now treats `promotedAt && !mergeSha` with
  no findable merge as unproven and fails retryably, publishing nothing." New regression tests replace
  "the
  evidence-withholding success case with retryable-failure regressions (unrecoverable stamp, and a stamped
  repo with no promote deps), plus a recovery case proving a stamp missing only its SHA is repaired rather
  than refused."
- **Verdict:** satisfied. The fix's own detailed commit message maps clause-for-clause onto the finding
  ("no recoverable merge SHA can still finalize without usable post-merge evidence and permanently close
  the retry path" ↔ "fails retryably... publishing nothing" / "a stamp missing only its SHA is repaired
  rather than refused"), confirmed against the actual diff.
- **Follow-up:** none — satisfied.
