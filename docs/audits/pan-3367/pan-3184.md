# PAN-3184 — boot resume candidacy / clean-shutdown-vs-crash classification

- **Failure (negAt):** 2026-07-27T06:05:17.116Z, `blocked/pending`, note (verbatim): "FR-3 measures
  clean-shutdown freshness at grace expiry instead of bootStartedAt; the required marker round-trip test
  is also missing" — source: `worklist.md`, confirmed stable (not a stale first-note artifact — this note
  is the actual finding).
- **Reset / pass:** reset 2026-07-27T06:35:01.334Z, pass 2026-07-27T06:56:52.947Z; commits between: 11
  (method: pr-commits, `gh pr view 3199 --json commits`, filtered from 11 total commits on the PR — the
  bulk of this issue's 11 checklist-implementing commits already landed before negAt; the two that land
  *inside* the window, `fix(cloister): anchor clean boot freshness (PAN-3184)` and `test(codex): await
  request log before cleanup (PAN-3184)`, are the direct fix response); branch verified: yes.
- **Failing AC (quoted):** the finding names FR-3 directly, which corresponds to the issue-body checklist
  item "Classify the boot: clean shutdown expires to `resume_all`, crash boot to `hold_all`" — source: PR
  #3199 body.
- **Classification:** genuine-finding — a real time-of-check bug: `isCleanShutdownBoot` originally measured
  freshness as `now.getTime() - markerMs`, so a boot that started while the clean-shutdown marker was
  still fresh could later reclassify as stale purely because the grace period elapsed while evaluating —
  the *classification* could flip mid-grace even though nothing about the actual shutdown changed. The
  finding also flags a missing regression test for the settings-door marker round-trip.
- **Merged diff:** [PR #3199](https://github.com/eltmon/overdeck/pull/3199), merge commit
  `a1d94b6ccd5a6cc8df514a3200ca0ad1d8336586`. Fix commit: `26fef2b19c1e7f19d552f42c17eb8c35c8631c8a`
  ("fix(cloister): anchor clean boot freshness (PAN-3184)").
- **Diff evidence:** `src/lib/cloister/boot-reconciliation.ts:81-89` (at merge commit `a1d94b6ccd`) —
  `isCleanShutdownBoot()` (defined line 81) changed from `now.getTime() - markerMs <=
  CLEAN_SHUTDOWN_FRESHNESS_MS` (a `now: Date = new Date()` parameter) to, at lines 84-89,
  `bootStartedAtMs - markerMs <= CLEAN_SHUTDOWN_FRESHNESS_MS`, reading `bootStartedAt` from
  `getBootReconciliationState()` — freshness is now anchored to when boot started, immune to grace-period
  elapsed time. The call site at line 235 (`isCleanShutdownBoot() ? 'resume_all' : 'hold_all'`) is
  unchanged, confirming the fix is purely internal to the freshness calculation. The existing test `'expires to resume_all when the clean shutdown marker is fresh'` was
  rewritten to `'expires to resume_all when the marker was fresh at boot but crosses the threshold during
  grace'`, directly exercising the FR-3 scenario. `tests/unit/lib/overdeck/control-settings.test.ts` gains
  a new `describe('clean shutdown marker')` block with `'round-trips the marker through the synchronous
  settings door'` — the missing test the finding named.
- **Verdict:** satisfied. The fix anchors the freshness check to `bootStartedAt` exactly as FR-3 requires,
  the existing test was rewritten to prove the grace-period-immune behavior, and the previously-missing
  marker round-trip test was added.
- **Follow-up:** none — satisfied.
