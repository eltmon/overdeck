# MIN-879 — PostHog product analytics (frontend + backend)

> **Confidentiality note:** `mind-your-now` is a private repository; `eltmon/overdeck` (this report's
> home) is public. Per the PAN-3367 review's security finding, this report describes the verdict and its
> reasoning without quoting private source code, file paths, or Linear/GitLab text verbatim. Commit SHAs
> are identifiers only (not content) and are kept for traceability by anyone with repo access. Anyone with
> `mind-your-now` access can independently verify against the merge request linked below.

- **Failure (negAt):** 2026-07-26T05:58:18.535Z, `passed/failed` — an infra note ("review infrastructure
  repeatedly failed"), not the real finding — source: `worklist.md`.
- **⚠ Methodology correction — two distinct findings, and the worklist's recorded pass was transient, not
  final.** The full raw review history shows:
  1. **Finding 1** (2026-07-26T05:59:17.149Z): a real analytics-identity bug — logout and session-expiry
     paths did not reset the analytics library's identity, so the next user's activity could be attributed
     to the previous customer.
  2. Fixed by a logout-identity-reset commit. Re-review reached a `passed/passed` state at
     2026-07-26T06:41:35.735Z — **this is exactly the timestamp `worklist.md` recorded as `passAt`** — but
     it was a *transient* pass: 108ms later it reverted to `blocked/passed` with a **second, distinct
     finding**: the logout fix was over-eager and also reset the *anonymous* identity on login, breaking
     the ability to merge a visitor's pre-login activity into their account once identified.
  3. Fixed by a follow-up commit that preserves the anonymous identity across login while still resetting
     it on real logout. The **true durable pass** came 14 minutes after the worklist's recorded `passAt`.
  This is a **second methodology hazard beyond H11**: the reproduction script's "first `passed/passed`
  within the hour window" can match a transient pass that reverts seconds later, understating both the
  true pass timestamp and the commit window. Recorded as hazard H12 in `continue.json`.
- **Reset / pass (corrected):** reset 2026-07-26T06:20:38.473Z (worklist, matches Finding 1's reset);
  **true pass 2026-07-26T06:55:43.099Z** (not the worklist's 06:41:35.735Z). At least 3 commits landed
  between negAt and the true pass; the worklist's narrower negAt→06:41:35 window only captured the first of
  these plus a merge commit (method: mr-commits).
- **Failing AC (paraphrased):** the issue calls for identifying the logged-in customer to the analytics
  library and, by necessary implication of a correct identify/reset lifecycle, resetting that identity on
  logout — source: Linear MIN-879 (`https://linear.app/mind-your-now/issue/MIN-879`, access required).
- **Classification:** genuine-finding (both rounds) — real analytics-identity correctness bugs: cross-user
  ID leakage on logout (Finding 1), then anonymous-activity-merge breakage introduced by the Finding-1 fix
  (Finding 2).
- **Merged diff:** [MR !77](https://gitlab.com/eltmon/mind-your-now/-/merge_requests/77) (private — link
  resolves for repo members). Fix commits (in the private frontend repo): `e523b2209d7e99317cdb78f388becb4064d6dc54`
  and `29945b9ca15e4c4f25df4c5b3c492d4fc62a8bc2`.
- **Diff evidence (paraphrased; file:line pointers only, no source content):** the Finding-1 fix commit
  touches `src/axiosInterceptor.js` (three hunks, changed lines 1-9, 114-116, 159-163 — the session-expiry
  interceptor), `src/components/auth/logout-button.jsx` (three hunks, changed lines 4-14, 21-33, 35-45 —
  the logout button), and `src/utils/logoutCleanup.ts` (two hunks, changed lines 1-6 and 192-198 — the
  shared cleanup routine), plus the admin/account-settings logout paths — every place a session ends
  outside the normal logout button — adding the missing identity-reset call at each. The Finding-2 fix
  adds an explicit "preserve analytics identity" option to the shared pre-login cleanup routine and
  threads it through `src/components/auth/GoogleOneTap.tsx:61-67` and
  `src/components/auth/MagicLogin.jsx:68-74,164-173` (both call sites that run before a user is
  identified — the two third-party sign-in flows), so the "clear all previous user's local data" step on
  login no longer also clears the anonymous analytics identity — directly matching the second finding.
- **Verdict:** satisfied. Both findings have commits that match their descriptions exactly, confirmed by
  reading the actual diffs; the true final review note explicitly names both halves as resolved.
- **Follow-up:** none — satisfied.
