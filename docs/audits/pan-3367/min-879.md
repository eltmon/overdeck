# MIN-879 — PostHog product analytics (frontend + backend)

- **Failure (negAt):** 2026-07-26T05:58:18.535Z, `passed/failed`, note (verbatim): "Review bypassed:
  verification passed but review infrastructure repeatedly failed." — source: `worklist.md`, an infra
  note, not the real finding.
- **⚠ Methodology correction — two distinct findings, and the worklist's recorded pass was transient, not
  final.** The full raw series (`evidence/events-min-879.json.gz`, 116 rows) shows:
  1. **Finding 1** (sequence 806046, 2026-07-26T05:59:17.149Z, `blocked/failed`): "Common logout and
     session-expiry paths bypass `posthog.reset()`, retaining the previous customer's distinct ID."
  2. Fixed by `fix: reset analytics identity on logout (MIN-879)`. Re-review reaches `passed/passed` at
     sequence 807750 (2026-07-26T06:41:35.735Z) — **this is exactly the timestamp `worklist.md` recorded
     as `passAt`**, but it is a *transient* pass: 108ms later, sequence 807858 reverts to `blocked/passed`
     with a **second, distinct finding**: "Pre-login cleanup now resets PostHog's anonymous identity
     before `identify()`, so anonymous activity cannot merge on login." — a regression introduced by the
     fix for Finding 1.
  3. Fixed by `fix: preserve anonymous analytics identity on login (MIN-879)`. The **true durable pass**
     is at sequence 808437 (2026-07-26T06:55:43.099Z): "Cycle-4 login fix preserves anonymous PostHog
     identity while real logout paths still reset it; no new blocker remains."
  This is a **second methodology hazard beyond H11**: the reproduction script's "first `passed/passed`
  within the hour window" can match a transient pass that reverts seconds later, understating both the
  true pass timestamp and the commit window. Recorded as hazard H12 in `continue.json`.
- **Reset / pass (corrected):** reset 2026-07-26T06:20:38.473Z (worklist, matches Finding 1's reset);
  **true pass 2026-07-26T06:55:43.099Z** (not the worklist's 06:41:35.735Z). Commits between negAt and the
  *true* pass: at least 3 identifiable — `fix: reset analytics identity on logout (MIN-879)`
  (2026-07-26T06:16:07Z), `fix: dispatch analytics capture asynchronously (MIN-879)`, and `fix: preserve
  anonymous analytics identity on login (MIN-879)` (2026-07-26T06:50:30Z) — the worklist's narrower
  negAt→06:41:35 window only captured the first of these plus a merge commit (method: mr-commits;
  `glab api …/merge_requests/77/commits`).
- **Failing AC (quoted):** the PRD's scope (canonical spec `myn/.pan/drafts/min-879.md`, referenced from
  the Linear description) calls for `identify(String(customer.id))` in `CustomerProvider` and (by
  necessary implication of a correct identify/reset lifecycle) a matching reset on logout — source: Linear
  MIN-879 description (`https://linear.app/mind-your-now/issue/MIN-879`).
- **Classification:** genuine-finding (both rounds) — real analytics-identity correctness bugs: cross-user
  ID leakage on logout (Finding 1), then anonymous-activity-merge breakage introduced by the Finding-1 fix
  (Finding 2).
- **Merged diff:** [MR !77](https://gitlab.com/eltmon/mind-your-now/-/merge_requests/77). Fix commits (in
  `~/Projects/myn/frontend`): `e523b2209d7e99317cdb78f388becb4064d6dc54` ("fix: reset analytics identity on
  logout (MIN-879)") and `29945b9ca15e4c4f25df4c5b3c492d4fc62a8bc2` ("fix: preserve anonymous analytics
  identity on login (MIN-879)").
- **Diff evidence:** Finding 1's fix touches `src/axiosInterceptor.js`, `src/components/auth/
  logout-button.jsx`, `src/components/admin/AdminDashboard.tsx`, `src/components/settings/
  AccountInfo.jsx`, `src/providers/CustomerProvider.jsx`, and `src/utils/logoutCleanup.ts` — covering the
  logout button, session-expiry (axios interceptor), and admin/settings logout paths the finding named.
  Finding 2's fix adds a `preserveAnalyticsIdentity: true` option threaded through
  `performLogoutCleanup()` calls in `src/components/auth/GoogleOneTap.tsx` and
  `src/components/auth/MagicLogin.jsx` (both pre-login cleanup call sites), so the "clear all previous
  user data before login" step no longer resets the anonymous PostHog identity — directly matching "Pre-
  login cleanup now resets PostHog's anonymous identity before identify()."
- **Verdict:** satisfied. Both findings have file-scoped fix commits that match their descriptions exactly,
  confirmed by reading the actual diffs; the true final review note explicitly names both halves as
  resolved ("preserves anonymous PostHog identity while real logout paths still reset it").
- **Follow-up:** none — satisfied.
