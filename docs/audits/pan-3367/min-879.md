# MIN-879 — PostHog product analytics (frontend + backend)

> **Confidentiality note:** the source repository for this issue is private (Mind Your Now frontend);
> `eltmon/overdeck` (this report's home) is public. Per the PAN-3367 review's security findings (cycles 1
> and 2), this report states only the verdict and its classification. It deliberately omits the merge
> request URL, commit identifiers, file paths, line numbers, and any description of the private diff's
> contents or behavior — all of that was verified directly against the private repository but is not
> reproduced here. A reader with access to the Mind Your Now frontend repository can locate the relevant
> merge request via the issue tracker (MIN-879) and verify independently.

- **Failure (negAt):** 2026-07-26T05:58:18.535Z — the recorded note at this timestamp was an infra
  bypass note, not the real finding (see "Methodology note" below).
- **Methodology note — two distinct findings, and the recorded pass was transient, not final.** The
  full review history for this issue (read locally against the private repository, not reproduced here)
  shows a genuine analytics-identity correctness finding shortly after the recorded infra note, fixed, and
  a *second*, related finding surfaced by the immediate re-review — a regression introduced by the first
  fix. The pass timestamp this audit's worklist mechanically derived was a transient state that reverted
  within two minutes to the second finding; the true durable pass came about 14 minutes later, after a
  second fix. This transient-pass pattern is documented as a general audit-methodology hazard in this
  report set's synthesis (`README.md`).
- **Reset / pass (corrected):** the true durable pass is later than the worklist's mechanically-derived
  timestamp, for the reason above. At least two fix commits landed between the original finding and the
  true durable pass.
- **Failing AC (paraphrased, no verbatim source-tracker text):** the product-analytics integration should
  correctly identify a logged-in user to the analytics system and reset that identity on logout, without
  breaking the separate case of attributing a visitor's pre-login activity to their account once they do
  log in.
- **Classification:** genuine-finding (both rounds) — two real, related analytics-identity correctness
  bugs, the second introduced by the fix for the first.
- **Merged diff:** verified directly against the private merge request (URL withheld per the
  confidentiality note above).
- **Diff evidence:** independently confirmed, by reading both fix commits' diffs directly against the
  private repository, that (a) the first fix adds the missing identity-reset call to every code path where
  a session ends outside the primary logout control, and (b) the second fix adds an explicit
  "preserve identity" option to the shared pre-login cleanup routine and threads it through both
  call sites that run before a user is identified, so that routine's account-data-clearing behavior no
  longer also clears the pre-login analytics identity. No file names, line numbers, commit identifiers, or
  code content are reproduced here.
- **Verdict:** satisfied. Both findings have fix commits whose diffs I read directly and which match their
  descriptions; the true final review state explicitly confirms both are resolved.
- **Follow-up:** none — satisfied.
