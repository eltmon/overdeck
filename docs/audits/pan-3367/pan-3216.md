# PAN-3216 — Briefing reports "Troubled agents: 60" when the troubled gate holds 1

- **Failure (negAt):** 2026-07-28T06:40:30.326Z, `failed/pending`, note (verbatim): "Verification error:
  Verification worker 1874133 exited before writing a result" — source: `evidence/events-pan-3216.json.gz`
  (confirmed: this is the only distinct note in the collapsed negative run; no richer finding hides behind it).
- **Reset / pass:** reset 2026-07-28T07:07:50.045Z, pass 2026-07-28T07:25:42.825Z; commits between: 0
  (method: pr-commits, `gh pr view 3219 --json commits`); branch verified: yes (`feature/pan-3216` still
  exists on origin).
- **Failing AC (quoted):** "Filter the briefing 'Troubled agents' line on the troubled gate alone and pin
  it with a regression test" — source: PR #3219 body (`gh pr view 3219 --json body`), restating the
  issue's "Suggested fix" option 1 ("Count what the label says — filter on `agent.troubled` alone.
  Recommended, because 'troubled' already has a precise meaning elsewhere in the system").
- **Classification:** environment-blocked. The recorded failure note is an infrastructure error
  (a verification worker process exited before writing a result) — not a review finding about the diff's
  correctness. Nothing in the full raw event series for this issue (`evidence/events-pan-3216.json.gz`,
  35 rows) shows a distinct correctness/security/requirements finding; the diff was already complete and
  correct at the time verification infra failed to report a result, and the eventual pass reflects a
  successful re-run of the same verification, not a code change addressing a finding.
- **Merged diff:** [PR #3219](https://github.com/eltmon/overdeck/pull/3219), merge commit
  `9f3bbf43054820a5ce24f6e5ce90a22d246c49f9`.
- **Diff evidence:** `src/lib/briefing-assembler.ts:151` (pre-fix) changed from
  `snapshot.agents.filter((agent) => agent.troubled || (agent.consecutiveFailures ?? 0) > 0)` to
  `snapshot.agents.filter((agent) => agent.troubled === true)` — confirmed present unchanged on current
  `main` at `src/lib/briefing-assembler.ts:156`. `tests/unit/lib/briefing-assembler.test.ts` (new file,
  83 lines) adds two regression cases: one snapshot with `troubled: true` plus three agents that only
  have `consecutiveFailures > 0` asserts `- Troubled agents: 1`; a second snapshot with only
  `consecutiveFailures` agents (no `troubled: true`) asserts `- Troubled agents: 0`.
- **Verdict:** satisfied. The diff filters the "Troubled agents" briefing line on `agent.troubled === true`
  alone, exactly matching the issue's recommended fix (option 1: "count what the label says"), and the
  new test file pins both the true-positive and the previously-inflated-to-zero cases so a regression
  would fail the suite.
- **Follow-up:** none — satisfied.
