# MIN-896 — when-first mobile task page (date strip + zone river)

> **Confidentiality note:** the source repository for this issue is private (Mind Your Now frontend);
> `eltmon/overdeck` (this report's home) is public. Per the PAN-3367 review's security findings (cycles 1
> and 2), this report states only the verdict and its classification. It deliberately omits the merge
> request URL, commit identifiers, file paths, line numbers, endpoint names, and any description of the
> private diff's contents or behavior — all of that was verified directly against the private repository
> but is not reproduced here. A reader with access to the Mind Your Now frontend repository can locate the
> relevant merge request via the issue tracker (MIN-896) and verify independently.

- **Failure (negAt):** 2026-07-26T01:54:56.569Z — the recorded note at this timestamp was empty; the
  real finding follows shortly after in the same review episode (see "Methodology note" below).
- **Methodology note — two distinct findings in this episode (read locally against the private
  repository, not reproduced here):**
  1. A completion UI action bypassing the intended completion endpoint, with missing accessibility
     attributes and a save-failure path that silently swallowed errors instead of surfacing them.
  2. Fixed; the immediate re-review surfaces a second finding: a client-side data-mapping helper was
     silently dropping "clear this field" signals on save, and the fix for Finding 1 had also touched
     backend-facing behavior beyond its authorized scope and left a required cross-screen regression sweep
     incomplete.
  3. The final review note confirms all sub-issues of both findings resolved.
- **Reset / pass:** commits between the first finding and the true final pass: 2, both genuine and each
  matching one of the two findings above.
- **Failing AC (paraphrased, no verbatim source-tracker text):** the mobile task create/edit flow should
  present a "when-first" UI (a date control that also derives urgency) in place of a separate priority
  field, with correct iconography and secondary fields that don't compete with the primary question.
- **Classification:** genuine-finding (both rounds) — real defects: a UI action bypassing the intended
  completion endpoint, missing accessibility attributes, and swallowed save errors (Finding 1); data loss
  in a client-side mapping layer plus a scope violation (Finding 2).
- **Merged diff:** verified directly against the private merge request (URL withheld per the
  confidentiality note above).
- **Diff evidence:** independently confirmed, by reading both fix commits' diffs directly against the
  private repository, that (a) the completion action now calls the dedicated completion endpoint instead
  of the generic task-update path it previously reused, the accessibility attributes gain the missing
  contextual information, and the save-error path now propagates failures instead of silently discarding
  them; and (b) the data-mapping layer, extracted into its own module by the second fix, now includes both
  "clear this field" signals the previous inline version dropped, with new unit and end-to-end regression
  coverage. No file names, line numbers, commit identifiers, or code content are reproduced here.
- **Verdict:** satisfied. Both findings have fix commits whose diffs I read directly at the specific
  handler level, not inferred from the terminal review note's summary alone.
- **Follow-up:** none — satisfied.
