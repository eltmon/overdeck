# MIN-896 — when-first mobile task page (date strip + zone river)

> **Confidentiality note:** `mind-your-now` is a private repository; `eltmon/overdeck` (this report's
> home) is public. Per the PAN-3367 review's security finding, this report describes the verdict and its
> reasoning without quoting private source code, file paths, or Linear/GitLab text verbatim. Commit SHAs
> are identifiers only (not content) and are kept for traceability by anyone with repo access. Anyone with
> `mind-your-now` access can independently verify against the merge request linked below.

- **Failure (negAt):** 2026-07-26T01:54:56.569Z, `failed/pending`, note recorded empty in the worklist row
  — source: `worklist.md`. Read locally, not committed to this public repository (see H11 note in
  `README.md`).
- **⚠ Methodology correction (H11) — two distinct findings in this episode:**
  1. A completion UI action bypassing the intended completion API, with missing accessibility labels and
     failed-save handling — a requirements/completeness gap against three named requirements.
  2. Fixed; re-review surfaces a second finding: a client-side data-mapping helper was silently dropping
     "clear this field" flags on save, and the fix cycle for Finding 1 had also violated a stated
     no-backend-change constraint and left a required cross-screen visual regression sweep stale.
  3. Final pass note confirms all three sub-issues of Finding 2 resolved (clear flags preserved through
     mapping, the API-touching change was operator-authorized, the visual sweep is complete) alongside
     Finding 1.
- **Reset / pass:** reset 2026-07-26T01:55:12.065Z (worklist row); pass 2026-07-26T02:38:31.158Z; commits
  between: 2 (method: mr-commits) — both genuine, matching fixes for Findings 1 and 2 respectively.
- **Failing AC (paraphrased):** the mobile task create/edit flow should present a "when-first" UI (a date
  picker that also sets urgency), with no separate priority field, correct zone iconography, and secondary
  fields that don't compete with the primary question — source: Linear MIN-896
  (`https://linear.app/mind-your-now/issue/MIN-896`, access required; draft ACs). The two review findings
  are correctness/completeness gaps in this delivery, not separate stated ACs.
- **Classification:** genuine-finding (both rounds) — real defects: a UI action bypassing the intended
  completion API, missing accessibility labels and failed-save handling (Finding 1); and data loss in the
  task-to-API mapping layer plus process/scope violations (Finding 2).
- **Merged diff:** [MR !89](https://gitlab.com/eltmon/mind-your-now/-/merge_requests/89) (private — link
  resolves for repo members). Fix commits (in the private frontend repo): `ac06fac22b249d2657cf94e6b87dc9bd7fcff514`
  (Finding 1) and `363bfca89244cab12bb9abf2263f562edb60c2e6` (Finding 2).
- **Diff evidence — independently read from both fix commits' diffs, not from the final review note alone
  (paraphrased; file:line pointers only, no source content):**
  - **Finding 1, completion endpoint (fix commit `ac06fac22`):** `src/mobile/screens/
    TaskWhenFirstScreen.tsx`, the `onCompleteTask` handler (found at line 510 in the post-fix file) — I
    read this hunk directly: it previously called the same generic task-save function used for ordinary
    edits, setting a completed flag and status field on the payload; the fix replaces that with a direct
    call to the dedicated task-completion REST endpoint (a POST to `/api/v2/unified-tasks/{id}/complete`),
    with its own auth-token check and error handling on a non-OK response. This is the literal "Complete
    bypasses the completion endpoint" fix, confirmed by reading the diff rather than trusting the
    checklist match.
  - **Finding 1, aria labels (same commit):** `src/mobile/components/task/DayStrip.tsx:57` and
    `src/mobile/components/task/ZoneRiver.tsx:80` (post-fix line numbers) — both `aria-label` strings gain
    an interpolated zone/date term that the pre-fix version omitted (I compared the before/after hunks
    directly), matching "required aria labels also break FR-8/FR-10/NFR-3."
  - **Finding 1, failed-save handling (same commit):** the same file's save-chain logic changed a
    `.catch(() => undefined)` on the promise chain (which silently swallowed save errors) to propagate the
    error through a separate `latestSave` ref that the completion flow now awaits — read directly in the
    diff, not inferred from the finding text.
  - **Finding 2, clear flags (fix commit `363bfca89`):** `src/components/tasks/hooks/
    taskActionMappers.js:17,33` (new module) and, in the older inline call site being replaced,
    `src/components/tasks/hooks/useTaskActions.js:175,178` (post-fix line numbers) both carry the two
    "clear this field" boolean flags the previous inline version silently dropped — directly closing the
    data-loss half of Finding 2 — alongside a new unit test (`taskActionMappers.test.js`) and an updated
    end-to-end visual-regression test covering the when-first screens.
  - The final review note's three clauses (clear flags preserved, the API change authorized, the visual
    sweep complete) map one-for-one onto Finding 2's three named sub-issues, corroborating but not
    substituting for the diff evidence above.
- **Verdict:** satisfied. Both findings have commits whose diffs I read directly and which match their
  descriptions at the specific handler/line level, not just via the terminal review note's summary.
- **Follow-up:** none — satisfied.
