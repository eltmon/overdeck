# MIN-896 — when-first mobile task page (date strip + zone river)

- **Failure (negAt):** 2026-07-26T01:54:56.569Z, `failed/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **⚠ Methodology correction (H11) — two distinct findings in this episode**
  (`evidence/events-min-896.json.gz`, full history read):
  1. Sequence 795881 (2026-07-26T01:55:14.090Z, `blocked/pending`): "Complete bypasses the completion
     endpoint; clear operations, failed-save handling, and required aria labels also break
     FR-8/FR-10/NFR-3."
  2. Fixed; re-review at sequence 796653 (2026-07-26T02:16:54.371Z, `blocked/pending`) surfaces a second
     finding: "Clear flags are stripped by `mapTaskToPOJO`; the cycle also violates the no-backend-change
     constraint and leaves the required skin sweep stale."
  3. Final pass note (sequence 797442): "All prior blockers are resolved: clear flags survive mapping,
     the minimal API extension is operator-authorized, and the when-first routes are covered by the full
     skin sweep."
- **Reset / pass:** reset 2026-07-26T01:55:12.065Z (worklist row); pass 2026-07-26T02:38:31.158Z; commits
  between: 2 (method: mr-commits, `glab api …/merge_requests/89/commits`) — both genuine, matching fixes:
  `fix: address when-first review findings` (2026-07-26T02:13:06Z) and `fix: preserve clear flags through
  task adapter` (2026-07-26T02:28:18Z).
- **Failing AC (quoted):** "Task create/edit on mobile presents the when-first flow (day strip + zone
  river)," "No Priority form field; priority is derived from the chosen start date and vice versa," "Zone
  rows show the real zone icons and skin colors," "Secondary fields accessible but not competing with the
  when question" — source: Linear MIN-896 description (draft ACs,
  `https://linear.app/mind-your-now/issue/MIN-896`). The two review findings are correctness/completeness
  gaps in this delivery, not separate stated ACs.
- **Classification:** genuine-finding (both rounds) — real defects: a UI action bypassing the intended
  completion API, missing accessibility labels and failed-save handling (Finding 1); and data loss in the
  task-to-API mapping layer plus process/scope violations (Finding 2).
- **Merged diff:** [MR !89](https://gitlab.com/eltmon/mind-your-now/-/merge_requests/89). Fix commits (in
  `~/Projects/myn/frontend`): `ac06fac22` ("fix: address when-first review findings") and `363bfca89`
  ("fix: preserve clear flags through task adapter").
- **Diff evidence:** `363bfca89` extracts `mapTaskToPOJO` into a new
  `src/components/tasks/hooks/taskActionMappers.js` module (+45 lines) whose object literal explicitly
  includes `clearDueDate: task.clearDueDate === true` and `clearRecurrenceRule: task.clearRecurrenceRule
  === true` — directly preserving the clear flags the finding says were stripped — alongside a new
  `taskActionMappers.test.js` and an updated `tests/e2e/mobile/skin-visual-sweep.spec.ts`. The final
  review note ("clear flags survive mapping, the minimal API extension is operator-authorized, and the
  when-first routes are covered by the full skin sweep") maps its three clauses onto Finding 2's three
  clauses (clear flags, no-backend-change constraint, skin sweep) one-for-one.
- **Verdict:** satisfied. Both findings have named fix commits whose diffs match their descriptions, and
  the terminal review note explicitly confirms resolution of all three sub-issues raised by the second,
  more detailed finding.
- **Follow-up:** none — satisfied.
