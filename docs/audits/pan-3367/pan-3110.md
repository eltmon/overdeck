# PAN-3110 — auto-merge retry/circuit-breaker recovery

- **Failure (negAt):** 2026-07-26T12:02:21.024Z, `failed/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **⚠ Methodology correction (H11):** the full raw series (`evidence/events-pan-3110.json.gz`, 8 rows)
  shows a real finding 2 minutes later in a *different* `blocked/pending` state (sequence 820631,
  2026-07-26T12:07:08.628Z): **"Retry exhaustion cannot persist merging-to-blocked, and recovery still
  mishandles partial verdict failures and missing workspaces."**
- **Reset / pass:** reset 2026-07-26T12:02:45.956Z (worklist row); the richer finding's own episode
  resets at 2026-07-26T12:14:29.790Z; pass 2026-07-26T12:43:15.248Z. Commits between (worklist window):
  2 (method: pr-commits, `gh pr view 3136 --json commits`); branch verified: no
  (`feature/pan-3110` deleted post-merge).
- **Failing AC (quoted):** none of the 5 issue-body checklist items name this exact defect directly (they
  cover preserving pipeline verdicts across work-agent restarts, direct-merging clean PRs, server-side
  rebase fallback, requeueing retryable failures with a circuit-breaker cap, and documentation) — the
  finding is a review-discovered gap in the retry/recovery machinery this PR introduces. Quoted from the
  review event — source: `evidence/events-pan-3110.json.gz`, sequence 820631/820631.
- **Classification:** genuine-finding — a correctness gap in the new retry/circuit-breaker logic: the
  in-progress circuit-breaker transition wasn't persisted, partial-verdict resets weren't conservative
  enough, and missing workspaces / agent timeouts weren't classified as retryable failures.
- **Merged diff:** [PR #3136](https://github.com/eltmon/overdeck/pull/3136), merge commit
  `bb89254d50f81ca947cbb306bc279440bbbc9341`. Fix commit: `fc8b96e50f817113cc5c06cb3c7d81242608c647`
  ("fix: close merge recovery review gaps (PAN-3110)").
- **Diff evidence:** the fix commit touches `src/dashboard/server/services/auto-merge-executor.ts`
  (persists the in-progress circuit-breaker transition, +10 lines),
  `src/lib/cloister/verdict-preservation.ts` (conservative partial-verdict reset, +6 lines), and
  `src/dashboard/server/routes/workspaces/merge-ops.ts` / `merge-strike.ts` (classifies missing
  workspaces and agent timeouts as retryable). `docs/FLYWHEEL.md` and `docs/MERGE-WORKFLOW.md` are
  updated to state explicitly: "This includes a missing local monorepo workspace, a non-conflict
  server-side rebase failure, an agent that stops, and a live agent that times out without pushing" —
  directly answering "recovery still mishandles partial verdict failures and missing workspaces." New/
  extended regression tests in `auto-merge-executor.test.ts`, `verdict-preservation.test.ts`,
  `merge-ops-server-rebase.test.ts`, and `merge-sync-pending-auto-merges.test.ts`.
- **Verdict:** satisfied. The fix commit's own message ("Persist the in-progress auto-merge
  circuit-breaker transition. Reset partial verdicts conservatively. Classify missing workspaces and
  agent timeouts as retryable.") maps sentence-for-sentence onto the three clauses of the finding, and
  each clause has corresponding diff + test coverage.
- **Follow-up:** none — satisfied.
