# PAN-3109 — shared branch-containment lens / merge-completeness Row 4-5 fallback

- **Failure (negAt):** 2026-07-26T06:27:58.716Z, `failed/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **Full episode context:** the complete raw series for PAN-3109 (`evidence/events-pan-3109.json.gz`, 8
  rows) shows only two distinct `failed/pending` states, both effectively empty/infra: sequence 807329 has
  no note; sequence 807335 (same state, collapsed away per H11) carries a tmux `new-session` command
  failure. Neither is a substantive finding. The episode goes straight from dispatch retries
  ("Review dispatch skipped — already running") to `passed/passed`: "Approved: shared containment lens and
  DoD fallbacks satisfy all requirements; focused tests, typecheck, lint, and boundary checks pass." No
  blocking finding appears anywhere in the history.
- **Reset / pass:** reset 2026-07-26T06:30:31.344Z, pass 2026-07-26T06:50:05.157Z; commits between: 0
  (method: pr-commits, `gh pr view 3112 --json commits`); branch verified: no (`feature/pan-3109` deleted
  post-merge — 0-commit result independently plausible, consistent with no finding ever being recorded).
- **Failing AC (quoted):** all 4 issue-body checklist items — extract the shared branch-containment lens
  in `pipeline-membership-gather`, Row 4 branch-containment fallback in `checkMergedRow`, Row 5
  containment-evidenced non-PR landing acceptance, and documentation — source: PR #3112 body.
- **Classification:** environment-blocked. No finding was ever recorded; negative states are exclusively
  tmux dispatch-launch failures.
- **Merged diff:** [PR #3112](https://github.com/eltmon/overdeck/pull/3112), merge commit
  `79bb8b3f4008000b6e94ee1a8bbcce44d045d516`.
- **Diff evidence (independently read from the merged diff at merge commit `79bb8b3f40`, not from the
  review's own note):**
  - **Checklist item 1** (shared branch-containment lens): `src/lib/pipeline-membership-gather.ts:352`
    exports `gatherIssueBranchContainment()` (new, ~55 lines) that classifies every `feature/*`/`strike/*`
    ref for an issue as `unmerged`, `merged-work` (landed via a non-PR merge), or `pointer` (untouched),
    via `classifyBranchRefLine()` (line 341) and `snapshotBranchRefs()` (line 310), both also new. The
    pre-existing inline branch-classification logic inside `gatherProjectLensSignals()` (formerly
    duplicated ~20 lines) is replaced with a call to this shared pair — this is the "extraction," confirmed
    by the diff removing the old inline `for-each-ref` calls and tip-SHA/first-parent comparison from
    `gatherProjectLensSignals` and replacing them with the shared functions.
  - **Checklist item 2** (Row 4 fallback in `checkMergedRow`): `src/lib/lifecycle/dod-gate.ts:247` defines
    `checkMergedRow()`; a new block at line 304 (`if (merged.status === 'miss') { const containment = await
    ...readBranchContainment...; if (containment.mergedWorkRefs.length > 0 && containment.unmergedRefs
    .length === 0) { merged.status = 'pass'; merged.evidence = 'branch-containment'; ... } }`) runs only
    when the row would otherwise miss, and only accepts the fallback when there is positive merged-work
    evidence and zero unmerged refs — i.e. it cannot fire on ambiguous or partially-unmerged branch state.
  - **Checklist item 3** (Row 5 accepts containment-evidenced non-PR landings): the same file's
    `checkPostMergeRow()` (line 321) gains a new `merged?: MergedDodRowResult` parameter and, at line 342,
    a branch: `if (!lifecycleObserved && merged?.evidence === 'branch-containment') { return result
    ('post-merge', runningAgents.length === 0 ? 'pass' : 'miss', 'non-PR landing (branch-containment
    evidence) —
    post-merge lifecycle not applicable; ...') }` — Row 5 now recognizes the Row 4 fallback's evidence
    tag instead of unconditionally requiring the normal merged-PR lifecycle signal. `evaluateDodGate()`'s
    call site is updated to pass `merged` into `deps.postMerge(ctx, merged)` so this tag actually reaches
    Row 5.
  - **Checklist item 4** (documentation): `docs/DEFINITION-OF-DONE.md` and `docs/PIPELINE-MEMBERSHIP.md`
    are both changed in the diff.
  - Test coverage: `tests/unit/lib/lifecycle/dod-gate.test.ts` and
    `tests/unit/lib/pipeline-membership-gather.test.ts` both gain new cases in this diff.
- **Verdict:** satisfied. All 4 checklist items are independently visible in the merged diff at the
  specific functions/blocks cited above — this verdict does not rest on the review's own approval note.
- **Follow-up:** none — satisfied.
