# PAN-3206 — Synthesized review verdicts are not durably applied

- **Failure (negAt):** 2026-07-27T18:37:07.982Z, `failed/pending`, note (verbatim): "Verification error:
  Verification worker 2045091 exited before writing a result" — source: `worklist.md` /
  `evidence/commit-counts.json`. **This note is also stale** — see methodology correction below.
- **⚠ Methodology correction:** the full raw series in `evidence/events-pan-3206.json.gz` (68 rows) shows
  the real finding landed 8 minutes later in a *different* collapsed state (`blocked/pending`, sequence
  881527, 2026-07-27T18:53:44.807Z): **"Recovered verdicts omit passed anchors, fail polyrepo HEAD
  matching, and can misroute or inject feedback commands."** The worklist's negAt/negNotes pair (drawn from
  the *first* negative state in the episode) captured only the initial infra-flavored verification error,
  not this substantive correctness finding that followed. Same caution as PAN-3192 — see hazard H11.
- **Reset / pass:** reset 2026-07-27T18:45:43.531Z (worklist row), pass 2026-07-27T19:29:51.362Z; the
  richer finding's own reset was 2026-07-27T18:48:15.525Z per the full series. Commits between (worklist
  window, negAt→passAt): 2 (method: pr-commits, `gh pr view 3213 --json commits`), both explicitly
  issue-tagged: `chore(cli): refresh close command manifest PAN-3206` and `fix(review): harden verdict
  reconciliation PAN-3206`; branch verified: yes.
- **Failing AC (quoted):** the PR body's checklist does not name this exact defect (it lists building the
  shared verdict-report module, the deacon sweep, adopting the shared helper, the live-session feedback
  tier, regression tests, and docs) — the finding is a correctness gap discovered in the new
  `reconcileUnappliedReviewVerdicts` machinery itself. Quoted from the review event: "Recovered verdicts
  omit passed anchors, fail polyrepo HEAD matching, and can misroute or inject feedback commands" — source:
  `evidence/events-pan-3206.json.gz`, sequence 881527/881527.
- **Classification:** genuine-finding, and a serious one — "can misroute or inject feedback commands"
  describes a correctness defect with a security-adjacent consequence (feedback intended for one
  agent/issue reaching another, or arbitrary text being interpreted as a command) in the very sweep this
  issue introduces to auto-recover stranded verdicts.
- **Merged diff:** [PR #3213](https://github.com/eltmon/overdeck/pull/3213), merge commit
  `d4d143ff32d2df228158ae7242f37c7724e3a57b`. Fix commit: `02f918c6bae00f03900ced2df298d71ebb599ad6`
  ("fix(review): harden verdict reconciliation PAN-3206").
- **Diff evidence (independently read from the merged diff at merge commit `d4d143ff32`, not from the fix
  commit message alone):**
  - **"omit passed anchors":** `src/lib/cloister/deacon-review-unsignaled.ts:273` — the head-anchor
    comparison changed from `if (context.headSha && currentHead !== context.headSha) continue;` — which
    *skipped the comparison entirely* whenever `context.headSha` was falsy — to computing `reviewedHead`
    via `reviewAnchorFromContext()` (line 129) and then `if (!reviewedHead || currentHead !== reviewedHead)
    continue;` at line 273 — a missing/unreadable anchor now blocks the recovery instead of silently
    passing through it. This is the literal "omit" bug: the old code omitted the check, not just the
    anchor.
  - **"fail polyrepo HEAD matching":** `reviewAnchorFromContext()` (`deacon-review-unsignaled.ts:129`)
    handles a `context.repos: Array<{ repoKey, headSha }>` shape — joining each repo's `repoKey@headSha`
    pair — in addition to the old single `context.headSha` string, so a polyrepo review context (multiple
    repos, each with its own head) now produces a comparable anchor instead of falling through to the
    `undefined`-anchor case above.
  - **"misroute... feedback":** `src/lib/cloister/feedback-target.ts:23` — `isWorkFeedbackTarget()`
    changed from a regex-based guess against the agent *ID string* (`!/-(?:review|test|inspect|ship|plan)$/
    .test(normalizedId)`) to a direct check against the agent's own `role` field (`role === 'work'`) — the
    ID pattern was a heuristic that could misclassify an agent whose ID didn't follow the expected suffix
    convention; the role field is authoritative.
  - **"...or inject... commands":** `src/lib/cloister/deacon-review-unsignaled.ts:146` —
    `buildReviewCompletionCommand()` with a `shellQuote()` helper (`'${value.replaceAll("'",
    "'\\''")}'`), replacing what was previously unescaped string interpolation of `issueId`/`notes` into a
    shell command sent to the review agent — closing the injection vector.
  - The final review event (sequence 881879) reads: "Prior blockers are fixed; focused tests and typecheck
    pass with no remaining merge-blocking findings," and the terminal event (882058, `passed/passed`)
    carries the same note.
- **Verdict:** satisfied. All three named defects — anchor omission, polyrepo HEAD matching, and
  command-injection risk — are independently visible in the merged diff at the specific functions cited
  above; this verdict does not rest on the commit message or the review's own note alone.
- **Follow-up:** none — satisfied.
