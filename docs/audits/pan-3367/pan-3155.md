# PAN-3155 — strike-branch cascade / deploy row DoD dependency decoupling

- **Failure (negAt):** 2026-07-26T20:40:40.607Z, `blocked/pending`, note recorded empty in the worklist row
  — source: `worklist.md`.
- **⚠ Methodology correction (H11):** the real finding is 13 seconds later in the same `blocked/pending`
  run (`evidence/events-pan-3155.json.gz`, sequence 848330, 2026-07-26T20:40:53.723Z): **"Deploy row
  checks dashboard health before the missing-merge dependency, so row 7 still cascades a second miss when
  row 4 misses."**
- **Reset / pass:** reset 2026-07-26T20:40:54.244Z, pass 2026-07-26T21:22:35.349Z; commits between: 1
  (method: pr-commits, `gh pr view 3160 --json commits`); branch verified: yes.
- **Failing AC (quoted):** "Decouple DoD deploy row from the merged-row cascade" — source: PR #3160 body
  checklist (alongside strike-branch cascade, PR-ref ranking, and docs). This checklist item maps directly
  onto the finding.
- **Classification:** genuine-finding — an ordering bug in `checkDeployRow`: it read live dashboard health
  before checking whether the prerequisite merged-row (row 4) had actually resolved a merge commit, so a
  row-4 miss produced a confusing *second* miss on row 7 (deploy) instead of a clean skip.
- **Merged diff:** [PR #3160](https://github.com/eltmon/overdeck/pull/3160), merge commit
  `7fcdb16422a2fcb4e96ffec5f580b6062b1d8a94`. Fix commit: `1963eae4353ad06d65422869cbfa3bdc88e345a1`
  ("fix: short-circuit deploy dependency checks (PAN-3155)").
- **Diff evidence:** `src/lib/lifecycle/dod-gate.ts:396-405` (at merge commit `7fcdb16422`) —
  `checkDeployRow` (defined line 396) now has its `if (!merge.mergeCommit) {…}` early-return block
  (skip/miss based on `merge.mergedRowStatus`) at line 405, *before* the dashboard `fetch`/health-parsing
  block that now starts afterward (the health-check `fetch` and `health.buildDirty` read moved to line
  ~438, after the guard) — moved from its previous position *after* that block, so a missing merge commit
  now short-circuits before any network call. The regression test in `dod-gate.test.ts` was renamed from
  `'skips deploy ancestry when
  the merged row missed without a merge commit'` to `'skips deploy ancestry before reading dashboard
  health when the merged row missed without a merge commit'`, and a `dashboardUrl` spy asserts the health
  endpoint is never called in that case.
- **Verdict:** satisfied. The fix reorders exactly the two checks the finding names, and the renamed test
  explicitly pins the new "check dependency before dashboard health" ordering.
- **Follow-up:** none — satisfied.
