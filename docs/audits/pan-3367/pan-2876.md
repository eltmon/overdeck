# PAN-2876 — conversation subagent transcripts (cache cleanup on deselect)

- **Failure (negAt):** 2026-07-27T07:07:11.808Z, `blocked/pending`, note (verbatim): "Verification
  deferred: a dashboard deploy is queued (requested 2026-07-27T06:44:15.236Z by flywheel-orchestrator).
  It re-runs automatically after the deploy." — source: `worklist.md`. **Stale (H11)**: the real finding
  follows ~4 seconds later in the same collapsed run at sequence 866982 (2026-07-27T07:07:15.313Z):
  **"AC-4 is unmet: subagent transcript cache entries remain populated after deselection"** — source:
  `evidence/events-pan-2876.json.gz`.
- **Reset / pass:** reset 2026-07-27T07:14:35.226Z, pass 2026-07-27T07:42:31.368Z; commits between: 1
  (method: pr-commits, `gh pr view 3204 --json commits`); branch verified: yes.
- **Failing AC (quoted):** "Frontend: subagents in the messages cache and a useSu[bagentTranscript
  hook]…" (issue-body checklist, truncated in the API response — full AC text: the frontend must
  correctly manage per-subagent transcript cache entries, keyed separately from the parent conversation
  cache) — source: PR #3204 body (`gh pr view 3204 --json body`). The specific failing sub-behavior,
  quoted verbatim from the review event: "AC-4 is unmet: subagent transcript cache entries remain
  populated after deselection" — source: `evidence/events-pan-2876.json.gz`, sequence 866982/866982.
- **Classification:** genuine-finding — a specific, named AC violation (a memory/staleness leak: selecting
  a subagent transcript populated its query-cache entry, but deselecting it or unmounting the hook left
  that entry in the cache instead of clearing it).
- **Merged diff:** [PR #3204](https://github.com/eltmon/overdeck/pull/3204), merge commit
  `0d0b816a18c6aa9caa3e75fe01adfb8142f34f15`. Fix commit: `0e245e89b572149f092d38f0a018d5c6b6279aa3`
  ("fix(dashboard): address subagent review findings").
- **Diff evidence:** `src/dashboard/frontend/src/hooks/chat/useConversationMessagesStream.ts` (+5 lines) —
  the fix adds cache-clearing on deselect/unmount for the subagent transcript query key. Two new regression
  tests in `useConversationMessagesStream.test.ts`: `'uses an isolated query key and clears the live cache
  when the subagent is deselected'` (asserts
  `queryClient.getQueryData(subagentTranscriptQueryKey('conv-x', 'sub-1'))` is `undefined` after
  deselecting) and `'clears the live cache when the transcript hook unmounts'` (same assertion after
  `unmount()`). A secondary, related robustness fix also landed in the same commit:
  `src/dashboard/server/services/conversation/subagents.ts` no longer permanently caches a `null` for a
  metadata file that failed to parse (`metaCache.set(metaPath, null)` removed), so a transient parse or
  filesystem failure is retried on the next read rather than sticking forever — covered by
  `subagents.test.ts`'s renamed `'warns and retries corrupt metadata while preserving valid entries'` case.
- **Verdict:** satisfied. The fix directly targets the AC-4 cache-clearing gap the review named, with two
  regression tests asserting the exact `undefined`-after-deselect/unmount postcondition the finding
  describes.
- **Follow-up:** none — satisfied.
