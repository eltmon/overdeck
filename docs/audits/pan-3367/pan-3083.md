# PAN-3083 — review dispatch guards / reviewRequestedAt lifecycle

- **Failure (negAt):** 2026-07-25T23:55:09.301Z, `failed/pending`, note (verbatim): "Review role spawn
  failed: Command failed: tmux -L overdeck -f /home/eltmon/.overdeck/tmux/overdeck.tmux.conf new-session
  -d -s agent-pan-3083-review …" — source: `worklist.md`.
- **Full episode context:** the complete raw series for PAN-3083 (`evidence/events-pan-3083.json.gz`, 21
  rows) is dominated by infra dispatch/verification failures of the same shape as PAN-3052 (tmux
  `new-session`/`paste-buffer` command failures, plus one "Verification worker 934115 exited before
  writing a result"). The first successful review pass (sequence 789864, well before the eventual
  `passed/passed`) already reads: "Approved: no correctness, security, requirements, or performance
  blockers; focused tests, typecheck, and lint pass" — every subsequent state keeps a variant of this same
  approving note (details tightened across re-reviews — "dispatch guards, serviced-intent consumption,
  durable fallback clearing, UAT regression coverage, and documentation satisfy the issue" — but never a
  blocking finding). No distinct correctness/security/requirements/performance finding appears anywhere.
- **Reset / pass:** reset 2026-07-25T23:58:02.288Z, pass 2026-07-26T00:17:07.793Z; commits between: 0
  (method: pr-commits, `gh pr view 3089 --json commits`); branch verified: no (`feature/pan-3083`
  deleted post-merge — 0-commit result independently plausible: the diff was approved from the first
  successful review pass onward).
- **Failing AC (quoted):** all 5 issue-body checklist items — guard `needsReviewDispatch` against
  passed/skipped verdicts and `readyForMerge`, terminal verdict consumes a serviced `reviewRequestedAt`,
  add `reviewRequestedAt` to `FALLBACK_CLEARABLE_FIELDS`, regression test for stale-intent non-invalidation,
  document the lifecycle — source: PR #3089 body.
- **Classification:** environment-blocked. No finding was ever recorded; negative states are exclusively
  tmux dispatch-launch failures and one verification-worker crash, not review outcomes.
- **Merged diff:** [PR #3089](https://github.com/eltmon/overdeck/pull/3089), merge commit
  `f8e2cb34d8448586246b063e0c247b39480b212e`.
- **Diff evidence (at merge commit `f8e2cb34d8`):** `src/lib/review-dispatch-decision.ts:27,32` adds a
  `readyForMerge?: boolean` param to `needsReviewDispatch` and an early `if (params.readyForMerge) return
  false;` guard — the dispatch guard checklist item. `src/lib/review-status.ts:238-242` adds the
  terminal-verdict-consumes-request logic (`terminalReviewVerdict` check clearing `reviewRequestedAt`
  when a newer request wasn't made after the current review spawned) — the "terminal verdict consumes a
  serviced `reviewRequestedAt`" checklist item. **Correcting my own earlier draft:** `FALLBACK_CLEARABLE_
  FIELDS` is not in `review-status.ts` as I first wrote — it's in
  `src/lib/overdeck/review-status-record-sync.ts:94-97`, which adds `'reviewRequestedAt'` to that array —
  covered by `review-status-record-sync-drain.test.ts` and `-fallback.test.ts`.
  `tests/unit/lib/cloister/uat-reconciler.test.ts` covers the regression scenario (a passed+ready issue
  with a stale intent not invalidating its UAT generation); `docs/REVIEW-AGENT-ARCHITECTURE.md` is
  updated — all 5 checklist items are directly visible in the diff, matching every review pass's approving
  note from the first attempt onward.
- **Verdict:** satisfied. The diff matches its checklist item-for-item and every review pass — including
  the first — approved it with no blockers; the negative states between negAt and pass are entirely
  dispatch/verification infra retries.
- **Follow-up:** none — satisfied.
