{
  "vBRIEFInfo": {
    "version": "0.5",
    "created": "2026-04-04T12:00:00Z"
  },
  "plan": {
    "id": "pan-445",
    "title": "Planning Abort button uses native JS confirm() instead of styled dialog",
    "status": "approved",
    "tags": [
      "frontend",
      "ux",
      "bug-fix"
    ],
    "narratives": {
      "Problem": "The PlanDialog Abort buttons use native window.confirm() instead of the styled useConfirm() dialog from DialogProvider. One abort button (error state) has no confirmation at all. Additionally, the abort-planning endpoint's label removal error handling silently swallows all errors.",
      "Proposal": "Replace all native confirm() calls with useConfirm() from DialogProvider using the established pattern (variant: 'destructive'). Add a styled confirm to the error-state abort button. Improve error logging in the abort-planning endpoint's label removal catch block."
    },
    "items": [
      {
        "id": "styled-confirm-dialog",
        "title": "Replace native confirm() with useConfirm() in PlanDialog",
        "status": "completed",
        "metadata": {
          "difficulty": "simple",
          "issueLabel": "pan-445"
        },
        "narrative": {
          "Action": "In PlanDialog.tsx: (1) import useConfirm from DialogProvider, (2) call useConfirm() at component top level, (3) convert handleAbortPlanning to async and replace window.confirm() with await confirm({ title, message, confirmLabel, variant: 'destructive' }), (4) wrap the error-state abort button (line 847) with the same confirm pattern. This covers all 3 abort buttons: two that route through handleAbortPlanning and one in the error state."
        },
        "subItems": [
          {
            "id": "styled-confirm-dialog.ac1",
            "title": "Clicking Abort in pre-start/resume view shows styled dark-themed confirmation dialog, not native browser dialog",
            "status": "completed",
            "metadata": {
              "kind": "acceptance_criterion"
            }
          },
          {
            "id": "styled-confirm-dialog.ac2",
            "title": "Clicking Abort in active planning toolbar shows styled confirmation dialog",
            "status": "completed",
            "metadata": {
              "kind": "acceptance_criterion"
            }
          },
          {
            "id": "styled-confirm-dialog.ac3",
            "title": "Clicking Abort in error/failed state shows styled confirmation dialog before aborting",
            "status": "completed",
            "metadata": {
              "kind": "acceptance_criterion"
            }
          },
          {
            "id": "styled-confirm-dialog.ac4",
            "title": "Cancelling the confirmation dialog does NOT trigger the abort mutation",
            "status": "completed",
            "metadata": {
              "kind": "acceptance_criterion"
            }
          }
        ]
      },
      {
        "id": "label-removal-logging",
        "title": "Add error logging to abort-planning label removal",
        "status": "completed",
        "metadata": {
          "difficulty": "trivial",
          "issueLabel": "pan-445"
        },
        "narrative": {
          "Action": "In issues.ts abort-planning endpoint: replace the silent catch block at line 811 with a catch that logs the error via console.log('[abort-planning] Warning: Could not remove planning label:', labelErr). This makes label removal failures diagnosable without changing behavior."
        },
        "subItems": [
          {
            "id": "label-removal-logging.ac1",
            "title": "Label removal errors are logged with '[abort-planning]' prefix instead of silently swallowed",
            "status": "completed",
            "metadata": {
              "kind": "acceptance_criterion"
            }
          }
        ]
      }
    ],
    "edges": []
  }
}