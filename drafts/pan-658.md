# PAN-658 — Shared Sessions v0

**Issue:** [PAN-658](https://github.com/eltmon/overdeck/issues/658)
**Status:** Planned (draft PRD, revision 2)
**Date:** 2026-07-10 (rev 2; rev 1 was 2026-05-21 at `docs/prds/planned/PAN-658-shared-sessions.md`, now retired)
**Related:** [PAN-2565](https://github.com/eltmon/overdeck/issues/2565) multi-agent conversations (forward-compat constraint), [PAN-2566](https://github.com/eltmon/overdeck/issues/2566) Traycer parity epic, PAN-659-lineage collaborative markdown editing (followup)

**Revision 2 changes:** stale facts fixed (PAN-1249 has merged — `src/lib/errors.ts` exists on main; DB schema is now v59, not v38; the PTY hub lives in `src/dashboard/server/pty-hub.ts`, not inline in `ws-terminal.ts`); wire format and room model generalized to **share scopes + streams** so sharing a multi-agent task ([PAN-2565](https://github.com/eltmon/overdeck/issues/2565)) later requires no transport rewrite; requirements numbered (FR/NFR); work items numbered with traces, named tests, and a documentation work item; acceptance criteria mapped 1:1 to work items.

---

## Glossary

| Term | Definition |
| --- | --- |
| **Share** | One act of sharing: a host exposes a **share scope** to invited viewers via a link. Identified by a room. |
| **Share scope** | What is being shared. v0 supports exactly one kind: a single conversation (`{ kind: 'conversation', id }`). The type is a discriminated union so a future `{ kind: 'task', id }` (a PAN-2565 multi-agent task) is additive. |
| **Stream** | One agent session's event sequence inside a share. A v0 conversation share has exactly **one** stream. Every content envelope carries a `streamId` from day one so multi-stream shares are a data change, not a format change. |
| **Room** | The signaling-service record for one share: membership, lobby, block list, controller. Holds no content. |
| **Data owner** | The machine that owns the shared conversation (JSONL, tmux session) and acts as the WebRTC hub. Fixed for the life of the share. |
| **Controller** | The participant currently driving: admits/kicks/revokes, picks and sends prompt drafts, grants contributor. Exactly one at a time; transferable; the data owner can always reclaim it. |
| **Viewer** | An admitted participant. Renders the share in their own local Overdeck dashboard. View-only unless granted contributor. |
| **Contributor** | A viewer holding the **direct-submit** grant: their prompts post straight to the conversation without controller approval. Not the controller. |
| **Lobby** | The waiting room between GitHub authentication and host admission. A lobby occupant receives zero content. |
| **Signaling service** | New deployable at `overdeck.ai`: GitHub OAuth, room registry, SDP/ICE relay, TURN credential issuance. Never sees content. |
| **Identity JWT** | Short-lived token the signaling service mints after GitHub OAuth, asserting a viewer's GitHub identity; verified by the host against the service's JWKS. |
| **Wire format** | The scoped, versioned schema (in `@overdeck/contracts`) for everything crossing host→viewer data channels. Deliberately NOT the internal `PanRpcGroup`/`ChatMessage` types. |

## Problem

Overdeck is a bundled local client/server app — every user runs the dashboard on their own
localhost, and the conversation data, the Claude Code JSONL session, and the tmux session all
live on that one machine behind that user's NAT. There is no way for two people on a refinement
call to look at the same conversation, watch the same investigation unfold, or collaboratively
shape a prompt. The natural model — "give somebody a link" — does not work: the link would point
at a machine the recipient cannot route to.

The killer use case: a refinement call where one person drives a codebase investigation in
Overdeck and the rest of the team watches live, suggests prompts, and collaboratively converges
on the right next step.

## Goal

Let an Overdeck host share **one conversation** with other Overdeck users via an
`overdeck.ai/s/<id>` link. Viewers render that conversation live in their own local
Overdeck dashboard. Delivered as:

1. A new **signaling service** deployed at `overdeck.ai` (WebRTC handshake, GitHub OAuth, TURN).
2. A **host-side WebRTC transport** in the dashboard server, hub for N viewers.
3. A **viewer-side** shared-conversation surface in the dashboard frontend (browser-native WebRTC).
4. **Invite-only access control**: host-approval lobby, admit / kick / revoke, per-session block list.
5. **Host role & handoff**, **link lifecycle**, **disconnect/reconnect resilience**.
6. **Input model**: live prompt-suggestion drafts + an opt-in direct-submit (contributor) permission.
7. **Two first-class clients**: dashboard and CLI (`pan share`, `pan join`).

This issue is **Shared Sessions v0**. Per the repo policy on complete features, all work items in
this PRD ship together under PAN-658 — the numbering is implementation sequencing, not separate
deliverables.

## Requirements

Functional requirements. Work items reference these via their **Traces** line.

- **FR-1** A host can create a share for one conversation and receive an `overdeck.ai/s/<shortCode>` link (dashboard action and `pan share <conversationId>`).
- **FR-2** A link recipient authenticates via GitHub OAuth (browser redirect flow, or device flow for the CLI) before reaching the lobby.
- **FR-3** No participant receives any conversation/draft/terminal/presence content before the controller admits them from the lobby. Link possession alone grants nothing.
- **FR-4** The controller can admit, kick (soft — re-lobby-able), revoke (hard — per-room block list), and un-revoke participants, and can revoke the entire link.
- **FR-5** An admitted viewer's local dashboard renders the shared conversation live, including full-history backfill on join.
- **FR-6** Presence shows GitHub avatar + handle of every participant, marking the controller and the host.
- **FR-7** Viewers compose prompt drafts that sync live (keystroke-level snapshots) to all participants; the controller sees a stacked list and can pick, optionally edit, and send one.
- **FR-8** The controller can grant/revoke **direct-submit** per viewer; a contributor's prompts post directly to the conversation.
- **FR-9** A viewer can toggle to a **read-only** terminal view of the host's tmux session; viewer keystrokes structurally cannot reach the host shell.
- **FR-10** The controller role is transferable to any admitted viewer; the data owner can reclaim it unilaterally; exactly one controller at a time.
- **FR-11** Link lifetime = conversation lifetime (no fixed-duration expiry); the link dies when the host ends the share; manual whole-link revoke is always available.
- **FR-12** After a host network drop or `pan` restart, admitted viewers auto-restore without re-admission if the host returns within 5 minutes; past the window the room tears down and re-joining goes through the lobby.
- **FR-13** `pan join <link-or-code>` authenticates via device flow, auto-starts the local dashboard if needed, and lands the user in the lobby.
- **FR-14** Concurrent contributor submissions are strict-FIFO queued; submissions arriving while the agent is mid-turn are queued and flushed at turn end; a "composing / agent is working" indicator is shown. (Resolves the issue's open question with its stated lean.)

Non-functional requirements:

- **NFR-1** Content (conversation, drafts, terminal bytes) never transits the signaling service — peer-to-peer DataChannels only, TURN relay at most. Verified by an e2e test instrumenting the signaling WS.
- **NFR-2** Everything crossing a data channel uses the scoped, versioned wire format from `@overdeck/contracts` — never internal RPC types. Version skew between host and viewer degrades gracefully (notice, not breakage).
- **NFR-3** Every content envelope carries a `streamId`; the share scope is a discriminated union — extending to multi-stream task shares (PAN-2565) must require **no breaking wire change and no schema migration rewrite** (see *Forward compatibility*).
- **NFR-4** The host's WebRTC hub gates content on its **own** admitted set — the signaling service is never the sole authority for content access.
- **NFR-5** Viewers require no native dependency (browser-native `RTCPeerConnection`); the host-side WebRTC dependency must not break `bun run` dev mode unless a native addon is unavoidable (prefer pure-TS `werift`).
- **NFR-6** The host dashboard itself remains localhost-only; sharing exposes only the shared scope's channels, only to admitted peers.
- **NFR-7** Works through symmetric NATs via TURN relay (`coturn`), with short-lived HMAC credentials.

## Design Goals

- **Invite-only is non-negotiable.** Possession of a link never, by itself, grants access.
- **Content stays peer-to-peer.** The signaling service relays handshake + membership only.
- **The viewer needs no native dependency.** The host's Node server is the WebRTC hub; viewers connect with browser-native WebRTC.
- **Survive a host blip.** A wifi drop or a `pan` restart inside a 5-minute window does not end the session or force re-admission.
- **Two clients, one access model.** Dashboard and CLI both share and join; both funnel through the same lobby. The CLI is not a back door.
- **Defense in depth.** The host's WebRTC peer independently gates content on its own admitted set.
- **Don't box in the roadmap.** The share model must extend to multi-agent tasks (PAN-2565) and inform the collaborative-editing followup without a rewrite (see *Forward compatibility*).

---

## Forward compatibility — multi-agent tasks and the parity roadmap

[PAN-2565](https://github.com/eltmon/overdeck/issues/2565) introduces task surfaces containing
**N agent sessions**. Sharing such a task is a followup to both issues — but v0 decisions here
must not force a rewrite there. Four commitments, all cheap now and expensive later:

1. **Share scope is a discriminated union.** Everywhere the design says "what is shared", the
   type is `ShareScope = { kind: 'conversation'; conversationId: string }` — v0's only variant.
   A future `{ kind: 'task'; taskId: string }` is an additive variant. The signaling service
   treats the scope as opaque (it already never inspects content); only host and viewer interpret it.
2. **Every content envelope carries `streamId` from day one.** A v0 conversation share has
   exactly one stream (`streamId` = the conversation id). Multi-stream shares later reuse the
   same five channel labels — the envelope, not the channel, identifies the member session.
   Late-added streams announce themselves via the `presence` channel (`streams` array), so a
   viewer can render per-stream tabs without new channels.
3. **`shared_sessions` is keyed by room, not by conversation.** Primary key `room_id`, with
   `scope_kind` + `scope_id` columns and a uniqueness constraint on `(scope_kind, scope_id, status='active')`
   semantics (enforced in code: one active share per scope). Adding task shares is a new
   `scope_kind` value, not a table rewrite.
4. **Version negotiation advertises supported scope kinds.** The connect handshake exchanges
   `{ v, scopeKinds: ['conversation'] }`. A future host offering a task share to an old viewer
   detects the gap and shows "viewer's Overdeck is too old for task shares" instead of breaking.

Also anticipated (no code impact in v0, recorded so the design stays honest):

- **Collaborative editing (PAN-659 lineage / PAN-2566 §3–4):** the `drafts` channel's
  last-write-wins snapshots are explicitly a v0 simplification. If CRDT co-editing lands later,
  it rides a **new channel** (`collab`) rather than mutating `drafts` semantics.
- **Persistent roles (PAN-2566 §4):** the lobby/admit model is deliberately session-scoped. If
  durable multi-user projects arrive, they are a different authorization layer on top; nothing in
  the room model assumes membership outlives the room.

---

## Dependencies & Sequencing

**PAN-1249 (Effect migration of `src/lib/`) has merged** — `src/lib/errors.ts` exists on `main`
with the shared `Data.TaggedError` classes. Rev-1's sequencing hedge is obsolete. Consequences
that remain in force:

- New shared error tags go in `src/lib/errors.ts` (see *New Error Types*), following the
  established convention — not scattered into local files.
- New host-side modules under `src/dashboard/server/` are written **Effect-native** (the dashboard
  server already is). New CLI modules under `src/cli/` stay Promise-based and may consume either
  the Promise or `*Effect` variant of any `src/lib/` API.

### WebRTC dependency (decided at WI-2 start)

| Option | Type | Trade-off |
|---|---|---|
| `werift` | **Pure TypeScript** WebRTC | No native addon — eliminates build/runtime risk. Preferred if data-channel support is adequate (it is data-channel-first). |
| `@roamhq/wrtc` | Native Node addon | Battle-tested, but native-addon finickiness (same class as `node-pty`). Node 22 only; breaks under Bun. |

**Decision rule:** evaluate `werift` first with a two-peer data-channel spike (loopback + forced-TURN);
adopt it if the spike passes throughput ≥ 5 MB/s and 1k-message ordering. Fall back to `@roamhq/wrtc`
only on spike failure. If the native addon is chosen: externalize it in the server `tsdown.config.ts`
bundle, add it to `.claude/rules/dashboard-node22-only.md`, and document that sharing requires the
built `dist/dashboard/server.js` under Node 22. The viewer side uses browser-native
`RTCPeerConnection` either way.

---

## Architecture

### Components

```
   HOST machine                  overdeck.ai               VIEWER machine
 ┌────────────────┐          ┌──────────────────────┐         ┌────────────────────┐
 │ dashboard       │  WS      │  signaling service    │   WS    │ dashboard frontend │
 │  server (Node)  │◄────────►│  - room registry      │◄───────►│  (browser)         │
 │  - conversation │ signaling│  - GitHub OAuth        │signaling│  - RTCPeerConnection│
 │    store, JSONL │          │  - TURN cred issuer    │         │  - shared conv view│
 │  - tmux session │          │  - SDP/ICE relay only  │         └─────────┬──────────┘
 │  - WebRTC hub   │          └──────────────────────┘                    │
 │   (werift/wrtc) │                                                       │
 │       │         │           WebRTC DataChannels (DTLS-encrypted)         │
 │       └─────────┼───────────────────────────────────────────────────────┘
 │                 │       STUN-direct (~80%) or TURN-relayed (~20%)
 └────────────────┘
```

- **Host** — the machine that owns the conversation + tmux session. Its dashboard **server** is
  the WebRTC hub: one `RTCPeerConnection` per viewer. Source of truth.
- **Viewer** — runs their own Overdeck. The **browser** frontend holds one `RTCPeerConnection`
  to the host server and renders the share. The viewer's local server is a thin bootstrap only
  (receives the join intent, opens the frontend route).
- **Signaling service** (`overdeck.ai`) — new deployable. WebRTC handshake relay, GitHub
  OAuth, TURN credential issuance, room registry. **Never sees content.**
- **TURN server** — `coturn` for the ~20% of peer pairs that cannot NAT-traverse directly.

### Two-role decomposition of "host"

| Role | What it is | Mobility |
|---|---|---|
| **Data owner** | The machine with the conversation + tmux session; the WebRTC hub. | Fixed for the share. The conversation does not migrate machines in v0. |
| **Controller** | The participant who *drives*: admit/kick/revoke, pick/send prompts, grant contributor, hand off. | Transferable to any admitted viewer; the data owner can always reclaim it. Exactly one at a time. |

"Host" is modeled as a **role on the room**, not a property of a connection.

### Why the hub is the host's server, not the host's browser

- The conversation source of truth, the JSONL session, and the tmux session all live in the
  host's Node server. Terminal sharing (`node-pty tmux attach-session`) originates there.
- The host's browser tab is ephemeral; the server is always-on under `pan up`. A host closing or
  refreshing a tab must not drop every viewer.
- Disconnect/reconnect resilience (5-minute window, survives `pan` restart) only makes sense with
  a long-lived server peer.

### Reuse: the terminal-sharing primitive

`src/dashboard/server/pty-hub.ts` already implements a multi-client hub — `activePtyHubs`,
`addClientToHub`, `broadcastToHub`, and a single designated `inputClient` — consumed by
`src/dashboard/server/ws-terminal.ts` (one `node-pty tmux attach-session` per session, N clients).
v0 adds a **WebRTC DataChannel consumer** of that same hub's byte stream — the PTY plumbing is
unchanged; the terminal data channel is just another subscriber alongside the raw-WebSocket clients.

---

## The Signaling Service

A new deployable workspace: **`services/signaling-service/`** (Bun workspace; Node 22 runtime).

**Hosting:** deploy to `overdeck.ai` behind TLS. Recommended target: **Fly.io** (Overdeck
already has Fly tooling and the `pan-fly` skill). `coturn` runs as a companion Fly app or VM.
Final infra placement is an ops decision confirmed at WI-1 kickoff (**implementation checkpoint**;
fallback: any TLS-terminated VM — nothing in the design is Fly-specific).

The signaling service is **stateless except for in-memory room state** — no database. Room state
is small and ephemeral; losing it on a deploy ends in-flight shares (acceptable for v0, documented).

### HTTP API

| Method & path | Caller | Purpose |
|---|---|---|
| `POST /api/sessions` | host | Create a room. Body `{ scope: ShareScope, mode }`. Returns `{ roomId, hostToken, shortCode, joinUrl }`. `joinUrl = https://overdeck.ai/s/<shortCode>`. The service stores `scope` opaquely. |
| `DELETE /api/sessions/:roomId` | host (`hostToken`) | Revoke the link; tear the room down; drop all participants. |
| `GET /s/:shortCode` | viewer browser | Join landing page. Triggers GitHub OAuth, then localhost-detect / `pan join` handoff. |
| `GET /oauth/github/start?room=<shortCode>` | viewer browser | Redirect into GitHub OAuth (`read:user` scope only). |
| `GET /oauth/github/callback` | GitHub | Exchange code, mint a viewer **identity JWT**, redirect to the handoff page. |
| `POST /api/oauth/device/start` | CLI | Begin GitHub **device flow** for `pan join`. |
| `POST /api/oauth/device/poll` | CLI | Poll device flow; on success returns the viewer identity JWT. |
| `POST /api/turn-credentials` | host + viewer | Issue short-lived `coturn` REST credentials (HMAC, ~10-min TTL). |

### WebSocket API — `WS /signal`

- `?role=host&token=<hostToken>` — host parks here. Receives `viewer-joined` (lobby),
  `viewer-signal` (SDP/ICE from a viewer), `viewer-left`. Sends `signal` (SDP/ICE to a viewer),
  `admit`/`kick`/`revoke`/`contributor`/`handoff` control commands, `room-state` snapshots.
- `?role=viewer&room=<roomId>&identity=<viewerJWT>` — viewer signaling channel. Exchanges SDP/ICE
  with the host; receives lobby/admit/kick/revoke notifications.

### Room state (in-memory)

```ts
interface Room {
  roomId: string;            // opaque, 128-bit
  shortCode: string;         // URL-safe, 8-char base32 — the /s/<shortCode> segment
  hostToken: string;         // bearer secret, host-only
  scope: ShareScope;         // opaque to the service; v0: { kind: 'conversation', conversationId }
  mode: 'conversation' | 'terminal';
  controllerGithubId: string;
  hostConnected: boolean;
  hostLastSeen: number;      // epoch ms — drives the 5-minute reconnect TTL
  participants: Map<string /*githubId*/, {
    login: string; name: string; avatarUrl: string;
    state: 'lobby' | 'admitted';
    contributor: boolean;
    connId: string;
  }>;
  blockList: Set<string>;    // githubIds barred from the lobby for this room's life
}
```

**Lifecycle / TTL:** a room is destroyed when (a) the host `DELETE`s it, (b) the host signals
session-end, or (c) `hostConnected === false` for **5 minutes** (`now - hostLastSeen > 300_000`).
There is no fixed-duration link expiry — link lifetime = conversation lifetime (FR-11).

### What the signaling service can and cannot see

- **Sees:** room membership (GitHub identities of host + viewers), admit/kick/revoke/handoff
  control commands, presence join/leave, SDP/ICE blobs, the opaque scope descriptor.
- **Never sees:** conversation messages, terminal bytes, prompt drafts (NFR-1). Enforced by the
  wire format (content channels are never sent to the signaling WS) and verified by an e2e test.

---

## Identity & GitHub OAuth

- **OAuth scope: `read:user` only.** Enough for login + avatar. The collaborative-markdown-editor
  followup may later need `gist`; if so we request that scope then and accept a one-time
  re-consent. We do not ask for gist write access before a feature uses it.
- The GitHub OAuth app is registered to `overdeck.ai` and its client secret lives **only**
  on the signaling service.
- After OAuth (browser redirect flow) or device flow (CLI), the signaling service mints a
  short-lived **identity JWT**, signed with the service's private key:

  ```
  { iss: "overdeck.ai", sub: "<github-user-id>", login, name,
    avatar_url, room: "<roomId>", iat, exp }   // exp ~15 min
  ```

- The **host verifies this JWT** against the signaling service's published JWKS. The host does
  *not* perform OAuth itself for viewers — it trusts the signaling service's signed identity
  assertion. This is why a viewer's claimed GitHub identity is trustworthy in the lobby.
- **Host identity:** the host is a participant too (shown in presence). The host's Overdeck
  performs a one-time GitHub **device-flow** OAuth the first time it shares; the resulting identity
  is cached at `~/.overdeck/github-identity.json`. No repeated logins.

GitHub OAuth supplies *identity/accountability*. The lobby + block list supply *authorization*.
Both are required; neither alone is sufficient.

---

## Access Control — Invite-Only

Invite-only is a **hard requirement** (FR-3, FR-4). The `/s/<shortCode>` link is a routing token,
not a capability — every participant is in the room only because the controller let them in.

### The lobby

1. A joiner authenticates via GitHub and connects to `WS /signal?role=viewer`.
2. They land in the room's **lobby** (`state: 'lobby'`). The signaling service forwards a
   `viewer-joined` event to the host with the joiner's GitHub avatar + handle.
3. **No WebRTC offer is created and no DataChannel is opened** until the controller admits them.
   A lobby viewer receives zero conversation/terminal/draft/presence content.
4. The controller **admits** → the host creates an `RTCPeerConnection`, exchanges SDP/ICE via the
   signaling relay, opens the data channels, and backfills history.

### Controller actions

| Action | Effect |
|---|---|
| **admit** | Move a lobby joiner into the room; host opens their peer connection + channels. |
| **kick** *(soft)* | Tear down that viewer's `RTCPeerConnection` immediately; their UI shows "removed by host". A kicked user who re-clicks the link **re-enters the lobby** and can be re-admitted. For "not right now". |
| **revoke** *(hard)* | Tear down channels **and** add the GitHub id to the room's `blockList`. Works on a connected viewer *or* a lobby joiner. A revoked user re-clicking the link cannot re-enter the lobby and cannot be re-admitted unless the controller un-revokes. For "not at all". The block list is what makes invite-only enforceable against a leaked link. |
| **un-revoke** | Remove a GitHub id from the `blockList`. |
| **revoke link** | `DELETE /api/sessions/:roomId` — kills the whole room; drops every participant (lobby + admitted). |

### Defense in depth — the host gates content itself (NFR-4)

The host's WebRTC hub keeps its **own** in-memory admitted set and **only opens content data
channels to viewers it has itself admitted** — it never treats the signaling service's room state
as the sole authority. A compromised or buggy signaling service could mis-report membership, but
it cannot make the host stream content to an un-admitted peer.

On a host **process restart** within the reconnect window, the host's in-memory set is gone; it
re-reads the admitted + block lists from the signaling service room state to resume. That is a
narrow, documented trust window on an Overdeck-operated service, and only membership metadata
(never content) is involved.

---

## Host Role & Handoff

- The **controller** role is transferable to any admitted viewer mid-session ("you drive for a
  while"). The data-owner machine stays the hub and the authority — it accepts control commands
  (admit/kick/revoke, contributor grants, send-prompt) only from the participant who currently
  holds the controller role (FR-10).
- **Handoff:** the current controller assigns the role to a chosen viewer (a `handoff` control
  message). The **data owner can always reclaim** the controller role unilaterally — it is their
  machine and their conversation.
- Exactly one controller exists at any time. `controllerGithubId` in the room state is the single
  source of truth; the host enforces it on every inbound control command.

---

## Link Lifecycle & Disconnect/Reconnect

### Link lifecycle (FR-11)

- **Link lifetime = conversation lifetime.** The `/s/<shortCode>` link is valid as long as the
  host keeps the conversation shared. It expires automatically when the host ends/closes the
  shared conversation (the room is torn down).
- **No fixed-duration expiry** — no 24-hour timer.
- **Manual link revoke** stays available at any time (`DELETE /api/sessions/:roomId`), dropping
  every participant including those in the lobby.

### Host disconnect & reconnect (FR-12)

- If the host's network blips or its dashboard process restarts, viewers show a
  "host disconnected — reconnecting…" state and always have a manual **Leave** action.
- **Reconnect window: 5 minutes.** The signaling service holds the room (id, host token, admitted
  list, block list, contributor grants) for 5 minutes after `hostConnected` goes false.
- **Within the window:** the host re-parks on `WS /signal` with the same `hostToken`, re-reads
  room state, and **admitted viewers auto-restore** — their peer connections are re-negotiated
  with no re-admission. This holds even across a full host *process* restart, because membership
  lives in the signaling room state, not only in host memory.
- **Past the window:** the room is destroyed, the link dies; a fresh share creates a fresh room
  and everyone re-joins through the lobby.

To survive a process restart, the host persists a minimal record locally (see *Data Model*) so it
knows which room to re-park on after `pan up` comes back.

---

## Transport & Wire Format

### Peer connections and data channels

One `RTCPeerConnection` per viewer (host hub ⇄ viewer browser). Each carries five labeled,
**reliable, ordered** WebRTC DataChannels:

| Channel | Direction | Payload |
|---|---|---|
| `control` | bidirectional | admit / kick / revoke / contributor-grant / handoff / send-prompt + acks |
| `conversation` | host → viewer | per-stream history backfill + live message append/update |
| `drafts` | bidirectional | per-viewer prompt-draft snapshots, broadcast to all participants |
| `terminal` | host → viewer | raw tmux bytes — only while the viewer has terminal view toggled |
| `presence` | host → viewer | lobby + admitted roster, join/leave, contributor flags, stream roster |

ICE: STUN-direct preferred; `coturn` TURN relay fallback. DataChannels are DTLS-encrypted by
default — content is encrypted in transit end to end.

**Multi-stream note (NFR-3):** channels are *not* duplicated per stream. The envelope's `streamId`
multiplexes member sessions over the same five channels; the `presence` channel announces the
stream roster. v0 has exactly one stream.

### Scoped, versioned wire format (NFR-2, NFR-3)

Conversation events cross the channel in a wire format defined **specifically for sharing** — not
the internal `PanRpcGroup` / `ChatMessage` types (`packages/contracts/src/rpc.ts:145`). Host and
viewer run independently-updated Overdeck versions; coupling the wire format to an internal type
would break rendering on version skew. The wire format is an explicit allow-list of fields
permitted to cross the host→viewer boundary. It lives in `packages/contracts/src/sharing.ts` and
is exported from `@overdeck/contracts`.

```ts
type ShareScope =
  | { kind: 'conversation'; conversationId: string };
  // future (PAN-2565 followup): | { kind: 'task'; taskId: string }

interface SharedEnvelope {
  v: 1;                                   // wire format version — bump on breaking change
  ch: 'control'|'conversation'|'drafts'|'terminal'|'presence';
  streamId: string;                       // member session id; v0: always the shared conversation's id
  seq: number;                            // per-channel, per-stream monotonic, for gap detection
  t: number;                              // host epoch ms
  payload: unknown;                       // channel-specific, schemas below
}

// connect handshake (first message on `control`, both directions)
interface SharedHello {
  v: 1;
  scopeKinds: Array<'conversation'>;      // additive: ['conversation','task'] later
  scope: ShareScope;
}

// conversation channel
type ConversationPayload =
  | { kind: 'history'; messages: SharedMessage[] }      // full backfill on join (per stream)
  | { kind: 'append';  message: SharedMessage }
  | { kind: 'update';  message: SharedMessage };        // streaming token updates

interface SharedMessage {                 // explicit subset mapped from internal ChatMessage
  id: string;
  role: 'user' | 'assistant' | 'system';
  text: string;
  turnId?: string;
  author?: { login: string; avatarUrl: string };        // which human sent a user message
  createdAt: string;
  completedAt?: string;
  streaming?: boolean;
  sequence?: number;
}

// presence channel
interface PresencePayload {
  controllerGithubId: string;
  participants: Array<{
    githubId: string; login: string; avatarUrl: string;
    state: 'lobby' | 'admitted';
    contributor: boolean;
    isHost: boolean;
  }>;
  streams: Array<{                        // stream roster; v0: exactly one entry
    streamId: string;
    label: string;                        // e.g. conversation title
    kind: 'conversation';                 // additive union later (e.g. 'terminal-agent')
  }>;
}

// drafts channel
interface DraftPayload {
  authorGithubId: string;
  authorLogin: string;
  text: string;                           // full snapshot, last-write-wins per author
  updatedAt: number;
}
```

The host maps its internal `ChatMessage` to `SharedMessage` at the transport boundary; the viewer
renders `SharedMessage` directly. Version negotiation: `SharedHello` exchanges `v` and
`scopeKinds`; a viewer on an older wire version renders best-effort and shows a "host is on a
newer Overdeck" notice rather than breaking; a viewer missing the share's scope kind gets an
explicit "your Overdeck is too old for this share type" state.

---

## Conversation Sharing

### Host side

- A new host module subscribes to the host's local conversation event source (the same source
  feeding the conversation read model — `src/dashboard/server/services/conversation-service.ts`).
- On viewer admit: send a `conversation` `history` backfill (the **full** conversation, read from
  the host's local store / JSONL), then stream live `append` / `update` events.
- **Full history on join** is locked (FR-5) — the data lives in the host's local store and the
  DataChannel can carry it. A progressive lazy-load is a future optimization, not v0.

### Viewer side

- The viewer's dashboard frontend opens a route, e.g. `/#/shared/<roomId>`, mounted when the join
  intent arrives.
- It establishes the browser `RTCPeerConnection`, renders the `conversation` channel through the
  existing conversation UI components (`ConversationPanel` / `MessagesTimeline` in
  `src/dashboard/frontend/src/components/chat/`), reusing message rendering as-is.
- The viewer's surface is **read of conversation + compose of drafts** — see *Input Model*.

### Default surface & terminal toggle (FR-9)

The **conversation panel** is the default shared surface. A **terminal view toggle** lets a viewer
flip to the host's live tmux pane.

- The `terminal` channel carries raw bytes from the existing PTY hub (`pty-hub.ts`) — the WebRTC
  transport subscribes to `broadcastToHub()` output as an additional consumer.
- **Strictly one-directional, host → viewer.** Viewer keystrokes never reach the host's shell.
  Read-only is locked, not optional — terminal sharing is "watch me work", not "drive my shell".
  The terminal data channel has no inbound handler on the host.

---

## Input Model

- **View-only by default.** A newly-admitted viewer cannot drive the conversation; they compose
  **prompt suggestions** in their own local draft box (FR-7).
- **Suggestion UI — stacked list.** The controller sees every viewer's pending draft as a stacked
  list with author avatars (queue-of-questions feel) and picks one to send, optionally editing it
  first.
- **Drafts sync live as people type** — plain-text full-snapshot updates over the `drafts` channel.
  No CRDT, no rich editor in v0 (a CRDT `collab` channel is the followup's job — see *Forward
  compatibility*). Last-write-wins per draft, scoped to the author.
- **Direct-submit (contributor) permission (FR-8).** The controller can grant any admitted viewer
  **direct-submit**: that viewer's prompts post straight into the conversation without the
  controller picking each one. Per-viewer, controller-granted, revocable.
  - Direct-submit is **not** host handoff — a contributor sends prompts but cannot
    admit/kick/revoke or hand off. Many contributors may exist; there is still exactly one
    controller.
  - Direct-submit does **not** bypass the lobby. Everyone is host-admitted first; contributor is a
    second, separate grant applied after admission.

### Concurrent submission ordering — decided (FR-14)

The issue's one open question is resolved with its stated lean: **strict FIFO queue** on the host;
submissions arriving while the agent is mid-turn are queued and flushed in order when the turn
ends; the presence-driven composer shows "agent is working" and "N queued" so contributors see why
their prompt hasn't landed. Auto-contributor pre-designation (grant on admit by GitHub handle) is
a nice-to-have and **out of v0**.

---

## Clients & Join Paths

Two first-class clients in v0: the **dashboard** and the **CLI**. Both can host-share and join.
All join paths funnel through the same host-approval lobby.

### Dashboard

- **Host (FR-1):** a "Share this conversation" action in the conversation panel calls
  `POST /api/sessions` (via the host's dashboard server) and surfaces the `/s/<shortCode>` link +
  a participant/lobby management panel.
- **Viewer (FR-2):** open the link in a browser → GitHub OAuth on `overdeck.ai` →
  **localhost-detect**: the share page probes for a running local Overdeck at `localhost:<port>`
  and POSTs the join intent there → the local dashboard opens `/#/shared/<roomId>` → lobby.
  A `pan://` protocol handler was considered and rejected (OS-level registration is finicky
  cross-platform; note the desktop app (`apps/desktop/src/protocol.ts`) may later register one as
  an *additional* path — not required for v0).

### CLI (FR-13)

- `pan share <conversationId>` — host side. Prints the `/s/<shortCode>` link and a short join code.
- `pan join <link-or-code>` — viewer side. Authenticates via GitHub **device flow**, posts the
  join intent straight to its own local dashboard (it already knows the local port — no
  localhost-detect needed), lands in the lobby; on admit, the shared conversation opens in that
  user's local dashboard.

### Cold start

A viewer who clicks the **browser** link with no local Overdeck running cannot be auto-helped by
the share page — a web page cannot spawn a local process. The page detects "no localhost" and
instructs the user to run `pan join <link-or-code>`. **`pan join` auto-starts the local dashboard
if it is not already running**, then proceeds to the lobby — so the CLI is the cold-start recovery
path for an installed-but-not-running viewer. A viewer with **no Overdeck installed at all** must
install it first; a hosted read-only web view for the truly-uninstalled case is a possible
followup (it would put conversation content on `overdeck.ai`, breaking the no-content
guarantee) and is out of scope for v0.

---

## Data Model

### Host-side persistence

The host persists a minimal record so it can re-park on the signaling room after a `pan` restart.
New SQLite table (bump `SCHEMA_VERSION` in `src/lib/database/schema.ts` — **59 at rev-2 writing
time**; use whatever is current at implementation and add the next migration):

```sql
CREATE TABLE shared_sessions (
  room_id         TEXT PRIMARY KEY,     -- forward-compat: keyed by room, not by conversation
  scope_kind      TEXT NOT NULL,        -- 'conversation' (only value in v0)
  scope_id        TEXT NOT NULL,        -- conversations.name in v0
  short_code      TEXT NOT NULL,
  host_token      TEXT NOT NULL,        -- bearer secret; never leaves the host
  mode            TEXT NOT NULL,        -- 'conversation' | 'terminal'
  status          TEXT NOT NULL,        -- 'active' | 'ended'
  created_at      TEXT NOT NULL,
  ended_at        TEXT
);
CREATE INDEX idx_shared_sessions_scope ON shared_sessions(scope_kind, scope_id);
-- invariant enforced in the accessor, not the schema: at most one 'active' row per (scope_kind, scope_id)
```

Participant state, contributor grants, and the block list are **not** persisted host-side — they
live in the signaling service room state (membership, not content) and the host re-reads them on
reconnect. This keeps host persistence minimal and makes auto-restore-across-restart correct.

### Signaling service

In-memory only (the `Room` struct above). No database. Documented consequence: a signaling-service
redeploy ends in-flight shares; acceptable for v0.

---

## New Error Types

New tagged errors are added to **`src/lib/errors.ts`** (exists on `main`; PAN-1249 landed),
following its existing conventions:

```ts
export class SignalingError      extends Data.TaggedError('SignalingError')<{ readonly reason: string; readonly cause?: unknown }> {}
export class WebRtcError         extends Data.TaggedError('WebRtcError')<{ readonly stage: 'offer'|'answer'|'ice'|'datachannel'; readonly cause?: unknown }> {}
export class ShareSessionError   extends Data.TaggedError('ShareSessionError')<{ readonly reason: string }> {}
export class OAuthError          extends Data.TaggedError('OAuthError')<{ readonly provider: 'github'; readonly reason: string }> {}
export class RoomNotFound        extends Data.TaggedError('RoomNotFound')<{ readonly roomOrCode: string }> {}
export class ParticipantBlocked  extends Data.TaggedError('ParticipantBlocked')<{ readonly githubId: string }> {}
```

Signaling-service-internal errors (its own deployable) may define local tagged errors in
`services/signaling-service/src/errors.ts`.

---

## Security Considerations

- **Invite-only enforced in two layers:** the lobby (signaling service) and the host's own
  admitted-set gating of content channels. Neither alone is trusted.
- **Link is not a capability.** `shortCode` only routes a joiner to the lobby; it grants nothing.
- **Identity is signed.** Viewer identity is a signaling-service-signed JWT the host verifies
  against published JWKS — the host does not trust a self-asserted GitHub handle.
- **Content is end-to-end encrypted** over DTLS DataChannels and never transits the signaling
  service. Verified by an e2e test asserting the signaling WS sees no conversation bytes.
- **`hostToken` is a bearer secret** — generated by the signaling service, stored only on the host
  (`shared_sessions.host_token`), never sent to viewers, never logged.
- **TURN credentials are ephemeral** — `coturn` REST API HMAC credentials, ~10-minute TTL.
- **Terminal sharing is read-only** — the host exposes no inbound terminal path; viewer keystrokes
  are structurally impossible to deliver to the host shell.
- **Rate-limit `POST /api/sessions`** by IP on the signaling service to prevent room-spam.
- **The host dashboard remains localhost-only and unauthenticated** for its own UI — sharing does
  not expose the host dashboard to the internet; only the explicitly-shared scope's channels are
  reachable, and only by admitted peers.

---

## Work Items

All ship together under PAN-658 (repo policy: complete features). Numbering is sequencing.
Intersecting repo rules restated for executors: **no `execSync` in server-reachable code** (use
`execAsync`/`spawn`); **fake timers** (`vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync`) for
every retry/backoff/TTL test — the 5-minute reconnect window and 10-minute TURN TTL tests MUST NOT
sleep real time; async tmux primitives only; dashboard runs Node 22 (never Bun).

**WI-1 — Signaling service.** *Traces: FR-2, FR-11, FR-12, NFR-1, NFR-7.*
New Bun workspace `services/signaling-service/`: room registry (the `Room` struct), HTTP API,
`WS /signal` relay, GitHub OAuth (web redirect + device flow), identity JWT + JWKS endpoint, TURN
credential issuance, IP rate-limiting on room creation. Infra: Fly.io deploy config + `coturn`
(`infra/signaling/`). Scope stored opaquely (`ShareScope`).
Tests: room state machine unit tests (lobby→admitted, kick→lobby, revoke→blockList, 5-min TTL
expiry via fake timers), JWT mint/verify/reject (expired, wrong room), TURN HMAC + TTL, relay
integration test with two stub WS clients.

**WI-2 — Wire format + WebRTC dependency decision.** *Traces: NFR-2, NFR-3, NFR-5.*
`packages/contracts/src/sharing.ts` (+ export from `index.ts`): `ShareScope`, `SharedEnvelope`
(with `streamId`), `SharedHello` (with `scopeKinds`), channel payload schemas, version-negotiation
helpers. Run the `werift` vs `@roamhq/wrtc` spike per the decision rule above; record the outcome
in this PRD.
Tests: envelope round-trip, `ChatMessage`→`SharedMessage` mapping (allow-list — assert no extra
fields leak), per-stream `seq` gap detection, hello negotiation (unknown scope kind → typed error).

**WI-3 — Host WebRTC transport.** *Traces: FR-5, NFR-1, NFR-4, NFR-5.*
Effect-native modules: `src/dashboard/server/sharing/signaling-client.ts` (WS client, re-park on
restart), `peer-hub.ts` (one `RTCPeerConnection` per viewer, channel mux keyed by envelope
`streamId`), `access-control.ts` (host-side admitted set + contributor grants; content gated here).
Tests: peer-hub unit tests with a loopback pair; access-control property "no channel opens for a
non-admitted githubId".

**WI-4 — Conversation bridge.** *Traces: FR-5, NFR-2.*
`src/dashboard/server/sharing/conversation-bridge.ts`: subscribe to the conversation event source
(`conversation-service.ts`), map to `SharedMessage`, full-history backfill on admit, live
append/update streaming. `streamId` = shared conversation id.
Tests: backfill ordering, streaming update coalescing, mapping fidelity.

**WI-5 — Access control UX + presence.** *Traces: FR-3, FR-4, FR-6.*
Frontend: `LobbyPanel.tsx` (admit/kick/revoke/un-revoke, pending joiners with avatar+handle),
presence roster rendering (controller + host marked). Host route module
`src/dashboard/server/routes/sharing.ts` (`/api/sessions/*` proxy, join-intent endpoint), register
in `server.ts`.
Tests: Playwright — lobby occupant sees zero content (FR-3 e2e); admit/kick/revoke flows.

**WI-6 — Viewer surface.** *Traces: FR-5, FR-6, NFR-5.*
Frontend: `/#/shared/<roomId>` route, `webrtc-viewer.ts` (browser `RTCPeerConnection` + channels),
`SharedConversationView.tsx` rendering via existing `ConversationPanel`/`MessagesTimeline`
components; "host disconnected — reconnecting…" and "removed by host" states; Leave action.
Tests: component tests for state transitions; Playwright two-context live-update test.

**WI-7 — Terminal channel.** *Traces: FR-9.*
`src/dashboard/server/sharing/terminal-bridge.ts`: subscribe to the `pty-hub.ts` byte stream as an
additional consumer; viewer terminal-view toggle; no inbound handler (read-only structural).
Tests: Playwright — viewer sees tmux output; keystrokes in viewer terminal produce no host-side
effect; hub continues serving existing WS clients unchanged.

**WI-8 — Input model.** *Traces: FR-7, FR-8, FR-14.*
`drafts` channel plumbing both sides; `SuggestionList.tsx` (stacked list, pick/edit/send);
contributor grant/revoke flow; host-side FIFO submit queue with turn-aware flushing +
"agent is working / N queued" indicator.
Tests: draft LWW semantics; FIFO ordering under concurrent submits (fake timers); grant/revoke
reverts to suggestion-only.

**WI-9 — Controller role & handoff.** *Traces: FR-10.*
`handoff` control message, host-side enforcement (`controllerGithubId` checked on every control
command), reclaim path, UI affordances.
Tests: integration — A hands to B, B admits C, data owner reclaims; non-controller control
commands rejected.

**WI-10 — Lifecycle & resilience.** *Traces: FR-11, FR-12.*
`shared_sessions` table + accessor (`src/lib/database/shared-sessions-db.ts`, schema bump),
share-end teardown, host re-park + viewer auto-restore inside the 5-minute window, room teardown
past it.
Tests: schema migration test; reconnect integration (fake timers for the window); restart re-park.

**WI-11 — CLI.** *Traces: FR-1, FR-2, FR-13.*
`src/cli/commands/share.ts`, `src/cli/commands/join.ts` (device flow, dashboard auto-start),
register in `src/cli/index.ts`. Wrapper skills `skills/pan-share/SKILL.md`,
`skills/pan-join/SKILL.md` **in the same commit as the CLI verbs** (Skills↔CLI convention;
`scripts/lint-skills.sh` enforces).
Tests: command unit tests; device-flow poll with fake timers.

**WI-12 — Hardening + e2e + privacy verification.** *Traces: FR-3, NFR-1, NFR-6, NFR-7.*
Cold-start handling (share page "run `pan join`" path), TURN forced-relay test, the signaling
privacy e2e (instrument the signaling WS; assert no conversation/draft/terminal bytes), rate-limit
verification, full acceptance-criteria sweep.

**WI-13 — Documentation.** *Traces: all.*
New `docs/SHARED-SESSIONS.md` (operator + user guide: sharing, joining, roles, lifecycle,
troubleshooting); link from `docs/INDEX.md`; Mintlify user page (`configuration/` or `reference/`
per docs-site convention); update `CLAUDE.md` harness notes if the WebRTC dep is native
(+ `.claude/rules/dashboard-node22-only.md`); `pan share`/`pan join` skill docs are WI-11's.
Acceptance: every shipped surface is documented; no doc references the retired rev-1 PRD path.

---

## Acceptance Criteria

Mapped to work items; all mechanically checkable.

- [ ] AC-1 (WI-5, WI-11): "Share this conversation" (dashboard) and `pan share <conversationId>` (CLI) both return a working `overdeck.ai/s/<id>` link
- [ ] AC-2 (WI-1, WI-5): browser link recipient hits GitHub OAuth, then localhost-detect hands off to their local dashboard
- [ ] AC-3 (WI-11): `pan join <link-or-code>` authenticates via device flow and lands in the lobby; auto-starts the local dashboard when not running
- [ ] AC-4 (WI-5, WI-12): a joiner with a valid link receives **no** conversation/draft/terminal/presence data until admitted (e2e-verified)
- [ ] AC-5 (WI-5): controller sees pending joiners (avatar + handle) and can admit each one
- [ ] AC-6 (WI-4, WI-6): admitted viewer renders the conversation in real time, including full-history backfill on join
- [ ] AC-7 (WI-5, WI-6): presence shows all participants with controller and host marked
- [ ] AC-8 (WI-8): viewer drafts appear live in the controller's stacked list as they type; controller can pick, edit, send
- [ ] AC-9 (WI-8): contributor grant → direct posts; revoke → back to suggestion-only; concurrent submits land strict-FIFO with mid-turn queueing
- [ ] AC-10 (WI-7): viewer terminal toggle shows the host tmux session live; viewer keystrokes have no host-side effect
- [ ] AC-11 (WI-5): kick → "removed by host", re-click → lobby, re-admittable; revoke → blocked from lobby until un-revoked
- [ ] AC-12 (WI-5, WI-10): whole-link revoke drops every participant; link dies when the host ends the share; no fixed-duration expiry
- [ ] AC-13 (WI-9): handoff to an admitted viewer works; new controller can admit/kick/revoke and send drafts; data owner can reclaim at any time
- [ ] AC-14 (WI-10): host drop < 5 min → admitted viewers auto-restore without re-admission; > 5 min → room torn down, re-join via lobby
- [ ] AC-15 (WI-12): connection succeeds through symmetric NATs via TURN relay
- [ ] AC-16 (WI-2): all channel traffic uses the versioned wire format with `streamId` on every envelope; hello negotiates `scopeKinds`; internal `PanRpcGroup` types never cross the wire
- [ ] AC-17 (WI-12): signaling service never sees content (instrumented e2e)
- [ ] AC-18 (WI-2, WI-3): new tagged errors live in `src/lib/errors.ts`
- [ ] AC-19 (WI-10): `shared_sessions` is keyed by `room_id` with `scope_kind`/`scope_id`; one active share per scope enforced by the accessor
- [ ] AC-20 (WI-13): docs shipped per WI-13

## Test Plan

**Unit** — wire format (envelope versioning, allow-list mapping, per-stream `seq` gap detection,
hello negotiation); room state machine (all transitions + TTL via fake timers); identity JWT;
TURN credential HMAC + TTL (fake timers); `shared_sessions` CRUD + migration; FIFO submit queue.

**Integration** — signaling create→park→join→admit→SDP/ICE→channel-open; full join lifecycle
(share → OAuth → lobby → admit → backfill → live append → kick → re-lobby → revoke → blocked);
handoff chain incl. reclaim; reconnect inside/outside the 5-minute window (fake timers);
contributor grant/revoke; forced-TURN relay.

**E2E / Playwright** — two browser contexts (host + viewer) live-update flow; lobby-occupant
zero-content negative test; signaling-privacy instrumentation test; read-only terminal test.

All retry/backoff/TTL tests use `vi.useFakeTimers()` per the repo rule — no real-time sleeps.

## Out of Scope (followups)

- Sharing **multi-agent task surfaces** ([PAN-2565](https://github.com/eltmon/overdeck/issues/2565)) — the wire format and room model are ready for it (see *Forward compatibility*); the implementation is a followup issue after both land
- Sharing entire **agent sessions** (watch-an-agent-with-friends) — separate issue
- Sharing kanban / inspector / other dashboard panels — v2+
- Multi-conversation sharing in one link — subsumed by the future task scope
- Rich **collaborative markdown editing** for prompt drafts (CRDT, gist-backed) — separate issue (PAN-659 lineage); rides a new `collab` channel; may require the `gist` OAuth scope
- **Persistent roles / team workspaces** (owner-editor-viewer that outlive a session) — see [PAN-2566](https://github.com/eltmon/overdeck/issues/2566) §4; deliberately a different authorization layer
- **Hosted read-only web view** for viewers with no Overdeck installed — different privacy model
- Voice / video — never (use Zoom/Meet alongside)
- Persisting signaling-service room state across redeploys — v0 accepts in-flight shares ending on deploy
- Auto-contributor pre-designation by GitHub handle — nice-to-have, post-v0

## Files Likely Touched

**New — signaling service**: `services/signaling-service/` (workspace), `services/signaling-service/src/errors.ts`, `infra/signaling/` (Fly + coturn).

**New — host transport (Effect-native)**: `src/dashboard/server/sharing/{signaling-client,peer-hub,conversation-bridge,terminal-bridge,access-control}.ts`, `src/dashboard/server/routes/sharing.ts`.

**Modified**: `src/dashboard/server/server.ts` (register route layer), `src/dashboard/server/pty-hub.ts` (expose byte stream to the terminal bridge), `src/lib/database/schema.ts` (schema bump), `src/lib/database/shared-sessions-db.ts` (new accessor), `src/lib/errors.ts` (new tags), `packages/contracts/src/sharing.ts` (new) + `index.ts`, server `tsdown.config.ts` (externalize WebRTC dep if native), `.claude/rules/dashboard-node22-only.md` (if native).

**New — frontend**: `src/dashboard/frontend/src/components/sharing/{SharedConversationView,LobbyPanel,SuggestionList,ShareDialog}.tsx`, `src/dashboard/frontend/src/lib/webrtc-viewer.ts`; modified `src/dashboard/frontend/src/components/chat/ConversationPanel.tsx` (Share action).

**New — CLI**: `src/cli/commands/{share,join}.ts`, `src/cli/index.ts` (register), `skills/pan-share/SKILL.md`, `skills/pan-join/SKILL.md`.

**Docs**: `docs/SHARED-SESSIONS.md` (new), `docs/INDEX.md`, Mintlify user page.
