# PAN-2683 — Policies panel: per-issue Standing-crew override + tier-suspension messaging

**Issue:** https://github.com/eltmon/overdeck/issues/2683
**Reviewed:** rev 2 incorporates `drafts/pan-2683-FEEDBACK-gpt-5.6-sol.md` (source-verified, 2026-07-15).
**Follows:** PAN-2681 (Policies popover, merged via #2682) · **Terminology contract:** the settings-side redesign PAN-2684 renames tiers to **crews**; this issue uses "Standing crew" (the PAN-2383 canonical name) and "crew routing" in operator copy.

## Scope correction (2026-07-15 audit — supersedes the issue's item 1 framing)

`Stage/cockpit/PlanCard.tsx` (the old tiered select) lives only in `IssueCockpitBody.tsx`, which is **mounted nowhere** (dead code — `git grep IssueCockpitBody` hits only itself + test). The per-issue tiered-execution override therefore has **no live UI**; this issue adds the only surface. PlanCard is left untouched (surgical-change rule); no mirror/no-loss question remains.

## Glossary

- **Policies panel** — `src/dashboard/frontend/src/components/IssuePolicyStrip.tsx` (post-#2682): trigger button + override chips + popover with Review (Mode/Re-review/Model) and Work (Model/Swarm) sections; `Segmented` control at `:39-49`; staffing GET payload includes `resolved.tiered` (`:16`) and drives the `work: Default · tiered` label (`:168`).
- **Standing-crew override** — the per-issue tiered-execution record override (PAN-2383): `record.tieredExecutionOverride: 'on' | 'off'`, resolved by `resolveTieredExecutionEnabledForIssue` (`src/lib/agents/tier-table.ts:168`). WRITE door (verified): **`PATCH /api/workspaces/:issueId/tiered-execution`** with body **`{ override: 'on' | 'off' | null }`** (null clears) — `workspace-data.ts:923,939`. READ door: **none exists today** — see D-R below.

## Requirements

- **D-R (read contract — one additive backend change).** Extend the staffing GET the strip already calls (`GET /api/issues/:issueId/staffing`, `projects.ts:727-740`) with a `tieredExecution: { effective, source: 'issue'|'plan'|'global', override: 'on'|'off'|null }` block, AND fix the route to pass **plan metadata** into `resolveTieredExecutionEnabledForIssue` (today it omits it — `projects.ts:727` — so plan-level `tiered_execution` overrides are invisible; spawn paths DO pass it, `spawn-prep.ts:297`). The existing `resolved.tiered` boolean is kept for compatibility.
- **FR-1** The Work section gains a **Standing crew** row: `Segmented` control `Default · <effective> (<source>)` / `On` / `Off` reading from D-R's block; override chip `crew · on|off` renders only when overridden (overrides-only rule); reset link clears via `PATCH { override: null }`; the panel's **Reset all** (`IssuePolicyStrip.tsx:203`) is extended to include this override.
- **FR-2** When tiered execution is *effective* for the issue and a **work-model override** is set, the Work Model row shows helper text: "Overrides crew routing — every item on this issue runs this model." and the collapsed chip reads `work · <model> · replaces crews`.
- **FR-3** The `work: Default · tiered` label becomes `Default · crews` (the label copy assigned to whichever of PAN-2683/2684 lands second — if 2684 is not yet merged, keep `tiered` and leave a TODO referencing this FR).
- **NFR-1** Backend changes limited EXACTLY to D-R (one additive read block + the plan-metadata fix on that one route). Resolver, write door, and spawn paths untouched.

## Work items

1. **Standing-crew row + chip** in `IssuePolicyStrip.tsx` (new query for the tiered-execution endpoint, mirroring the swarm query's shape). **Tests:** row renders effective value + source (all THREE sources: record override, plan metadata, global); override→chip; reset clears; Reset-all includes it; component test alongside the existing strip tests.
2. **Tier-suspension messaging (FR-2)** using `staffing.resolved.tiered` already in the payload. **Tests:** helper + chip suffix appear only when `tiered && workModel override`.
3. **Label copy (FR-3)** per the landing-order rule. **Tests:** label matches the decided copy.
4. **Docs:** `docs/TIERED-EXECUTION.md` "Settings UI"/issue-view paragraph — replace the stale PlanCard claim with the Policies-panel location. No Mintlify page covers the Policies panel (stated explicitly).

## Acceptance criteria

- **AC-1** Standing-crew override round-trips from the panel; chip overrides-only; source shown in the Default label.
- **AC-2** Suspension messaging exactly per FR-2 conditions.
- **AC-3** Docs updated; typecheck/lint/test green.

## Intersecting repo rules

Style guide (amber only if a human must act — none here; primary tint for override chips), overrides-only visibility (PAN-2681), Karpathy surgical changes (PlanCard untouched).
