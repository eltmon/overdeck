# PAN-3762 — Adopt t3code-style multi-machine environment federation

**Issue:** [PAN-3762](https://github.com/eltmon/overdeck/issues/3762)  
**Epic:** [PAN-2350](https://github.com/eltmon/overdeck/issues/2350) (Overdeck Anywhere)  
**Status:** draft  
**Author:** Codex conversation, 2026-08-19  
**Reference revision:** t3code `f2d5fc91e` (`main`, fetched 2026-08-19)

## Glossary

| Term | Meaning |
| --- | --- |
| **Environment** | One independently running Overdeck server and the machine-local projects, agents, conversations, terminals, credentials, and runtime state it owns. |
| **Environment ID** | A stable opaque ID generated by an Overdeck server. Every client-visible project, issue, agent, conversation, and terminal reference is paired with it. |
| **Connection catalog** | Client-owned persisted records for known environments: label, endpoint/transport, environment ID, protected session credential, last connection result, and optional SSH launch metadata. |
| **Environment descriptor** | Server-reported identity, platform, Overdeck version, and capability flags returned during connection bootstrap. |
| **Endpoint provider** | A way to reach an environment: same-origin local, direct HTTPS, Tailscale HTTPS, SSH-forwarded loopback, or a future Anywhere relay. Providers converge on the same HTTP/WebSocket client contract. |
| **Pairing credential** | A short-lived, single-use secret printed or displayed by the server. A client exchanges it for a revocable device session. It is not the root internal token. |
| **Device session** | A revocable credential stored by one client for one environment after pairing. |
| **Scoped reference** | `{ environmentId, localId }`, where `localId` is an existing project, issue, agent, conversation, or terminal ID meaningful only to its environment. |
| **Federated client** | The dashboard shell that holds several environment connections and routes every read, mutation, stream, and terminal to the environment named in the scoped reference. |
| **Origin machine** | The environment that owns an entity and executes every operation on it. This issue does not migrate live ownership between machines. |

## Problem

Overdeck Anywhere currently treats remote use as access to one home orchestrator. [PAN-2350](https://github.com/eltmon/overdeck/issues/2350) adds tunnels, a relay, mobile access, and distributed agent metadata around that central instance. This works for reaching one machine from a phone and for shared sessions, but it makes the common desktop-plus-laptop case depend on a shared control/data plane.

t3code ships a simpler model. Every machine runs a complete server. Clients save several environments and connect directly to each one. The machine remains authoritative for its files, git state, terminals, and provider sessions. The client scopes identities by environment, handles connection/version state separately, and routes operations to the selected server. Direct HTTPS, Tailscale, SSH forwarding, and relay connections are transport choices under one connection catalog.

Overdeck should adopt that architecture for single-operator multi-machine use. Existing relay work remains useful as an optional endpoint provider and shared-session substrate, but it is no longer the prerequisite for using several operator-owned machines from one dashboard.

## Verified prior art

### t3code at `f2d5fc91e`

- `/home/eltmon/Projects/t3code/packages/contracts/src/environment.ts` defines `ExecutionEnvironmentDescriptor`, connection states, capability flags, and `ScopedProjectRef` / `ScopedThreadRef` with `environmentId`.
- `/home/eltmon/Projects/t3code/apps/desktop/src/app/DesktopConnectionCatalogStore.ts` persists an encrypted connection catalog and migrates older saved-environment records.
- `/home/eltmon/Projects/t3code/apps/desktop/src/settings/DesktopSavedEnvironments.ts` protects saved bearer tokens through the desktop secret-storage boundary.
- `/home/eltmon/Projects/t3code/packages/ssh/src/tunnel.ts` probes a remote host, starts or reuses the remote server, issues a pairing credential, and maintains a local SSH port forward.
- `/home/eltmon/Projects/t3code/packages/shared/src/remote.ts` normalizes direct hosts and hosted pairing URLs into one remote pairing target.
- `/home/eltmon/Projects/t3code/docs/user/remote-access.md` documents a transport-neutral environment flow: direct desktop backend, headless server, Tailscale endpoint, or desktop-managed SSH launch.

The architectural contracts are prior art. The implementation must fit Overdeck's Effect, state-door, authentication, and desktop-app conventions; do not mechanically copy t3code source.

### Overdeck today

- `src/dashboard/frontend/src/lib/wsTransport.ts:25-36` resolves one RPC URL from `window.location` or one build-time `VITE_API_URL`. `WsTransport` holds one managed RPC runtime (`:148-161`). There is no environment registry.
- `src/dashboard/frontend/src/lib/wsTransport.ts:57-89` consumes a URL hash token and sends it as `x-overdeck-internal-token` to mint the browser session. The token is the machine root today.
- `src/dashboard/server/routes/dashboard-auth.ts:290-313` permits session minting through the internal token or trusted local peer and checks one root-derived browser session.
- `src/lib/internal-token.ts:1-19` states that one root token authenticates internal endpoints; `:69-97` creates the machine-local 32-byte secret.
- `src/dashboard/server/routes/origin-validation.ts:46-89` builds a trusted-origin list for one server deployment.
- `src/dashboard/server/ws-terminal.ts:119-131` gates terminal upgrades by origin only pending [PAN-1166](https://github.com/eltmon/overdeck/issues/1166). Remote pairing cannot ship until terminal authentication is restored.
- `src/dashboard/server/server.ts:137-141` exposes `/api/health` with dashboard identity, which is the seed for an environment descriptor but lacks a versioned capability contract.
- `docs/VISION.md:9-26` describes one local Electron app per machine and local git operations. The federation model preserves those ownership boundaries.

## Architecture comparison and decision

| Concern | Existing Anywhere direction | Adopted direction |
| --- | --- | --- |
| Authority | One home orchestrator is the primary instance. | Every machine is an independent authority for its local entities. |
| Multi-machine UI | Relay/remote state planes make another machine visible. | The client opens one connection per saved environment and composes the UI. |
| Runtime state | Agent metadata is distributed so one server can locate remote runtime. | Runtime stays local. The scoped reference routes the request to its owner. |
| Reachability | Cloudflare/relay is central. | Direct HTTPS, Tailscale, and SSH work first; relay is another endpoint provider. |
| Identity | Scoped tokens on the home server plus relay identity. | Pairing and revocable device sessions are per environment; relay identity can wrap the same contract. |
| Failure mode | Relay or home instance failure can hide the whole system. | One failed environment is isolated; other connections remain live. |
| Version skew | Planned around relay/client compatibility. | Every descriptor advertises server version and capability flags; clients gate actions per environment. |
| Cross-machine continuation | Requires transcript/state distribution. | Browse and control stay on the origin first. Ownership transfer remains separate in [PAN-2609](https://github.com/eltmon/overdeck/issues/2609) / [PAN-3513](https://github.com/eltmon/overdeck/issues/3513). |

**Decision:** Implement client-side environment federation as the default Overdeck Anywhere architecture for one operator using several machines. Keep [PAN-2356](https://github.com/eltmon/overdeck/issues/2356) as an optional relay endpoint and shared-session service. Do not require relay deployment, global agent state, or transcript replication for v1 federation.

## Requirements

### Functional requirements

- **FR-1 — Stable environment identity.** Each server owns a stable opaque `environmentId` under `OVERDECK_HOME`. It returns that ID, a user-facing machine label, OS/architecture, exact Overdeck version, and capabilities from a versioned descriptor endpoint/RPC.
- **FR-2 — Scoped references.** All federated client state and navigation use `{ environmentId, localId }`. The environment ID must participate in query/cache keys, selection state, URLs, event routing, terminal attachment, mutation dispatch, and persisted recent-history entries.
- **FR-3 — Connection catalog.** Desktop stores several environment profiles. Each profile carries label, endpoint-provider configuration, expected environment ID, protected device credential, last connected descriptor, and connection status. Secrets use the Electron secret-storage boundary and never localStorage/plain JSON.
- **FR-4 — Local compatibility.** With no catalog configured, the current same-origin server is materialized as the implicit local environment. Existing URLs and single-machine behavior continue to work.
- **FR-5 — Independent connections.** The client maintains one RPC/SSE/terminal transport context per connected environment. A failure, reconnect, auth expiry, or version mismatch in one context does not reset another.
- **FR-6 — Environment UI.** Settings → Connections lists local and remote environments with label, machine, endpoint type, status, version, and actions to add, reconnect, rename, forget, or revoke. The app shell shows the active environment and supports switching. Aggregated lists label every row by environment and never merge colliding local IDs.
- **FR-7 — One-time pairing.** `pan pair` and the dashboard issue a short-lived single-use credential and pairing URL. The target server exchanges it for a revocable device session. The root internal token never crosses the machine boundary or appears in the pairing URL.
- **FR-8 — Revocation.** Server and UI/CLI list paired devices/sessions and revoke them immediately. Revocation closes or rejects HTTP, SSE, RPC, and terminal access for that session without changing other devices.
- **FR-9 — Direct endpoints.** A headless server can advertise configured HTTPS/Tailscale endpoints. Pairing output distinguishes unreachable loopback endpoints from reachable ones and never claims network reachability it has not verified.
- **FR-10 — Desktop-managed SSH.** The desktop accepts an SSH target, probes non-interactive Node/Overdeck availability, starts or reuses `pan serve` on remote loopback, creates a local port forward, pairs, saves the environment, monitors the child processes, and reconnects after desktop restart. Generated remote launcher state is namespaced under `~/.overdeck/ssh-launch/<host-key>/` on the remote machine.
- **FR-11 — Version and capabilities.** The client compares its contract version with each descriptor. It shows actionable version mismatch state and gates unsupported actions from advertised capabilities before dispatch.
- **FR-12 — Transport-neutral routing.** Direct, Tailscale, SSH, and future relay profiles all produce the same resolved HTTP/WS base URL and device-session provider. Feature code does not branch on transport type.
- **FR-13 — Ownership clarity.** Every project, agent, conversation, terminal, issue action, AskUserQuestion prompt, and flywheel surface shows or inherits its environment. Mutations always execute on the owning environment.
- **FR-14 — Existing Anywhere integration.** [PAN-2350](https://github.com/eltmon/overdeck/issues/2350), [PAN-2351](https://github.com/eltmon/overdeck/issues/2351), [PAN-2352](https://github.com/eltmon/overdeck/issues/2352), [PAN-2356](https://github.com/eltmon/overdeck/issues/2356), [PAN-2609](https://github.com/eltmon/overdeck/issues/2609), and [PAN-3513](https://github.com/eltmon/overdeck/issues/3513) receive an explicit disposition: prerequisite, absorbed, relay-only, continuation-only, or superseded.

### Non-functional requirements

- **NFR-1 — Isolation.** One environment cannot read or mutate another environment's server state. The client is a router, not a new canonical store.
- **NFR-2 — Secret safety.** Pairing and device secrets never appear in query strings, logs, activity payloads, analytics, or unencrypted catalog files. Pairing URLs put the credential in the fragment when browser delivery is required.
- **NFR-3 — No new state door.** Servers continue to use each domain's canonical read resolver and write surface. Federation adds routing before those doors; it does not add direct DB, state-branch, `state.json`, or tracker access.
- **NFR-4 — Server runtime.** Dashboard production remains Node 22 and built `dist/dashboard/server.js`. No `execSync` is introduced in server-reachable code.
- **NFR-5 — Test timing.** SSH reconnect, pairing expiry, retry, backoff, heartbeat, and revocation timing tests use Vitest fake timers. Real-time network checks live in a separate `@slow` integration suite.
- **NFR-6 — Compatibility.** Local-only startup, routes, command palette actions, issue lifecycle, conversations, terminals, flywheel, Machine Room, and settings remain available. A no-loss audit test accounts for each pre-federation route/action/status/view.
- **NFR-7 — Failure reporting.** Connection errors name the affected environment, failed stage, and next action. An offline laptop must not produce a global dashboard failure banner for a healthy desktop environment.
- **NFR-8 — Performance.** Two idle connected environments add no more than two persistent control connections each. Aggregation must not multiply server polling when an event subscription already exists.
- **NFR-9 — Platform scope.** Linux/macOS desktop and headless Linux are v1. Windows direct connections are supported if the server already runs; desktop-managed SSH launch on Windows is deferred unless the existing desktop SSH process layer proves portable without extra work.

## Decisions

1. **The environment is the routing boundary.** Do not create a global ID migration. Existing local IDs stay unchanged and become unique when paired with `environmentId`.
2. **The catalog is client state.** It stores connection metadata and protected credentials only. It is not canonical issue, agent, conversation, or pipeline state.
3. **One active environment first, aggregation second.** The first usable increment switches the whole shell between environments. Aggregated overview/Needs-You/search follows once scoped query keys and navigation are mechanically safe.
4. **SSH forwards loopback.** The remote server remains bound to loopback for SSH profiles; the desktop owns the forward. Direct profiles require explicit network binding and trusted HTTPS/Tailscale configuration.
5. **Pairing creates server-side sessions.** Do not extend the root `internal-token` bootstrap to remote clients. [PAN-2351](https://github.com/eltmon/overdeck/issues/2351)'s scoped-token work may supply storage/verification primitives, but the user contract is paired devices and revocable sessions.
6. **The relay implements an endpoint provider.** [PAN-2356](https://github.com/eltmon/overdeck/issues/2356) may provide reachability, identity assertion, and push. It must not replace the environment descriptor, scoped refs, or per-environment authorization checks.
7. **No live ownership transfer in v1.** A laptop can operate a desktop-owned agent through the desktop environment. Moving the conversation or agent to the laptop is separate continuation/migration work.
8. **Server update is explicit.** The descriptor advertises update capability. The client may guide or invoke a supported update, but never silently restarts a machine with active work.

## Before/after implementation sketches

### Transport ownership

Current (`src/dashboard/frontend/src/lib/wsTransport.ts:148-161`):

```ts
export class WsTransport {
  private readonly runtime: ManagedRuntime.ManagedRuntime<RpcClient.Protocol, never>

  constructor(url?: string) {
    this.runtime = ManagedRuntime.make(createPanRpcProtocolLayer(url))
    this.clientPromise = ensureDashboardSession(url).then(() => /* one client */)
  }
}
```

Target shape (names are normative; exact Effect plumbing may follow existing frontend conventions):

```ts
export interface EnvironmentConnection {
  readonly environmentId: EnvironmentId
  readonly descriptor: EnvironmentDescriptor
  readonly transport: WsTransport
  readonly status: EnvironmentConnectionState
}

export interface EnvironmentRegistry {
  connect(profileId: ConnectionProfileId): Effect.Effect<EnvironmentConnection, ConnectionError>
  get(environmentId: EnvironmentId): Option.Option<EnvironmentConnection>
  disconnect(environmentId: EnvironmentId): Effect.Effect<void>
}
```

### Entity references

Current conceptual form:

```ts
openConversation(conversationId)
attachTerminal(agentId)
```

Target:

```ts
openConversation({ environmentId, conversationId })
attachTerminal({ environmentId, agentId })
```

The router resolves the environment connection before calling the unchanged environment-local server contract.

### Pairing bootstrap

Current (`src/dashboard/frontend/src/lib/wsTransport.ts:71-79`):

```ts
const token = consumeDashboardBootstrapToken()
await fetch(sessionUrl, {
  method: 'POST',
  headers: token ? { 'x-overdeck-internal-token': token } : undefined,
})
```

Target:

```ts
const pairingCredential = consumePairingCredential()
const session = await exchangePairingCredential(endpoint, pairingCredential)
await connectionCatalog.storeProtectedSession(profileId, session)
```

Local implicit bootstrap may keep the trusted-peer path. Remote bootstrap never sends `x-overdeck-internal-token`.

## Numbered work items

### W1 — Contracts and stable environment identity (FR-1, FR-2, FR-11)

Files:

- `packages/contracts/src/environment.ts` (new)
- `packages/contracts/src/index.ts`
- `src/dashboard/server/identity.ts`
- `src/dashboard/server/server.ts`
- `src/lib/paths.ts`

Steps:

1. Add branded `EnvironmentId`, `ScopedProjectRef`, `ScopedIssueRef`, `ScopedAgentRef`, `ScopedConversationRef`, and `ScopedTerminalRef` schemas.
2. Add a versioned `EnvironmentDescriptor` with label, platform, server version, protocol version, and named capability booleans.
3. Generate and persist the stable environment ID atomically with mode `0600`; expose a CLI-safe read helper.
4. Extend `/api/health` or add `GET /api/environment` through the normal route layer. The response must not contain paths, tokens, usernames, or secrets.
5. Add schema round-trip, corrupted-ID recovery, descriptor compatibility, and capability-default tests.

### W2 — Pairing and device-session authorization (FR-7, FR-8; NFR-2)

Files:

- `src/dashboard/server/routes/dashboard-auth.ts`
- `src/dashboard/server/routes/pairing.ts` (new)
- `src/dashboard/server/server.ts`
- `src/lib/overdeck/device-sessions.ts` (new canonical resolver/writer surface)
- `src/cli/commands/pair.ts` (new)
- `src/cli/index.ts`
- `src/dashboard/server/ws-terminal.ts`

Steps:

1. Implement hashed, expiring, single-use pairing credentials and hashed revocable device sessions with labels and last-used timestamps.
2. Add `pan pair` output for host, code, direct pairing URL, hosted-compatible URL, and QR rendering. Put secrets in URL fragments.
3. Add exchange, list, and revoke routes. Emit attributable activity for pairing, exchange, and revocation.
4. Generalize dashboard auth to accept local root-derived sessions or paired device sessions while retaining CSRF checks.
5. Complete the authenticated terminal upgrade prerequisite from [PAN-1166](https://github.com/eltmon/overdeck/issues/1166); origin-only authorization is forbidden for remote environments.
6. Test expiry/revocation with fake timers and verify all HTTP, RPC, SSE, and terminal surfaces reject revoked sessions.

### W3 — Desktop connection catalog and environment registry (FR-3, FR-4, FR-5, FR-12)

Files:

- `src/desktop/` connection/IPC modules discovered during implementation
- `src/dashboard/frontend/src/environments/contracts.ts` (new)
- `src/dashboard/frontend/src/environments/registry.ts` (new)
- `src/dashboard/frontend/src/lib/wsTransport.ts`
- `src/dashboard/frontend/src/lib/api.ts` and query-client setup discovered during plan lowering

Steps:

1. Define transport-neutral direct, SSH, and relay connection profiles.
2. Persist the catalog through desktop IPC. Encrypt/protect its secret payload with the existing Electron platform secret API; keep renderer storage secret-free.
3. Materialize the current same-origin backend as an implicit local environment when no profile exists.
4. Refactor transport construction into per-environment contexts. Remove module-global session/CSRF state that could cross-contaminate hosts.
5. Key query caches, streams, mutations, reconnect state, and navigation by environment ID.
6. Prove with tests that two fake environments using identical issue/agent/conversation IDs remain isolated.

### W4 — Connection and environment UX (FR-6, FR-13)

Files:

- `src/dashboard/frontend/src/pages/SettingsPage.tsx` or current settings route components
- `src/dashboard/frontend/src/components/AppShell.tsx` or current shell owner
- `src/dashboard/frontend/src/components/CommandPalette.tsx` or current registry
- environment UI components under `src/dashboard/frontend/src/components/environments/` (new)

Steps:

1. Add Settings → Connections with local/remote cards, add-direct flow, connection state, descriptor/version details, rename, forget, and revoke.
2. Add an active-environment selector to the shell and command palette.
3. Keep existing deep links working for implicit local. Add environment-qualified routes for remote entities.
4. Add environment labels to every cross-environment row, prompt, notification, and error.
5. Add aggregated overview/search/Needs-You only after switch-mode parity passes.
6. Verify desktop UI with Playwright screenshots at 1440px and a narrow viewport; inspect screenshots during implementation.

### W5 — Direct and Tailscale endpoint advertisement (FR-9)

Files:

- `src/cli/commands/serve.ts`
- `src/cli/commands/pair.ts`
- `src/dashboard/server/config.ts`
- endpoint helpers under `src/lib/remote-access/` (new)

Steps:

1. Discover configured loopback, LAN, Tailscale IP/MagicDNS, and HTTPS endpoints without treating discovery as proof of reachability.
2. Add explicit server flags/config for host binding and advertised endpoints.
3. Support opt-in Tailscale Serve setup only through explicit user action; surface the command/config change before applying it.
4. Select a safe default pairing endpoint and explain when hosted HTTPS cannot connect to plain HTTP because of mixed-content rules.
5. Test endpoint normalization and preference stability across changing LAN addresses.

### W6 — Desktop-managed SSH launch (FR-10; NFR-9)

Files:

- desktop main-process modules under `src/desktop/ssh/` (new; adapt to actual desktop tree)
- shared SSH command/tunnel modules under `src/lib/remote-access/ssh/` (new)
- Connections UI from W4

Steps:

1. Probe the target with non-interactive `sh -lc`, resolving Node 22 and `pan` from standard paths/version-manager shims.
2. Install a versioned generated launcher under remote `~/.overdeck/ssh-launch/<host-key>/`.
3. Start or reuse a loopback-only `pan serve`, parse structured readiness output, and create a local port forward.
4. Issue a pairing credential through the remote CLI, exchange it through the forward, then persist the profile/session.
5. Supervise forward/server-launch processes asynchronously. No `execSync`; redact credentials and SSH command output.
6. On launcher-version mismatch, replace only launcher-managed remote processes and state. Never kill unrelated `pan` or Node processes.
7. Test command construction, noisy shell output, missing Node, incompatible Node, stale PID/port, reconnect, and cancellation. Use fake timers for retries.

### W7 — Version skew and supported update guidance (FR-11)

Files:

- contracts from W1
- environment registry from W3
- Connections UI from W4
- `src/cli/commands/update.ts`

Steps:

1. Define protocol compatibility rules separately from package-version equality.
2. Gate UI actions using descriptor capabilities. Do not probe unsupported routes.
3. Show whether the server is desktop-managed, CLI/headless, SSH-launch-managed, or relay-managed and give the matching update action.
4. Require operator confirmation before any update that restarts active work; show the affected environment.

### W8 — Anywhere no-loss audit and migration (FR-14; NFR-6)

Files:

- `docs/REMOTE-ACCESS.md` or its successor
- `docs/VISION.md`
- drafts/issues for the related Anywhere work
- focused audit test/fixture under `src/dashboard/frontend/src/environments/__tests__/anywhere-parity.test.ts`

Steps:

1. Inventory every command, action, status, route, view, and affordance promised by [PAN-2350](https://github.com/eltmon/overdeck/issues/2350) and its child issues.
2. Map each item to local environment federation, optional relay, shared sessions, continuation, or explicit retirement with rationale.
3. Update the related issue bodies/PRDs so implementation agents do not build two competing control planes.
4. Add a fixture-driven no-loss test that fails when an old promised surface lacks a disposition.
5. Migrate existing single-host settings into the implicit local profile without user action. Never auto-create or expose a network endpoint.

### W9 — Documentation (all FRs)

Files:

- `configuration/remote-access.mdx` (new user guide; adjust route name to Mintlify navigation conventions)
- `docs/REMOTE-ACCESS.md` (developer architecture/runbook)
- `docs/VISION.md`
- `docs/DESKTOP-APP.md`
- `docs/INDEX.md`
- CLI `--help` text for `pan serve`, `pan pair`, and device-session management

Steps:

1. Explain that every machine runs an independent Overdeck and owns its local state.
2. Document direct HTTPS/Tailscale and SSH launch, pairing/revocation, version skew, failure isolation, and security boundaries.
3. Explain that the relay is optional reachability/shared-session infrastructure, not the canonical multi-machine state plane.
4. Document unsupported ownership transfer and point to continuation work.

## Acceptance criteria

- **AC-1 (W1):** Two servers created from separate `OVERDECK_HOME` directories report different stable IDs across restarts. Descriptor schema/version tests pass.
- **AC-2 (W2):** A one-time credential exchanges once, expires on schedule, and produces a revocable device session. The root internal token is absent from client storage, URLs, and logs.
- **AC-3 (W2):** Revoking one device closes/rejects its HTTP, RPC, SSE, and terminal access within five seconds while another paired device stays connected.
- **AC-4 (W3):** A client connects to two fixtures with the same local issue ID and agent ID. Reads, writes, events, and terminal bytes remain attached to the selected environment in automated tests.
- **AC-5 (W3/W4):** With no saved profiles, existing local routes and workflows pass unchanged. The implicit local environment requires no pairing prompt.
- **AC-6 (W4):** The operator adds, renames, switches, reconnects, forgets, and revokes environments from Settings. Playwright screenshots confirm clear machine ownership and per-environment failure state.
- **AC-7 (W5):** A headless server advertises a configured reachable Tailscale/HTTPS endpoint and prints a usable pairing URL; loopback-only output clearly says another device cannot reach it.
- **AC-8 (W6):** From the desktop app, an SSH profile starts or reuses a remote Overdeck, forwards loopback, pairs, and reconnects after desktop restart. Killing the forward marks only that environment disconnected.
- **AC-9 (W7):** A fixture server missing a capability never receives that action. A protocol-incompatible server shows an actionable mismatch rather than reconnecting forever.
- **AC-10 (W8):** The fixture-driven Anywhere no-loss audit accounts for every promised surface in the existing epic and fails on an unmapped item.
- **AC-11 (W9):** A new user can follow the docs to connect a second machine and revoke it without using the root token or editing config files by hand.
- **AC-12 (quality):** Typecheck, lint, targeted unit/integration tests, and desktop Playwright checks pass. Boot-path changes are runtime-boot-tested on a throwaway instance before any live dashboard restart.

## Implementation checkpoints

1. **After W1:** Review the scoped-reference inventory. If any remotely actionable entity lacks an environment owner, stop before refactoring UI caches.
2. **After W2:** Run a focused security review covering token leakage, CSRF, Origin handling, WS upgrades, session revocation, and SSH output redaction.
3. **After W3:** Demonstrate two simultaneous fixture environments with colliding IDs. If isolation is not mechanical in cache keys and navigation types, do not begin aggregation.
4. **Before W6:** Verify the current desktop app exposes an appropriate persistent main-process service boundary. If not, add that boundary as a named prerequisite instead of placing SSH process ownership in the renderer.
5. **Before live restart:** Any new boot service must pass a throwaway Node 22 `dist/dashboard/server.js` boot test. Use `pan restart --health-timeout 120` only after that gate.

## Out of scope

- Concurrent multi-machine editing or multi-writer ownership of one agent/conversation.
- Automatic transcript replication, conversation adoption, or agent migration; those remain [PAN-2609](https://github.com/eltmon/overdeck/issues/2609) / [PAN-3513](https://github.com/eltmon/overdeck/issues/3513) concerns.
- Replacing project `overdeck-state` semantics with a global cloud state store.
- Multi-user shared-session authorization and collaboration UI from [PAN-658](https://github.com/eltmon/overdeck/issues/658).
- Building the public relay in [PAN-2356](https://github.com/eltmon/overdeck/issues/2356), except for defining its endpoint-provider interface.
- Silent remote installation or silent server updates.
- Native mobile-specific work; mobile clients may consume the same environment contracts later.

## Intersecting repository rules

- Use one canonical read resolver and one write surface per domain; connection routing happens before those doors.
- Use Node 22 and built `dist/dashboard/server.js` for the dashboard. Never use Bun for production dashboard runtime.
- Never add `execSync` to server- or desktop-reachable paths; SSH lifecycle and process supervision are asynchronous.
- Use fake timers for retry, backoff, expiry, heartbeat, and delay tests.
- Runtime-boot-test every boot-path change on a throwaway instance before restarting the live dashboard.
- Refactors require the W8 no-loss audit; no existing surface may disappear silently.
- Keep reasoning-effort defaults at `high` or inherited; federation must not introduce model defaults.
